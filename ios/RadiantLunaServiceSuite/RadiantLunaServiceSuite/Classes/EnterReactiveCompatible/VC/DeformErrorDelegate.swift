
//: Declare String Begin

/*: "cancelCall" :*/
fileprivate let notiTimeMsg:[Character] = ["c","a"]
fileprivate let userAppStr:String = "ncemake"

/*: "data" :*/
fileprivate let constPriceMsg:[UInt8] = [0xac,0xa9,0xbc,0xa9]

				private func titleTheory(blue num: UInt8) -> UInt8 {
					return num ^ 200
				}

/*: "logId" :*/
fileprivate let show_addWindowKey:String = "logIdreturn share by"

/*: "#322870" :*/
fileprivate let kMakeText:String = "#322870"

/*: "Tap to start video chatting" :*/
fileprivate let data_logTitle:String = "Tap tas resume"
fileprivate let userMainUrl:[Character] = [" ","v","i","d","e","o"," ","c","h","a","t","t","i","n","g"]

/*: "icon_close_nor" :*/
fileprivate let k_viewPanNowId:[Character] = ["i","c","o","n","_","c","l","o","s","e","_","n","o","r"]

/*: "icon_views_number" :*/
fileprivate let data_dayName:String = "ICON"
fileprivate let userRefreshName:String = "view sun view case topws_n"
fileprivate let appTouchId:String = "UMBER"

/*: "%@ chatting" :*/
fileprivate let const_sourceStr:[Character] = ["%","@"," ","c","h","a","t"]
fileprivate let constFeeContent:String = "tinsize"

/*: "icon_lounge" :*/
fileprivate let kMakeBotData:[Character] = ["i","c","o","n","_","l","o","u","n","g","e"]

/*: "icon_rank_coin" :*/
fileprivate let showIndexMessage:String = "self shareicon_"
fileprivate let user_inputKey:String = "centeroin"

/*: "vip_btn_top" :*/
fileprivate let data_keySharedText:String = "videoi"
fileprivate let const_lessTitle:[Character] = ["p","_","b","t","n","_","t","o","p"]

/*: "btn_nearby_settings_nor" :*/
fileprivate let userAssetName:String = "btn_ncolor height"
fileprivate let appClickStr:String = "_settinterest let guard contact block"
fileprivate let data_nameText:String = "photo location_nor"

/*: "%@ Coins/min" :*/
fileprivate let dataToTitlePath:String = "play stop frame path view%@ Co"
fileprivate let kExplainId:String = "user view name voiceins/min"

/*: "get json error" :*/
fileprivate let user_levelKey:String = "self view imageget js"

/*: "matchId" :*/
fileprivate let show_commitName:String = "for event pathmatchId"

/*: "errno" :*/
fileprivate let data_kitEqualName:[UInt8] = [0xc1,0xce,0xce,0xca,0xcb]

				fileprivate func addCurrent(make num: UInt8) -> UInt8 {
					let value = Int(num) - 92
					if value < 0 {
						return UInt8(value + 256)
					} else {
						return UInt8(value)
					}
				}

/*: "VIPFee" :*/
fileprivate let main_modelUrl:String = "VIPFepop name"
fileprivate let appGiftTitle:String = "E"

/*: "Matching now" :*/
fileprivate let app_viewTitle:[Character] = ["M","a","t","c","h","i"]
fileprivate let user_directionPath:String = "title layer viewng now"

/*: "userTouch" :*/
fileprivate let showMessagePopText:String = "style"
fileprivate let noti_targetId:[Character] = ["s","e","r","T","o","u","c","h"]

/*: "mf/freeCall/getVideoInfo" :*/
fileprivate let kAppIconKey:String = "click self namemf/fre"
fileprivate let dataManagerMakeMessage:String = "with reply label whitel/getVid"
fileprivate let const_dismissName:[Character] = ["e"]
fileprivate let data_replyStr:String = "oInfolet view"

/*: "fromType" :*/
fileprivate let dataTextStr:[UInt8] = [0x65,0x70,0x79,0x54,0x6d,0x6f,0x72,0x66]

/*: "You've been barred from receiving calls" :*/
fileprivate let mainMagnitudeId:[UInt8] = [0xd6,0xe0,0xfa,0xa8,0xf9,0xea,0xaf,0xed,0xea,0xea,0xe1,0xaf,0xed,0xee,0xfd,0xfd,0xea,0xeb,0xaf,0xe9,0xfd,0xe0,0xe2,0xaf,0xfd,0xea,0xec,0xea,0xe6,0xf9,0xe6,0xe1,0xe8,0xaf,0xec,0xee,0xe3,0xe3,0xfc]

				private func makeStroke(show num: UInt8) -> UInt8 {
					return num ^ 143
				}

/*: "source" :*/
fileprivate let userPushName:[UInt8] = [0x51,0x4d,0x57,0x50,0x41,0x47]

				private func labShare(table num: UInt8) -> UInt8 {
					return num ^ 34
				}

/*: "type" :*/
fileprivate let notiSucceedStr:[UInt8] = [0xf5,0xf8,0xf1,0xe4]

/*: "fromFreeCall" :*/
fileprivate let const_colorValue:String = "fromFreguard aspect on"
fileprivate let const_giftText:String = "eCalllet make reply"

/*: "requestCall" :*/
fileprivate let userLayerValue:[Character] = ["r","e","q","u","e"]
fileprivate let mainExhibitMsg:[Character] = ["s","t","C","a","l","l"]

/*: "onVideoMatch" :*/
fileprivate let app_userMsg:[Character] = ["o","n","V","i","d"]
fileprivate let notiNowFormat:String = "object"
fileprivate let dataMakeId:String = "Matchchange size decision self user"

/*: "[Automatic video] Receive video call incoming" :*/
fileprivate let noti_labelLayerContent:[UInt8] = [0x84,0x9e,0xaa,0xab,0xb0,0xb2,0xbe,0xab,0xb6,0xbc,0xff,0xa9,0xb6,0xbb,0xba,0xb0,0x82,0xff,0x8d,0xba,0xbc,0xba,0xb6,0xa9,0xba,0xff,0xa9,0xb6,0xbb,0xba,0xb0,0xff,0xbc,0xbe,0xb3,0xb3,0xff,0xb6,0xb1,0xbc,0xb0,0xb2,0xb6,0xb1,0xb8]

				private func afterType(user num: UInt8) -> UInt8 {
					return num ^ 223
				}

/*: "refuseCall" :*/
fileprivate let user_sharedFieldText:String = "rinputfus"

/*: "onCancelCall" :*/
fileprivate let const_indexKey:String = "stack by agent classonCanc"
fileprivate let constLabFormat:[Character] = ["e","l","C","a","l","l"]

/*: "retryAfter" :*/
fileprivate let mainFrameContent:String = "retryAelse model moment"
fileprivate let notiFileData:String = "ftcontainerr"

/*: "onRequestCall" :*/
fileprivate let constModelId:[Character] = ["o","n","R","e","q","u","e","s","t","C"]
fileprivate let showComponentValue:String = "ALL"

/*: "failType" :*/
fileprivate let dataImageTapMsg:String = "tap actual show labelfailType"

/*: "callWaitingTime" :*/
fileprivate let show_picUrl:[Character] = ["c","a","l","l","W"]
fileprivate let userByUrl:[Character] = ["a","i","t","i","n","g","T","i","m","e"]

/*: "toUid" :*/
fileprivate let data_viewFormat:String = "toUidreturn make right let lab"

/*: "Free 1 min" :*/
fileprivate let appMatchId:[Character] = ["F","r","e","e"," ","1"," ","m","i","n"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  DeformErrorDelegate.swift
//  AbroadTalking
//
//  Created by young on 2023/8/22.
//

//: import UIKit
import UIKit

//: class TalkingRandomVideoViewController: TalkingBaseViewController {
class DeformErrorDelegate: LightThen {
    //: var fee = 0
    var fee = 0 // 原价
    //: var vipFee = 0
    var vipFee = 0 // VIP价格
    //: var chattingNum = 0
    var chattingNum = 0 // 当前匹配人数
    //: private var matchId: Int = 0
    private var matchId: Int = 0 // 速配批次Id
    //: private var waitingTimer: Timer?
    private var waitingTimer: Timer? // 呼叫等待定时（等待15s）
    //: private var retryAfterTimer: Timer?
    private var retryAfterTimer: Timer? // 匹配失败重试定时器
    //: private var logId = 0
    private var logId = 0 // 通话id
    //: private var isShowFreeVideo = false
    private var isShowFreeVideo = false // 是否已展示免费视频挽留弹窗
    //: var beginTime: TimeInterval?
    var beginTime: TimeInterval?
    //: private var dispatchWorkItem: DispatchWorkItem?
    private var dispatchWorkItem: DispatchWorkItem?
    //: private var closeBtnWorkItem: DispatchWorkItem?
    private var closeBtnWorkItem: DispatchWorkItem?
    //: var isFirstRandow = true
    var isFirstRandow = true
    //: var isBeginRand = false
    var isBeginRand = false

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1.0) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1.0) {
            //: if !self.isFirstRandow {
            if !self.isFirstRandow {
                //: self.refreshVideoCallPrice()
                self.sizePrice()
            }
        }
        //: self.resetCamera()
        self.upGift()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true

        //: setupSubviews()
        arcWindowBackground()
        //: setupSubViewsConstraint()
        setupImage()
        //: bindInteraction()
        colorGift()
        //: videoManager.startPreview(isMatch: true)
        videoManager.fromSub(isMatch: true)
        //: setPlayer(isRandon: false)
        sign(isRandon: false)
        //: if isBeginRand {
        if isBeginRand {
            //: start_randomVideo()
            iconLay()
        }
    }

    //: deinit {
    deinit {
        //: if waitingTimer != nil {
        if waitingTimer != nil {
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": logId]])
            GiftCommentSocketDelegate.shared.agnomen(info: ["cmd": (String(notiTimeMsg) + userAppStr.replacingOccurrences(of: "make", with: "l") + "Call"), String(bytes: constPriceMsg.map{titleTheory(blue: $0)}, encoding: .utf8)!: [(String(show_addWindowKey.prefix(5))): logId]])
        }
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: destroyWaitingTimer()
        fromConversion()
        //: if TalkingSocketManager.shared.isName == false {
        if GiftCommentSocketDelegate.shared.isName == false {
            //: TalkingSocketManager.shared.videoMatchDelegate = nil
            GiftCommentSocketDelegate.shared.videoMatchDelegate = nil
            //: TalkingSocketManager.shared.callerDelegate = nil
            GiftCommentSocketDelegate.shared.callerDelegate = nil
            //: TalkingSocketManager.shared.calledPartyDelegate = nil
            GiftCommentSocketDelegate.shared.calledPartyDelegate = nil
            //: TalkingSocketManager.shared.errorDelegate = nil
            GiftCommentSocketDelegate.shared.errorDelegate = nil
        }
    }

    // MARK: - Lazy Load

    //: private lazy var videoManager: TalkingVideoChatManager = {
    private lazy var videoManager: RouteChatManager = {
        //: let m = TalkingVideoChatManager()
        let m = RouteChatManager()
        //: let info = TalkingVideoChatModel()
        let info = PokingChatModel()
        //: info.videoStage = .Calling
        info.videoStage = .Calling
        //: info.renderPostion = .selfOnBg
        info.renderPostion = .selfOnBg
        //: m.infoModel = info
        m.infoModel = info
        // 视图传递给manager
        //: m.bgVideoView = self.renderView
        m.bgVideoView = self.renderView
        //: return m
        return m
        //: }()
    }()

    // 视频画面渲染视图
    //: private lazy var renderView: UIView = {
    private lazy var renderView: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = .appTitleColor()
        v.backgroundColor = .sendTitle()
        //: return v
        return v
        //: }()
    }()

    //: private lazy var gradientBtn: UIButton = {
    private lazy var gradientBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.addTarget(self, action: #selector(start_randomVideo), for: .touchUpInside)
        btn.addTarget(self, action: #selector(iconLay), for: .touchUpInside)
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        // 渐变
        //: let layer = CAGradientLayer()
        let layer = CAGradientLayer()
        //: layer.frame = CGRect(x: 0, y: 0, width: ScreenWidth+1, height: ScreenHeight+1)
        layer.frame = CGRect(x: 0, y: 0, width: main_colorData + 1, height: constRecordServerKey + 1)
        //: layer.startPoint = CGPoint(x: 0.5, y: 0)
        layer.startPoint = CGPoint(x: 0.5, y: 0)
        //: layer.endPoint = CGPoint(x: 0.5, y: 1)
        layer.endPoint = CGPoint(x: 0.5, y: 1)
        //: layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: "#322870")!.cgColor]
        layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: (kMakeText.capitalized))!.cgColor]
        //: layer.locations = [0, 1]
        layer.locations = [0, 1]
        //: btn.layer.addSublayer(layer)
        btn.layer.addSublayer(layer)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var svgaPlayer: SVGAPlayer = {
    private lazy var svgaPlayer: SVGAPlayer = {
        //: let player = SVGAPlayer.init()
        let player = SVGAPlayer()
        //: player.loops = 0
        player.loops = 0
        //: player.clearsAfterStop = false
        player.clearsAfterStop = false
        //: player.isUserInteractionEnabled = false
        player.isUserInteractionEnabled = false
        //: player.contentMode = .scaleAspectFill
        player.contentMode = .scaleAspectFill

        //: return player
        return player
        //: }()
    }()

    //: private lazy var tipsLab: UILabel = {
    private lazy var tipsLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.boldSystemFont(ofSize: 20)
        lab.font = UIFont.boldSystemFont(ofSize: 20)
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "Tap to start video chatting".localized
        lab.text = (String(data_logTitle.prefix(5)) + "o start" + String(userMainUrl)).localized
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var closeBtn: UIButton = {
    private lazy var closeBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_close_nor"), for: .normal)
        btn.setImage(UIImage.bundle(name: (String(k_viewPanNowId))), for: .normal)
        //: btn.addTarget(self, action: #selector(closeButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(executeLabel), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var chattingNumBtn: TalkingButton = {
    private lazy var chattingNumBtn: BlockShouldShirtButton = {
        //: let btn = TalkingButton()
        let btn = BlockShouldShirtButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_views_number"), for: .normal)
        btn.setImage(UIImage.bundle(name: (data_dayName.lowercased() + "_vie" + String(userRefreshName.suffix(4)) + appTouchId.lowercased())), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_views_number"), for: .disabled)
        btn.setImage(UIImage.bundle(name: (data_dayName.lowercased() + "_vie" + String(userRefreshName.suffix(4)) + appTouchId.lowercased())), for: .disabled)
        //: btn.spaceBetweenTitleAndImage = 5
        btn.spaceBetweenTitleAndImage = 5
        //: btn.isEnabled = false
        btn.isEnabled = false
        //: btn.setTitleColor(UIColor.white, for: .normal)
        btn.setTitleColor(UIColor.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Regular, fontSize: 15)
        btn.titleLabel?.font = UIFont.replyName(type: .Regular, fontSize: 15)
        //: btn.setTitle("%@ chatting".localizedArguments(chattingNum), for: .normal)
        btn.setTitle((String(const_sourceStr) + constFeeContent.replacingOccurrences(of: "size", with: "g")).cellArguments(chattingNum), for: .normal)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var priceStackView: UIStackView = {
    private lazy var priceStackView: UIStackView = {
        //: let v = UIStackView()
        let v = UIStackView()
        //: v.axis = .horizontal
        v.axis = .horizontal
        //: v.alignment = .center
        v.alignment = .center
        //: v.spacing = 4
        v.spacing = 4
        //: v.isUserInteractionEnabled = false
        v.isUserInteractionEnabled = false
        //: return v
        return v
        //: }()
    }()

    //: private lazy var vipIcon: UIImageView = {
    private lazy var vipIcon: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.BundleImageNamed(name: "icon_lounge")
        img.image = UIImage.bundle(name: (String(kMakeBotData)))
        //: img.contentMode = .scaleAspectFill
        img.contentMode = .scaleAspectFill
        //: return img
        return img
        //: }()
    }()

    //: private lazy var goldCoinIcon: UIImageView = {
    private lazy var goldCoinIcon: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.BundleImageNamed(name: "icon_rank_coin")
        img.image = UIImage.bundle(name: (String(showIndexMessage.suffix(5)) + "rank_" + user_inputKey.replacingOccurrences(of: "center", with: "c")))
        //: return img
        return img
        //: }()
    }()

    //: private lazy var priceLab: UILabel = {
    private lazy var priceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.pingfangFont(type: .Regular, fontSize: 15)
        lab.font = UIFont.replyName(type: .Regular, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var vipTopImgView: UIImageView = {
    private lazy var vipTopImgView: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.isHidden = true
        imgV.isHidden = true
        //: imgV.image  = UIImage.BundleImageNamed(name: "vip_btn_top")
        imgV.image = UIImage.bundle(name: (data_keySharedText.replacingOccurrences(of: "video", with: "v") + String(const_lessTitle)))
        //: return imgV
        return imgV
        //: }()
    }()

    // 会员折扣价
    //: private lazy var vipPriceBtn: TalkingButton = {
    private lazy var vipPriceBtn: BlockShouldShirtButton = {
        //: let btn = TalkingButton()
        let btn = BlockShouldShirtButton()
        //: btn.backgroundColor = UIColor.white.withAlphaComponent(0.1)
        btn.backgroundColor = UIColor.white.withAlphaComponent(0.1)
        //: btn.layer.cornerRadius = actualWidth(w: 27)
        btn.layer.cornerRadius = actualWidth(w: 27)
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.addTarget(self, action: #selector(maleVipPriceButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(modelFor), for: .touchUpInside)
        //: btn.isHidden = true
        btn.isHidden = true
        //: btn.setBackgroundImage(UIImage.BundleImageNamed(name: "btn_nearby_settings_nor"), for: .normal)
        btn.setBackgroundImage(UIImage.bundle(name: (String(userAssetName.prefix(5)) + "earby" + String(appClickStr.prefix(5)) + "ings" + String(data_nameText.suffix(4)))), for: .normal)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var vipPriceImg: UIImageView = {
    private lazy var vipPriceImg: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.image  = UIImage.BundleImageNamed(name: "icon_rank_coin")
        imgV.image = UIImage.bundle(name: (String(showIndexMessage.suffix(5)) + "rank_" + user_inputKey.replacingOccurrences(of: "center", with: "c")))
        //: return imgV
        return imgV
        //: }()
    }()

    //: private lazy var vipPriceLab: UILabel = {
    private lazy var vipPriceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .userVipColor()
        lab.textColor = .videoColor()
        //: lab.text = "%@ Coins/min".localizedArguments(vipFee)
        lab.text = (String(dataToTitlePath.suffix(5)) + String(kExplainId.suffix(7))).cellArguments(vipFee)
        //: lab.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        lab.font = UIFont.replyName(type: .Medium, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var norPriceLab: UILabel = {
    private lazy var norPriceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: return lab
        return lab
        //: }()
    }()

    // 免费视频挽留弹窗
    //: private lazy var freeVideoAlert: TalkingLiveAlertView = {
    private lazy var freeVideoAlert: LevelReactiveCompatible = //: return LevelReactiveCompatible(frame: .zero, type: .freeVideo)
        .init(frame: .zero, type: .freeVideo)
    //: }()
}

// MARK: - Request & 数据处理

//: extension TalkingRandomVideoViewController {
extension DeformErrorDelegate {
    //: private func setPlayer(isRandon: Bool) {
    private func sign(isRandon: Bool) {
        //: let url = SVGAEffectTool.default.getZipEffectPath(type: isRandon ? .Random_video_bg : .Random_video_bg_nor )
        let url = ThemeReactiveCompatible.default.windowMake(type: isRandon ? .Random_video_bg : .Random_video_bg_nor)
        //: do {
        do {
            //: let data = try Data(contentsOf: url)
            let data = try Data(contentsOf: url)
            //: let parser = SVGAParser.init()
            let parser = SVGAParser()
            //: parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
            parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: svgaPlayer.videoItem = videoItem
                svgaPlayer.videoItem = videoItem
                //: svgaPlayer.startAnimation()
                svgaPlayer.startAnimation()
            }
            //: } catch {
        } catch {
            //: printLog(message: "get json error")
            printLog(message: (String(user_levelKey.suffix(6)) + "on error"))
        }
    }

    /// 开始随机视频（男）
    /// - Parameters:
    ///   - cmd: 事件名
    ///   - errno: cmd的错误码
    //: private func req_randomVideoToMatch(cmd: String, errno: String) {
    private func nameAreaPlayer(cmd: String, errno: String) {
        //: beginTime = Date().timeIntervalSince1970
        beginTime = Date().timeIntervalSince1970
        //: let params = ["matchId": self.matchId,
        let params = [(String(show_commitName.suffix(7))): self.matchId,
                      //: "cmd": cmd,
                      "cmd": cmd,
                      //: "errno": errno] as [String: Any]
                      String(bytes: data_kitEqualName.map{addCurrent(make: $0)}, encoding: .utf8)!: errno] as [String: Any]
        //: TalkingQuickVideoManager.req_randomVideoToMatch(params: params) { succeed, result, errorModel in
        ScreenVideoManager.alongNeed(params: params) { succeed, result, _ in
            //: guard succeed == true else {
            guard succeed == true else {
                //: self.stop_randomVideo()
                self.pathTap()
                //: return
                return
            }
            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())
            //: self.matchId = json["matchId"].intValue
            self.matchId = json[(String(show_commitName.suffix(7)))].intValue
        }
    }

    /// 更新VIP价格
    //: @objc private func req_updateVIPFee() {
    @objc private func rangeLicense() {
        //: TalkingQuickVideoManager.req_videoMatchCheck() { succeed, result, errorModel in
        ScreenVideoManager.roundView { succeed, result, _ in
            //: guard succeed else { return }
            guard succeed else { return }
            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())
            //: self.vipFee = json["VIPFee"].intValue
            self.vipFee = json[(String(main_modelUrl.prefix(5)) + appGiftTitle.lowercased())].intValue
            //: self.refreshVideoCallPrice()
            self.sizePrice()
        }
    }
}

// MARK: - Event

//: extension TalkingRandomVideoViewController {
extension DeformErrorDelegate {
    /// 男性侧vip价格按钮点击事件
    //: @objc private func maleVipPriceButtonClick() {
    @objc private func modelFor() {
        //: CombineAccountEnclaveDecisionMakerThen.share.func__pushToSubscribeAlert()
        CombineAccountEnclaveDecisionMakerThen.share.colorMoment()
    }

    /// 关闭按钮点击事件
    //: @objc func closeButtonClick() {
    @objc func executeLabel() {
        //: dispatchWorkItem?.cancel()
        dispatchWorkItem?.cancel()
        //: showCloseBtn()
        toReply()
        //: uploadRecord.uploadRecordEvent(eventID: ClickRandomvideoCancelNoP)
        showVideoPath.referenceStr(eventID: show_statusTitle)

        //: if IconContainerAppManager.share.appConfigMode.displayMatchV4PopUp, IconContainerAppManager.share.loginUserMode.freeCallTimes > 0,
        if IconContainerAppManager.share.appConfigMode.displayMatchV4PopUp, IconContainerAppManager.share.loginUserMode.freeCallTimes > 0,
           //: isShowFreeVideo == false {
           isShowFreeVideo == false
        {
            //: self.freeVideoAlert.show()
            self.freeVideoAlert.largesseName()
            //: isShowFreeVideo = true
            isShowFreeVideo = true
            //: return
            return
        }
        //: currentViewController()?.navigationController?.popViewController(animated: true)
        actionLatest()?.navigationController?.popViewController(animated: true)
    }

    /// 开始随机视频
    //: @objc private func start_randomVideo() {
    @objc private func iconLay() {
        //: guard TalkingSocketManager.shared.isName == false else {
        guard GiftCommentSocketDelegate.shared.isName == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.explain(showMsg: user_topPath)
            //: return
            return
        }
        //: guard gradientBtn.isEnabled == true else { return }
        guard gradientBtn.isEnabled == true else { return }
        //: afterShowClose()
        towardBringHome()
        //: self.matchId = 0
        self.matchId = 0 // 重置匹配池
        //: gradientBtn.isEnabled = false
        gradientBtn.isEnabled = false
        //: tipsLab.text = "Matching now".localized
        tipsLab.text = (String(app_viewTitle) + String(user_directionPath.suffix(6))).localized
        //: setPlayer(isRandon: true)
        sign(isRandon: true)
        //: priceStackView.isHidden = true
        priceStackView.isHidden = true
        //: vipPriceBtn.isHidden = true
        vipPriceBtn.isHidden = true
        //: vipTopImgView.isHidden = true
        vipTopImgView.isHidden = true
        //: if IconContainerAppManager.share.loginUserMode.randomVideoType == 2 {
        if IconContainerAppManager.share.loginUserMode.randomVideoType == 2 {
            //: uploadRecord.uploadRecordEvent(eventID: ClickRobotRandomButton)
            showVideoPath.referenceStr(eventID: showInformationTextValue)
            //: req_getVideoInfo()
            technicality()
            //: } else {
        } else {
            //: uploadRecord.uploadRecordEvent(eventID: ClickRandomvideoCallNoP)
            showVideoPath.referenceStr(eventID: show_methodName)

            //: TalkingSocketManager.shared.videoMatchDelegate = self
            GiftCommentSocketDelegate.shared.videoMatchDelegate = self
            //: TalkingSocketManager.shared.callerDelegate = self
            GiftCommentSocketDelegate.shared.callerDelegate = self
            //: TalkingSocketManager.shared.calledPartyDelegate = self
            GiftCommentSocketDelegate.shared.calledPartyDelegate = self
            //: TalkingSocketManager.shared.errorDelegate = self
            GiftCommentSocketDelegate.shared.errorDelegate = self
            //: restart_randomVideo()
            levelTo()
        }
    }

    /// 重新开始视频速配
    /// - Parameters:
    ///   - cmd: 事件名（默认用户主动点击）
    ///   - errno: cmd的错误码
    //: private func restart_randomVideo(cmd: String = "userTouch", errno: String = "0") {
    private func levelTo(cmd: String = (showMessagePopText.replacingOccurrences(of: "style", with: "u") + String(noti_targetId)), errno: String = "0") {
        // 停止定时器
        //: destroyWaitingTimer()
        fromConversion()
        //: req_randomVideoToMatch(cmd: cmd, errno: errno)
        nameAreaPlayer(cmd: cmd, errno: errno)
    }

    /// 停止随机视频
    //: private func stop_randomVideo() {
    private func pathTap() {
        // 停止定时器
        //: destroyWaitingTimer()
        fromConversion()
        //: showCloseBtn()
        toReply()

        //: gradientBtn.isEnabled = true
        gradientBtn.isEnabled = true
        //: tipsLab.text = "Tap to start video chatting".localized
        tipsLab.text = (String(data_logTitle.prefix(5)) + "o start" + String(userMainUrl)).localized
        //: setPlayer(isRandon: false)
        sign(isRandon: false)
        //: self.isFirstRandow = false
        self.isFirstRandow = false
        //: if TalkingSocketManager.shared.isName == false {
        if GiftCommentSocketDelegate.shared.isName == false {
            //: TalkingSocketManager.shared.videoMatchDelegate = nil
            GiftCommentSocketDelegate.shared.videoMatchDelegate = nil
            //: TalkingSocketManager.shared.callerDelegate = nil
            GiftCommentSocketDelegate.shared.callerDelegate = nil
            //: TalkingSocketManager.shared.calledPartyDelegate = nil
            GiftCommentSocketDelegate.shared.calledPartyDelegate = nil
            //: TalkingSocketManager.shared.errorDelegate = nil
            GiftCommentSocketDelegate.shared.errorDelegate = nil
        }
        //: refreshVideoCallPrice()
        sizePrice()
    }

    /// 销毁定时器
    //: private func destroyWaitingTimer() {
    private func fromConversion() {
        //: if self.waitingTimer != nil {
        if self.waitingTimer != nil {
            //: self.waitingTimer?.invalidate()
            self.waitingTimer?.invalidate()
            //: self.waitingTimer = nil
            self.waitingTimer = nil
        }

        //: if self.retryAfterTimer != nil {
        if self.retryAfterTimer != nil {
            //: self.retryAfterTimer?.invalidate()
            self.retryAfterTimer?.invalidate()
            //: self.retryAfterTimer = nil
            self.retryAfterTimer = nil
        }
    }

    /// 获取用户主动拨打视频信息
    //: @objc private func req_getVideoInfo() {
    @objc private func technicality() {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = SaveThen()
        //: reqModel.requestPath = "mf/freeCall/getVideoInfo"
        reqModel.requestPath = (String(kAppIconKey.suffix(6)) + "eCal" + String(dataManagerMakeMessage.suffix(8)) + String(const_dismissName) + String(data_replyStr.prefix(5)))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["fromType": "1"]
        reqModel.params = [String(bytes: dataTextStr.reversed(), encoding: .utf8)!: "1"]
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        mainMaleKey.neighbor(model: reqModel) { succeed, result, errorModel in

            //: guard succeed == true else {
            guard succeed == true else {
                //: self.stop_randomVideo()
                self.pathTap()
                //: if errorModel?.errorCode ?? 1 > 0 {
                if errorModel?.errorCode ?? 1 > 0 {
                    //: IconContainerAppManager.share.loginUserMode.randomVideoType = 1
                    IconContainerAppManager.share.loginUserMode.randomVideoType = 1
                    //: self.start_randomVideo()
                    self.iconLay()
                    //: return
                    return
                }
                //: self.func__showStatusBarErrorMsg(showMsg: errorModel?.errorMsg ?? "")
                self.explain(showMsg: errorModel?.errorMsg ?? "")
                //: return
                return
            }
            //: guard let result = result as? Dictionary<String, Any> else {
            guard let result = result as? [String: Any] else {
                //: return
                return
            }
            //: if let model = TalkingVideoInitivCallModel.deserialize(from: result as? Dictionary) {
            if let model = ExaggerateHandyJSON.deserialize(from: result as? Dictionary) {
                /// 预加载
                //: let  player = TalkingVideoPlayerManager.init()
                let player = EventReactiveCompatible()
                //: player.setMute(bEnable: true)
                player.mute(bEnable: true)
                //: player.playerWithUrlAndVideoView(url: model.winVideoUrl, view: UIView.init())
                player.cableTelevision(url: model.winVideoUrl, view: UIView())

                //: TalkingVideoInitivCallManager.shared.videoCallModel = model
                AtNameThen.shared.videoCallModel = model

                //: TalkingPermissionTool.checkCameraAndMicrophone { [weak self] isOpen in
                CellPermissionTool.progressAction { [weak self] isOpen in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: guard isOpen == true else { return }
                    guard isOpen == true else { return }

                    //: self.putThrough()
                    self.bluntThrough()
                }
            }
        }
    }

    /// 接通电话
    //: private func putThrough() {
    private func bluntThrough() {
        //: uploadRecord.uploadRecordEvent(eventID: clickVideoAcceptButton)
        showVideoPath.referenceStr(eventID: appFailMessage)
        /// 视频通话被禁用
        //: guard TalkingVideoInitivCallManager.shared.videoCallModel.isBan == 0 else {
        guard AtNameThen.shared.videoCallModel.isBan == 0 else {
            //: self.func__showStatusBarErrorMsg(showMsg: "You've been barred from receiving calls".localized)
            self.explain(showMsg: String(bytes: mainMagnitudeId.map{makeStroke(show: $0)}, encoding: .utf8)!.localized)
            //: return
            return
        }
        //: self.dispatchWorkItem = DispatchWorkItem {
        self.dispatchWorkItem = DispatchWorkItem {
            //: TalkingVideoInitivCallManager.shared.req_userOperation(type: 1) { succeed, result, errorModel in
            AtNameThen.shared.pathCompletion(type: 1) { succeed, _, _ in
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: guard succeed == true else {
                guard succeed == true else {
                    //: self.stop_randomVideo()
                    self.pathTap()
                    //: return
                    return
                }
                //: let vc = TalkingVideoInitivCallVC()
                let vc = ResponseUserPlayerDelegate()
                //: vc.isRandomVCPush = true
                vc.isRandomVCPush = true
                //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
                self.actionLatest()?.navigationController?.pushViewController(vc, animated: true)
                //: self.stop_randomVideo()
                self.pathTap()
            }
        }
        // 添加延时任务
        //: DispatchQueue.main.asyncAfter(deadline: .now() + 3.0, execute: self.dispatchWorkItem!)
        DispatchQueue.main.asyncAfter(deadline: .now() + 3.0, execute: self.dispatchWorkItem!)
    }

    /// 匹配时x秒显示关闭按钮
    //: private func afterShowClose() {
    private func towardBringHome() {
        //: if IconContainerAppManager.share.appUserConfigMode.matchCancelBtnDelayTime > 0 {
        if IconContainerAppManager.share.appUserConfigMode.matchCancelBtnDelayTime > 0 {
            //: self.closeBtn.isHidden = true
            self.closeBtn.isHidden = true
            //: closeBtnWorkItem = DispatchWorkItem { [weak self] in
            closeBtnWorkItem = DispatchWorkItem { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.closeBtn.isHidden = false
                self.closeBtn.isHidden = false
            }
            //: DispatchQueue.main.asyncAfter(deadline: .now() + Double(IconContainerAppManager.share.appUserConfigMode.matchCancelBtnDelayTime), execute: closeBtnWorkItem!)
            DispatchQueue.main.asyncAfter(deadline: .now() + Double(IconContainerAppManager.share.appUserConfigMode.matchCancelBtnDelayTime), execute: closeBtnWorkItem!)
        }
    }

    //: private func showCloseBtn() {
    private func toReply() {
        //: closeBtnWorkItem?.cancel()
        closeBtnWorkItem?.cancel()
        //: self.closeBtn.isHidden = false
        self.closeBtn.isHidden = false
    }

    //: private func resetCamera() {
    private func upGift() {
        //: guard TalkingSocketManager.shared.isName == false else {
        guard GiftCommentSocketDelegate.shared.isName == false else {
            //: return
            return
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2.0) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2.0) {
            //: self.videoManager.infoModel?.videoStage = .Waiting
            self.videoManager.infoModel?.videoStage = .Waiting
            //: self.videoManager.config.openCamera = true
            self.videoManager.config.openCamera = true
            //: self.videoManager.isOpenTRTCPreview = false
            self.videoManager.isOpenTRTCPreview = false
            //: self.videoManager.startPreview()
            self.videoManager.fromSub()
        }
    }
}

// MARK: - ThirdObjectProtocol

//: extension TalkingRandomVideoViewController: SocketManagerVideoMatchDelegate {
extension DeformErrorDelegate: ThirdObjectProtocol {
    /// 视频速配成功
    //: func socket_match_onVideoMatch(data: [String: Any]) {
    func rime(data: [String: Any]) {
        // 拨打视频通话
        //: guard let model = TalkingMatchResultModel.deserialize(from: data) else { return }
        guard let model = IndexNameModelType.deserialize(from: data) else { return }
        //: self.matchId = model.matchId
        self.matchId = model.matchId
        //: var callData: [String: Any] = ["matchId": model.matchId,
        var callData: [String: Any] = [(String(show_commitName.suffix(7))): model.matchId,
                                       //: "source": model.source,
                                       String(bytes: userPushName.map{labShare(table: $0)}, encoding: .utf8)!: model.source,
                                       //: "type": model.type,
                                       String(bytes: notiSucceedStr.map{$0^129}, encoding: .utf8)!: model.type,
                                       //: "uid": model.uid]
                                       "uid": model.uid]
        //: let index = EnableFreeCallType.randow
        let index = CommentBinaryNumber.randow
        //: if IconContainerAppManager.share.loginUserMode.freeCallTimes > 0 && IconContainerAppManager.share.appUserConfigMode.enableFreeCallType & index.rawValue == index.rawValue {
        if IconContainerAppManager.share.loginUserMode.freeCallTimes > 0 && IconContainerAppManager.share.appUserConfigMode.enableFreeCallType & index.rawValue == index.rawValue {
            //: callData.updateValue(1, forKey: "fromFreeCall")
            callData.updateValue(1, forKey: (String(const_colorValue.prefix(7)) + String(const_giftText.prefix(5))))
            //: TalkingSocketManager.shared.isFreeCall = true
            GiftCommentSocketDelegate.shared.isFreeCall = true
        }
        //: let info: [String: Any] = ["cmd": "requestCall", "data": callData]
        let info: [String: Any] = ["cmd": (String(userLayerValue) + String(mainExhibitMsg)), String(bytes: constPriceMsg.map{titleTheory(blue: $0)}, encoding: .utf8)!: callData]
        //: TalkingSocketManager.shared.sendMessage(info: info)
        GiftCommentSocketDelegate.shared.agnomen(info: info)
        // 15s未接听主动挂断
        //: waitingTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(15), block: { [weak self] timer in
        waitingTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(15), block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.logId]])
            GiftCommentSocketDelegate.shared.agnomen(info: ["cmd": (String(notiTimeMsg) + userAppStr.replacingOccurrences(of: "make", with: "l") + "Call"), String(bytes: constPriceMsg.map{titleTheory(blue: $0)}, encoding: .utf8)!: [(String(show_addWindowKey.prefix(5))): self.logId]])
            //: self.restart_randomVideo(cmd: "onVideoMatch")
            self.levelTo(cmd: (String(app_userMsg) + notiNowFormat.replacingOccurrences(of: "object", with: "eo") + String(dataMakeId.prefix(5))))
            //: }, repeats: false)
        }, repeats: false)
    }

    //: func socket_match_onRequestCall(data: [String: Any]) {
    func cardPlayer(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: self.logId = json["logId"].intValue
        self.logId = json[(String(show_addWindowKey.prefix(5)))].intValue
    }
}

// MARK: - GestureObjectProtocol【视频拨打方处理】

//: extension TalkingRandomVideoViewController: SocketManagerVideoChatCallerDelegate {
extension DeformErrorDelegate: GestureObjectProtocol {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {}
    func imageSocketModel(data _: [String: Any]) {}

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {}
    func bindRequest(data _: [String: Any]) {}

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {}
    func contentData(msg _: String, data _: [String: Any]) {}

    /// 对方接受了来电，建立视频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func untilAttentionRemove(data: [String: Any]) {
        // 应用在后台，先提醒用户
        //: if UIApplication.shared.applicationState == .background {
        if UIApplication.shared.applicationState == .background {
            //: AppDelegateHelper.pushLocalNotification(body: "[Automatic video] Receive video call incoming".localized)
            BlockReactiveCompatible.face(body: String(bytes: noti_labelLayerContent.map{afterType(user: $0)}, encoding: .utf8)!.localized)
        }
        //: let model = TalkingMatchResultModel.deserialize(from: data)
        let model = IndexNameModelType.deserialize(from: data)
        //: if let uid = model?.uid {
        if let uid = model?.uid {
            //: uploadSuccessTime(toUid: String(uid))
            eventUid(toUid: String(uid))
        }
        //: stop_randomVideo()
        pathTap()
        // 开启视频通话时先销毁当前预览画面
        //: videoManager.shouldReleaseAllResource()
        videoManager.liveResource()
        //: IconContainerAppManager.share.start1v1TalkCall(info: data, situation: .Calling)
        IconContainerAppManager.share.comeDoing(info: data, situation: .Calling)
    }
}

// MARK: - CaliberObjectProtocol【接收到回执代理】

//: extension TalkingRandomVideoViewController: SocketManagerVideoChatCalledPartyDelegate {
extension DeformErrorDelegate: CaliberObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func socketOrRawDataEqual(data _: [String: Any]) {
        //: restart_randomVideo(cmd: "refuseCall")
        levelTo(cmd: (user_sharedFieldText.replacingOccurrences(of: "input", with: "e") + "eCall"))
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {}
    func theTool(data _: [String: Any]) {}
}

// MARK: - SupplantObjectProtocol

//: extension TalkingRandomVideoViewController: TalkingSocketManagerErrorDelegate {
extension DeformErrorDelegate: SupplantObjectProtocol {
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func titleData(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
        //: if cmd == "onCancelCall" {  // 取消通话异常
        if cmd == (String(const_indexKey.suffix(6)) + String(constLabFormat)) { // 取消通话异常
            //: stop_randomVideo()
            pathTap()
            //: return
            return
        }
        //: if cmd == "onVideoMatch", errorNo == 404 { // 匹配池用完
        if cmd == (String(app_userMsg) + notiNowFormat.replacingOccurrences(of: "object", with: "eo") + String(dataMakeId.prefix(5))), errorNo == 404 { // 匹配池用完
            //: stop_randomVideo()
            pathTap()
            //: let json = JSON(data)
            let json = JSON(data)
            //: self.matchId = json["matchId"].intValue
            self.matchId = json[(String(show_commitName.suffix(7)))].intValue
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.explain(showMsg: msg)
            //: uploadFailTime(type: 2)
            fileAdd(type: 2)
            //: return
            return
        }
        //: if cmd == "onVideoMatch", errorNo == 449 { // 匹配池用完，延时重试
        if cmd == (String(app_userMsg) + notiNowFormat.replacingOccurrences(of: "object", with: "eo") + String(dataMakeId.prefix(5))), errorNo == 449 { // 匹配池用完，延时重试
            //: let json = JSON(data)
            let json = JSON(data)
            //: retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json["retryAfter"].intValue), block: { [weak self] timer in
            retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json[(String(mainFrameContent.prefix(6)) + notiFileData.replacingOccurrences(of: "container", with: "e"))].intValue), block: { [weak self] _ in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.matchId = json["matchId"].intValue
                self.matchId = json[(String(show_commitName.suffix(7)))].intValue
                //: self.restart_randomVideo(cmd: cmd, errno: String(errorNo))
                self.levelTo(cmd: cmd, errno: String(errorNo))
                //: }, repeats: false)
            }, repeats: false)
            //: return
            return
        }
        //: if cmd == "onRequestCall", errorNo == VideoChatErrorCode.MoneyLack.rawValue { // 余额不足
        if cmd == (String(constModelId) + showComponentValue.lowercased()), errorNo == BushidoDefaultsSerializable.MoneyLack.rawValue { // 余额不足
            //: stop_randomVideo()
            pathTap()
            //: guard IconContainerAppManager.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard IconContainerAppManager.share.appStatus == MinExpressionConvertible.normal.rawValue else { return }
            //: CombineAccountEnclaveDecisionMakerThen.share.func__jumpToWebRecharge(clickEvent: clickRandomvideoButton, sufficient: false)
            CombineAccountEnclaveDecisionMakerThen.share.toMake(clickEvent: mainProductionName, sufficient: false)
            //: uploadFailTime(type: 1)
            fileAdd(type: 1)
            //: return
            return
        }

        //: restart_randomVideo(cmd: cmd, errno: String(errorNo))
        levelTo(cmd: cmd, errno: String(errorNo))
    }
}

// MARK: - 埋点

//: extension TalkingRandomVideoViewController {
extension DeformErrorDelegate {
    //: func uploadFailTime(type: Int) {
    func fileAdd(type: Int) {
        //: let nowtime = Date().timeIntervalSince1970
        let nowtime = Date().timeIntervalSince1970
        //: let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        //: var parm = Dictionary<String, Any>()
        var parm = [String: Any]()
        //: parm["failType"] = type
        parm[(String(dataImageTapMsg.suffix(8)))] = type
        //: parm["callWaitingTime"] = time
        parm[(String(show_picUrl) + String(userByUrl))] = time
        //: uploadRecord.uploadRecordEvent(eventID: RandomvideoFailwaitTime, parameterStr: parm)
        showVideoPath.iconStr(eventID: kVideoPath, parameterStr: parm)
    }

    //: func uploadSuccessTime(toUid: String) {
    func eventUid(toUid: String) {
        //: let nowtime = Date().timeIntervalSince1970
        let nowtime = Date().timeIntervalSince1970
        //: let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        //: var parm = Dictionary<String, Any>()
        var parm = [String: Any]()
        //: parm["callWaitingTime"] = time
        parm[(String(show_picUrl) + String(userByUrl))] = time
        //: parm["toUid"] = toUid
        parm[(String(data_viewFormat.prefix(5)))] = toUid
        //: uploadRecord.uploadRecordEvent(eventID: RandomvideowaitTime, parameterStr: parm)
        showVideoPath.iconStr(eventID: show_beginTitle, parameterStr: parm)
    }
}

// MARK: - Layout

//: extension TalkingRandomVideoViewController {
extension DeformErrorDelegate {
    /// 刷新视频通话价格
    //: @objc private func refreshVideoCallPrice() {
    @objc private func sizePrice() {
        //: guard IconContainerAppManager.share.appStatus == AppSkinStatus.normal.rawValue else {
        guard IconContainerAppManager.share.appStatus == MinExpressionConvertible.normal.rawValue else {
            //: priceStackView.isHidden = true
            priceStackView.isHidden = true
            //: vipPriceBtn.isHidden = true
            vipPriceBtn.isHidden = true
            //: vipTopImgView.isHidden = true
            vipTopImgView.isHidden = true
            //: return
            return
        }

        //: vipIcon.isHidden = !IconContainerAppManager.share.loginUserMode.loungePlus
        vipIcon.isHidden = !IconContainerAppManager.share.loginUserMode.loungePlus
        //: priceStackView.isHidden = false
        priceStackView.isHidden = false
        //: let indexType = EnableFreeCallType.randow
        let indexType = CommentBinaryNumber.randow
        //: if IconContainerAppManager.share.loginUserMode.freeCallTimes > 0 && IconContainerAppManager.share.appUserConfigMode.enableFreeCallType & indexType.rawValue == indexType.rawValue {
        if IconContainerAppManager.share.loginUserMode.freeCallTimes > 0, IconContainerAppManager.share.appUserConfigMode.enableFreeCallType & indexType.rawValue == indexType.rawValue { // 有免费次数
            //: if IconContainerAppManager.share.appUserConfigMode.callBtnStyle == 1 {
            if IconContainerAppManager.share.appUserConfigMode.callBtnStyle == 1 {
                //: priceLab.text = "Free 1 min".localized
                priceLab.text = (String(appMatchId)).localized
                //: } else {
            } else {
                //: priceStackView.isHidden = true
                priceStackView.isHidden = true
            }
            //: vipPriceBtn.isHidden = true
            vipPriceBtn.isHidden = true
            //: vipTopImgView.isHidden = true
            vipTopImgView.isHidden = true

            //: } else {
        } else { // 无免费次数, 展示价格
            //: let price = IconContainerAppManager.share.loginUserMode.loungePlus ? vipFee:fee
            let price = IconContainerAppManager.share.loginUserMode.loungePlus ? vipFee : fee
            //: priceLab.text = "%@/min".localizedArguments(price)
            priceLab.text = "%@/min".cellArguments(price)

            //: let attr = NSMutableAttributedString(string: "%@/min".localizedArguments(price), attributes: [.strikethroughStyle : NSUnderlineStyle.single.rawValue, .foregroundColor: UIColor.white.withAlphaComponent(0.5), .font: UIFont.pingfangMediumFont(fontSize: 10)])
            let attr = NSMutableAttributedString(string: "%@/min".cellArguments(price), attributes: [.strikethroughStyle: NSUnderlineStyle.single.rawValue, .foregroundColor: UIColor.white.withAlphaComponent(0.5), .font: UIFont.font(fontSize: 10)])
            //: norPriceLab.attributedText = attr
            norPriceLab.attributedText = attr
            //: vipPriceBtn.isHidden = IconContainerAppManager.share.loginUserMode.loungePlus
            vipPriceBtn.isHidden = IconContainerAppManager.share.loginUserMode.loungePlus
            //: vipTopImgView.isHidden = IconContainerAppManager.share.loginUserMode.loungePlus
            vipTopImgView.isHidden = IconContainerAppManager.share.loginUserMode.loungePlus

            //: priceStackView.isHidden = !IconContainerAppManager.share.loginUserMode.loungePlus
            priceStackView.isHidden = !IconContainerAppManager.share.loginUserMode.loungePlus
        }
    }

    /// 添加视图
    //: private func setupSubviews() {
    private func arcWindowBackground() {
        //: view.addSubview(renderView)
        view.addSubview(renderView)
        //: view.addSubview(gradientBtn)
        view.addSubview(gradientBtn)
        //: view.addSubview(svgaPlayer)
        view.addSubview(svgaPlayer)
        //: view.addSubview(tipsLab)
        view.addSubview(tipsLab)
        //: view.addSubview(chattingNumBtn)
        view.addSubview(chattingNumBtn)
        //: view.addSubview(priceStackView)
        view.addSubview(priceStackView)
        //: priceStackView.addArrangedSubview(vipIcon)
        priceStackView.addArrangedSubview(vipIcon)
        //: priceStackView.addArrangedSubview(goldCoinIcon)
        priceStackView.addArrangedSubview(goldCoinIcon)
        //: priceStackView.addArrangedSubview(priceLab)
        priceStackView.addArrangedSubview(priceLab)
        //: view.addSubview(vipPriceBtn)
        view.addSubview(vipPriceBtn)
        //: view.addSubview(vipTopImgView)
        view.addSubview(vipTopImgView)
        //: vipPriceBtn.addSubview(vipPriceImg)
        vipPriceBtn.addSubview(vipPriceImg)
        //: vipPriceBtn.addSubview(vipPriceLab)
        vipPriceBtn.addSubview(vipPriceLab)
        //: vipPriceBtn.addSubview(norPriceLab)
        vipPriceBtn.addSubview(norPriceLab)

        //: view.addSubview(closeBtn)
        view.addSubview(closeBtn)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func setupImage() {
        //: renderView.snp.makeConstraints { make in
        renderView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: gradientBtn.snp.makeConstraints { make in
        gradientBtn.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: svgaPlayer.snp.makeConstraints { make in
        svgaPlayer.snp.makeConstraints { make in
            //: make.leading.equalTo(-8)
            make.leading.equalTo(-8)
            //: make.trailing.equalTo(8)
            make.trailing.equalTo(8)
            //: make.centerY.equalToSuperview()
            make.centerY.equalToSuperview()
            //: make.height.equalTo(actualWidth(w: 395))
            make.height.equalTo(actualWidth(w: 395))
        }
        //: tipsLab.snp.makeConstraints { make in
        tipsLab.snp.makeConstraints { make in
            //: make.bottom.equalTo(svgaPlayer.snp.bottom).offset(-40)
            make.bottom.equalTo(svgaPlayer.snp.bottom).offset(-40)
            //: make.width.centerX.equalToSuperview()
            make.width.centerX.equalToSuperview()
        }
        //: chattingNumBtn.snp.makeConstraints { make in
        chattingNumBtn.snp.makeConstraints { make in
            //: make.top.equalTo(tipsLab.snp.bottom).offset(35)
            make.top.equalTo(tipsLab.snp.bottom).offset(35)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }

        //: priceStackView.snp.makeConstraints { make in
        priceStackView.snp.makeConstraints { make in
            //: make.top.equalTo(chattingNumBtn.snp.bottom).offset(8)
            make.top.equalTo(chattingNumBtn.snp.bottom).offset(8)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.height.equalTo(31)
            make.height.equalTo(31)
            //: make.trailing.lessThanOrEqualToSuperview()
            make.trailing.lessThanOrEqualToSuperview()
        }
        //: vipIcon.snp.makeConstraints { make in
        vipIcon.snp.makeConstraints { make in
            //: make.width.height.equalTo(15)
            make.width.height.equalTo(15)
        }
        //: goldCoinIcon.snp.makeConstraints { make in
        goldCoinIcon.snp.makeConstraints { make in
            //: make.width.height.equalTo(actualWidth(w: 15))
            make.width.height.equalTo(actualWidth(w: 15))
        }
        //: priceLab.snp.makeConstraints { make in
        priceLab.snp.makeConstraints { make in
            //: make.height.equalToSuperview()
            make.height.equalToSuperview()
        }

        //: vipPriceBtn.snp.makeConstraints { make in
        vipPriceBtn.snp.makeConstraints { make in
            //: make.top.equalTo(priceStackView.snp.bottom).offset(16)
            make.top.equalTo(priceStackView.snp.bottom).offset(16)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: actualWidth(w: 240), height: actualWidth(w: 54)))
            make.size.equalTo(CGSize(width: actualWidth(w: 240), height: actualWidth(w: 54)))
        }
        //: vipTopImgView.snp.makeConstraints { make in
        vipTopImgView.snp.makeConstraints { make in
            //: make.top.equalTo(vipPriceBtn.snp.top).offset(-12)
            make.top.equalTo(vipPriceBtn.snp.top).offset(-12)
            //: make.leading.equalTo(vipPriceBtn.snp.leading)
            make.leading.equalTo(vipPriceBtn.snp.leading)
            //: make.size.equalTo(CGSize(width: actualWidth(w: 32), height: actualWidth(w: 24)))
            make.size.equalTo(CGSize(width: actualWidth(w: 32), height: actualWidth(w: 24)))
        }

        //: vipPriceImg.snp.makeConstraints { make in
        vipPriceImg.snp.makeConstraints { make in
            //: make.top.equalToSuperview().offset(actualWidth(w: 12))
            make.top.equalToSuperview().offset(actualWidth(w: 12))
            //: make.width.height.equalTo(15)
            make.width.height.equalTo(15)
            //: make.trailing.equalTo(vipPriceLab.snp.leading).offset(-2)
            make.trailing.equalTo(vipPriceLab.snp.leading).offset(-2)
        }

        //: vipPriceLab.snp.makeConstraints { make in
        vipPriceLab.snp.makeConstraints { make in
            //: make.centerY.equalTo(vipPriceImg)
            make.centerY.equalTo(vipPriceImg)
            //: make.centerX.equalToSuperview().offset(8)
            make.centerX.equalToSuperview().offset(8)
        }
        //: norPriceLab.snp.makeConstraints { make in
        norPriceLab.snp.makeConstraints { make in
            //: make.top.equalTo(vipPriceImg.snp.bottom).offset(5)
            make.top.equalTo(vipPriceImg.snp.bottom).offset(5)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }

        //: closeBtn.snp.makeConstraints { make in
        closeBtn.snp.makeConstraints { make in
            //: make.top.equalTo(StatusBarHeight+6)
            make.top.equalTo(app_itemTitle + 6)
            //: make.trailing.equalTo(-10)
            make.trailing.equalTo(-10)
            //: make.width.height.equalTo(actualWidth(w: 44))
            make.width.height.equalTo(actualWidth(w: 44))
        }
    }

    /// 绑定事件
    //: private func bindInteraction() {
    private func colorGift() {
        // index/getconfig接口回调刷新UI
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(refreshVideoCallPrice),
                                               selector: #selector(sizePrice),
                                               //: name: UPDATE_INDEX_GETCONFIG_NOTIFICATION,
                                               name: appFeatureBarTitle,
                                               //: object: nil)
                                               object: nil)
        // 个人信息更新成功刷新UI
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(req_updateVIPFee),
                                               selector: #selector(rangeLicense),
                                               //: name: USER_GETMYINFO_SUCCEED_NOTIFICATION,
                                               name: appRecordScreenPath,
                                               //: object: nil)
                                               object: nil)
    }
}
