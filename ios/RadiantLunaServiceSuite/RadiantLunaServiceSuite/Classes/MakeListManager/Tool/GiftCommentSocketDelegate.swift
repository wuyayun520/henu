
//: Declare String Begin

/*: "SOCKET_ADDR_KEY" :*/
fileprivate let dataIconName:[Character] = ["S","O","C","K","E","T","_","A","D","D"]
fileprivate let app_containRoundViewUrl:[Character] = ["R","_","K","E","Y"]

/*: "SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION" :*/
fileprivate let showViewFormat:[UInt8] = [0xc7,0xdb,0xd7,0xdf,0xd1,0xc0,0xcb,0xc2,0xdd,0xd0,0xd1,0xdb,0xd7,0xdc,0xd5,0xc0,0xcb,0xc6,0xd1,0xc5,0xc1,0xd1,0xc7,0xc0,0xd7,0xd5,0xd8,0xd8,0xcb,0xda,0xdb,0xc0,0xdd,0xd2,0xdd,0xd7,0xd5,0xc0,0xdd,0xdb,0xda]

				private func makePhase(factor num: UInt8) -> UInt8 {
					return num ^ 148
				}

/*: "RECIVE_FLOATSCREEN_NOTIFICATION" :*/
fileprivate let noti_loadMessage:[UInt8] = [0xa6,0x99,0x97,0x9d,0xaa,0x99,0xb3,0x9a,0xa0,0xa3,0x95,0xa8,0xa7,0x97,0xa6,0x99,0x99,0xa2,0xb3,0xa2,0xa3,0xa8,0x9d,0x9a,0x9d,0x97,0x95,0xa8,0x9d,0xa3,0xa2]

				fileprivate func labBorder(model num: UInt8) -> UInt8 {
					let value = Int(num) - 84
					if value < 0 {
						return UInt8(value + 256)
					} else {
						return UInt8(value)
					}
				}

/*: "isCalling" :*/
fileprivate let mainListText:String = "show"
fileprivate let app_colorMonthText:String = "with make post start returnsCal"
 
/*: "SOCKET 连接成功." :*/
fileprivate let dataPointName:String = "SOtrackE"
fileprivate let const_rawPath:String = "T 连\u{63a5}成\u{529f}."

/*: "SOCKET 连接失败, error: :*/
fileprivate let notiNoseWriteContent:String = "SpageKE"
fileprivate let main_whichData:[Character] = ["T"," ","连"]
fileprivate let k_replyInviteValue:String = "接失败, eevaluate make path image"

/*: "SOCKET 失败 :*/
fileprivate let kScaleValue:[Character] = ["S","O","C","K"]
fileprivate let app_atId:String = "ET 失败view female let"

/*: 次, 切换过Socket域名重新尝试，地址： :*/
fileprivate let data_topContent:String = "\u{6b21}, \u{5207}换过S"
fileprivate let constCountColorTitle:[Character] = ["o","c","k"]
fileprivate let showTillUrl:String = "et域名重新if value temp make"

/*: "SOCKET 已关闭, code: :*/
fileprivate let userVoiceMessage:[Character] = ["S","O","C","K","E","T"," ","已","关","闭",","," ","c","o"]
fileprivate let dataNumberimateValue:String = "self cell image self timede:"

/*: , reason: :*/
fileprivate let constMainTitle:String = "make text empty make of, re"
fileprivate let show_toName:String = "ason:var value"

/*: &platform=iphone&version= :*/
fileprivate let constZoneData:[Character] = ["&","p","l","a","t","f","o","r","m","=","i","p","h","o","n","e"]
fileprivate let showMakeKey:String = "eye"
fileprivate let noti_packageMmKey:String = "data model selfversion="

/*: &packageId= :*/
fileprivate let dataIndexPath:[Character] = ["&","p","a","c","k","a","g"]
fileprivate let main_emptyFormat:String = "eId=add var"

/*: "1v1server. :*/
fileprivate let user_targetId:String = "1v1semake equal var activity"
fileprivate let noti_fillMsg:[Character] = ["."]

/*: .com:7766" :*/
fileprivate let show_sizeMessage:String = "."
fileprivate let show_proposalMsg:String = "title action title main taskcom:7766"

/*: "43.129.185.194:7767" :*/
fileprivate let mainNorFormat:String = "43.12"
fileprivate let show_equalName:String = "9.185.1"
fileprivate let data_levelStr:String = "94:7767"

/*: "SOCKET 网络reachabilityChanged监听，断网关闭." :*/
fileprivate let noti_modelName:[UInt8] = [0xa8,0xb4,0xb8,0xb0,0xbe,0xaf,0xdb,0x1c,0x46,0x6a,0x1c,0x40,0x67,0x89,0x9e,0x9a,0x98,0x93,0x9a,0x99,0x92,0x97,0x92,0x8f,0x82,0xb8,0x93,0x9a,0x95,0x9c,0x9e,0x9f,0x1c,0x60,0x6a,0x1e,0x6b,0x57,0x14,0x47,0x77,0x1d,0x6d,0x56,0x1c,0x46,0x6a,0x1e,0x7e,0x48,0x12,0x6c,0x56,0xd5]

				private func specialCell(page num: UInt8) -> UInt8 {
					return num ^ 251
				}

/*: "ping" :*/
fileprivate let kCircleId:[Character] = ["p","i","n","g"]

/*: "callHeartbeat" :*/
fileprivate let showShrinkMessage:[Character] = ["c","a","l","l","H"]
fileprivate let main_normalValue:String = "eawith"

/*: "recievedDate" :*/
fileprivate let const_labName:String = "usercieve"
fileprivate let show_makeSharedData:String = "make label styledDate"

/*: "liveHeartBeat" :*/
fileprivate let data_photoWhenKey:String = "insert searchliveHear"
fileprivate let show_viewBuildHiddenId:String = "as to maketBeat"

/*: "🔗 关闭 socket" :*/
fileprivate let constAddTitle:[Character] = ["🔗"," ","关","\u{95ed}"," ","s","o","c","k","e","t"]

/*: "The user logs out" :*/
fileprivate let dataBlockUrl:String = "The usreturn add view item"
fileprivate let userFinishMessage:[Character] = ["o","u","t"]

/*: "SOCKET  :*/
fileprivate let data_awayMsg:[Character] = ["S","O","C","K","E","T"]
fileprivate let userToolPath:[Character] = [" "]

/*: 重连. 重试次数： :*/
fileprivate let const_areaContentId:String = "\u{91cd}连. \u{91cd}\u{8bd5}次"
fileprivate let k_modelData:String = "raw："

/*: "SOCKET 连接正常 不需要重新连接." :*/
fileprivate let showAreaName:String = "SOCKbackground"
fileprivate let mainToKey:String = "\u{8981}重新连接."

/*: "SOCKET 正在连接 不需要重新连接." :*/
fileprivate let app_contentPath:[Character] = ["S","O"]
fileprivate let user_makeFormat:[Character] = ["C","K","E","T"," ","正","在","连","\u{63a5}"," ","\u{4e0d}","需","要","\u{91cd}","新","连","接","."]

/*: "&resume=1" :*/
fileprivate let dataViewInputStr:String = "string para show normal&resume"
fileprivate let mainUserValue:[Character] = ["=","1"]

/*: "&lang= :*/
fileprivate let data_pageStr:[Character] = ["&","l","a","n","g","="]

/*: "SOCKET 开始连接, 地址： :*/
fileprivate let dataImageValue:[Character] = ["S","O","C","K","E"]
fileprivate let app_levelName:[Character] = ["T"," ","开","始","连","\u{63a5}",","," ","地","址","\u{ff1a}"]

/*: "onNetDisabled" :*/
fileprivate let showAspectId:[Character] = ["o","n","N","e","t","D","i","s","a","b"]
fileprivate let kModelCustomerName:String = "lecustom"

/*: "errno" :*/
fileprivate let k_mainId:[Character] = ["e","r","r","n","o"]

/*: "data" :*/
fileprivate let app_tableData:String = "dquicka"

/*: "SOCKET 连接保活cmd: ping." :*/
fileprivate let showSizeText:String = "SOpullK"
fileprivate let const_targetMessage:String = "活cmd: pcard following no"
fileprivate let show_interactionFormat:String = "manager lab in dateing."

/*: "SOCKET trtc保活cmd: callHeartbeat." :*/
fileprivate let userWrapData:[UInt8] = [0x2f,0x33,0x3f,0x37,0x39,0x28,0x5c,0x8,0xe,0x8,0x1f,0x98,0xc3,0xe1,0x9a,0xc8,0xc7,0x1f,0x11,0x18,0x46,0x5c,0x1f,0x1d,0x10,0x10,0x34,0x19,0x1d,0xe,0x8,0x1e,0x19,0x1d,0x8,0x52]

/*: "SOCKET live保活cmd: liveHeartBeat." :*/
fileprivate let showActionSnapId:[UInt8] = [0x2e,0x74,0x61,0x65,0x42,0x74,0x72,0x61,0x65,0x48,0x65,0x76,0x69,0x6c,0x20,0x3a,0x64,0x6d,0x63,0xbb,0xb4,0xe6,0x9d,0xbf,0xe4,0x65,0x76,0x69,0x6c,0x20,0x54,0x45,0x4b,0x43,0x4f,0x53]

/*: "🔗 处理消息：cmdName: :*/
fileprivate let k_liveUpUrl:String = "\u{0001f517} \u{5904}理消息"

/*: , errNo: :*/
fileprivate let appCenterName:[Character] = [","," ","e","r","r","N","o",":"]

/*: , data: :*/
fileprivate let noti_transformTitle:String = ", data:if layer user you"

/*: "SOCKET 接收异常消息：cmdName: :*/
fileprivate let userJumpData:String = "SOCKET 接near add false"
fileprivate let showEmptyKey:[Character] = ["收","异","常","消"]
fileprivate let showGiftFormat:String = "息：cmview to"

/*: "onReceivedGift" :*/
fileprivate let const_valuePath:String = "wrap asideonRec"
fileprivate let kMeanTitle:[Character] = ["f","t"]

/*: "onVideoMatch" :*/
fileprivate let notiShowTitle:[Character] = ["o","n","V","i","d","e","o","M","a","t","c"]
fileprivate let kPanCellId:String = "scale"

/*: "onRequestCall" :*/
fileprivate let kSizeToolStr:String = "onReshould quick success if layer"
fileprivate let dataButtonValue:[Character] = ["q","u","e","s","t","C","a","l","l"]

/*: "onCancelCall" :*/
fileprivate let kKeyStr:[Character] = ["o","n","C","a","n","c","e"]
fileprivate let main_topPath:String = "lCallsuper video m var make"

/*: "onRefuseCall" :*/
fileprivate let main_equalName:String = "onReobject edge equal pair name"

/*: "onAcceptCall" :*/
fileprivate let k_sinceMakerTitle:String = "onAccepcancel model date"
fileprivate let app_resultPath:[Character] = ["t","C","a","l","l"]

/*: "onStopCall" :*/
fileprivate let noti_imageBeId:[Character] = ["o","n","S","t","o","p","C","a","l","l"]

/*: "freeCallTimes" :*/
fileprivate let showCreateKey:String = "FREE"
fileprivate let userBottomMsg:String = "app lab to error whiteTimes"

/*: "mfCoin" :*/
fileprivate let app_recordFormat:String = "pop"
fileprivate let show_snapColorFormat:String = "add datafCoin"

/*: "onMissCoin" :*/
fileprivate let main_kitIndexValue:String = "O"
fileprivate let app_errorFormat:String = "quick view reference listnMiss"

/*: "acceptCall" :*/
fileprivate let showRowFormat:String = "acceptCalvalue false plain return"
fileprivate let noti_videoStr:[Character] = ["l"]

/*: "stopCall" :*/
fileprivate let notiToMsg:[Character] = ["s","t","o","p","C","a","l"]
fileprivate let notiPushUrl:String = "first"

/*: "requestCall" :*/
fileprivate let notiAppUrl:String = "modeleque"
fileprivate let app_makeMessage:String = "self succeedstCall"

/*: "[Invite you to a video call...]" :*/
fileprivate let mainTotaleractionStr:[UInt8] = [0xd3,0xc1,0xe6,0xfe,0xe1,0xfc,0xed,0xa8,0xf1,0xe7,0xfd,0xa8,0xfc,0xe7,0xa8,0xe9,0xa8,0xfe,0xe1,0xec,0xed,0xe7,0xa8,0xeb,0xe9,0xe4,0xe4,0xa6,0xa6,0xa6,0xd5]

/*: "type" :*/
fileprivate let app_videoData:String = "typtransaction"

/*: "[Invite you to a voice call...]" :*/
fileprivate let user_currentKey:[UInt8] = [0x3f,0x2d,0xa,0x12,0xd,0x10,0x1,0x44,0x1d,0xb,0x11,0x44,0x10,0xb,0x44,0x5,0x44,0x12,0xb,0xd,0x7,0x1,0x44,0x7,0x5,0x8,0x8,0x4a,0x4a,0x4a,0x39]

				private func centerOf(at num: UInt8) -> UInt8 {
					return num ^ 100
				}

/*: "nickname" :*/
fileprivate let app_backgroundKey:String = "nicknamuser"

/*: "headPic" :*/
fileprivate let data_edgeKey:String = "headPiview self equal top"
fileprivate let show_languageId:[Character] = ["c"]

/*: "onCallResume" :*/
fileprivate let kGiftData:String = "onCalkit return return"
fileprivate let appLayerMaleId:[Character] = ["u","m","e"]

/*: "cancelCall" :*/
fileprivate let userTheoryStr:[Character] = ["c","a","n","c","e","l","C","a","l","l"]

/*: "refuseCall" :*/
fileprivate let kMakeUrl:String = "refusign"
fileprivate let user_timeValue:[Character] = ["C","a","l","l"]

/*: "onCallMotivation" :*/
fileprivate let showFrameId:[Character] = ["o","n","C","a","l","l","M","o","t","i","v","a","t","i","o"]
fileprivate let show_labelFormat:[Character] = ["n"]

/*: "onLiveWarning" :*/
fileprivate let userToMessage:String = "onLmodel if"
fileprivate let notiAgoId:String = "case normal makeiveWar"

/*: "reason" :*/
fileprivate let main_addContent:[Character] = ["r","e","a","s","o","n"]

/*: "time" :*/
fileprivate let kUpMakeTitle:[Character] = ["t","i","m","e"]

/*: "onStopLive" :*/
fileprivate let appSuiteUrl:String = "onStopreturn year cell"
fileprivate let dataFileStr:String = "Livecell true source"

/*: "onSendProgramGift" :*/
fileprivate let app_listInputText:String = "onSeview extension to me"
fileprivate let kCookieMsg:String = "gramGiftlive card"

/*: "onGameFs" :*/
fileprivate let app_modelUrl:String = "icon height raw inonG"
fileprivate let user_toApplicationMessage:[Character] = ["a","m","e","F","s"]

/*: "broadStatus" :*/
fileprivate let user_interactionFormat:String = "bofad"
fileprivate let userModelIndexGenderText:[Character] = ["s"]

/*: "onCallChatMsg" :*/
fileprivate let mainCheckedTitle:[Character] = ["o","n","C","a","l","l","C","h","a","t","M","s","g"]

/*: "onNotificationPopupWin" :*/
fileprivate let main_sizeMessage:String = "s type stringonNot"
fileprivate let kStyleMsg:String = "index manager add info falsenPop"

/*: "onUserBlock" :*/
fileprivate let userImageStopKey:String = "topn"
fileprivate let constLineMsg:String = "view tapUserB"

/*: "freeCallWin" :*/
fileprivate let main_firstTitle:String = "live case return modelfreeCa"
fileprivate let noti_labelData:String = "llWinedge remove string"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  GiftCommentSocketDelegate.swift
//  AbroadTalking
//
//  Created by young on 2022/9/13.
//

//: import SocketRocket
import SocketRocket
//: import UIKit
import UIKit

// 保活心跳包ping的时间间隔
//: let SOCKET_PING_INTERVAL = 60.0
let app_systemReplaceScreenMsg = 60.0
// 视频通话心跳包时间间隔
//: let SOCKET_CALL_INTERVAL = 20.0
let show_workScreenFoundMessage = 20.0
// 直播心跳包时间间隔
//: let SOCKET_LIVE_INTERVAL = 20.0
let k_filterValue = 20.0
// 失败重试次数
//: let SOCKET_ADDR_RETRY_COUNT = 3
let noti_sizeStr = 3
// socket连接超时时间
//: let SOCKET_MANAGER_CONNECT_TIMEOUT = 30.0
let show_depthKey = 30.0
// socket连接成功缓存key
//: let SOCKET_ADDR_KEY = "SOCKET_ADDR_KEY"
let main_videoHalfKey = (String(dataIconName) + String(app_containRoundViewUrl))

// 错误码
//: let SOCKET_MANAGER_UNCONNECTED_CODE = 5001
let constSkipClickMessage = 5001
//: let SOCKET_MANAGER_SELF_BUSY_CODE   = 5002
let kBarNoTitle = 5002
//: let SOCKET_WITHOUT_NET_CODE         = 5003
let userMomentMessage = 5003
//: let SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE = 1002
let main_screenName = 1002

// 接收方收到音视频通话通知
//: public let SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION = NSNotification.Name(rawValue: "SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION")
public let userChangeTitle = NSNotification.Name(rawValue: String(bytes: showViewFormat.map{makePhase(factor: $0)}, encoding: .utf8)!)
// 游戏飘屏通知
//: public let RECIVE_FLOATSCREEN_NOTIFICATION = NSNotification.Name(rawValue: "RECIVE_FLOATSCREEN_NOTIFICATION")
public let appStatusBottomFormat = NSNotification.Name(rawValue: String(bytes: noti_loadMessage.map{labBorder(model: $0)}, encoding: .utf8)!)

// MARK: - protocol

// MARK: - socket失败代理

/// socket收到失败消息
//: protocol TalkingSocketManagerErrorDelegate: NSObjectProtocol {
protocol SupplantObjectProtocol: NSObjectProtocol {
    /// socket收到失败消息
    /// - Parameters:
    ///   - errorNo: 失败编码
    ///   - cmd: 命令
    ///   - msg: 失败信息
    ///  - data: 数据
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any])
    func titleData(errorNo: Int, cmd: String, msg: String, data: [String: Any])
}

// MARK: - 视频速配代理

/// 视频速配代理（男）
//: protocol SocketManagerVideoMatchDelegate: NSObjectProtocol {
protocol ThirdObjectProtocol: NSObjectProtocol {
    /// 视频速配成功
    /// - Parameter data: 速配数据
    //: func socket_match_onVideoMatch(data: [String: Any])
    func rime(data: [String: Any])

    /// 匹配成功，可以拨打通话
    /// - Parameter data: 速配数据
    //: func socket_match_onRequestCall(data: [String: Any])
    func cardPlayer(data: [String: Any])
}

// MARK: - 音视频通话代理

/// 视频通话【拨打方】代理
//: protocol SocketManagerVideoChatCallerDelegate: NSObjectProtocol {
protocol GestureObjectProtocol: NSObjectProtocol {
    /// 接收方接受了来电
    /// - Parameter data: 数据
    //: func socket_videoChat_acceptCall(data: [String: Any])
    func untilAttentionRemove(data: [String: Any])

    /// 接收方挂断了通话（通话中）
    /// - Parameter data: 数据
    //: func socket_videoChat_stopCall(data: [String: Any])
    func bindRequest(data: [String: Any])

    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_cancelCall(data: [String: Any])
    func imageSocketModel(data: [String: Any])

    /// 通话余额不足提醒
    /// - Parameters:
    ///   - msg: 提醒信息
    ///   - data: 数据
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any])
    func contentData(msg: String, data: [String: Any])
}

/// 视频通话【接收方】代理
//: @objc protocol SocketManagerVideoChatCalledPartyDelegate: NSObjectProtocol {
@objc protocol CaliberObjectProtocol: NSObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any])
    func socketOrRawDataEqual(data: [String: Any])

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    /// - Parameter data: 数据
    //: func socket_videoChat_onAcceptCall(data: [String: Any])
    func theTool(data: [String: Any])

    /// 女性通话积分激励动效展示
    /// - Parameters:
    ///   - data: 数据
    //: @objc optional func socket_videoChat_onCallMotivation(data: [String: Any])
    @objc optional func vox(data: [String: Any])
}

//: class TalkingSocketManager: NSObject {
class GiftCommentSocketDelegate: NSObject {
    //: enum SocketRetryType: String {
    enum SocketMaleEditionType: String {
        //: case AppLogin = "用户登录"
        case AppLogin = "用户登录"
        //: case NetWork = "网络重连"
        case NetWork = "网络重连"
        //: case EnterForeground = "回到APP"
        case EnterForeground = "回到APP"
        //: case SocketFail = "连接失败"
        case SocketFail = "连接失败"
        //: case SocketClose = "连接关闭"
        case SocketClose = "连接关闭"
        //: case SocketSendMsg = "发送消息失败"
        case SocketSendMsg = "发送消息失败"
    }

    /// 音视频最小化
    //: var currNameVC: AnyObject?
    var currNameVC: AnyObject? // 当前正在通话的音视频视图
    //: var videoMiniPosition = CGPoint(
    var videoMiniPosition = CGPoint( // 音视频小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: GlassLanguageManager.shared.direction == .rightToLeft ? 0 : main_colorData,
        //: y: StatusBarHeight+190
        y: app_itemTitle + 190
    )
    //: var liveMiniPosition = CGPoint(
    var liveMiniPosition = CGPoint( // 直播小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: GlassLanguageManager.shared.direction == .rightToLeft ? 0 : main_colorData,
        //: y: StatusBarHeight+190
        y: app_itemTitle + 190
    )
    //: var partyMiniPosition = CGPoint(
    var partyMiniPosition = CGPoint( // 语聊房小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: GlassLanguageManager.shared.direction == .rightToLeft ? 0 : main_colorData,
        //: y: StatusBarHeight+500
        y: app_itemTitle + 500
    )
    //: var containerView: UIView?
    var containerView: UIView? // 小窗口容器

    /// delegate
    //: weak var errorDelegate: TalkingSocketManagerErrorDelegate?
    weak var errorDelegate: SupplantObjectProtocol? // error !=0 的异常处理
    //: weak var videoMatchDelegate: SocketManagerVideoMatchDelegate?
    weak var videoMatchDelegate: ThirdObjectProtocol? // 视频通话速配
    //: weak var callerDelegate: SocketManagerVideoChatCallerDelegate?
    weak var callerDelegate: GestureObjectProtocol? // 视频通话【拨打方】
    //: weak var calledPartyDelegate: SocketManagerVideoChatCalledPartyDelegate?
    weak var calledPartyDelegate: CaliberObjectProtocol? // 视频通话【接收方】

    //: private var webSocket: SRWebSocket?
    private var webSocket: SRWebSocket?
    //: private var keepAliveTimer: Timer?
    private var keepAliveTimer: Timer? // socket保活心跳包
    //: private var connectUrlStr: String?
    private var connectUrlStr: String? // socket连接地址
    //: private var linkIndex = 0
    private var linkIndex = 0 // 当前连接地址索引
    //: private var linkAddrArr: [String]?
    private var linkAddrArr: [String]? // 连接地址数组
    //: private var failureCount = 0
    private var failureCount = 0 // 连接失败次数
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()

    /// talking
    //: var needReconnect = false
    var needReconnect = false // 断网重连socket
    //: @objc dynamic var isName = false
    @objc dynamic var isName = false // 是否视频通话中
    //: @objc dynamic var isCalling = false
    @objc dynamic var isCalling = false // 是否呼叫中
    //: private var talkTimer: Timer?
    private var talkTimer: Timer? // 视频通话心跳包
    //: private var liveTimer: Timer?
    private var liveTimer: Timer? // 直播心跳包
    //: private var unHandleCallData: [String: Any]?
    private var unHandleCallData: [String: Any]? // 在后台收到的接听消息
    //: private var cancelCall = false
    private var cancelCall = false // 拨打方是否停止呼叫（还未打通）
    //: var isFreeCall = false
    var isFreeCall = false // 是否发起免费通话

    // singleton
    //: static let shared = TalkingSocketManager()
    static let shared = GiftCommentSocketDelegate()
    //: override func copy() -> Any { return self }
    override func copy() -> Any { return self }
    //: override func mutableCopy() -> Any { return self }
    override func mutableCopy() -> Any { return self }
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        // 网络状态监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(valueBy(note:)),
                                               //: name: .reachabilityChanged,
                                               name: .reachabilityChanged,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(checkWebsocket),
                                               selector: #selector(websocketCheck),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // 音视频呼叫时
        //: self.rx
        self.rx
            //: .observeWeakly(Bool.self, "isCalling")
            .observeWeakly(Bool.self, (mainListText.replacingOccurrences(of: "show", with: "i") + String(app_colorMonthText.suffix(4)) + "ling"))
            //: .subscribe(onNext: {(value) in
            .subscribe(onNext: { value in
                //: if value == true {
                if value == true {
                    // 上报女性用户busy状态
                    //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
                    (EmploymentThen.shared as! EmploymentThen).caper()
                }

                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)

        // 音视频通话时
        //: self.rx
        self.rx
            .observeWeakly(Bool.self, "isName")
            //: .subscribe(onNext: { (value) in
            .subscribe(onNext: { value in
                //: if value == true {
                if value == true {
                    // 上报女性用户busy状态
                    //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
                    (EmploymentThen.shared as! EmploymentThen).caper()
                }

                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }
}

// MARK: - SRWebSocketDelegate

//: extension TalkingSocketManager: SRWebSocketDelegate {
extension GiftCommentSocketDelegate: SRWebSocketDelegate {
    //: func webSocketDidOpen(_ webSocket: SRWebSocket!) {
    func webSocketDidOpen(_ webSocket: SRWebSocket!) {
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }
        //: UploadLogTool.writeLog(msg: "SOCKET 连接成功.")
        BuildLogTool.fileView(msg: (dataPointName.replacingOccurrences(of: "track", with: "CK") + const_rawPath))
        //: sendPing()
        appPing()
        //: NotificationCenter.default.post(name: SOCKET_IS_OPEN_NOTIFICATION,
        NotificationCenter.default.post(name: user_restoreId,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: nil)
                                        userInfo: nil)
        // 还原断网重连状态
        //: self.needReconnect = false
        self.needReconnect = false

        //: self.failureCount = 0
        self.failureCount = 0

        // 保存成功连接的记录
        //: if self.linkIndex < self.linkAddrArr?.count ?? 0 {
        if self.linkIndex < self.linkAddrArr?.count ?? 0 {
            //: UserDefaults.standard.set(self.linkAddrArr?[self.linkIndex], forKey: SOCKET_ADDR_KEY)
            UserDefaults.standard.set(self.linkAddrArr?[self.linkIndex], forKey: main_videoHalfKey)
        }
        // 创建音视频最小化窗口容器
        //: mini_createContainerView()
        isRangeShow()
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didFailWithError error: Error!) {
    func webSocket(_ webSocket: SRWebSocket!, didFailWithError error: Error!) {
        //: UploadLogTool.writeLog(msg: "SOCKET 连接失败, error:\(error.localizedDescription).")
        BuildLogTool.fileView(msg: (notiNoseWriteContent.replacingOccurrences(of: "page", with: "OC") + String(main_whichData) + String(k_replyInviteValue.prefix(6)) + "rror:") + "\(error.localizedDescription).")
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }

        //: guard let status = IconContainerAppManager.share.reachability?.connection, status != .unavailable else {
        guard let status = IconContainerAppManager.share.reachability?.connection, status != .unavailable else {
            //: return
            return
        }
        //: if self.failureCount == SOCKET_ADDR_RETRY_COUNT {
        if self.failureCount == noti_sizeStr {
            //: self.failureCount = 0
            self.failureCount = 0
            //: self.linkIndex = (self.linkIndex == 0) ? 1:0
            self.linkIndex = (self.linkIndex == 0) ? 1 : 0
            //: self.connectUrlStr = getWebSocketPath(isRetry: true)
            self.connectUrlStr = outsideValue(isRetry: true)
            //: UploadLogTool.writeLog(msg: "SOCKET 失败\(SOCKET_ADDR_RETRY_COUNT)次, 切换过Socket域名重新尝试，地址：\(connectUrlStr ?? "").")
            BuildLogTool.fileView(msg: (String(kScaleValue) + String(app_atId.prefix(5))) + "\(noti_sizeStr)" + (data_topContent + String(constCountColorTitle) + String(showTillUrl.prefix(6)) + "\u{5c1d}试，地址\u{ff1a}") + "\(connectUrlStr ?? "").")
            //: } else {
        } else {
            //: self.failureCount += 1
            self.failureCount += 1
        }
        //: self.reconnectWebSocket(timeInterval: 2, type: .SocketFail)
        self.socket(timeInterval: 2, type: .SocketFail)
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didReceiveMessage message: Any!) {
    func webSocket(_ webSocket: SRWebSocket!, didReceiveMessage message: Any!) {
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }
        //: self.handleReceiveMessage(message: message as? String)
        self.cuttingEdgeInfo(message: message as? String)
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didCloseWithCode code: Int, reason: String!, wasClean: Bool) {
    func webSocket(_ webSocket: SRWebSocket!, didCloseWithCode code: Int, reason: String!, wasClean _: Bool) {
        //: UploadLogTool.writeLog(msg: "SOCKET 已关闭, code:\(code), reason:\(String(describing: reason)).")
        BuildLogTool.fileView(msg: (String(userVoiceMessage) + String(dataNumberimateValue.suffix(3))) + "\(code)" + (String(constMainTitle.suffix(4)) + String(show_toName.prefix(5))) + "\(String(describing: reason)).")
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }

        //: self.closeWebSocket(userLoginOut: false)
        self.scale(userLoginOut: false)
        //: if code == SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE {
        if code == main_screenName {
            //: return
            return
        }
        //: if let status = IconContainerAppManager.share.reachability?.connection, status != .unavailable {
        if let status = IconContainerAppManager.share.reachability?.connection, status != .unavailable {
            //: self.reconnectWebSocket(timeInterval: 5, type: .SocketClose)
            self.socket(timeInterval: 5, type: .SocketClose)
        }
    }

    /// 获取socket地址
    //: func getWebSocketPath(isRetry: Bool) -> String? {
    func outsideValue(isRetry: Bool) -> String? {
        //: var linkAddr = ""
        var linkAddr = ""
        //: self.linkAddrArr = IconContainerAppManager.share.appConfigMode.ws
        self.linkAddrArr = IconContainerAppManager.share.appConfigMode.ws
        //: if self.linkAddrArr == nil || self.linkAddrArr?.count == 0 {
        if self.linkAddrArr == nil || self.linkAddrArr?.count == 0 {
            //: self.linkAddrArr = defaultWSArr()
            self.linkAddrArr = cameraMatch()
        }
        //: let lastAddr = UserDefaults.standard.string(forKey: SOCKET_ADDR_KEY)
        let lastAddr = UserDefaults.standard.string(forKey: main_videoHalfKey)
        //: if lastAddr == nil || isRetry {
        if lastAddr == nil || isRetry {
            //: if let temLinkAddr = self.linkAddrArr?[safe: self.linkIndex] {
            if let temLinkAddr = self.linkAddrArr?[safe: self.linkIndex] {
                //: linkAddr = temLinkAddr
                linkAddr = temLinkAddr
                //: } else {
            } else {
                //: self.linkIndex = 0
                self.linkIndex = 0
                //: linkAddr = self.linkAddrArr!.first!
                linkAddr = self.linkAddrArr!.first!
            }
            //: } else {
        } else {
            //: if let index = self.linkAddrArr!.firstIndex(of: lastAddr!) {
            if let index = self.linkAddrArr!.firstIndex(of: lastAddr!) {
                //: self.linkIndex = index
                self.linkIndex = index
                //: linkAddr = lastAddr!
                linkAddr = lastAddr!
                //: }else{
            } else {
                //: self.linkIndex = 0
                self.linkIndex = 0
                //: linkAddr = self.linkAddrArr!.first!
                linkAddr = self.linkAddrArr!.first!
            }
        }

        //: let uid = IconContainerAppManager.share.loginUid
        let uid = IconContainerAppManager.share.loginUid
        //: let sid = IconContainerAppManager.share.loginSessionId
        let sid = IconContainerAppManager.share.loginSessionId
        //: let address = "ws://\(linkAddr)/?sid=\(sid)&uid=\(uid)&platform=iphone&version=\(AppNetVersion)&packageId=\(PackageID)"
        let address = "ws://\(linkAddr)/?sid=\(sid)&uid=\(uid)" + (String(constZoneData) + showMakeKey.replacingOccurrences(of: "eye", with: "&") + String(noti_packageMmKey.suffix(8))) + "\(noti_detailMsg)" + (String(dataIndexPath) + String(main_emptyFormat.prefix(4))) + "\(app_clickValue)"
        //: return address
        return address
    }

    /// 默认地址
    //: func defaultWSArr() -> [String] {
    func cameraMatch() -> [String] {
        //: return Environment_Formal ? ["1v1server.\(ReplaceUrlDomain).com:7766"] : ["43.129.185.194:7767"]
        return dataMessageId ? ["1v1server.\(app_liveUrl).com:7766"] : [(mainNorFormat.capitalized + show_equalName.capitalized + data_levelStr.capitalized)]
    }
}

// MARK: - NotificationCenter

//: extension TalkingSocketManager {
extension GiftCommentSocketDelegate {
    /// 网络监听通知
    //: @objc func reachabilityChanged(note: Notification) {
    @objc func valueBy(note: Notification) {
        //: let reachability = note.object as? Reachability
        let reachability = note.object as? Reachability
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            // 断网, 关闭socket
            //: UploadLogTool.writeLog(msg: "SOCKET 网络reachabilityChanged监听，断网关闭.")
            BuildLogTool.fileView(msg: String(bytes: noti_modelName.map{specialCell(page: $0)}, encoding: .utf8)!)
            //: self.closeWebSocket(userLoginOut: false)
            self.scale(userLoginOut: false)
            //: } else {
        } else {
            // 网络原因重连
            //: self.reconnectWebSocket(timeInterval: 2.0, type: .NetWork)
            self.socket(timeInterval: 2.0, type: .NetWork)
        }
    }

    /// 检测socket链接状态
    //: @objc func checkWebsocket() {
    @objc func websocketCheck() {
        //: self.reconnectWebSocket(timeInterval: 2.0, type: .EnterForeground)
        self.socket(timeInterval: 2.0, type: .EnterForeground)
        // 处理通话逻辑
        //: self.handleCallData()
        self.toUp()
    }
}

// MARK: - Event

//: extension TalkingSocketManager {
extension GiftCommentSocketDelegate {
    /// socket保活心跳包
    //: private func sendPing() {
    private func appPing() {
        //: self.keepAliveTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_PING_INTERVAL, block: { timer in
        self.keepAliveTimer = Timer.scheduledTimer(withTimeInterval: app_systemReplaceScreenMsg, block: { _ in
            //: self.sendMessage(info: ["cmd": "ping"])
            self.agnomen(info: ["cmd": (String(kCircleId))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 开启视频通话心跳包
    //: private func startTalkPing() {
    private func romancePing() {
        //: endTalkingPing()
        petitionThird()

        //: self.talkTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_CALL_INTERVAL, block: {  timer in
        self.talkTimer = Timer.scheduledTimer(withTimeInterval: show_workScreenFoundMessage, block: { _ in
            /// 不在通话中，不在音视频通话页，没有开启最小化时
            //: if !self.isName || (self.currentViewController()?.isKind(of: TalkingVideoChatViewController.self) == false  && self.currentViewController()?.isKind(of: TalkingVoiceChatViewController.self) == false ) && (self.currNameVC?.isKind(of: TalkingVideoChatViewController.self) == false  && self.currNameVC?.isKind(of: TalkingVoiceChatViewController.self) == false ) {
            if !self.isName || (self.actionLatest()?.isKind(of: GetTotaloCallerDelegate.self) == false && self.actionLatest()?.isKind(of: IconPartyDelegate.self) == false) && (self.currNameVC?.isKind(of: GetTotaloCallerDelegate.self) == false && self.currNameVC?.isKind(of: IconPartyDelegate.self) == false) {
                //: return
                return
            }
            //: self.sendMessage(info: ["cmd": "callHeartbeat"])
            self.agnomen(info: ["cmd": (String(showShrinkMessage) + main_normalValue.replacingOccurrences(of: "with", with: "rt") + "beat")])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 结束视频通话心跳包
    //: func endTalkingPing() {
    func petitionThird() {
        //: if self.talkTimer != nil {
        if self.talkTimer != nil {
            //: self.talkTimer?.invalidate()
            self.talkTimer?.invalidate()
            //: self.talkTimer = nil
            self.talkTimer = nil
        }
    }

    /// 处理通话逻辑
    //: func handleCallData() {
    func toUp() {
        //: if self.cancelCall == true {
        if self.cancelCall == true { // 拨打方已停止呼叫
            //: self.cancelCall = false
            self.cancelCall = false
            //: self.unHandleCallData = nil
            self.unHandleCallData = nil
            //: return
            return
        }

        //: if self.unHandleCallData != nil {
        if self.unHandleCallData != nil {
            //: let recievedTime = self.unHandleCallData!["recievedDate"] as! Date
            let recievedTime = self.unHandleCallData![(const_labName.replacingOccurrences(of: "user", with: "re") + String(show_makeSharedData.suffix(5)))] as! Date
            //: let currentTime = Date()
            let currentTime = Date()
            //: if currentTime.timeIntervalSince(recievedTime) < VIDEO_1v1_RECIEVED_CALL_TIMEOUT {
            if currentTime.timeIntervalSince(recievedTime) < noti_tabKey {
                //: IconContainerAppManager.share.recieve1v1TalkRequest(info: self.unHandleCallData!)
                IconContainerAppManager.share.isView(info: self.unHandleCallData!)
            }
            //: self.unHandleCallData = nil
            self.unHandleCallData = nil
        }
    }
}

// MARK: - 直播心跳包

//: extension TalkingSocketManager {
extension GiftCommentSocketDelegate {
    /// 开启直播心跳包
    //: func startLivePing() {
    func requestIn() {
        //: endLivePing()
        fertilizationAge()
        //: self.liveTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_LIVE_INTERVAL, block: { timer in
        self.liveTimer = Timer.scheduledTimer(withTimeInterval: k_filterValue, block: { _ in
            //: self.sendMessage(info: ["cmd": "liveHeartBeat"])
            self.agnomen(info: ["cmd": (String(data_photoWhenKey.suffix(8)) + String(show_viewBuildHiddenId.suffix(5)))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 结束直播心跳包
    //: func endLivePing() {
    func fertilizationAge() {
        //: if self.liveTimer != nil {
        if self.liveTimer != nil {
            //: self.liveTimer?.invalidate()
            self.liveTimer?.invalidate()
            //: self.liveTimer = nil
            self.liveTimer = nil
        }
    }
}

// MARK: - 音视频窗口最小化

//: extension TalkingSocketManager {
extension GiftCommentSocketDelegate {
    /// 创建窗口最小化容器
    //: private func mini_createContainerView() {
    private func isRangeShow() {
        //: guard self.containerView == nil else { return }
        guard self.containerView == nil else { return }

        //: let windowView = TalkingMiniContainerView()
        let windowView = MaleContainerView()
        //: let keyWindow = MaleMacroDefine.getWindow()
        let keyWindow = MaleMacroDefine.penumbra()
        //: windowView.frame = keyWindow.bounds
        windowView.frame = keyWindow.bounds
        //: windowView.backgroundColor = .clear
        windowView.backgroundColor = .clear
        //: keyWindow.addSubview(windowView)
        keyWindow.addSubview(windowView)
        //: self.containerView = windowView
        self.containerView = windowView
        //: self.containerView?.isHidden = !self.isName
        self.containerView?.isHidden = !self.isName
    }

    /// 移除窗口最小化容器
    //: private func mini_removeContainerView() {
    private func skirtImageView() {
        //: guard self.containerView != nil else { return }
        guard self.containerView != nil else { return }
        // 防止抢登录时，用户还在直播中/语聊房
        //: guard TalkingLiveManager.shared().isLive == false else { return }
        guard DraftCopyThen.fileForScope().isLive == false else { return }
        //: guard TalkingVoiceRoomManager.shared().isParty == false else { return }
        guard PopEventHandler.addMonth().isParty == false else { return }

        //: self.containerView?.removeAllSubviews()
        self.containerView?.removeAllSubviews()
        //: self.containerView?.removeFromSuperview()
        self.containerView?.removeFromSuperview()
        //: self.containerView = nil
        self.containerView = nil
    }
}

// MARK: - Socket

//: extension TalkingSocketManager {
extension GiftCommentSocketDelegate {
    /// 关闭socket
    /// - Parameter userLoginOut: 是否是用户退出
    //: func closeWebSocket(userLoginOut: Bool) {
    func scale(userLoginOut: Bool) {
        //: printLog(message: "🔗 关闭 socket")
        printLog(message: (String(constAddTitle)))
        //: if self.webSocket != nil {
        if self.webSocket != nil {
            //: self.webSocket?.delegate = nil
            self.webSocket?.delegate = nil
            //: if userLoginOut == true {
            if userLoginOut == true {
                //: self.webSocket?.close(withCode: SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE, reason: "The user logs out")
                self.webSocket?.close(withCode: main_screenName, reason: (String(dataBlockUrl.prefix(6)) + "er logs " + String(userFinishMessage)))
                //: } else {
            } else {
                //: self.webSocket?.close()
                self.webSocket?.close()
            }
            //: self.webSocket = nil
            self.webSocket = nil
        }

        //: if self.keepAliveTimer != nil {
        if self.keepAliveTimer != nil {
            //: self.keepAliveTimer?.invalidate()
            self.keepAliveTimer?.invalidate()
            //: self.keepAliveTimer = nil
            self.keepAliveTimer = nil
        }

        //: endTalkingPing()
        petitionThird()

        //: if userLoginOut == true {
        if userLoginOut == true {
            //: self.connectUrlStr = nil
            self.connectUrlStr = nil
            // 销毁最小化窗口
            //: mini_removeContainerView()
            skirtImageView()
        }
    }

    /// 更新socket连接
    //: func updateWebSocket() {
    func completeApp() {
        //: self.connectUrlStr = TalkingSocketManager.shared.getWebSocketPath(isRetry: false)
        self.connectUrlStr = GiftCommentSocketDelegate.shared.outsideValue(isRetry: false)
        //: self.reconnectWebSocket(timeInterval: 0, type: .AppLogin)
        self.socket(timeInterval: 0, type: .AppLogin)
    }

    /// 重连socket
    //: private func reconnectWebSocket(timeInterval: TimeInterval, type: SocketRetryType) {
    private func socket(timeInterval: TimeInterval, type: SocketMaleEditionType) {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + timeInterval) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + timeInterval) {
            //: UploadLogTool.writeLog(msg: "SOCKET \(type.rawValue)重连. 重试次数：\(self.failureCount), 地址：\(self.connectUrlStr ?? "").")
            BuildLogTool.fileView(msg: (String(data_awayMsg) + String(userToolPath)) + "\(type.rawValue)" + (const_areaContentId + k_modelData.replacingOccurrences(of: "raw", with: "数")) + "\(self.failureCount), 地址：\(self.connectUrlStr ?? "").")
            //: self.connectWebSocket(urlStr: self.connectUrlStr)
            self.connectAt(urlStr: self.connectUrlStr)
        }
    }

    /// 连接socket
    //: private func connectWebSocket(urlStr: String?) {
    private func connectAt(urlStr: String?) {
        //: guard urlStr?.isEmpty == false else { return }
        guard urlStr?.isEmpty == false else { return }

        //: if urlStr == self.connectUrlStr && self.webSocket != nil && (self.webSocket?.readyState == .OPEN || self.webSocket?.readyState == .CONNECTING) {
        if urlStr == self.connectUrlStr, self.webSocket != nil, self.webSocket?.readyState == .OPEN || self.webSocket?.readyState == .CONNECTING {
            //: if (self.webSocket?.readyState == .OPEN) {
            if self.webSocket?.readyState == .OPEN {
                //: UploadLogTool.writeLog(msg: "SOCKET 连接正常 不需要重新连接.")
                BuildLogTool.fileView(msg: (showAreaName.replacingOccurrences(of: "background", with: "ET") + " 连接正\u{5e38} 不\u{9700}" + mainToKey))
                //: } else {
            } else {
                //: UploadLogTool.writeLog(msg: "SOCKET 正在连接 不需要重新连接.")
                BuildLogTool.fileView(msg: (String(app_contentPath) + String(user_makeFormat)))
            }
            //: return
            return
        }

        //: self.connectUrlStr = urlStr
        self.connectUrlStr = urlStr

        //: var newStr = urlStr!
        var newStr = urlStr!
        //: if self.isName {
        if self.isName {
            //: newStr = newStr.appending("&resume=1")
            newStr = newStr.appending((String(dataViewInputStr.suffix(7)) + String(mainUserValue)))
        }
        //: newStr = newStr.appending("&lang=\(TalkingRequestAddrTool.share.interfaceLang)")
        newStr = newStr.appending((String(data_pageStr)) + "\(QuantityRequestAddrTool.share.interfaceLang)")
        //: closeWebSocket(userLoginOut: false)
        scale(userLoginOut: false)

        //: let url = URL(string: newStr)
        let url = URL(string: newStr)
        //: var request = URLRequest(url: url!)
        var request = URLRequest(url: url!)
        //: request.timeoutInterval = SOCKET_MANAGER_CONNECT_TIMEOUT
        request.timeoutInterval = show_depthKey
        //: self.webSocket = SRWebSocket(urlRequest: request)
        self.webSocket = SRWebSocket(urlRequest: request)
        //: self.webSocket?.delegate = self
        self.webSocket?.delegate = self
        //: self.webSocket?.open()
        self.webSocket?.open()
        //: UploadLogTool.writeLog(msg: "SOCKET 开始连接, 地址：\(newStr).")
        BuildLogTool.fileView(msg: (String(dataImageValue) + String(app_levelName)) + "\(newStr).")
    }

    /// 发送消息
    /// - Parameter info: 消息体
    /// - Returns: 发送结果
    //: @discardableResult
    @discardableResult
    //: func sendMessage(info: [String: Any]?) -> Bool {
    func agnomen(info: [String: Any]?) -> Bool {
        //: guard info != nil else { return false }
        guard info != nil else { return false }

        // 1. 判断是否有网络连接
        //: if IconContainerAppManager.share.reachability?.connection == nil || IconContainerAppManager.share.reachability?.connection == .unavailable {
        if IconContainerAppManager.share.reachability?.connection == nil || IconContainerAppManager.share.reachability?.connection == .unavailable {
            //: let info: [String: Any] = ["cmd": "onNetDisabled",
            let info: [String: Any] = ["cmd": (String(showAspectId) + kModelCustomerName.replacingOccurrences(of: "custom", with: "d")),
                                       //: "errno": "\(SOCKET_WITHOUT_NET_CODE)",
                                       (String(k_mainId)): "\(userMomentMessage)",
                                       //: "data": [:]]
                                       (app_tableData.replacingOccurrences(of: "quick", with: "at")): [:]]
            //: let data = try? JSONSerialization.data(withJSONObject: info, options: [])
            let data = try? JSONSerialization.data(withJSONObject: info, options: [])
            //: let message = String(data: data!, encoding: String.Encoding.utf8)
            let message = String(data: data!, encoding: String.Encoding.utf8)
            //: handleReceiveMessage(message: message)
            cuttingEdgeInfo(message: message)
            //: return false
            return false
        }

        // 2. 尝试发送消息
        //: if self.webSocket?.readyState == SRReadyState.OPEN {
        if self.webSocket?.readyState == SRReadyState.OPEN {
            //: let data = try? JSONSerialization.data(withJSONObject: info!, options: [])
            let data = try? JSONSerialization.data(withJSONObject: info!, options: [])
            //: let message = String(data: data!, encoding: String.Encoding.utf8)
            let message = String(data: data!, encoding: String.Encoding.utf8)
            //: if info!["cmd"] as? String == "ping" {
            if info!["cmd"] as? String == (String(kCircleId)) {
                //: UploadLogTool.writeLog(msg: "SOCKET 连接保活cmd: ping.")
                BuildLogTool.fileView(msg: (showSizeText.replacingOccurrences(of: "pull", with: "C") + "ET 连\u{63a5}保" + String(const_targetMessage.prefix(7)) + String(show_interactionFormat.suffix(4))))
                //: } else if info!["cmd"] as? String == "callHeartbeat" {
            } else if info!["cmd"] as? String == (String(showShrinkMessage) + main_normalValue.replacingOccurrences(of: "with", with: "rt") + "beat") {
                //: UploadLogTool.writeLog(msg: "SOCKET trtc保活cmd: callHeartbeat.")
                BuildLogTool.fileView(msg: String(bytes: userWrapData.map{$0^124}, encoding: .utf8)!)
                //: } else if info!["cmd"] as? String == "liveHeartBeat" {
            } else if info!["cmd"] as? String == (String(data_photoWhenKey.suffix(8)) + String(show_viewBuildHiddenId.suffix(5))) {
                //: UploadLogTool.writeLog(msg: "SOCKET live保活cmd: liveHeartBeat.")
                BuildLogTool.fileView(msg: String(bytes: showActionSnapId.reversed(), encoding: .utf8)!)
            }
            //: self.webSocket?.send(message)
            self.webSocket?.send(message)
            //: return true
            return true

            //: } else {
        } else {
            //: if info!["cmd"] as? String == "ping" {
            if info!["cmd"] as? String == (String(kCircleId)) {
                //: let info: [String: Any] = ["cmd": "onP",
                let info: [String: Any] = ["cmd": "onP",
                                           //: "errno": "\(SOCKET_MANAGER_UNCONNECTED_CODE)",
                                           (String(k_mainId)): "\(constSkipClickMessage)",
                                           //: "data": [:]]
                                           (app_tableData.replacingOccurrences(of: "quick", with: "at")): [:]]
                //: let data = try? JSONSerialization.data(withJSONObject: info, options: [])
                let data = try? JSONSerialization.data(withJSONObject: info, options: [])
                //: let message = String(data: data!, encoding: String.Encoding.utf8)
                let message = String(data: data!, encoding: String.Encoding.utf8)
                //: handleReceiveMessage(message: message)
                cuttingEdgeInfo(message: message)
            }
            //: self.reconnectWebSocket(timeInterval: 5.0, type: .SocketSendMsg)
            self.socket(timeInterval: 5.0, type: .SocketSendMsg)
            //: return false
            return false
        }
    }

    /// 处理接收到的消息
    /// - Parameter message: 消息体
    //: private func handleReceiveMessage(message: String?) {
    private func cuttingEdgeInfo(message: String?) {
        //: guard message != nil else { return }
        guard message != nil else { return }

        //: let json = JSON(parseJSON: message!)
        let json = JSON(parseJSON: message!)
        //: let cmdName = json["cmd"].stringValue
        let cmdName = json["cmd"].stringValue
        //: let errorNo = json["errno"].intValue
        let errorNo = json[(String(k_mainId))].intValue
        //: let msg = json["msg"].stringValue
        let msg = json["msg"].stringValue
        //: let data = json["data"].dictionaryObject ?? [:]
        let data = json[(app_tableData.replacingOccurrences(of: "quick", with: "at"))].dictionaryObject ?? [:]

        //: printLog(message: "🔗 处理消息：cmdName:\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data)")
        printLog(message: (k_liveUpUrl + "：cmdName:") + "\(cmdName)" + (String(appCenterName)) + "\(errorNo), msg:\(msg)" + (String(noti_transformTitle.prefix(7))) + "\(data)")

        // 检测是否需要恢复直播
        //: check_stopOrRestoreLive(cmdName)
        soundForthLive(cmdName)

        // 处理异常
        //: guard errorNo == 0 else {
        guard errorNo == 0 else {
            //: UploadLogTool.writeLog(msg: "SOCKET 接收异常消息：cmdName:\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data).")
            BuildLogTool.fileView(msg: (String(userJumpData.prefix(8)) + String(showEmptyKey) + String(showGiftFormat.prefix(4)) + "dName:") + "\(cmdName)" + (String(appCenterName)) + "\(errorNo), msg:\(msg)" + (String(noti_transformTitle.prefix(7))) + "\(data).")
            //: if errorNo == 444 {
            if errorNo == 444 {
                //: IconContainerAppManager.share.loginUserMode.freeCallTimes = 0
                IconContainerAppManager.share.loginUserMode.freeCallTimes = 0
                //: } else if errorNo == 60003, !IconContainerAppManager.share.loginUserMode.isNaUser {
            } else if errorNo == 60003, !IconContainerAppManager.share.loginUserMode.isNaUser {
                /// 没真人认证时，需要弹窗提示
                //: TalkingPopupWindowManager.shared.faceVerificationPopUpWindow()
                ManagerReactiveCompatible.shared.valueAction()
                //: return
                return
            }
            //: self.errorDelegate?.socket_didRecieveError(errorNo: errorNo, cmd: cmdName, msg: msg, data: data)
            self.errorDelegate?.titleData(errorNo: errorNo, cmd: cmdName, msg: msg, data: data)
            //: return
            return
        }

        // 处理正常接收的消息
        //: if cmdName == "onReceivedGift" { // 礼物
        if cmdName == (String(const_valuePath.suffix(5)) + "eivedGi" + String(kMeanTitle)) { // 礼物
            //: NotificationCenter.default.post(name: USER_RECIEVED_GIFT_NOTIFICATION, object: data)
            NotificationCenter.default.post(name: main_successId, object: data)

            //: } else if cmdName == "onVideoMatch" { // 视频速配
        } else if cmdName == (String(notiShowTitle) + kPanCellId.replacingOccurrences(of: "scale", with: "h")) { // 视频速配
            //: self.videoMatchDelegate?.socket_match_onVideoMatch(data: data)
            self.videoMatchDelegate?.rime(data: data)

            //: } else if cmdName == "onRequestCall" { // 拨打方发出requestCall回执
        } else if cmdName == (String(kSizeToolStr.prefix(4)) + String(dataButtonValue)) { // 拨打方发出requestCall回执
            //: self.isCalling = true
            self.isCalling = true
            // 视频速配 || 音视频通话
            //: self.videoMatchDelegate?.socket_match_onRequestCall(data: data)
            self.videoMatchDelegate?.cardPlayer(data: data)

            //: } else if cmdName == "onCancelCall" { // 拨打方发出cancelCall回执，目前无需处理
        } else if cmdName == (String(kKeyStr) + String(main_topPath.prefix(5))) { // 拨打方发出cancelCall回执，目前无需处理
            //: } else if cmdName == "onRefuseCall" { // 接收方发送refuseCall回执，目前无需处理
        } else if cmdName == (String(main_equalName.prefix(4)) + "fuseCall") { // 接收方发送refuseCall回执，目前无需处理
            //: } else if cmdName == "onAcceptCall" { // 接收方发送acceptCall回执
        } else if cmdName == (String(k_sinceMakerTitle.prefix(7)) + String(app_resultPath)) { // 接收方发送acceptCall回执
            //: self.isName = true
            self.isName = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            romancePing()
            //: self.calledPartyDelegate?.socket_videoChat_onAcceptCall(data: data)
            self.calledPartyDelegate?.theTool(data: data)

            //: } else if cmdName == "onStopCall" { // 接收方发送stopCall回执
        } else if cmdName == (String(noti_imageBeId)) { // 接收方发送stopCall回执
            //: self.isName = false
            self.isName = false
            //: self.isCalling = false
            self.isCalling = false
            //: endTalkingPing()
            petitionThird()
            //: if let freeCallTimes = data["freeCallTimes"] as? Int {
            if let freeCallTimes = data[(showCreateKey.lowercased() + "Call" + String(userBottomMsg.suffix(5)))] as? Int {
                //: IconContainerAppManager.share.loginUserMode.freeCallTimes = freeCallTimes
                IconContainerAppManager.share.loginUserMode.freeCallTimes = freeCallTimes
            }
            // 更新余额
            //: IconContainerAppManager.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue
            IconContainerAppManager.share.loginUserMode.mf_coin = json[(app_tableData.replacingOccurrences(of: "quick", with: "at"))][(app_recordFormat.replacingOccurrences(of: "pop", with: "m") + String(show_snapColorFormat.suffix(5)))].stringValue
            //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: dataKeyFormat, object: nil)
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: (TalkingApplication.shared as! TalkingApplication).femaleBusyStatusReport()
            (EmploymentThen.shared as! EmploymentThen).female()
            //: } else if cmdName == "onMissCoin" { // 余额不足提醒
        } else if cmdName == (main_kitIndexValue.lowercased() + String(app_errorFormat.suffix(5)) + "Coin") { // 余额不足提醒
            //: self.callerDelegate?.socket_videoChat_onMissCoin(msg: msg, data: data)
            self.callerDelegate?.contentData(msg: msg, data: data)

            //: } else if cmdName == "acceptCall" { // 接收方接受了来电，改变状态为通话中,开始推拉流
        } else if cmdName == (String(showRowFormat.prefix(9)) + String(noti_videoStr)) { // 接收方接受了来电，改变状态为通话中,开始推拉流
            //: self.isName = true
            self.isName = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            romancePing()
            //: self.callerDelegate?.socket_videoChat_acceptCall(data: data)
            self.callerDelegate?.untilAttentionRemove(data: data)

            //: } else if cmdName == "stopCall" { // 接收方/拨打方 挂断了通话
        } else if cmdName == (String(notiToMsg) + notiPushUrl.replacingOccurrences(of: "first", with: "l")) { // 接收方/拨打方 挂断了通话
            //: self.isName = false
            self.isName = false
            //: self.isCalling = false
            self.isCalling = false
            //: endTalkingPing()
            petitionThird()
            //: self.callerDelegate?.socket_videoChat_stopCall(data: data)
            self.callerDelegate?.bindRequest(data: data)
            // 更新余额
            //: IconContainerAppManager.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue
            IconContainerAppManager.share.loginUserMode.mf_coin = json[(app_tableData.replacingOccurrences(of: "quick", with: "at"))][(app_recordFormat.replacingOccurrences(of: "pop", with: "m") + String(show_snapColorFormat.suffix(5)))].stringValue

            //: isShowFreeStopCallPopUpView(data: data)
            collectionData(data: data)
            //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: dataKeyFormat, object: nil)
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: (TalkingApplication.shared as! TalkingApplication).femaleBusyStatusReport()
            (EmploymentThen.shared as! EmploymentThen).female()
            //: } else if cmdName == "requestCall" { // 接收方收到了拨打方的申请通话
        } else if cmdName == (notiAppUrl.replacingOccurrences(of: "model", with: "r") + String(app_makeMessage.suffix(6))) { // 接收方收到了拨打方的申请通话
            //: self.isCalling = true
            self.isCalling = true
            // 发送通知，告知外界处理逻辑
            //: NotificationCenter.default.post(name: SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION, object: self)
            NotificationCenter.default.post(name: userChangeTitle, object: self)

            //: if UIApplication.shared.applicationState != .background {
            if UIApplication.shared.applicationState != .background { // 应用在前台
                //: IconContainerAppManager.share.recieve1v1TalkRequest(info: data)
                IconContainerAppManager.share.isView(info: data)
                //: } else {
            } else {
                //: self.cancelCall = false
                self.cancelCall = false
                //: self.unHandleCallData = data
                self.unHandleCallData = data
                //: self.unHandleCallData?["recievedDate"] = Date()
                self.unHandleCallData?[(const_labName.replacingOccurrences(of: "user", with: "re") + String(show_makeSharedData.suffix(5)))] = Date()

                // 应用在后台，发送本地推送告知用户
                //: var body = "[Invite you to a video call...]".localized
                var body = String(bytes: mainTotaleractionStr.map{$0^136}, encoding: .utf8)!.localized
                //: if json["data"]["type"].intValue == 1 { // 音频
                if json[(app_tableData.replacingOccurrences(of: "quick", with: "at"))][(app_videoData.replacingOccurrences(of: "transaction", with: "e"))].intValue == 1 { // 音频
                    //: body = "[Invite you to a voice call...]".localized
                    body = String(bytes: user_currentKey.map{centerOf(at: $0)}, encoding: .utf8)!.localized
                }
                //: AppDelegateHelper.pushLocalNotification(uid: String(json["data"]["uid"].intValue),
                BlockReactiveCompatible.face(uid: String(json[(app_tableData.replacingOccurrences(of: "quick", with: "at"))]["uid"].intValue),
                                       //: title: data["nickname"] as? String,
                                       title: data[(app_backgroundKey.replacingOccurrences(of: "user", with: "e"))] as? String,
                                       //: body: body,
                                       body: body,
                                       //: imageUrl: data["headPic"] as? String)
                                       imageUrl: data[(String(data_edgeKey.prefix(6)) + String(show_languageId))] as? String)
                // 播放来电音乐
                //: AppDelegateHelper.shared.startPlayBGM()
                BlockReactiveCompatible.shared.imageComponent()
            }

            //: } else if cmdName == "onCallResume" { // 接收方/发送方 恢复通话中的页面，并开始推拉流
        } else if cmdName == (String(kGiftData.prefix(5)) + "lRes" + String(appLayerMaleId)) { // 接收方/发送方 恢复通话中的页面，并开始推拉流
            //: self.isName = true
            self.isName = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            romancePing()

            //: var resumeData = data
            var resumeData = data
            //: resumeData["onCallResume"] = "1"
            resumeData[(String(kGiftData.prefix(5)) + "lRes" + String(appLayerMaleId))] = "1"
            //: IconContainerAppManager.share.recieve1v1TalkResumeRequest(info: resumeData)
            IconContainerAppManager.share.afterClick(info: resumeData)
            //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
            (EmploymentThen.shared as! EmploymentThen).caper()

            //: } else if cmdName == "cancelCall" { // 拨打方拒绝了通话，接收方收到该指令
        } else if cmdName == (String(userTheoryStr)) { // 拨打方拒绝了通话，接收方收到该指令
            //: self.isName = false
            self.isName = false
            //: self.isCalling = false
            self.isCalling = false
            //: self.cancelCall = true
            self.cancelCall = true
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: endTalkingPing()
            petitionThird()
            //: self.callerDelegate?.socket_videoChat_cancelCall(data: data)
            self.callerDelegate?.imageSocketModel(data: data)
            // 应用在后台
            //: if UIApplication.shared.applicationState == .background {
            if UIApplication.shared.applicationState == .background {
                // 移除来电本地推送
                //: AppDelegateHelper.local_removeDeliveredNotification(identifier: String(json["data"]["uid"].intValue))
                BlockReactiveCompatible.tingData(identifier: String(json[(app_tableData.replacingOccurrences(of: "quick", with: "at"))]["uid"].intValue))
                // 停止播放来电音乐
                //: AppDelegateHelper.shared.stopPlayBGM()
                BlockReactiveCompatible.shared.header()
            }

            //: } else if cmdName == "refuseCall" { // 接收方拒绝了通话，拨打方收到该指令
        } else if cmdName == (kMakeUrl.replacingOccurrences(of: "sign", with: "se") + String(user_timeValue)) { // 接收方拒绝了通话，拨打方收到该指令
            //: self.isName = false
            self.isName = false
            //: self.isCalling = false
            self.isCalling = false
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: endTalkingPing()
            petitionThird()
            //: self.calledPartyDelegate?.socket_videoChat_refuseCall(data: data)
            self.calledPartyDelegate?.socketOrRawDataEqual(data: data)

            //: } else if cmdName == "onCallMotivation" { // 女性通话收到的积分（用于展示赚钱激励动效）
        } else if cmdName == (String(showFrameId) + String(show_labelFormat)) { // 女性通话收到的积分（用于展示赚钱激励动效）
            //: if IconContainerAppManager.share.loginUserMode.sex == Gender.female.rawValue {
            if IconContainerAppManager.share.loginUserMode.sex == ThemeTitleScalarLiteral.female.rawValue {
                //: self.calledPartyDelegate?.socket_videoChat_onCallMotivation?(data: data)
                self.calledPartyDelegate?.vox?(data: data)
            }

            //: } else if cmdName == "onLiveWarning" { // 直播违规警告
        } else if cmdName == (String(userToMessage.prefix(3)) + String(notiAgoId.suffix(6)) + "ning") { // 直播违规警告
            //: TalkingLiveManager.live_showWarningView(content: msg, reason: json["data"]["reason"].stringValue, time: json["data"]["time"].stringValue)
            DraftCopyThen.clickAcrossKey(content: msg, reason: json[(app_tableData.replacingOccurrences(of: "quick", with: "at"))][(String(main_addContent))].stringValue, time: json[(app_tableData.replacingOccurrences(of: "quick", with: "at"))][(String(kUpMakeTitle))].stringValue)

            //: } else if cmdName == "onStopLive" { // 停止直播
        } else if cmdName == (String(appSuiteUrl.prefix(6)) + String(dataFileStr.prefix(4))) { // 停止直播
            //: TalkingLiveManager.shared().live_releaseAllResource()
            DraftCopyThen.fileForScope().nonnegative()

            //: } else if cmdName == "onSendProgramGift" { // 视频通话节目送礼成功tost
        } else if cmdName == (String(app_listInputText.prefix(4)) + "ndPro" + String(kCookieMsg.prefix(8))) { // 视频通话节目送礼成功tost
            //: self.func__showStatusBarSuccessMsg(showMsg: msg)
            self.barPush(showMsg: msg)

            //: } else if cmdName == "onGameFs" { // 游戏中奖飘屏
        } else if cmdName == (String(app_modelUrl.suffix(3)) + String(user_toApplicationMessage)) { // 游戏中奖飘屏
            //: let broadStatus = data["broadStatus"] as? Int ?? -1
            let broadStatus = data[(user_interactionFormat.replacingOccurrences(of: "of", with: "ro") + "Statu" + String(userModelIndexGenderText))] as? Int ?? -1
            //: if IconContainerAppManager.share.appStatus == AppSkinStatus.normal.rawValue {
            if IconContainerAppManager.share.appStatus == MinExpressionConvertible.normal.rawValue {
                //: if broadStatus == 1 {
                if broadStatus == 1 {
                    //: NotificationCenter.default.post(name: RECIVE_FLOATSCREEN_NOTIFICATION, object: self, userInfo: data)
                    NotificationCenter.default.post(name: appStatusBottomFormat, object: self, userInfo: data)
                    //: NotificationCenter.default.post(name: USER_RECIEVED_GIFT_NOTIFICATION, object: data)
                    NotificationCenter.default.post(name: main_successId, object: data)
                    //: } else if broadStatus == -1 {
                } else if broadStatus == -1 {
                    //: NotificationCenter.default.post(name: RECIVE_FLOATSCREEN_NOTIFICATION, object: self, userInfo: data)
                    NotificationCenter.default.post(name: appStatusBottomFormat, object: self, userInfo: data)
                }
            }

            //: } else if cmdName == "onCallChatMsg" { // 收到视频通话中的聊天消息
        } else if cmdName == (String(mainCheckedTitle)) { // 收到视频通话中的聊天消息
            //: TalkingVideoDanmuManager.shared().onRecvDanmuNewMsg(msg: data)
            SourceReactiveCompatible.doModelSub().searched(msg: data)

            //: } else if cmdName == "onNotificationPopupWin" { // 审核拒绝提示弹窗
        } else if cmdName == (String(main_sizeMessage.suffix(5)) + "ificatio" + String(kStyleMsg.suffix(4)) + "upWin") { // 审核拒绝提示弹窗
            //: TalkingPopupWindowManager.shared.attestationRefusePopUpWindow(dic: data)
            ManagerReactiveCompatible.shared.toPhoto(dic: data)

            //: } else if cmdName == "onUserBlock" { // 用户被封号
        } else if cmdName == (userImageStopKey.replacingOccurrences(of: "top", with: "o") + String(constLineMsg.suffix(5)) + "lock") { // 用户被封号
            //: TalkingLiveManager.shared().live_releaseAllResource()
            DraftCopyThen.fileForScope().nonnegative()
            //: TalkingVoiceRoomManager.shared().voiceRoom_releaseAllResource()
            PopEventHandler.addMonth().waysAndMeansResource()
            //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
            NotificationCenter.default.post(name: showProgressMsg, object: nil, userInfo: nil)
        }
    }
}

// MARK: - 处理业务逻辑

//: extension TalkingSocketManager {
extension GiftCommentSocketDelegate {
    /// 检测是否需要停止或恢复直播
    /// - Parameter cmdName: socket命令
    //: private func check_stopOrRestoreLive(_ cmdName: String) {
    private func soundForthLive(_ cmdName: String) {
        // 【关闭直播】收到来电或主动通话时，关闭直播
        //: if cmdName == "requestCall" || cmdName == "onRequestCall" {
        if cmdName == (notiAppUrl.replacingOccurrences(of: "model", with: "r") + String(app_makeMessage.suffix(6))) || cmdName == (String(kSizeToolStr.prefix(4)) + String(dataButtonValue)) {
            //: if TalkingLiveManager.shared().isLive == true {
            if DraftCopyThen.fileForScope().isLive == true {
                //: NEED_RESTORE_LIVE = true
                k_successName = true
                //: TalkingLiveManager.req_liveStop(type: 3)
                DraftCopyThen.sendDoing(type: 3)
                //: TalkingLiveManager.shared().live_releaseAllResource()
                DraftCopyThen.fileForScope().nonnegative()
                //: return
                return
            }
        }

        // 【恢复直播】结束通话后，判断是否需要恢复直播
        //: guard NEED_RESTORE_LIVE == true else { return }
        guard k_successName == true else { return }
        //: if cmdName == "cancelCall" || cmdName == "onCancelCall" ||
        if cmdName == (String(userTheoryStr)) || cmdName == (String(kKeyStr) + String(main_topPath.prefix(5))) ||
            //: cmdName == "refuseCall" || cmdName == "onRefuseCall" ||
            cmdName == (kMakeUrl.replacingOccurrences(of: "sign", with: "se") + String(user_timeValue)) || cmdName == (String(main_equalName.prefix(4)) + "fuseCall") ||
            //: cmdName == "stopCall" || cmdName == "onStopCall" {
            cmdName == (String(notiToMsg) + notiPushUrl.replacingOccurrences(of: "first", with: "l")) || cmdName == (String(noti_imageBeId))
        {
            //: NEED_RESTORE_LIVE = false
            k_successName = false
            //: let alert = TalkingLiveAlertView(frame: .zero, type: .turnOnLive)
            let alert = LevelReactiveCompatible(frame: .zero, type: .turnOnLive)
            //: alert.rightBlcok = {
            alert.rightBlcok = {
                //: TalkingLiveManager.shared().checkLiveAuthAndTurnOn()
                DraftCopyThen.fileForScope().seeWithoutHandler()
            }
            //: alert.show()
            alert.largesseName()
        }
    }

    /// 免费1分钟通话结束弹窗
    //: private func isShowFreeStopCallPopUpView(data: [String: Any]) {
    private func collectionData(data: [String: Any]) {
        //: if let freeCallTimes = data["freeCallTimes"] as? Int {
        if let freeCallTimes = data[(showCreateKey.lowercased() + "Call" + String(userBottomMsg.suffix(5)))] as? Int {
            //: IconContainerAppManager.share.loginUserMode.freeCallTimes = freeCallTimes
            IconContainerAppManager.share.loginUserMode.freeCallTimes = freeCallTimes
        }
        //: let freeCallWin = data["freeCallWin"] as? Bool
        let freeCallWin = data[(String(main_firstTitle.suffix(6)) + String(noti_labelData.prefix(5)))] as? Bool
        //: if freeCallWin == true && IconContainerAppManager.share.loginUserMode.sex == Gender.male.rawValue {
        if freeCallWin == true, IconContainerAppManager.share.loginUserMode.sex == ThemeTitleScalarLiteral.male.rawValue {
            //: let view = TalkingFreeStopCallPopUpView.init()
            let view = TertiaryView()
            //: view.show()
            view.instill()
            //: view.setShowData(data: data)
            view.deal(data: data)
        }
    }
}
