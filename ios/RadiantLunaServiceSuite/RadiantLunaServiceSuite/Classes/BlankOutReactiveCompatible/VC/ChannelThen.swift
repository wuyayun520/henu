
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let dataShowId:[UInt8] = [0x37,0x30,0x37,0x2a,0x76,0x3d,0x31,0x3a,0x3b,0x2c,0x64,0x77,0x7e,0x36,0x3f,0x2d,0x7e,0x30,0x31,0x2a,0x7e,0x3c,0x3b,0x3b,0x30,0x7e,0x37,0x33,0x2e,0x32,0x3b,0x33,0x3b,0x30,0x2a,0x3b,0x3a]

/*: "Voice greeting" :*/
fileprivate let user_videoMsg:[Character] = ["V","o","i","c","e"," ","g","r","e","e","t","i","n","g"]

/*: "type" :*/
fileprivate let dataFromText:String = "tyfield"

/*: "addStatus" :*/
fileprivate let show_fileUrl:String = "female"
fileprivate let constMeContentId:String = "transform social atddSt"

/*: "list" :*/
fileprivate let userCenterUrl:[Character] = ["l","i","s","t"]

/*: "audio" :*/
fileprivate let constStatusMessage:[Character] = ["a","u","d","i","o"]

/*: "length" :*/
fileprivate let mainStyleStr:[Character] = ["l","e","n","g","t","h"]

/*: "Successfully added, please wait for approval" :*/
fileprivate let showTopFrameName:[UInt8] = [0x4c,0x6a,0x7c,0x7c,0x7a,0x6c,0x6c,0x79,0x6a,0x73,0x73,0x66,0x3f,0x7e,0x7b,0x7b,0x7a,0x7b,0x33,0x3f,0x6f,0x73,0x7a,0x7e,0x6c,0x7a,0x3f,0x68,0x7e,0x76,0x6b,0x3f,0x79,0x70,0x6d,0x3f,0x7e,0x6f,0x6f,0x6d,0x70,0x69,0x7e,0x73]

				private func imaginationImage(burn num: UInt8) -> UInt8 {
					return num ^ 31
				}

/*: "Are you sure to delete this message？" :*/
fileprivate let kSexStr:[UInt8] = [0x4,0x37,0x20,0x65,0x3c,0x2a,0x30,0x65,0x36,0x30,0x37,0x20,0x65,0x31,0x2a,0x65,0x21,0x20,0x29,0x20,0x31,0x20,0x65,0x31,0x2d,0x2c,0x36,0x65,0x28,0x20,0x36,0x36,0x24,0x22,0x20,0xaa,0xf9,0xda]

				private func cornerButton(make num: UInt8) -> UInt8 {
					return num ^ 69
				}

/*: "Cancel" :*/
fileprivate let kBlackBackStr:String = "Cancelarea start result"

/*: "Delete" :*/
fileprivate let userSumervalData:String = "fill"
fileprivate let noti_identityTitle:[Character] = ["e","l","e","t","e"]

/*: "Please wait for the audit result" :*/
fileprivate let app_extraId:[UInt8] = [0x5c,0x60,0x69,0x6d,0x7f,0x69,0x2c,0x7b,0x6d,0x65,0x78,0x2c,0x6a,0x63,0x7e,0x2c,0x78,0x64,0x69,0x2c,0x6d,0x79,0x68,0x65,0x78,0x2c,0x7e,0x69,0x7f,0x79,0x60,0x78]

				private func nextGallery(array num: UInt8) -> UInt8 {
					return num ^ 12
				}

/*: "The upper limit has been reached" :*/
fileprivate let kToMsg:[UInt8] = [0x64,0x65,0x68,0x63,0x61,0x65,0x72,0x20,0x6e,0x65,0x65,0x62,0x20,0x73,0x61,0x68,0x20,0x74,0x69,0x6d,0x69,0x6c,0x20,0x72,0x65,0x70,0x70,0x75,0x20,0x65,0x68,0x54]

/*: "filePath" :*/
fileprivate let notiWithTableImageTitle:String = "titlei"
fileprivate let const_sizeData:String = "return size progress data bottomlePath"

/*: "time" :*/
fileprivate let notiEffectStr:String = "ttome"

/*: "#FF506D" :*/
fileprivate let k_currentId:String = "message page#FF50"
fileprivate let mainShareKey:String = "place dismiss6D"

/*: "btn_message_delete_nor" :*/
fileprivate let notiFileFormat:String = "tool view mode mbtn_me"
fileprivate let notiSignMsg:String = "ssage_dview let add"
fileprivate let main_indexKey:[Character] = ["e","l","e","t","e","_","n","o","r"]

/*: "Currently mute" :*/
fileprivate let mainReloadMessage:String = "Curmedium add false video mode"
fileprivate let notiReplyUrl:String = "rlogntl"

/*: "Edit Title" :*/
fileprivate let showResFormat:String = "Editequal any type transform return"

/*: "remark" :*/
fileprivate let appImageRequestName:String = "tableematable"
fileprivate let constEnterFormat:String = "data"

/*: "Click the button below to add\ncontent" :*/
fileprivate let notiAtFormat:[UInt8] = [0x89,0xa6,0xa3,0xa9,0xa1,0xea,0xbe,0xa2,0xaf,0xea,0xa8,0xbf,0xbe,0xbe,0xa5,0xa4,0xea,0xa8,0xaf,0xa6,0xa5,0xbd,0xea,0xbe,0xa5,0xea,0xab,0xae,0xae,0xc0,0xa9,0xa5,0xa4,0xbe,0xaf,0xa4,0xbe]

				private func maleRaw(make num: UInt8) -> UInt8 {
					return num ^ 202
				}

/*: "Add multiple voice greetings, and the system will send voice messages by turns;\nClick the bubble to play the voice, slide to the voice area to delete it, and the voice title can be edited." :*/
fileprivate let userCellData:[UInt8] = [0xcb,0xee,0xee,0xaa,0xf7,0xff,0xf6,0xfe,0xf3,0xfa,0xf6,0xef,0xaa,0x0,0xf9,0xf3,0xed,0xef,0xaa,0xf1,0xfc,0xef,0xef,0xfe,0xf3,0xf8,0xf1,0xfd,0xb6,0xaa,0xeb,0xf8,0xee,0xaa,0xfe,0xf2,0xef,0xaa,0xfd,0x3,0xfd,0xfe,0xef,0xf7,0xaa,0x1,0xf3,0xf6,0xf6,0xaa,0xfd,0xef,0xf8,0xee,0xaa,0x0,0xf9,0xf3,0xed,0xef,0xaa,0xf7,0xef,0xfd,0xfd,0xeb,0xf1,0xef,0xfd,0xaa,0xec,0x3,0xaa,0xfe,0xff,0xfc,0xf8,0xfd,0xc5,0x94,0xcd,0xf6,0xf3,0xed,0xf5,0xaa,0xfe,0xf2,0xef,0xaa,0xec,0xff,0xec,0xec,0xf6,0xef,0xaa,0xfe,0xf9,0xaa,0xfa,0xf6,0xeb,0x3,0xaa,0xfe,0xf2,0xef,0xaa,0x0,0xf9,0xf3,0xed,0xef,0xb6,0xaa,0xfd,0xf6,0xf3,0xee,0xef,0xaa,0xfe,0xf9,0xaa,0xfe,0xf2,0xef,0xaa,0x0,0xf9,0xf3,0xed,0xef,0xaa,0xeb,0xfc,0xef,0xeb,0xaa,0xfe,0xf9,0xaa,0xee,0xef,0xf6,0xef,0xfe,0xef,0xaa,0xf3,0xfe,0xb6,0xaa,0xeb,0xf8,0xee,0xaa,0xfe,0xf2,0xef,0xaa,0x0,0xf9,0xf3,0xed,0xef,0xaa,0xfe,0xf3,0xfe,0xf6,0xef,0xaa,0xed,0xeb,0xf8,0xaa,0xec,0xef,0xaa,0xef,0xee,0xf3,0xfe,0xef,0xee,0xb8]

				fileprivate func acceptInput(voice num: UInt8) -> UInt8 {
					let value = Int(num) - 138
					if value < 0 {
						return UInt8(value + 256)
					} else {
						return UInt8(value)
					}
				}

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ChannelThen.swift
//  AbroadTalking
//
//  Created by Charlotte on 2023/2/9.
//

//: import UIKit
import UIKit

//: class TalkingGreetAudioVC: TalkingBaseViewController {
class ChannelThen: LightThen {
    //: var addStatus = -1
    var addStatus = -1 // 1：可以添加；2：已有正在审核中得音频，不能添加；3：已有20条审核通过的音频，不能添加；4：已有20条审核通过的文字，不能添加；5：已有20条审核中或审核通过的图片，不能添加；
    //: var recordView: TalkingGreetRecordAudioView?
    var recordView: UserToolDelegate?
    //: var isPlaying = false
    var isPlaying = false
    //: var playIndex: IndexPath?
    var playIndex: IndexPath?
    //: var player: TalkingAudioPlayTool?
    var player: ValuePlayerDelegate?
    //: var isMute = false
    var isMute = false
    //: var currenCell: TalkingGreetAudioCell?
    var currenCell: StreamViewThen?
    //: init() {
    init() {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: dataShowId.map{$0^94}, encoding: .utf8)!)
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.title = "Voice greeting".localized
        self.title = (String(user_videoMsg)).localized
        //: self.view.backgroundColor = UIColor.appBgColor()
        self.view.backgroundColor = UIColor.pushAside()
        //: designView()
        instance()
        //: getAudioListData()
        action()
        //: setMutedDetection()
        stopEmpty()
        //: NotificationCenter.default.addObserver(self, selector: #selector(cancelPlayerAudio(notification:)), name: SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(cuttingEdge(notification:)), name: userChangeTitle, object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(interruptionChange(notification:)), name: AVAudioSession.interruptionNotification, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(component(notification:)), name: AVAudioSession.interruptionNotification, object: nil)

        // 添加接收到音视频通话通知
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(stopPlay),
                                               selector: #selector(capture),
                                               //: name: SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION,
                                               name: userChangeTitle,
                                               //: object: nil)
                                               object: nil)
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: if isPlaying {
        if isPlaying {
            //: self.player?.stopPlay()
            self.player?.slate()
        }
    }

    // MARK: - Lazy load

    //: private lazy var dataSourceArr: [TalkingGreetModel] = {
    private lazy var dataSourceArr: [SizeTransformable] = {
        //: var array = [TalkingGreetModel]()
        var array = [SizeTransformable]()
        //: return array
        return array
        //: }()
    }()

    //: lazy var MainTable: UITableView = {
    lazy var MainTable: UITableView = {
        //: let table = UITableView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight-88), style: UITableView.Style.plain)
        let table = UITableView(frame: CGRect(x: 0, y: 0, width: main_colorData, height: constRecordServerKey - 88), style: UITableView.Style.plain)
        //: table.backgroundColor = UIColor.clear
        table.backgroundColor = UIColor.clear
        //: return table
        return table
        //: }()
    }()

    //: lazy var emptyView: EmptyView = {
    lazy var emptyView: EmptyView = {
        //: let view = EmptyView()
        let view = EmptyView()
        //: return view
        return view
        //: }()
    }()

    //: lazy var addButton: UIButton = {
    lazy var addButton: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.setTitle("Add".localized, for: .normal)
        btn.setTitle("Add".localized, for: .normal)
        //: btn.setTitleColor(.white, for: .normal)
        btn.setTitleColor(.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 18)
        btn.titleLabel?.font = UIFont.replyName(type: .Medium, fontSize: 18)
        //: btn.setBackgroundImage(UIImage.imageGradientColor(colors: UIColor.appGradientColor(), size: CGSize(width: 267, height: 50)), for: .normal)
        btn.setBackgroundImage(UIImage.smallSize(colors: UIColor.equalAction(), size: CGSize(width: 267, height: 50)), for: .normal)
        //: btn.layer.cornerRadius = 25
        btn.layer.cornerRadius = 25
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.addTarget(self, action: #selector(addButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(holdfast), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingGreetAudioVC {
extension ChannelThen {
    //: func getAudioListData() {
    func action() {
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["type"] = 1
        dict[(dataFromText.replacingOccurrences(of: "field", with: "pe"))] = 1
        //: ProgressHUD.show()
        MakeView.notToPic()
        //: TalkingMeRequestTool.req_GetGreetList(params: dict) {[weak self] succeed, result, errorModel in
        NoneventRequestTool.visualAspect(params: dict) { [weak self] succeed, result, _ in

            //: ProgressHUD.dismiss()
            MakeView.presentAddLab()
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if (self.MainTable.backgroundView == nil) {
            if self.MainTable.backgroundView == nil {
                //: self.setupEmptyDataView()
                self.searchCreateView()
            }
            //: if succeed {
            if succeed {
                //: guard let dict = result as? Dictionary<String, Any> else {
                guard let dict = result as? [String: Any] else {
                    //: return
                    return
                }
                //: self.addStatus = dict["addStatus"] as! Int
                self.addStatus = dict[(show_fileUrl.replacingOccurrences(of: "female", with: "a") + String(constMeContentId.suffix(4)) + "atus")] as! Int
                //: let array: Array =  dict["list"] as! Array<Any>
                let array: Array = dict[(String(userCenterUrl))] as! [Any]
                //: var dataArr: [TalkingGreetModel] = []
                var dataArr: [SizeTransformable] = []
                //: for i in 0..<array.count {
                for i in 0 ..< array.count {
                    //: if let model = JSONDeserializer<TalkingGreetModel>.deserializeFrom(dict: array[i] as? Dictionary<String, Any>, designatedPath: nil) {
                    if let model = JSONDeserializer<SizeTransformable>.deserializeFrom(dict: array[i] as? [String: Any], designatedPath: nil) {
                        //: dataArr.append(model)
                        dataArr.append(model)
                    }
                }
                //: self.player = nil
                self.player = nil
                //: self.dataSourceArr = dataArr
                self.dataSourceArr = dataArr
                //: self.MainTable.reloadData()
                self.MainTable.reloadData()
            }
        }
    }

    //: func requestUploadAudioFileData(data: NSData, time: String) {
    func registerFile(data: NSData, time: String) {
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["type"] = 1
        dict[(dataFromText.replacingOccurrences(of: "field", with: "pe"))] = 1
        //: dict["audio"] = data
        dict[(String(constStatusMessage))] = data
        //: dict["length"] = Int(time)
        dict[(String(mainStyleStr))] = Int(time)

        //: ProgressHUD.show()
        MakeView.notToPic()
        //: TalkingMeRequestTool.req_UploadGreetAdd(params: dict) {[weak self] succeed, result, errorModel in
        NoneventRequestTool.untilYear(params: dict) { [weak self] _, result, _ in

            //: ProgressHUD.dismiss()
            MakeView.presentAddLab()
            //: guard let self = self else {return}
            guard let self = self else { return }

            //: guard let dict = result as? Dictionary<String, Any> else {
            guard let dict = result as? [String: Any] else {
                //: return
                return
            }
            //: let url: String = dict["url"] as! String
            let url: String = dict["url"] as! String
            //: if !url.isEmptyString {
            if !url.isEmptyString {
                //: self.func__showStatusBarSuccessMsg(showMsg: "Successfully added, please wait for approval".localized)
                self.barPush(showMsg: String(bytes: showTopFrameName.map{imaginationImage(burn: $0)}, encoding: .utf8)!.localized)
                //: self.getAudioListData()
                self.action()
                //: self.recordView?.dismiss()
                self.recordView?.sharedRemove()
                //: self.recordView = nil
                self.recordView = nil
            }
        }
    }

    //: func shoWdeleteAlert(index: Int) {
    func praise(index: Int) {
        //: let config = ShowAlertConfig()
        let config = InsertAlertConfig()
        //: config.textFont = UIFont.pingfangFont(type: .Medium, fontSize: 18)
        config.textFont = UIFont.replyName(type: .Medium, fontSize: 18)
        //: config.textlineBreakMode = .byCharWrapping
        config.textlineBreakMode = .byCharWrapping

        //: TalkingAlertShow.customAlert(message: "Are you sure to delete this message？".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Delete".localized, leftBlock: {
        CommentSourceThen.lockComponent(message: String(bytes: kSexStr.map{cornerButton(make: $0)}, encoding: .utf8)!.localized, leftBtnTitle: (String(kBlackBackStr.prefix(6))).localized, rightBtnTitle: (userSumervalData.replacingOccurrences(of: "fill", with: "D") + String(noti_identityTitle)).localized, leftBlock: {
            //: TalkingAlertShow.hideAlert()
            CommentSourceThen.alertDataTo()
            //: }, rightBlock: { [weak self] in
        }, rightBlock: { [weak self] in

            //: guard let self = self else {return}
            guard let self = self else { return }
            //: TalkingAlertShow.hideAlert()
            CommentSourceThen.alertDataTo()
            //: self.stopPlay()
            self.capture()

            //: let model: TalkingGreetModel = self.dataSourceArr[index]
            let model: SizeTransformable = self.dataSourceArr[index]
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["id"] = model.greetId
            dict["id"] = model.greetId
            //: TalkingMeRequestTool.req_DeleteAudio(params: dict) { succeed, result, errorModel in
            NoneventRequestTool.loadDeleteParamsChange(params: dict) { succeed, _, _ in
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: self.getAudioListData()
                self.action()
            }
            //: }, config: config)
        }, config: config)
    }
}

// MARK: - Event

//: extension TalkingGreetAudioVC {
extension ChannelThen {
    //: @objc func cancelPlayerAudio(notification: NSNotification) -> Void {
    @objc func cuttingEdge(notification _: NSNotification) {
        //: stopPlay()
        capture()
    }

    /// 播放过程中中断
    //: @objc func interruptionChange(notification: NSNotification) -> Void {
    @objc func component(notification: NSNotification) {
        //: let dic = notification.userInfo as![String: AnyObject]
        let dic = notification.userInfo as! [String: AnyObject]

        //: let changeReason = dic[AVAudioSessionInterruptionTypeKey]
        let changeReason = dic[AVAudioSessionInterruptionTypeKey]
        //: if changeReason?.intValue ?? 0 == AVAudioSession.InterruptionType.began.rawValue {
        if changeReason?.intValue ?? 0 == AVAudioSession.InterruptionType.began.rawValue {
            //: stopPlay()
            capture()
        }
    }

    //: @objc func addButtonClick() {
    @objc func holdfast() {
        //: guard TalkingSocketManager.shared.isName == false else {
        guard GiftCommentSocketDelegate.shared.isName == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.explain(showMsg: user_topPath)
            //: return
            return
        }

        //: TalkingPermissionTool.func__openRecordServiceWithBlock(true) { isOpen in
        CellPermissionTool.towardAction(true) { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.explain(showMsg: main_deviceName)
                //: return
                return
            }
            //: if self.addStatus == 2 {
            if self.addStatus == 2 {
                //: self.func__showStatusBarErrorMsg(showMsg: "Please wait for the audit result".localized)
                self.explain(showMsg: String(bytes: app_extraId.map{nextGallery(array: $0)}, encoding: .utf8)!.localized)
                //: return
                return
                    //: }else if (self.addStatus == 3) {
            } else if self.addStatus == 3 {
                //: self.func__showStatusBarErrorMsg(showMsg: "The upper limit has been reached".localized)
                self.explain(showMsg: String(bytes: kToMsg.reversed(), encoding: .utf8)!.localized)
                //: return
                return
            }
            //: self.stopPlay()
            self.capture()
            //: self.recordView = TalkingGreetRecordAudioView.init(frame: self.view.frame)
            self.recordView = UserToolDelegate(frame: self.view.frame)
            //: self.recordView?.show()
            self.recordView?.sizeShow()
            //: self.recordView?.audioFileUpload = {[weak self] dict in
            self.recordView?.audioFileUpload = { [weak self] dict in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: let data = NSData.init(contentsOfFile: dict["filePath"] as! String)
                let data = NSData(contentsOfFile: dict[(notiWithTableImageTitle.replacingOccurrences(of: "title", with: "f") + String(const_sizeData.suffix(6)))] as! String)
                //: guard data != nil else {
                guard data != nil else {
                    //: return
                    return
                }
                //: self.requestUploadAudioFileData(data: data!, time: dict["time"] as! String)
                self.registerFile(data: data!, time: dict[(notiEffectStr.replacingOccurrences(of: "to", with: "i"))] as! String)
            }
        }
    }
}

//: extension TalkingGreetAudioVC {
extension ChannelThen {
    //: func setMutedDetection() {
    func stopEmpty() {
        //: RBDMuteSwitch.shared.detectMuteSwitch()
        ScreenMute.shared.intervaleraction()
        //: RBDMuteSwitch.shared.isMutedBlock = { [weak self] mute in
        ScreenMute.shared.isMutedBlock = { [weak self] mute in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.isMute = mute
            self.isMute = mute
        }
    }
}

// MARK: - UITableViewDelegate, UITableViewDataSource

//: extension TalkingGreetAudioVC: UITableViewDelegate, UITableViewDataSource {
extension ChannelThen: UITableViewDelegate, UITableViewDataSource {
    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return 1
        return 1
    }

    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: MainTable.backgroundView?.isHidden = dataSourceArr.count > 0
        MainTable.backgroundView?.isHidden = dataSourceArr.count > 0
        //: MainTable.bounces = dataSourceArr.count > 0
        MainTable.bounces = dataSourceArr.count > 0
        //: return dataSourceArr.count
        return dataSourceArr.count
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt _: IndexPath) -> CGFloat {
        //: return 101
        return 101
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: let identifier = TalkingGreetAudioCell.className()
        let identifier = StreamViewThen.className()
        //: let cell: TalkingGreetAudioCell = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! TalkingGreetAudioCell
        let cell: StreamViewThen = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! StreamViewThen

        //: cell.setGreetAudioCell(model: self.dataSourceArr[indexPath.section], row: indexPath.section, isLast: indexPath.section == self.dataSourceArr.count-1)
        cell.audio(model: self.dataSourceArr[indexPath.section], row: indexPath.section, isLast: indexPath.section == self.dataSourceArr.count - 1)

        //: if isPlaying && playIndex?.row == indexPath.section {
        if isPlaying && playIndex?.row == indexPath.section {
            //: cell.syncPlayState(isPlaying: true)
            cell.ting(isPlaying: true)
            //: }else {
        } else {
            //: cell.syncPlayState(isPlaying: false)
            cell.ting(isPlaying: false)
        }

        //: cell.audioPlayActionBlock = { [weak self] in
        cell.audioPlayActionBlock = { [weak self] in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: RBDMuteSwitch.shared.detectMuteSwitch()
            ScreenMute.shared.intervaleraction()
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                //: self.playAudio(index: indexPath)
                self.naturalEvent(index: indexPath)
            }
        }
        //: cell.editActionBlock = { [weak self] in
        cell.editActionBlock = { [weak self] in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.stopPlay()
            self.capture()
            //: self.editTitle(index: indexPath.section)
            self.colorUp(index: indexPath.section)
        }
        //: return cell
        return cell
    }

    //: func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
    func tableView(_: UITableView, didSelectRowAt _: IndexPath) {}

    // ios11以前
    //: func tableView(_ tableView: UITableView, editActionsForRowAt indexPath: IndexPath) -> [UITableViewRowAction]? {
    func tableView(_: UITableView, editActionsForRowAt indexPath: IndexPath) -> [UITableViewRowAction]? {
        //: let deleteBtn = UITableViewRowAction(style: .normal, title: "Delete".localized) { action, indexP in
        let deleteBtn = UITableViewRowAction(style: .normal, title: (userSumervalData.replacingOccurrences(of: "fill", with: "D") + String(noti_identityTitle)).localized) { _, _ in

            //: self.shoWdeleteAlert(index: indexPath.section)
            self.praise(index: indexPath.section)
        }
        //: deleteBtn.backgroundColor = UIColor(hex: "#FF506D")
        deleteBtn.backgroundColor = UIColor(hex: (String(k_currentId.suffix(5)) + String(mainShareKey.suffix(2))))

        //: return [deleteBtn]
        return [deleteBtn]
    }

    //: @available(iOS 11.0, *)
    @available(iOS 11.0, *)
    //: func tableView(_ tableView: UITableView, trailingSwipeActionsConfigurationForRowAt indexPath: IndexPath) -> UISwipeActionsConfiguration? {
    func tableView(_: UITableView, trailingSwipeActionsConfigurationForRowAt indexPath: IndexPath) -> UISwipeActionsConfiguration? {
        //: let deleteBtn = UIContextualAction(style: .normal, title: nil) { action, view, completionHandler in
        let deleteBtn = UIContextualAction(style: .normal, title: nil) { _, _, completionHandler in

            //: self.shoWdeleteAlert(index: indexPath.section)
            self.praise(index: indexPath.section)
            //: completionHandler(false)
            completionHandler(false)
        }

        //: if let deleteBtnTrashcan =  UIImage.BundleImageNamed(name: "btn_message_delete_nor").cgImage {
        if let deleteBtnTrashcan = UIImage.bundle(name: (String(notiFileFormat.suffix(6)) + String(notiSignMsg.prefix(7)) + String(main_indexKey))).cgImage {
            //: deleteBtn.image = ImageWithoutRender(cgImage: deleteBtnTrashcan, scale: UIScreen.main.nativeScale, orientation: .up)
            deleteBtn.image = MakeWithoutRender(cgImage: deleteBtnTrashcan, scale: UIScreen.main.nativeScale, orientation: .up)
        }
        //: deleteBtn.backgroundColor = .white
        deleteBtn.backgroundColor = .white
        //: let actions = [deleteBtn]
        let actions = [deleteBtn]
        //: let config = UISwipeActionsConfiguration(actions: actions)
        let config = UISwipeActionsConfiguration(actions: actions)
        //: config.performsFirstActionWithFullSwipe = false
        config.performsFirstActionWithFullSwipe = false
        //: return config
        return config
    }

    //: func playAudio(index: IndexPath) {
    func naturalEvent(index: IndexPath) {
        //: guard !TalkingSocketManager.shared.isName else {
        guard !GiftCommentSocketDelegate.shared.isName else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.explain(showMsg: user_topPath)
            //: return
            return
        }
        //: if isPlaying && playIndex == index {
        if isPlaying && playIndex == index {
            //: isPlaying = false
            isPlaying = false
            //: MainTable.reloadData()
            MainTable.reloadData()
            //: self.player?.stopPlay()
            self.player?.slate()
            //: return
            return
        }
        //: if isPlaying {
        if isPlaying {
            //: currenCell = MainTable.cellForRow(at: playIndex ?? IndexPath()) as? TalkingGreetAudioCell
            currenCell = MainTable.cellForRow(at: playIndex ?? IndexPath()) as? StreamViewThen
            //: if currenCell != nil {
            if currenCell != nil {
                //: currenCell?.syncPlayState(isPlaying: false)
                currenCell?.ting(isPlaying: false)
            }
        }

        //: playIndex = index
        playIndex = index
        //: let model = self.dataSourceArr[index.section]
        let model = self.dataSourceArr[index.section]

        //: if isMute || AVAudioSession.sharedInstance().outputVolume <= 0 {
        if isMute || AVAudioSession.sharedInstance().outputVolume <= 0 {
            //: self.func__showStatusBarErrorMsg(showMsg: "Currently mute".localized)
            self.explain(showMsg: (String(mainReloadMessage.prefix(3)) + notiReplyUrl.replacingOccurrences(of: "log", with: "e") + "y mute").localized)
            //: return
            return
        }
        //: if self.player == nil {
        if self.player == nil {
            //: self.player = TalkingAudioPlayTool.shared
            self.player = ValuePlayerDelegate.shared
            //: self.player?.delegate = self
            self.player?.delegate = self
        }
        //: self.player?.stopPlay()
        self.player?.slate()
        //: self.player?.play(urlStr: model.content)
        self.player?.shellPlayer(urlStr: model.content)
        //: isPlaying = true
        isPlaying = true
        //: currenCell = MainTable.cellForRow(at: playIndex ?? IndexPath()) as? TalkingGreetAudioCell
        currenCell = MainTable.cellForRow(at: playIndex ?? IndexPath()) as? StreamViewThen
        //: if currenCell != nil {
        if currenCell != nil {
            //: currenCell?.syncPlayState(isPlaying: true)
            currenCell?.ting(isPlaying: true)
        }
    }

    //: func editTitle(index: Int) {
    func colorUp(index: Int) {
        //: let view = TalkingEditAlertView.init(title: "Edit Title".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "OK".localized, config: nil)
        let view = ActualViewDelegate(title: (String(showResFormat.prefix(4)) + " Title").localized, leftBtnTitle: (String(kBlackBackStr.prefix(6))).localized, rightBtnTitle: "OK".localized, config: nil)
        //: view.show()
        view.errorWith()

        //: view.rightBlock = { [weak self] text in
        view.rightBlock = { [weak self] text in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: ProgressHUD.show()
            MakeView.notToPic()

            //: var model: TalkingGreetModel = self.dataSourceArr[index]
            var model: SizeTransformable = self.dataSourceArr[index]
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["id"] = model.greetId
            dict["id"] = model.greetId
            //: dict["remark"] = text
            dict[(appImageRequestName.replacingOccurrences(of: "table", with: "r") + constEnterFormat.replacingOccurrences(of: "data", with: "k"))] = text

            //: TalkingMeRequestTool.req_UpdateAudioRemark(params: dict) { succeed, result, errorModel in
            NoneventRequestTool.equalCompletion(params: dict) { succeed, _, _ in
                //: ProgressHUD.dismiss()
                MakeView.presentAddLab()
                //: if succeed {
                if succeed {
                    //: model.remark = text
                    model.remark = text
                    //: self.dataSourceArr[index] = model
                    self.dataSourceArr[index] = model
                    //: self.MainTable.reloadData()
                    self.MainTable.reloadData()
                }
            }
        }
    }

    //: @objc func stopPlay() {
    @objc func capture() {
        //: if isPlaying {
        if isPlaying {
            //: isPlaying = false
            isPlaying = false
            //: self.player?.stopPlay()
            self.player?.slate()
            //: if currenCell != nil {
            if currenCell != nil {
                //: currenCell?.syncPlayState(isPlaying: false)
                currenCell?.ting(isPlaying: false)
            }
        }
    }
}

//: extension TalkingGreetAudioVC: TalkingAudioPlayToolDelegate {
extension ChannelThen: GiftThen {
    //: func playerStatusChange(status: AudioPlayerStatus) {
    func style(status: EventStrideable) {
        //: switch status {
        switch status {
        //: case .PlayerFinish:
        case .PlayerFinish:
            //: stopPlay()
            capture()
        //: break
        //: case .PlayerInterruption:
        case .PlayerInterruption:
            //: stopPlay()
            capture()
        //: break
        //: default:
        default:
            //: break
            break
        }
    }
}

// MARK: - Layout

//: extension TalkingGreetAudioVC {
extension ChannelThen {
    //: private func setupEmptyDataView() {
    private func searchCreateView() {
        //: var style = EmptyStyle()
        var style = ViewEmptyStyle()
        //: style.TipsTitle = "Click the button below to add\ncontent".localized
        style.TipsTitle = String(bytes: notiAtFormat.map{maleRaw(make: $0)}, encoding: .utf8)!.localized
        //: emptyView = EmptyView.init(frame: self.view.frame, style: style)
        emptyView = EmptyView(frame: self.view.frame, style: style)
        //: emptyView.isHidden = true
        emptyView.isHidden = true
        //: emptyView.isScrollEnabled = false
        emptyView.isScrollEnabled = false
        //: emptyView.backgroundColor = .appBgColor()
        emptyView.backgroundColor = .pushAside()
        //: MainTable.backgroundView = emptyView
        MainTable.backgroundView = emptyView

        //: emptyView.emptyBlock = { [weak self] in
        emptyView.emptyBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.MainTable.mj_header?.beginRefreshing()
            self.MainTable.mj_header?.beginRefreshing()
        }
    }

    //: private func designView() {
    private func instance() {
        //: self.view.addSubview(addButton)
        self.view.addSubview(addButton)
        //: addButton.snp.makeConstraints { make in
        addButton.snp.makeConstraints { make in
            //: make.bottom.equalToSuperview().offset(-34)
            make.bottom.equalToSuperview().offset(-34)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: 267, height: 50))
            make.size.equalTo(CGSize(width: 267, height: 50))
        }
        //: self.view.addSubview(MainTable)
        self.view.addSubview(MainTable)
        //: MainTable.register(TalkingGreetAudioCell.self, forCellReuseIdentifier: TalkingGreetAudioCell.className())
        MainTable.register(StreamViewThen.self, forCellReuseIdentifier: StreamViewThen.className())
        //: MainTable.separatorStyle = .none
        MainTable.separatorStyle = .none
        //: MainTable.dataSource = self
        MainTable.dataSource = self
        //: MainTable.delegate = self
        MainTable.delegate = self
        //: MainTable.snp.makeConstraints { make in
        MainTable.snp.makeConstraints { make in
            //: make.edges.equalTo(self.view)
            make.edges.equalTo(self.view)
            //: make.bottom.equalTo(addButton.snp.top).offset(-16)
            make.bottom.equalTo(addButton.snp.top).offset(-16)
        }
        //: MainTable.tableFooterView = UIView.init()
        MainTable.tableFooterView = UIView()

        //: let headerView = UIView.init(frame: CGRect.init(x: 0, y: 0, width: ScreenWidth, height: 145))
        let headerView = UIView(frame: CGRect(x: 0, y: 0, width: main_colorData, height: 145))
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.textColor = UIColor.appValueDetailColor()
        label.textColor = UIColor.valueColor()
        //: label.font = UIFont.pingfangRugularFont(fontSize: 15)
        label.font = UIFont.rugular(fontSize: 15)
        //: label.numberOfLines = 0
        label.numberOfLines = 0
        //: let string = "Add multiple voice greetings, and the system will send voice messages by turns;\nClick the bubble to play the voice, slide to the voice area to delete it, and the voice title can be edited.".localized
        let string = String(bytes: userCellData.map{acceptInput(voice: $0)}, encoding: .utf8)!.localized
        //: let attri = NSMutableAttributedString.init(string: string)
        let attri = NSMutableAttributedString(string: string)
        //: let style = NSMutableParagraphStyle.init()
        let style = NSMutableParagraphStyle()
        //: style.lineSpacing = 2
        style.lineSpacing = 2
        //: attri.yy_paragraphStyle = style
        attri.yy_paragraphStyle = style
        //: label.attributedText = attri
        label.attributedText = attri
        //: headerView.addSubview(label)
        headerView.addSubview(label)
        //: label.snp.makeConstraints { make in
        label.snp.makeConstraints { make in
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
            //: make.bottom.equalTo(-16)
            make.bottom.equalTo(-16)
            //: make.top.equalTo(12)
            make.top.equalTo(12)
        }
        //: MainTable.tableHeaderView = headerView
        MainTable.tableHeaderView = headerView
    }
}
