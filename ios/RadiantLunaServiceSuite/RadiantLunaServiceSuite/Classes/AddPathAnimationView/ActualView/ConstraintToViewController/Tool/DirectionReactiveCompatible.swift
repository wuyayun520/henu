
//: Declare String Begin

/*: "msgType" :*/
fileprivate let noti_labelData:String = "talk leading model formsgTyp"
fileprivate let noti_tapWhiteName:String = "user"

/*: "tips" :*/
fileprivate let show_sizeId:[Character] = ["t","i","p","s"]

/*: "extra" :*/
fileprivate let show_atFormat:[Character] = ["e","x","t","r","a"]

/*: "msgInfo" :*/
fileprivate let dataFirstMsg:[Character] = ["m","s"]
fileprivate let dataRawName:String = "index gift cancelgInfo"

/*: "user" :*/
fileprivate let main_showFormat:[Character] = ["u","s","e","r"]

/*: "nickname" :*/
fileprivate let showViewKey:[UInt8] = [0xc5,0xc2,0xc8,0xc0,0xc5,0xca,0xc6,0xce]

				private func endUser(interaction num: UInt8) -> UInt8 {
					return num ^ 171
				}

/*: "headPic" :*/
fileprivate let k_cornerPath:[UInt8] = [0x1,0xfe,0xfa,0xfd,0xe9,0x2,0xfc]

				fileprivate func phaseSpaceCustom(to num: UInt8) -> UInt8 {
					let value = Int(num) - 153
					if value < 0 {
						return UInt8(value + 256)
					} else {
						return UInt8(value)
					}
				}

/*: "gift" :*/
fileprivate let data_tableText:[Character] = ["g","i","f","t"]

/*: "isGroup" :*/
fileprivate let show_beginSocialElseKey:String = "item"
fileprivate let dataPairMakeLabelValue:[Character] = ["s","G","r","o","u","p"]

/*: "WantGift" :*/
fileprivate let constCameraMessage:[Character] = ["W","a","n"]
fileprivate let kEventCellPageMsg:String = "tGiftcontent let self else"

/*: "video" :*/
fileprivate let const_keyTitleMessage:[Character] = ["v","i","d","e","o"]

/*: "audio" :*/
fileprivate let data_fieldMessage:[Character] = ["a","u","d","i","o"]

/*: "messageType" :*/
fileprivate let userConstraintStartName:[Character] = ["m","e","s","s","a","g","e","T","y","p","e"]

/*: "contentType" :*/
fileprivate let data_theContent:[Character] = ["c","o","n","t","e","n","t","T","y","p","e"]

/*: "ImgMsg" :*/
fileprivate let appHomeData:[Character] = ["I","m","g","M","s","g"]

/*: "imageUri" :*/
fileprivate let kRefreshBlockUrl:String = "modeage"
fileprivate let const_viewUpKey:String = "color make dataUri"

/*: "type" :*/
fileprivate let data_addFormat:String = "tykey"

/*: "isBurn" :*/
fileprivate let show_viewName:[Character] = ["i","s","B","u","r","n"]

/*: "giftId" :*/
fileprivate let k_pushKey:String = "row target var self contentgiftI"
fileprivate let main_tableUrl:String = "clear"

/*: "params" :*/
fileprivate let noti_keyDataPath:[Character] = ["p","a","r","a","m","s"]

/*: "audioLength" :*/
fileprivate let show_nowViewId:[Character] = ["a","u","d","i","o","L","e","n","g","t","h"]

/*: "audioSandbox" :*/
fileprivate let appContentUrl:[Character] = ["a","u","d","i","o","S","a","n","d","b","o","x"]

/*: "senderId" :*/
fileprivate let dataRecordUrl:String = "server"
fileprivate let showButtonName:String = "price if model return mynderId"

/*: "AudioMsg" :*/
fileprivate let k_userMessage:[Character] = ["A","u","d","i","o","M","s","g"]

/*: "content" :*/
fileprivate let const_mediumMsg:String = "contcornernt"

/*: "TxtMsg" :*/
fileprivate let constCenterMsg:[Character] = ["T"]
fileprivate let app_statusStr:String = "model selfxtMsg"

/*: "message" :*/
fileprivate let userManagerData:String = "meappage"

/*: "quoteMsgId" :*/
fileprivate let const_blockContent:String = "qpair"
fileprivate let main_countMsg:String = "oteMsgIdlab lab else point self"

/*: "quoteMsgInfo" :*/
fileprivate let user_rangeTitle:String = "instance toquoteM"

/*: "rYMsgType" :*/
fileprivate let notiResultMessageTitle:String = "rYMsgTymodel touch push birthday any"
fileprivate let notiViewText:[Character] = ["p","e"]

/*: "GJ:Sess:AutoGreet:TxtCustom" :*/
fileprivate let userCropName:String = "GJ:Sesmanager angle front layer"
fileprivate let appLiveUrl:String = "to guard push main viewtoGreet:"
fileprivate let appTotalPath:[Character] = ["T"]
fileprivate let kLabelTitle:[Character] = ["x","t","C","u","s","t","o","m"]

/*: "changeDirection" :*/
fileprivate let userPlainKey:[Character] = ["c","h","a","n","g","e","D","i","r","e","c"]
fileprivate let const_gestureId:String = "tclickon"

/*: "system" :*/
fileprivate let constMakeId:String = "syswindowm"

/*: "title" :*/
fileprivate let main_fatalMessage:String = "TITLE"

/*: "icon" :*/
fileprivate let app_modelMessage:String = "ICON"

/*: "icon_chats_pcr" :*/
fileprivate let show_feedbackName:[Character] = ["i","c","o","n","_","c","h","a","t","s","_","p"]
fileprivate let main_actionContent:String = "server"

/*: "status" :*/
fileprivate let k_dataStr:[Character] = ["s","t","a","t","u","s"]

/*: "msgIncome" :*/
fileprivate let mainModelText:String = "section re page iconmsgI"

/*: "msgId" :*/
fileprivate let user_selectedCenterValue:String = "msgIdto quote make make image"

/*: "EnterHashable" :*/
fileprivate let appBeautyPath:String = "pick sizeTMsgTr"
fileprivate let k_containerSpeechPath:String = "text"
fileprivate let const_gestureValueName:String = "nviewlat"

/*: "TMsgTranslatedContent" :*/
fileprivate let notiImageCountMsg:String = "TMsgTrmatch search list"
fileprivate let noti_videoTopTitle:String = "tedCself add right"

/*: "formatType" :*/
fileprivate let constTableStr:[Character] = ["f","o","r","m","a","t","T","y"]
fileprivate let const_indexTitle:[Character] = ["p","e"]

/*: "plain" :*/
fileprivate let data_mValue:String = "pimagein"

/*: "imageData" :*/
fileprivate let data_byFormat:[Character] = ["i","m","a","g","e"]
fileprivate let main_characteristicFormat:String = "during var for manager sizeData"

/*: "VideoMsg" :*/
fileprivate let notiCenterId:[Character] = ["V","i","d","e","o","M","s","g"]

/*: "match" :*/
fileprivate let showLiveData:[Character] = ["m","a","t","c","h"]

/*: "userIntro" :*/
fileprivate let constToolTopSearchUrl:String = "usuite"
fileprivate let show_logAppData:String = "erIntrouser self in ok"

/*: "targetId" :*/
fileprivate let k_succeedItemUrl:[Character] = ["t","a","r","g","e","t"]
fileprivate let constMakePopFormat:[Character] = ["I","d"]

/*: "yyyy-MM-dd HH:mm:ss" :*/
fileprivate let data_nameMsg:String = "bag superyyyy-"
fileprivate let main_modelName:[Character] = [" ","H","H",":","m","m",":","s","s"]

/*: "sendTime" :*/
fileprivate let noti_infoNowUrl:String = "back new ifsendTi"
fileprivate let data_currentText:String = "mno"

/*: "isRead" :*/
fileprivate let data_lockUrl:[Character] = ["i","s","R","e","a","d"]

/*: "audioData" :*/
fileprivate let showTaskName:String = "make live app succeedaudioD"
fileprivate let kIndexPath:[Character] = ["a","t","a"]

/*: "audioUri" :*/
fileprivate let const_atKey:String = "send"
fileprivate let mainBottomKey:[Character] = ["u","d","i","o","U","r","i"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  DirectionReactiveCompatible.swift

//: import Foundation
import Foundation

// MARK: - 枚举定义

//: @objc public enum TXMessageContentType: UInt {
@objc public enum ValueBasicType: UInt {
    //: case TXMessageContentType_text = 1
    case TXMessageContentType_text = 1
    //: case TXMessageContentType_picture
    case TXMessageContentType_picture
    //: case TXMessageContentType_gift
    case TXMessageContentType_gift
    //: case TXMessageContentType_askforGift
    case TXMessageContentType_askforGift
    //: case TXMessageContentType_voice
    case TXMessageContentType_voice
    //: case TXMessageContentType_local
    case TXMessageContentType_local
    //: case TXMessageContentType_tips
    case TXMessageContentType_tips
    //: case TXMessageContentType_userCard
    case TXMessageContentType_userCard
    //: case TXMessageContentType_video
    case TXMessageContentType_video
}

// MARK: - DirectionReactiveCompatible

//: @objcMembers public class TUISocialChatManager: NSObject {
@objcMembers public class DirectionReactiveCompatible: NSObject {
    // MARK: - 公共方法

    /// 获取消息插入的时间戳 毫秒
    //: public class func getMessageInsertTime() -> Int64 {
    public class func scaleValue() -> Int64 {
        //: let curDate = Date().timeIntervalSince1970
        let curDate = Date().timeIntervalSince1970
        //: let msInterval = Int64(floor(curDate * 1000))
        let msInterval = Int64(floor(curDate * 1000))
        //: var deltaTime = Int64(V2TIMManager.sharedInstance().getServerTime())
        var deltaTime = Int64(V2TIMManager.sharedInstance().getServerTime()) // 秒为单位
        //: if deltaTime < 1 {
        if deltaTime < 1 {
            //: deltaTime = 0
            deltaTime = 0
        }
        //: let timeStamp = msInterval - deltaTime
        let timeStamp = msInterval - deltaTime
        //: return timeStamp
        return timeStamp
    }

    // MARK: - 腾讯IM消息处理

    /// 腾讯IM消息插入
    //: public class func insertTXMessage(withExtral extral: [String: Any], toUid: String, isSender: Bool, isTip: Bool) -> AddCellData? {
    public class func rangePacketModel(withExtral extral: [String: Any], toUid: String, isSender: Bool, isTip: Bool) -> AddCellData? {
        //: let data = handleTXMessageSend(withExtral: extral, isSender: isSender, isTip: isTip)
        let data = impingeOpen(withExtral: extral, isSender: isSender, isTip: isTip)
        //: if let data = data {
        if let data = data {
            //: EditManageressV2Listener.shared.func__handleMsg(targetId: toUid, msgCellData: data, isPrivete: true)
            EditManageressV2Listener.shared.funcTabColor(targetId: toUid, msgCellData: data, isPrivete: true)
        }
        //: return data
        return data
    }

    /// 腾讯IM发送消息组装
    //: public class func handleTXMessageSend(withExtral extral: [String: Any], isSender: Bool, isTip: Bool) -> AddCellData? {
    public class func impingeOpen(withExtral extral: [String: Any], isSender: Bool, isTip: Bool) -> AddCellData? {
        //: if isTip {
        if isTip {
            //: var extralDict = extral
            var extralDict = extral
            //: extralDict["msgType"] = "tips"
            extralDict[(String(noti_labelData.suffix(6)) + noti_tapWhiteName.replacingOccurrences(of: "user", with: "e"))] = (String(show_sizeId))
            //: let extraDict = ["extra": extralDict]
            let extraDict = [(String(show_atFormat)): extralDict]
            //: let extra = parseTXMessageJsonStr(data: extraDict)
            let extra = fromAccess(data: extraDict)
            //: let data = TalkingChatTipsMsgCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = SuccessAppThen(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extra.data(using: .utf8)
            let cData = extra.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: data.innerMessage.isExcludedFromLastMessage = true
            data.innerMessage.isExcludedFromLastMessage = true
            //: let model = AbTalkingChatMsgBaseModel()
            let model = ScoreModel()
            //: if let tipModel = try? AbTalkingChatMsgTipsModel(dictionary: extralDict) {
            if let tipModel = try? EqualShareModel(dictionary: extralDict) {
                //: model.tips = tipModel
                model.tips = tipModel
            }
            //: data.msgModel = model
            data.msgModel = model
            //: return data
            return data
        }

        //: var tempDict = extral
        var tempDict = extral
        //: guard let msgInfo = extral["msgInfo"] as? [String: Any] else {
        guard let msgInfo = extral[(String(dataFirstMsg) + String(dataRawName.suffix(5)))] as? [String: Any] else {
            //: return nil
            return nil
        }

        //: tempDict["user"] = [
        tempDict[(String(main_showFormat))] = [
            //: "uid": IconContainerAppManager.share.loginUserMode.userID,
            "uid": IconContainerAppManager.share.loginUserMode.userID,
            //: "nickname": IconContainerAppManager.share.loginUserMode.nickname,
            String(bytes: showViewKey.map{endUser(interaction: $0)}, encoding: .utf8)!: IconContainerAppManager.share.loginUserMode.nickname,
            //: "headPic": IconContainerAppManager.share.loginUserMode.headPic,
            String(bytes: k_cornerPath.map{phaseSpaceCustom(to: $0)}, encoding: .utf8)!: IconContainerAppManager.share.loginUserMode.headPic,
            //: "age": IconContainerAppManager.share.loginUserMode.age,
            "age": IconContainerAppManager.share.loginUserMode.age,
        ]

        //: let extraDict = ["extra": tempDict]
        let extraDict = [(String(show_atFormat)): tempDict]
        //: let extraString = parseTXMessageJsonStr(data: extraDict)
        let extraString = fromAccess(data: extraDict)

        // 礼物
        //: if msgInfo["msgType"] as? String == "gift" {
        if msgInfo[(String(noti_labelData.suffix(6)) + noti_tapWhiteName.replacingOccurrences(of: "user", with: "e"))] as? String == (String(data_tableText)) {
            //: var data: AddCellData
            var data: AddCellData
            //: if msgInfo["isGroup"] as? String == "1" {  // 群聊
            if msgInfo[(show_beginSocialElseKey.replacingOccurrences(of: "item", with: "i") + String(dataPairMakeLabelValue))] as? String == "1" { // 群聊
                //: data = TalkingGroupChatGiftMsgCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = GiveawayEthnicGroupChatValueCompartmentMetadataCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else {
                //: data = GiftEffectThen(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = GiftEffectThen(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }

            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? ScoreModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 索要礼物
        //: if msgInfo["msgType"] as? String == "WantGift" {
        if msgInfo[(String(noti_labelData.suffix(6)) + noti_tapWhiteName.replacingOccurrences(of: "user", with: "e"))] as? String == (String(constCameraMessage) + String(kEventCellPageMsg.prefix(5))) {
            //: let data =  MakeCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = MakeCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? ScoreModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 文字
        //: if msgInfo["msgType"] as? String == "txt" {
        if msgInfo[(String(noti_labelData.suffix(6)) + noti_tapWhiteName.replacingOccurrences(of: "user", with: "e"))] as? String == "txt" {
            //: var data: AddCellData
            var data: AddCellData
            //: if msgInfo["isGroup"] as? String == "1" {  // 群聊
            if msgInfo[(show_beginSocialElseKey.replacingOccurrences(of: "item", with: "i") + String(dataPairMakeLabelValue))] as? String == "1" { // 群聊
                //: data = ScreenThen(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = ScreenThen(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else {
                //: data = MaxCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = MaxCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? ScoreModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 图片
        //: if msgInfo["msgType"] as? String == "img" {
        if msgInfo[(String(noti_labelData.suffix(6)) + noti_tapWhiteName.replacingOccurrences(of: "user", with: "e"))] as? String == "img" {
            //: let data = FountainheadCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = FountainheadCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? ScoreModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 视频
        //: if msgInfo["msgType"] as? String == "video" {
        if msgInfo[(String(noti_labelData.suffix(6)) + noti_tapWhiteName.replacingOccurrences(of: "user", with: "e"))] as? String == (String(const_keyTitleMessage)) {
            //: let data = EigenvalueOfAMatrixReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = EigenvalueOfAMatrixReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? ScoreModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 语音
        //: if msgInfo["msgType"] as? String == "audio" {
        if msgInfo[(String(noti_labelData.suffix(6)) + noti_tapWhiteName.replacingOccurrences(of: "user", with: "e"))] as? String == (String(data_fieldMessage)) {
            //: let data = ValueCustomCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = ValueCustomCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? ScoreModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 打招呼
        //: if extral["gift"] != nil && (msgInfo["messageType"] as? Int) == 4 {
        if extral[(String(data_tableText))] != nil && (msgInfo[(String(userConstraintStartName))] as? Int) == 4 {
            //: let data = MaxCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = MaxCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? ScoreModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        //: return nil
        return nil
    }

    /// 腾讯IM重发消息组装 （只有私聊在用）
    //: public class func handleTXMessageReSend(withData cellData: AddCellData) -> AddCellData? {
    public class func addColor(withData cellData: AddCellData) -> AddCellData? {
        // 图片
        //: if let reCellData = cellData as? FountainheadCellData {
        if let reCellData = cellData as? FountainheadCellData {
            //: let paths = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true)
            let paths = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true)
            //: var filePath = paths.first!
            var filePath = paths.first!
            //: filePath = (filePath as NSString).appendingPathComponent(MaleMacroDefine.getImageFileName())
            filePath = (filePath as NSString).appendingPathComponent(MaleMacroDefine.currentName())
            //: filePath = (filePath as NSString).appendingPathComponent((reCellData.imageUrl! as NSString).lastPathComponent)
            filePath = (filePath as NSString).appendingPathComponent((reCellData.imageUrl! as NSString).lastPathComponent)

            //: if !FileManager.default.fileExists(atPath: filePath) {
            if !FileManager.default.fileExists(atPath: filePath) {
                //: return nil
                return nil
            }

            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]
            //: msgInfo["contentType"] = "ImgMsg"
            msgInfo[(String(data_theContent))] = (String(appHomeData))
            //: msgInfo["msgType"] = "img"
            msgInfo[(String(noti_labelData.suffix(6)) + noti_tapWhiteName.replacingOccurrences(of: "user", with: "e"))] = "img"
            //: msgInfo["imageUri"] = filePath
            msgInfo[(kRefreshBlockUrl.replacingOccurrences(of: "mode", with: "im") + String(const_viewUpKey.suffix(3)))] = filePath
            //: dic["msgInfo"] = msgInfo
            dic[(String(dataFirstMsg) + String(dataRawName.suffix(5)))] = msgInfo

            //: params["type"] = "ImgMsg"
            params[(data_addFormat.replacingOccurrences(of: "key", with: "pe"))] = (String(appHomeData))
            //: params["pic"] = filePath
            params["pic"] = filePath

            // 重发私密照片
            //: let isBurn = reCellData.msgModel.msgInfo.customSend.isBurn
            let isBurn = reCellData.msgModel.msgInfo.customSend.isBurn
            //: if !isBurn.isEmpty {
            if !isBurn.isEmpty {
                //: params["isBurn"] = isBurn
                params[(String(show_viewName))] = isBurn
                //: } else {
            } else {
                //: params["isBurn"] = "0"
                params[(String(show_viewName))] = "0"
            }

            //: let giftId = reCellData.msgModel.msgInfo.customSend.giftId
            let giftId = reCellData.msgModel.msgInfo.customSend.giftId
            //: if !giftId.isEmpty {
            if !giftId.isEmpty {
                //: params["giftId"] = giftId
                params[(String(k_pushKey.suffix(5)) + main_tableUrl.replacingOccurrences(of: "clear", with: "d"))] = giftId
                //: } else {
            } else {
                //: params["giftId"] = "0"
                params[(String(k_pushKey.suffix(5)) + main_tableUrl.replacingOccurrences(of: "clear", with: "d"))] = "0"
            }

            //: dic["params"] = params
            dic[(String(noti_keyDataPath))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = impingeOpen(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        // 视频
        //: if let reCellData = cellData as? EigenvalueOfAMatrixReactiveCompatible {
        if let reCellData = cellData as? EigenvalueOfAMatrixReactiveCompatible {
            //: reCellData.status = .Msg_Status_Fail
            reCellData.status = .Msg_Status_Fail
            //: return reCellData
            return reCellData
        }

        // 语音
        //: if let reCellData = cellData as? ValueCustomCellData {
        if let reCellData = cellData as? ValueCustomCellData {
            //: let voiceModel = DBUserInfoManager.cache_getAudioDB(message: reCellData.innerMessage)
            let voiceModel = DirectionInfoManager.inputObject(message: reCellData.innerMessage)
            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]

            //: if !voiceModel.db_audioLength.isEmpty {
            if !voiceModel.db_audioLength.isEmpty {
                //: msgInfo["audioLength"] = voiceModel.db_audioLength
                msgInfo[(String(show_nowViewId))] = voiceModel.db_audioLength
                //: params["len"] = voiceModel.db_audioLength
                params["len"] = voiceModel.db_audioLength
            }
            //: if !voiceModel.db_filePath.isEmpty {
            if !voiceModel.db_filePath.isEmpty {
                //: msgInfo["audioSandbox"] = voiceModel.db_filePath
                msgInfo[(String(appContentUrl))] = voiceModel.db_filePath
                //: params["audioSandbox"] = voiceModel.db_filePath
                params[(String(appContentUrl))] = voiceModel.db_filePath
            }
            //: msgInfo["msgType"] = "audio"
            msgInfo[(String(noti_labelData.suffix(6)) + noti_tapWhiteName.replacingOccurrences(of: "user", with: "e"))] = (String(data_fieldMessage))
            //: msgInfo["senderId"] = IconContainerAppManager.share.loginUserMode.userID
            msgInfo[(dataRecordUrl.replacingOccurrences(of: "server", with: "se") + String(showButtonName.suffix(6)))] = IconContainerAppManager.share.loginUserMode.userID
            //: dic["msgInfo"] = msgInfo
            dic[(String(dataFirstMsg) + String(dataRawName.suffix(5)))] = msgInfo

            //: params["type"] = "AudioMsg"
            params[(data_addFormat.replacingOccurrences(of: "key", with: "pe"))] = (String(k_userMessage))
            //: dic["params"] = params
            dic[(String(noti_keyDataPath))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = impingeOpen(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        // 文本
        //: if let reCellData = cellData as? MaxCellData {
        if let reCellData = cellData as? MaxCellData {
            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]

            //: msgInfo["content"] = reCellData.contentStr ?? ""
            msgInfo[(const_mediumMsg.replacingOccurrences(of: "corner", with: "e"))] = reCellData.contentStr ?? ""
            //: msgInfo["msgType"] = "txt"
            msgInfo[(String(noti_labelData.suffix(6)) + noti_tapWhiteName.replacingOccurrences(of: "user", with: "e"))] = "txt"
            //: dic["msgInfo"] = msgInfo
            dic[(String(dataFirstMsg) + String(dataRawName.suffix(5)))] = msgInfo
            //: params["type"] = "TxtMsg"
            params[(data_addFormat.replacingOccurrences(of: "key", with: "pe"))] = (String(constCenterMsg) + String(app_statusStr.suffix(5)))
            //: params["message"] = reCellData.contentStr ?? ""
            params[(userManagerData.replacingOccurrences(of: "app", with: "ss"))] = reCellData.contentStr ?? ""

            //: if reCellData.msgModel.quoteMsgInfo.isQuoteMsg() == true {
            if reCellData.msgModel.quoteMsgInfo.messageTo() == true { // 引用消息重发
                //: params["quoteMsgId"] = reCellData.msgModel.quoteMsgInfo.msgId
                params[(const_blockContent.replacingOccurrences(of: "pair", with: "u") + String(main_countMsg.prefix(8)))] = reCellData.msgModel.quoteMsgInfo.msgId
                //: dic["quoteMsgInfo"] = reCellData.msgModel.quoteMsgInfo
                dic[(String(user_rangeTitle.suffix(6)) + "sgInfo")] = reCellData.msgModel.quoteMsgInfo
                    //: .toDictionary()
                    .toDictionary()
            }

            //: dic["params"] = params
            dic[(String(noti_keyDataPath))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = impingeOpen(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        //: return nil
        return nil
    }

    /// 腾讯IM接收消息组装
    //: public class func handleTXReceiveMessage(withExtral dic: [String: Any], cusData: Data, tempModel: V2TIMMessage) -> AddCellData? {
    public class func toViewModel(withExtral dic: [String: Any], cusData: Data, tempModel: V2TIMMessage) -> AddCellData? {
        //: var cellData: AddCellData?
        var cellData: AddCellData?
        //: let extraDict = dic["extra"] as? [String: Any]
        let extraDict = dic[(String(show_atFormat))] as? [String: Any]
        //: let msgInfoDict = extraDict?["msgInfo"] as? [String: Any]
        let msgInfoDict = extraDict?[(String(dataFirstMsg) + String(dataRawName.suffix(5)))] as? [String: Any]
        //: let user = dic["user"] as? [String: Any] ?? [:]
        let user = dic[(String(main_showFormat))] as? [String: Any] ?? [:]

        //: guard let model = try? AbTalkingChatMsgBaseModel(dictionary: extraDict) else {
        guard let model = try? ScoreModel(dictionary: extraDict) else {
            //: return nil
            return nil
        }
        //: var isSelf = (model.user.uid == IconContainerAppManager.share.loginUserMode.userID.intValue)
        var isSelf = (model.user.uid == IconContainerAppManager.share.loginUserMode.userID.intValue)

        // 后台代发送的自动打招呼消息
        //: var isAutoSendMsg = false
        var isAutoSendMsg = false
        //: if model.msgInfo.uid.intValue == IconContainerAppManager.share.loginUserMode.userID.intValue &&
        if model.msgInfo.uid.intValue == IconContainerAppManager.share.loginUserMode.userID.intValue &&
            //: model.msgInfo.messageType.rawValue == 3 {
            model.msgInfo.messageType.rawValue == 3
        {
            //: isSelf = true
            isSelf = true
            //: isAutoSendMsg = true
            isAutoSendMsg = true
        }

        //: if dic["rYMsgType"] as? String == "GJ:Sess:AutoGreet:TxtCustom" {
        if dic[(String(notiResultMessageTitle.prefix(7)) + String(notiViewText))] as? String == (String(userCropName.prefix(6)) + "s:Au" + String(appLiveUrl.suffix(8)) + String(appTotalPath) + String(kLabelTitle)) {
            //: isSelf = msgInfoDict?["changeDirection"] as? Bool ?? false
            isSelf = msgInfoDict?[(String(userPlainKey) + const_gestureId.replacingOccurrences(of: "click", with: "i"))] as? Bool ?? false
        }

        //: let contentType = messageCellType(dic)
        let contentType = finderSFee(dic)

        // 系统消息
        //: if extraDict?["msgType"] as? String == "system" ||
        if extraDict?[(String(noti_labelData.suffix(6)) + noti_tapWhiteName.replacingOccurrences(of: "user", with: "e"))] as? String == (constMakeId.replacingOccurrences(of: "window", with: "te")) ||
            //: contentType == .TXMessageContentType_tips {
            contentType == .TXMessageContentType_tips
        {
            //: let model = AbTalkingChatMsgBaseModel()
            let model = ScoreModel()
            //: let tipModel = try? AbTalkingChatMsgTipsModel(dictionary: extraDict)
            let tipModel = try? EqualShareModel(dictionary: extraDict)
            //: cellData = TalkingChatTipsMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = SuccessAppThen(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: if let content = dic["content"] as? String {
            if let content = dic[(const_mediumMsg.replacingOccurrences(of: "corner", with: "e"))] as? String {
                //: tipModel?.content = content
                tipModel?.content = content
            }
            //: if tipModel != nil {
            if tipModel != nil {
                //: model.tips = tipModel!
                model.tips = tipModel!
            }
            //: cellData?.msgModel = model
            cellData?.msgModel = model
            //: return cellData
            return cellData
        }

        // 其他消息类型的创建...
        //: switch contentType {
        switch contentType {
        //: case .TXMessageContentType_picture:
        case .TXMessageContentType_picture:
            //: cellData = FountainheadCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = FountainheadCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: case .TXMessageContentType_video:
        case .TXMessageContentType_video:
            //: cellData = EigenvalueOfAMatrixReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = EigenvalueOfAMatrixReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: case .TXMessageContentType_gift:
        case .TXMessageContentType_gift:
            //: if let groupID = tempModel.groupID, !groupID.isEmpty {
            if let groupID = tempModel.groupID, !groupID.isEmpty { // 群聊gift
                //: cellData = TalkingGroupChatGiftMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = GiveawayEthnicGroupChatValueCompartmentMetadataCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else { // 私聊gift
                //: cellData = GiftEffectThen(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = GiftEffectThen(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }

        //: case .TXMessageContentType_askforGift:
        case .TXMessageContentType_askforGift: // 索要礼物
            //: let tempDic = parseTXMessageData(data: cusData)
            let tempDic = atSizeData(data: cusData)
            //: if let extraData = tempDic["extra"] as? [String: Any],
            if let extraData = tempDic[(String(show_atFormat))] as? [String: Any],
               //: let msgInfoData = extraData["msgInfo"] as? [String: Any],
               let msgInfoData = extraData[(String(dataFirstMsg) + String(dataRawName.suffix(5)))] as? [String: Any],
               //: !msgInfoData.isEmpty {
               !msgInfoData.isEmpty
            {
                //: if let info = try? AbTalkingChatMsgInfoModel(dictionary: msgInfoData) {
                if let info = try? TitleErrorModel(dictionary: msgInfoData) {
                    //: model.msgInfo = info
                    model.msgInfo = info
                }
            }
            //: cellData =  MakeCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = MakeCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: cellData?.msgModel = model
            cellData?.msgModel = model

        //: case .TXMessageContentType_voice:
        case .TXMessageContentType_voice:
            //: cellData = ValueCustomCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = ValueCustomCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: if let audioCellData = cellData as? ValueCustomCellData {
            if let audioCellData = cellData as? ValueCustomCellData {
                //: if audioCellData.voiceModel.db_voiceCacheWrap.msgId.isEmpty {
                if audioCellData.voiceModel.db_voiceCacheWrap.msgId.isEmpty {
                    //: let mainKey = checkPrimaryKey(withMsgModel: tempModel)
                    let mainKey = loadTop(withMsgModel: tempModel)
                    //: var cacheWrap = WCDBVoiceMsgTable.db_getVoiceMsg(with: mainKey)
                    var cacheWrap = ContainerMsgTable.wok(with: mainKey)
                    //: if cacheWrap == nil {
                    if cacheWrap == nil {
                        //: cacheWrap = WCDBVoiceMsgTable.db_inserVoiceMsgToDB(packageDict(withTXMessage: tempModel))
                        cacheWrap = ContainerMsgTable.errorHide(showMessage(withTXMessage: tempModel))
                        //: audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        //: } else {
                    } else {
                        //: audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                    }
                }
            }

        //: case .TXMessageContentType_local:
        case .TXMessageContentType_local:
            //: cellData = TalkingChatMatchMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = MaterializationReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: default:
        default:
            //: let title = extraDict?["title"] as? String ?? ""
            let title = extraDict?[(main_fatalMessage.lowercased())] as? String ?? ""
            //: let content = extraDict?["content"] as? String ?? ""
            let content = extraDict?[(const_mediumMsg.replacingOccurrences(of: "corner", with: "e"))] as? String ?? ""
            //: let img = extraDict?["img"] as? String ?? ""
            let img = extraDict?["img"] as? String ?? ""
            //: if !title.isEmpty || !content.isEmpty || !img.isEmpty {
            if !title.isEmpty || !content.isEmpty || !img.isEmpty { // 系统新消息
                //: cellData = TalkingChatSystemNotifJumpMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = OptionCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: cellData?.extraDict = extraDict
                cellData?.extraDict = extraDict
                //: } else if let groupID = tempModel.groupID, !groupID.isEmpty {
            } else if let groupID = tempModel.groupID, !groupID.isEmpty { // 群聊text
                //: cellData = ScreenThen(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = ScreenThen(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else { // 私聊text
                //: cellData = MaxCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = MaxCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }
        }

        // 设置用户信息
        //: if model.user.nickname.isEmpty {
        if model.user.nickname.isEmpty {
            //: var userDict = user
            var userDict = user
            //: userDict.merge(msgInfoDict ?? [:]) { (_, new) in new }
            userDict.merge(msgInfoDict ?? [:]) { _, new in new }
            //: if let userModel = try? AbTalkingChatMsgUserModel(dictionary: userDict) {
            if let userModel = try? ResponseModel(dictionary: userDict) {
                //: if userModel.uid == 0 {
                if userModel.uid == 0 {
                    //: userModel.uid = userDict["id"] as? Int ?? 0
                    userModel.uid = userDict["id"] as? Int ?? 0
                }
                //: if userModel.headPic.isEmpty == true {
                if userModel.headPic.isEmpty == true {
                    //: userModel.headPic = userDict["icon"] as? String ?? ""
                    userModel.headPic = userDict[(app_modelMessage.lowercased())] as? String ?? ""
                }

                //: if isAutoSendMsg {
                if isAutoSendMsg {
                    //: userModel.uid = model.msgInfo.toUid.intValue
                    userModel.uid = model.msgInfo.toUid.intValue
                    //: userModel.headPic =
                    userModel.headPic =
                        //: IconContainerAppManager.share.loginUserMode.headPic ?? ""
                        IconContainerAppManager.share.loginUserMode.headPic ?? ""
                }
                //: model.user = userModel
                model.user = userModel
            }
        }

        //: if model.msgInfo == nil {
        if model.msgInfo == nil {
            //: let msgInfoModel = AbTalkingChatMsgInfoModel()
            let msgInfoModel = TitleErrorModel()
            //: msgInfoModel.content = dic["content"] as? String ?? ""
            msgInfoModel.content = dic[(const_mediumMsg.replacingOccurrences(of: "corner", with: "e"))] as? String ?? ""
            //: msgInfoModel.jumps = extraDict?["extra"] as? [Any] ?? []
            msgInfoModel.jumps = extraDict?[(String(show_atFormat))] as? [Any] ?? []
            //: model.msgInfo = msgInfoModel
            model.msgInfo = msgInfoModel
        }

        // 聊天室送礼消息
        //: if contentType == .TXMessageContentType_gift && tempModel.groupID != nil && !tempModel.groupID!.isEmpty {
        if contentType == .TXMessageContentType_gift && tempModel.groupID != nil && !tempModel.groupID!.isEmpty {
            //: if model.toUser.uid.isEmpty {
            if model.toUser.uid.isEmpty { // 送礼所有人
                //: let toUserInfo = AbTalkingChatMsgToUserModel()
                let toUserInfo = UserModel()
                //: toUserInfo.uid = "all"
                toUserInfo.uid = "all"
                //: toUserInfo.nickname = model.gift.sendNum
                toUserInfo.nickname = model.gift.sendNum
                //: toUserInfo.headPic = "icon_chats_pcr"
                toUserInfo.headPic = (String(show_feedbackName) + main_actionContent.replacingOccurrences(of: "server", with: "cr"))
                //: model.toUser = toUserInfo
                model.toUser = toUserInfo
            }
        }
        //: cellData?.msgModel = model
        cellData?.msgModel = model

        // 本地自定义字段处理
        //: let customStr = String(data: cusData, encoding: .utf8) ?? ""
        let customStr = String(data: cusData, encoding: .utf8) ?? ""
        //: let customDic = NSDictionary.dictionary(withJsonString: customStr) ?? [:]
        let customDic = NSDictionary.sexText(withJsonString: customStr) ?? [:]
        //: let statusStr = customDic["status"] as? String ?? "0"
        let statusStr = customDic[(String(k_dataStr))] as? String ?? "0"
        //: let status = TMsgStatus(rawValue: UInt(statusStr) ?? 0) ?? .Msg_Status_Init
        let status = TMsgStatus(rawValue: UInt(statusStr) ?? 0) ?? .Msg_Status_Init
        //: cellData?.status = status
        cellData?.status = status
        //: cellData?.msgIncome = CGFloat(customDic["msgIncome"] as? Float ?? 0)
        cellData?.msgIncome = CGFloat(customDic[(String(mainModelText.suffix(4)) + "ncome")] as? Float ?? 0)
        //: cellData?.messageType = msgInfoDict?["messageType"] as? Int ?? 0
        cellData?.messageType = msgInfoDict?[(String(userConstraintStartName))] as? Int ?? 0

        //: if let msgId = customDic["msgId"] as? String {
        if let msgId = customDic[(String(user_selectedCenterValue.prefix(5)))] as? String {
            //: cellData?.msgModel.msgInfo.msgId = msgId
            cellData?.msgModel.msgInfo.msgId = msgId
        }

        // 文本翻译
        //: if let translateStatus = customDic["TMsgTranslateStatus"] as? Int {
        if let translateStatus = customDic[(String(appBeautyPath.suffix(6)) + k_containerSpeechPath.replacingOccurrences(of: "text", with: "a") + const_gestureValueName.replacingOccurrences(of: "view", with: "s") + "eStatus")] as? Int {
            //: cellData?.translateStatus = TMsgTranslateStatus(rawValue: UInt(translateStatus)) ?? .MsgTranslate_init
            cellData?.translateStatus = EnterHashable(rawValue: UInt(translateStatus)) ?? .MsgTranslate_init
        }
        //: if let translatedContent = customDic["TMsgTranslatedContent"] as? String {
        if let translatedContent = customDic[(String(notiImageCountMsg.prefix(6)) + "ansla" + String(noti_videoTopTitle.prefix(4)) + "ontent")] as? String {
            //: cellData?.translatedContent = translatedContent
            cellData?.translatedContent = translatedContent
        }

        // 私密照片Cache
        //: if contentType == .TXMessageContentType_picture {
        if contentType == .TXMessageContentType_picture {
            //: let dict = AbTalkingChatMsgIntimatePhotoModel.loadLocalIntimatePhotoDict()
            let dict = BlueprintModel.title()
            //: if let photoData = dict[tempModel.msgID as Any] as? String {
            if let photoData = dict[tempModel.msgID as Any] as? String {
                //: cellData?.msgModel.msgInfo.intimatePhoto = AbTalkingChatMsgIntimatePhotoModel(string: photoData, error: nil)
                cellData?.msgModel.msgInfo.intimatePhoto = BlueprintModel(string: photoData, error: nil)
            }
        }

        // 私密视频Cache
        //: if contentType == .TXMessageContentType_video {
        if contentType == .TXMessageContentType_video {
            //: let dict = AbTalkingChatMsgVideoModel.loadLocalIntimateVideoDict()
            let dict = EliteModel.publicTransportCell()
            //: if let videoData = dict[tempModel.msgID as Any] as? String {
            if let videoData = dict[tempModel.msgID as Any] as? String {
                //: cellData?.msgModel.msgInfo.video = AbTalkingChatMsgVideoModel(string: videoData, error: nil)
                cellData?.msgModel.msgInfo.video = EliteModel(string: videoData, error: nil)
            }
        }

        // 索要礼物Cache
        //: if contentType == .TXMessageContentType_askforGift {
        if contentType == .TXMessageContentType_askforGift {
            //: let arr = WantGiftModel.loadAskForGiftArr()
            let arr = PinModel.no()
            //: if arr.contains(tempModel.msgID as Any) {
            if arr.contains(tempModel.msgID as Any) { // 已发送
                //: cellData?.msgModel.msgInfo.wantGift.sendStatus = 1
                cellData?.msgModel.msgInfo.wantGift.sendStatus = 1
            }
        }

        //: return cellData
        return cellData
    }

    // MARK: - 辅助方法

    /// 根据字典获取消息类型
    //: public class func messageCellType(_ extraDic: [String: Any]) -> TXMessageContentType {
    public class func finderSFee(_ extraDic: [String: Any]) -> ValueBasicType {
        //: let dic = extraDic["extra"] as? [String: Any]
        let dic = extraDic[(String(show_atFormat))] as? [String: Any]
        //: let msgInfo = dic?["msgInfo"] as? [String: Any]
        let msgInfo = dic?[(String(dataFirstMsg) + String(dataRawName.suffix(5)))] as? [String: Any]
        //: guard let model = try? AbTalkingChatMsgBaseModel(dictionary: dic) else {
        guard let model = try? ScoreModel(dictionary: dic) else {
            //: return .TXMessageContentType_text
            return .TXMessageContentType_text
        }

        // 系统消息 官方小密
        //: if dic?["formatType"] as? String == "plain" {
        if dic?[(String(constTableStr) + String(const_indexTitle))] as? String == (data_mValue.replacingOccurrences(of: "image", with: "la")) {
            //: let imageUri = dic?["imageUri"] as? String ?? ""
            let imageUri = dic?[(kRefreshBlockUrl.replacingOccurrences(of: "mode", with: "im") + String(const_viewUpKey.suffix(3)))] as? String ?? ""
            //: let imageData = dic?["imageData"] as? String ?? ""
            let imageData = dic?[(String(data_byFormat) + String(main_characteristicFormat.suffix(4)))] as? String ?? ""
            //: let isImageStyle = !imageData.isEmpty || !imageUri.isEmpty
            let isImageStyle = !imageData.isEmpty || !imageUri.isEmpty
            //: if isImageStyle {
            if isImageStyle {
                //: return .TXMessageContentType_picture
                return .TXMessageContentType_picture
            }
        }

        // 图片||私密照片
        //: if !model.msgInfo.imageUri.isEmpty || !model.msgInfo.imageData.isEmpty || !model.msgInfo.intimatePhoto.originUrl.isEmpty {
        if !model.msgInfo.imageUri.isEmpty || !model.msgInfo.imageData.isEmpty || !model.msgInfo.intimatePhoto.originUrl.isEmpty {
            //: return .TXMessageContentType_picture
            return .TXMessageContentType_picture
        }

        // 接收的视频消息 || 本人发送的视频消息
        //: if !model.msgInfo.video.videoUrl.isEmpty || model.msgInfo.contentType == "VideoMsg" {
        if !model.msgInfo.video.videoUrl.isEmpty || model.msgInfo.contentType == (String(notiCenterId)) {
            //: return .TXMessageContentType_video
            return .TXMessageContentType_video
        }

        // 礼物，非打招呼消息
        //: if !model.gift.name.isEmpty && model.msgInfo.messageType.rawValue != 4 {
        if !model.gift.name.isEmpty && model.msgInfo.messageType.rawValue != 4 {
            //: return .TXMessageContentType_gift
            return .TXMessageContentType_gift
        }

        //: let isVoiceStyle = checkVoiceMsg(msginfo: msgInfo ?? [:])
        let isVoiceStyle = smallnessMsginfo(msginfo: msgInfo ?? [:])
        //: if isVoiceStyle {
        if isVoiceStyle {
            //: return .TXMessageContentType_voice
            return .TXMessageContentType_voice
        }

        //: if (dic?["match"] as? Int) == 1 || (msgInfo?["match"] as? Int) == 1 {
        if (dic?[(String(showLiveData))] as? Int) == 1 || (msgInfo?[(String(showLiveData))] as? Int) == 1 {
            // 匹配消息
            //: return .TXMessageContentType_local
            return .TXMessageContentType_local
        }

        //: if dic?["msgType"] as? String == "tips" || (dic?["messageType"] as? Int) == 17 {
        if dic?[(String(noti_labelData.suffix(6)) + noti_tapWhiteName.replacingOccurrences(of: "user", with: "e"))] as? String == (String(show_sizeId)) || (dic?[(String(userConstraintStartName))] as? Int) == 17 {
            // 提示消息,包含超时未回复
            //: return .TXMessageContentType_tips
            return .TXMessageContentType_tips
        }

        //: if (dic?["formatType"] as? String == "mf") && ((dic?["messageType"] as? Int) == 15 || (dic?["messageType"] as? Int) == 16) {
        if (dic?[(String(constTableStr) + String(const_indexTitle))] as? String == "mf") && ((dic?[(String(userConstraintStartName))] as? Int) == 15 || (dic?[(String(userConstraintStartName))] as? Int) == 16) {
            // 提示消息，打招呼提示送出神秘礼物,对方设置价格消息提示
            //: return .TXMessageContentType_tips
            return .TXMessageContentType_tips
        }

        //: if dic?["msgType"] as? String == "userIntro" {
        if dic?[(String(noti_labelData.suffix(6)) + noti_tapWhiteName.replacingOccurrences(of: "user", with: "e"))] as? String == (constToolTopSearchUrl.replacingOccurrences(of: "suite", with: "s") + String(show_logAppData.prefix(7))) {
            //: return .TXMessageContentType_userCard
            return .TXMessageContentType_userCard
        }

        //: if msgInfo?["msgType"] as? String == "WantGift" || (msgInfo?["messageType"] as? Int) == 25 {
        if msgInfo?[(String(noti_labelData.suffix(6)) + noti_tapWhiteName.replacingOccurrences(of: "user", with: "e"))] as? String == (String(constCameraMessage) + String(kEventCellPageMsg.prefix(5))) || (msgInfo?[(String(userConstraintStartName))] as? Int) == 25 {
            //: return .TXMessageContentType_askforGift
            return .TXMessageContentType_askforGift
        }

        //: return .TXMessageContentType_text
        return .TXMessageContentType_text
    }

    // MARK: - 语音消息处理

    /// 从TX的model里提取参数
    //: public class func packageDict(withTXMessage message: V2TIMMessage) -> [String: Any] {
    public class func showMessage(withTXMessage message: V2TIMMessage) -> [String: Any] {
        //: let extra = String(data: message.customElem.data, encoding: .utf8) ?? ""
        let extra = String(data: message.customElem.data, encoding: .utf8) ?? ""
        //: let dic = NSDictionary.dictionary(withJsonString: extra) ?? [:]
        let dic = NSDictionary.sexText(withJsonString: extra) ?? [:]
        //: let extraData = dic["extra"] as? [String: Any]
        let extraData = dic[(String(show_atFormat))] as? [String: Any]
        //: let msgInfo = extraData?["msgInfo"] as? [String: Any]
        let msgInfo = extraData?[(String(dataFirstMsg) + String(dataRawName.suffix(5)))] as? [String: Any]
        //: let user = extraData?["user"] as? [String: Any]
        let user = extraData?[(String(main_showFormat))] as? [String: Any]

        //: let senderId = msgInfo?["senderId"] as? String ?? ""
        let senderId = msgInfo?[(dataRecordUrl.replacingOccurrences(of: "server", with: "se") + String(showButtonName.suffix(6)))] as? String ?? ""
        //: var info = msgInfo
        var info = msgInfo

        // 自己发送的和接收的数据结构不一样
        //: if senderId == IconContainerAppManager.share.loginUserMode.userID {
        if senderId == IconContainerAppManager.share.loginUserMode.userID {
            //: info?["senderId"] = senderId
            info?[(dataRecordUrl.replacingOccurrences(of: "server", with: "se") + String(showButtonName.suffix(6)))] = senderId
            //: info?["targetId"] = message.userID ?? (user?["targetId"] as? String ?? "")
            info?[(String(k_succeedItemUrl) + String(constMakePopFormat))] = message.userID ?? (user?[(String(k_succeedItemUrl) + String(constMakePopFormat))] as? String ?? "")
            //: } else {
        } else {
            //: info?["senderId"] = "\(msgInfo?["uid"] as? Int ?? 0)"
            info?[(dataRecordUrl.replacingOccurrences(of: "server", with: "se") + String(showButtonName.suffix(6)))] = "\(msgInfo?["uid"] as? Int ?? 0)"
            //: info?["targetId"] = "\(msgInfo?["uid"] as? Int ?? 0)"
            info?[(String(k_succeedItemUrl) + String(constMakePopFormat))] = "\(msgInfo?["uid"] as? Int ?? 0)"
        }

        //: if msgInfo?.keys.contains("msgId") ?? false {
        if msgInfo?.keys.contains((String(user_selectedCenterValue.prefix(5)))) ?? false {
            //: info?["msgId"] = "\(msgInfo?["msgId"] ?? "")"
            info?[(String(user_selectedCenterValue.prefix(5)))] = "\(msgInfo?[(String(user_selectedCenterValue.prefix(5)))] ?? "")"
            //: } else {
        } else {
            //: let customStr = String(data: message.localCustomData, encoding: .utf8) ?? ""
            let customStr = String(data: message.localCustomData, encoding: .utf8) ?? ""
            //: let customDic = NSDictionary.dictionary(withJsonString: customStr) ?? [:]
            let customDic = NSDictionary.sexText(withJsonString: customStr) ?? [:]
            //: if customDic["msgId"] != nil {
            if customDic[(String(user_selectedCenterValue.prefix(5)))] != nil {
                //: info?["msgId"] = "\(customDic["msgId"] ?? "")"
                info?[(String(user_selectedCenterValue.prefix(5)))] = "\(customDic[(String(user_selectedCenterValue.prefix(5)))] ?? "")"
                //: } else {
            } else {
                //: let txMsgId = !message.msgID.isEmpty ? message.msgID : "\(Int64(Date().timeIntervalSince1970))"
                let txMsgId = !message.msgID.isEmpty ? message.msgID : "\(Int64(Date().timeIntervalSince1970))"
                //: info?["msgId"] = txMsgId
                info?[(String(user_selectedCenterValue.prefix(5)))] = txMsgId
            }
        }

        //: if msgInfo?.keys.contains("audioSandbox") ?? false {
        if msgInfo?.keys.contains((String(appContentUrl))) ?? false {
            //: info?["audioSandbox"] = "\(msgInfo?["audioSandbox"] ?? "")"
            info?[(String(appContentUrl))] = "\(msgInfo?[(String(appContentUrl))] ?? "")"
        }
        //: if msgInfo?.keys.contains("audioLength") ?? false {
        if msgInfo?.keys.contains((String(show_nowViewId))) ?? false {
            //: info?["audioLength"] = "\(msgInfo?["audioLength"] ?? "")"
            info?[(String(show_nowViewId))] = "\(msgInfo?[(String(show_nowViewId))] ?? "")"
        }

        //: let formatter = DateFormatter()
        let formatter = DateFormatter()
        //: formatter.dateFormat = "yyyy-MM-dd HH:mm:ss"
        formatter.dateFormat = (String(data_nameMsg.suffix(5)) + "MM-dd" + String(main_modelName))
        //: info?["sendTime"] = formatter.string(from: message.timestamp ?? Date())
        info?[(String(noti_infoNowUrl.suffix(6)) + data_currentText.replacingOccurrences(of: "no", with: "e"))] = formatter.string(from: message.timestamp ?? Date())
        //: info?["isRead"] = senderId == IconContainerAppManager.share.loginUserMode.userID ? "1" : "0"
        info?[(String(data_lockUrl))] = senderId == IconContainerAppManager.share.loginUserMode.userID ? "1" : "0"

        //: return info ?? [:]
        return info ?? [:]
    }

    //: public class func checkPrimaryKey(withMsgModel tempModel: V2TIMMessage) -> String {
    public class func loadTop(withMsgModel tempModel: V2TIMMessage) -> String {
        //: let v2Info = packageDict(withTXMessage: tempModel)
        let v2Info = showMessage(withTXMessage: tempModel)
        //: let key = "\(v2Info["msgId"] ?? "")"
        let key = "\(v2Info[(String(user_selectedCenterValue.prefix(5)))] ?? "")"
        //: print("key = \(key)")
        //: return key
        return key
    }

    // MARK: - JSON处理方法

    //: @objc public class func parseTXMessageData(data: Data) -> [String: Any] {
    @objc public class func atSizeData(data: Data) -> [String: Any] {
        //: do {
        do {
            //: let dict = try JSONSerialization.jsonObject(
            let dict = try JSONSerialization.jsonObject(
                //: with: data, options: .mutableContainers)
                with: data, options: .mutableContainers
            )
            //: return dict as? [String: Any] ?? [:]
            return dict as? [String: Any] ?? [:]
            //: } catch {
        } catch {
            //: return [:]
            return [:]
        }
    }

    //: @objc public class func parseTXMessageJsonStr(data: [String: Any]) -> String {
    @objc public class func fromAccess(data: [String: Any]) -> String {
        //: do {
        do {
            //: let jsonData = try JSONSerialization.data(
            let jsonData = try JSONSerialization.data(
                //: withJSONObject: data, options: [])
                withJSONObject: data, options: []
            )
            //: return String(data: jsonData, encoding: .utf8) ?? ""
            return String(data: jsonData, encoding: .utf8) ?? ""
            //: } catch {
        } catch {
            //: return ""
            return ""
        }
    }

    // MARK: - 语音消息检测

    //: @objc public class func checkVoiceMsg(msginfo: [String: Any]) -> Bool {
    @objc public class func smallnessMsginfo(msginfo: [String: Any]) -> Bool {
        //: if let msgType = msginfo["msgType"] as? String, msgType == "audio" {
        if let msgType = msginfo[(String(noti_labelData.suffix(6)) + noti_tapWhiteName.replacingOccurrences(of: "user", with: "e"))] as? String, msgType == (String(data_fieldMessage)) {
            //: return true
            return true
        }
        //: if let contentType = msginfo["contentType"] as? String,
        if let contentType = msginfo[(String(data_theContent))] as? String,
           //: contentType == "AudioMsg"
           contentType == (String(k_userMessage))
        {
            //: return true
            return true
        }
        //: if let audioData = msginfo["audioData"] as? String, !audioData.isEmpty {
        if let audioData = msginfo[(String(showTaskName.suffix(6)) + String(kIndexPath))] as? String, !audioData.isEmpty {
            //: return true
            return true
        }
        //: if let audioUri = msginfo["audioUri"] as? String, !audioUri.isEmpty {
        if let audioUri = msginfo[(const_atKey.replacingOccurrences(of: "send", with: "a") + String(mainBottomKey))] as? String, !audioUri.isEmpty {
            //: return true
            return true
        }
        //: return false
        return false
    }
}
