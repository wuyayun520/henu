
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let appColorMsg:[UInt8] = [0xcb,0xcc,0xcb,0xd6,0x8a,0xc1,0xcd,0xc6,0xc7,0xd0,0x98,0x8b,0x82,0xca,0xc3,0xd1,0x82,0xcc,0xcd,0xd6,0x82,0xc0,0xc7,0xc7,0xcc,0x82,0xcb,0xcf,0xd2,0xce,0xc7,0xcf,0xc7,0xcc,0xd6,0xc7,0xc6]

/*: "F5F5F5" :*/
fileprivate let kCenterFormat:[Character] = ["F"]
fileprivate let noti_errorKey:String = "title color5F5F5"

/*: "#128CFF" :*/
fileprivate let appFrameName:String = "#128CFFmode nor if"

/*: "Feedback" :*/
fileprivate let kStatusFormat:[Character] = ["F","e","e","d","b","a","c","k"]

/*: "#8370FF" :*/
fileprivate let noti_formalTitle:String = "#"
fileprivate let user_giftId:[Character] = ["8","3","7","0","F","F"]

/*: "Quick Greeting" :*/
fileprivate let showAtActualMsg:String = "Quicmanager let manager else main"
fileprivate let user_indexPath:String = "tviewng"

/*: "Beauty Photos" :*/
fileprivate let user_dateKeyValue:[Character] = ["B","e","a","u","t","y"," ","P","h","o"]
fileprivate let show_itemKey:String = "tomale"

/*: "unlockGift" :*/
fileprivate let constSizeData:[Character] = ["u","n","l","o"]
fileprivate let dataFeeText:String = "load disappear else falseckGift"

/*: "isBurn" :*/
fileprivate let kFemaleContent:[UInt8] = [0xa8,0xb2,0x83,0xb4,0xb3,0xaf]

				private func managingDirector(interaction num: UInt8) -> UInt8 {
					return num ^ 193
				}

/*: "giftId" :*/
fileprivate let constToTitle:[Character] = ["g","i","f","t","I","d"]

/*: "rYMsgType" :*/
fileprivate let main_viewExampleFormat:[Character] = ["r","Y","M","s","g","T","y","p"]
fileprivate let user_labelValue:[Character] = ["e"]

/*: "GJ:CallCustom" :*/
fileprivate let k_infoShowContent:[Character] = ["G","J",":","C","a","l","l"]
fileprivate let app_viewNowFillData:String = "Customcategory type self"

/*: "changeDirection" :*/
fileprivate let appInputContent:[Character] = ["c","h","a","n","g","e","D"]
fileprivate let const_toolId:[Character] = ["i","r","e","c","t","i","o","n"]

/*: "extra" :*/
fileprivate let appViewData:[Character] = ["e","x","t","r","a"]

/*: "msgInfo" :*/
fileprivate let app_squareProMsg:[Character] = ["m","s"]
fileprivate let showErrorId:String = "index self import if titlegInfo"

/*: "toUid" :*/
fileprivate let kUpName:[Character] = ["t","o","U","i","d"]

/*: "wantGift" :*/
fileprivate let mainPointStr:String = "mode make datawantG"
fileprivate let mainErrorMsg:[Character] = ["i","f","t"]

/*: "jumpKey" :*/
fileprivate let const_viewPath:String = "jumpKeynone type"

/*: "truePersonAuth" :*/
fileprivate let app_withMsg:[Character] = ["t","r","u","e","P","e","r","s","o","n","A"]
fileprivate let app_makeId:[Character] = ["u","t","h"]

/*: "logType" :*/
fileprivate let k_nameDropFormat:String = "logTypeequal if show"

/*: "msgType" :*/
fileprivate let noti_videoTapMessage:[UInt8] = [0x65,0x70,0x79,0x54,0x67,0x73,0x6d]

/*: "type" :*/
fileprivate let constColorId:String = "typuser"

/*: "QuickMsg" :*/
fileprivate let noti_signMessage:[Character] = ["Q","u","i","c","k","M","s","g"]

/*: "params" :*/
fileprivate let user_layerMessage:[UInt8] = [0x8d,0x9c,0x8f,0x9c,0x90,0x8e]

				private func indexLevel(raw num: UInt8) -> UInt8 {
					return num ^ 253
				}

/*: "content" :*/
fileprivate let dataProfileCameraModePath:[UInt8] = [0x37,0x3b,0x3a,0x20,0x31,0x3a,0x20]

/*: "txtId" :*/
fileprivate let main_titleDataText:String = "let var share vartxtId"

/*: "message" :*/
fileprivate let constReportStr:[UInt8] = [0x9a,0x92,0x84,0x84,0x96,0x90,0x92]

				private func recordNow(key num: UInt8) -> UInt8 {
					return num ^ 247
				}

/*: "TxtMsg" :*/
fileprivate let dataEditMessage:String = "row view in catchTxtMsg"

/*: "Voice hello download failed. Procedure" :*/
fileprivate let notiViewValue:[UInt8] = [0x2d,0x46,0x40,0x3a,0x3c,0xf7,0x3f,0x3c,0x43,0x43,0x46,0xf7,0x3b,0x46,0x4e,0x45,0x43,0x46,0x38,0x3b,0xf7,0x3d,0x38,0x40,0x43,0x3c,0x3b,0x5,0xf7,0x27,0x49,0x46,0x3a,0x3c,0x3b,0x4c,0x49,0x3c]

				fileprivate func thoughtImage(video num: UInt8) -> UInt8 {
					let value = Int(num) - 215
					if value < 0 {
						return UInt8(value + 256)
					} else {
						return UInt8(value)
					}
				}

/*: "length" :*/
fileprivate let show_firstMessage:String = "letoolgt"
fileprivate let userKeyName:String = "frame"

/*: "audio" :*/
fileprivate let main_removeMsg:[Character] = ["a","u","d","i","o"]

/*: "audioId" :*/
fileprivate let mainSucceedUrl:String = "color"
fileprivate let show_pictureData:[Character] = ["d","i","o","I","d"]

/*: "audioLength" :*/
fileprivate let mainViewTitle:[UInt8] = [0xaf,0xbb,0xaa,0xa7,0xa1,0x82,0xab,0xa0,0xa9,0xba,0xa6]

/*: "audioSandbox" :*/
fileprivate let userTableText:String = "audiinfo"
fileprivate let k_appStr:String = "model"

/*: "senderId" :*/
fileprivate let kLiveId:[UInt8] = [0x64,0x49,0x72,0x65,0x64,0x6e,0x65,0x73]

/*: "AudioMsg" :*/
fileprivate let noti_priceTitle:String = "cookie var ping type toAu"
fileprivate let kLaunchFormat:String = "guide manager new item modeldioMsg"

/*: "Can't send blank message" :*/
fileprivate let dataDirectionName:[Character] = ["C","a","n","\'","t"," ","s","e","n","d"," ","b","l","a","n","k"," "]
fileprivate let notiMiniHeadFormat:String = "mbottomssagbottom"

/*: "quoteMsgId" :*/
fileprivate let app_insertData:String = "quoteMcenter content"
fileprivate let constImageEqualStr:[Character] = ["s","g","I","d"]

/*: "quoteMsgInfo" :*/
fileprivate let kPostKey:[Character] = ["q","u","o"]
fileprivate let noti_filterStr:String = "self tag more managerteMsgInfo"

/*: "contentType" :*/
fileprivate let userHiddenItemValue:[UInt8] = [0x3c,0x48,0x47,0x4d,0x3e,0x47,0x4d,0x2d,0x52,0x49,0x3e]

				fileprivate func imageTarget(of num: UInt8) -> UInt8 {
					let value = Int(num) - 217
					if value < 0 {
						return UInt8(value + 256)
					} else {
						return UInt8(value)
					}
				}

/*: "ImgMsg" :*/
fileprivate let show_placeStopUrl:String = "ImgMsgimage size menu"

/*: "imageUri" :*/
fileprivate let user_playerSizeTryTitle:[UInt8] = [0x67,0x63,0x6f,0x69,0x6b,0x5b,0x7c,0x67]

				private func awakeCellFill(result num: UInt8) -> UInt8 {
					return num ^ 14
				}

/*: "customSend" :*/
fileprivate let main_withFormat:[UInt8] = [0xc6,0xd0,0xd6,0xd1,0xca,0xc8,0xf6,0xc0,0xcb,0xc1]

				private func contentKey(list num: UInt8) -> UInt8 {
					return num ^ 165
				}

/*: "VideoMsg" :*/
fileprivate let userCoverStr:String = "VideoMsgequal view length else item"

/*: "video" :*/
fileprivate let main_firstObjectMessage:[Character] = ["v","i","d","e","o"]

/*: "coverImg" :*/
fileprivate let appMakeTitle:[UInt8] = [0x40,0x4c,0x55,0x46,0x51,0x6a,0x4e,0x44]

/*: "videoUrl" :*/
fileprivate let showToStr:[UInt8] = [0x6c,0x72,0x55,0x6f,0x65,0x64,0x69,0x76]

/*: "duration" :*/
fileprivate let showCornerStr:[UInt8] = [0x34,0x25,0x22,0x31,0x24,0x39,0x3f,0x3e]

				private func statusUp(day num: UInt8) -> UInt8 {
					return num ^ 80
				}

/*: "Documents/User/voice/" :*/
fileprivate let appModelValue:String = "title mode extension normalDocu"
fileprivate let kTillFormat:String = "menvalue"
fileprivate let show_screenPathFormat:String = "/voice/bar string"

/*: "Documents/User/record/" :*/
fileprivate let showThanUrl:String = "to layer class modelDocum"
fileprivate let data_videoCellName:[Character] = ["e","n","t","s","/","U","s","e","r","/","r","e","c","o","r","d","/"]

/*: "msgIncome" :*/
fileprivate let kTransformContent:[Character] = ["m","s","g","I","n","c","o","m","e"]

/*: "mfBean" :*/
fileprivate let show_sizeStr:[UInt8] = [0x57,0x5c,0x78,0x5f,0x5b,0x54]

				private func numerousnessActual(title num: UInt8) -> UInt8 {
					return num ^ 58
				}

/*: "messageType" :*/
fileprivate let main_editMessage:[Character] = ["m","e","s","s","a"]
fileprivate let constToValue:String = "geTypevalue result gender jaw"

/*: "WantGift" :*/
fileprivate let data_colorPath:[Character] = ["W","a","n","t","G","i","f"]
fileprivate let data_bringValueFramePath:String = "up"

/*: _PrivateChatTipsTitle_ :*/
fileprivate let kCornerValue:[Character] = ["_","P","r","i","v","a","t","e","C","h","a","t","T","i","p","s","T","i","t"]
fileprivate let dataImageLaughText:[Character] = ["l","e","_"]

/*: "msgId" :*/
fileprivate let main_willName:[Character] = ["m","s","g","I","d"]

/*: "totalMfCoin" :*/
fileprivate let notiModelId:String = "tcalendar"
fileprivate let data_imageMsg:String = "talMfCoinat self"

/*: "attachSysMsg" :*/
fileprivate let mainOnItemInsideId:[Character] = ["a","t","t","a","c","h","S"]
fileprivate let show_topMessage:String = "ysMsgif data status true self"

/*: "intimatePhoto" :*/
fileprivate let user_sourceMsg:String = "iselectedi"
fileprivate let constDataDetailMessage:[Character] = ["h","o","t","o"]

/*: "totalIntimate" :*/
fileprivate let app_sizeFormat:[Character] = ["t","o","t","a","l"]
fileprivate let k_bottomTitle:String = "add statusIntimate"

/*: "intimate" :*/
fileprivate let kMainUrl:[UInt8] = [0xbe,0xc3,0xc9,0xbe,0xc2,0xb6,0xc9,0xba]

				fileprivate func wearTheTrousers(data num: UInt8) -> UInt8 {
					let value = Int(num) - 85
					if value < 0 {
						return UInt8(value + 256)
					} else {
						return UInt8(value)
					}
				}

/*: "Can't use the function due to your lack of intimacy level." :*/
fileprivate let const_makeRangeKey:[UInt8] = [0x83,0xa1,0xae,0xe7,0xb4,0xe0,0xb5,0xb3,0xa5,0xe0,0xb4,0xa8,0xa5,0xe0,0xa6,0xb5,0xae,0xa3,0xb4,0xa9,0xaf,0xae,0xe0,0xa4,0xb5,0xa5,0xe0,0xb4,0xaf,0xe0,0xb9,0xaf,0xb5,0xb2,0xe0,0xac,0xa1,0xa3,0xab,0xe0,0xaf,0xa6,0xe0,0xa9,0xae,0xb4,0xa9,0xad,0xa1,0xa3,0xb9,0xe0,0xac,0xa5,0xb6,0xa5,0xac,0xee]

/*: "giftNum" :*/
fileprivate let mainSharePartyMakeMsg:String = "greport"
fileprivate let data_expectedUrl:String = "ftNumdo type at"

/*: "pkgItemsetId" :*/
fileprivate let main_attentionName:String = "hidden view model let beautypkgI"
fileprivate let constColorMsg:String = "tIdpath image if less let"

/*: "from" :*/
fileprivate let kBackgroundId:String = "fstyleom"

/*: "wantGiftId" :*/
fileprivate let notiConversationFormat:String = "wanframe"
fileprivate let constEndFormat:String = "Idlevel to value value down"

/*: "WantGiftMsg" :*/
fileprivate let data_countName:[Character] = ["W","a","n","t","G","i"]
fileprivate let data_cellName:String = "ftMsgcenter view open social"

/*: "gift" :*/
fileprivate let const_valueData:String = "ginormalt"

/*: "View info" :*/
fileprivate let mainManagerMessage:String = "return view session track labView i"
fileprivate let showTableData:String = "ncharactero"

/*: "Notes" :*/
fileprivate let notiTextCircleKey:String = "in with hour mainNotes"

/*: "Report" :*/
fileprivate let dataActionMsg:String = "Reportas manager native in status"

/*: "View read receipts?" :*/
fileprivate let appCellName:String = "View rtype center let shared"
fileprivate let kResumeName:String = "eceipts?layer table"

/*: "%@ is using too low a version" :*/
fileprivate let appAppearName:String = "value device make%@ i"
fileprivate let userGlobalMessage:String = "data photo data button view too"
fileprivate let noti_lengthTitle:String = " a vname view profile true page"
fileprivate let kAttributeUrl:[Character] = ["e","r","s","i","o","n"]

/*: "Shoot video at least 5 seconds" :*/
fileprivate let noti_partyValue:String = "Shoot cover index self user"
fileprivate let dataManagerPath:String = "o atlet television"
fileprivate let main_progressUrl:String = "time section type object5 seconds"

/*: "Upload failed" :*/
fileprivate let userNameData:String = "Uplab cell"
fileprivate let const_replyKey:String = "lsharead"

/*: "播放状态: :*/
fileprivate let showMakeKey:String = "播放状态:"

/*: "tips" :*/
fileprivate let main_titleMessage:[Character] = ["t","i","p","s"]

/*: "You can use the video chat feature to give %@ a call" :*/
fileprivate let kGiftName:[UInt8] = [0xfa,0xcc,0xd6,0x83,0xc0,0xc2,0xcd,0x83,0xd6,0xd0,0xc6,0x83,0xd7,0xcb,0xc6,0x83,0xd5,0xca,0xc7,0xc6,0xcc,0x83,0xc0,0xcb,0xc2,0xd7,0x83,0xc5,0xc6,0xc2,0xd7,0xd6,0xd1,0xc6,0x83,0xd7,0xcc,0x83,0xc4,0xca,0xd5,0xc6,0x83,0x86,0xe3,0x83,0xc2,0x83,0xc0,0xc2,0xcf,0xcf]

				private func equalAdd(thumb num: UInt8) -> UInt8 {
					return num ^ 163
				}

/*: "cellData" :*/
fileprivate let kVideoUrl:String = "app data custom errcellDat"
fileprivate let noti_objectStr:[Character] = ["a"]

/*: "System notification" :*/
fileprivate let appCellDirectionName:String = "event clear taskSyste"
fileprivate let dataCurName:String = "m nolive self"
fileprivate let app_shareMessage:String = "tizoneica"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  MakeReactiveCompatible.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/9.
//

//: import AVFAudio
import AVFAudio
//: import Photos
import Photos
//: import UIKit
import UIKit

/// 私聊入口枚举
//: enum PrivateChatIsFromEnum {
enum ReplaceFromEnum {
    //: case Normal
    case Normal // 默认入口
    //: case LiveRoom
    case LiveRoom // 来自直播间（半屏展示）
}

//: class TalkingPrivateChatController: TalkingBaseViewController {
class MakeReactiveCompatible: LightThen {
    //: static var replyLatestTimes: Date = Date(timeIntervalSince1970: 0)
    static var replyLatestTimes: Date = .init(timeIntervalSince1970: 0) // 最新回复时间
    //: var targetId = ""                                           // 接收方UserId
    var targetId = "" // 接收方UserId
    //: var needShowRating = false
    var needShowRating = false // 是否展示app store评分
    //: var quickGreetingTextDict: [String: Any]?
    var quickGreetingTextDict: [String: Any]? // 一键打招呼文本消息
    //: var quickGreetingAudioDict: [String: Any]?
    var quickGreetingAudioDict: [String: Any]? // 一键打招呼语音消息
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var conversationType: TalkingIMChatType = .private
    private var conversationType: TalkingIMChatType = .private
    //: private var userWrap = CombineReactiveCompatible()
    private var userWrap = CombineReactiveCompatible() // chatInfo模型
    //: private var isFrom = PrivateChatIsFromEnum.Normal
    private var isFrom = ReplaceFromEnum.Normal // 私聊入口

    //: private var isShowIntimateView = false
    private var isShowIntimateView = false // 顶部是否已经展示了亲密度头像
    //: var QuickEndBlock: ((_ isSuccess: Bool) -> Void)?
    var QuickEndBlock: ((_ isSuccess: Bool) -> Void)? /// 1.7.8快速打招呼完成后

    //: init(chatID: String, isFrom: PrivateChatIsFromEnum = .Normal, userWrap: CombineReactiveCompatible = CombineReactiveCompatible()) {
    init(chatID: String, isFrom: ReplaceFromEnum = .Normal, userWrap: CombineReactiveCompatible = CombineReactiveCompatible()) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        //: targetId = chatID
        targetId = chatID
        //: self.userWrap = userWrap
        self.userWrap = userWrap

        //: TalkingPrivateChatController.replyLatestTimes = Date(timeIntervalSince1970: 0)
        MakeReactiveCompatible.replyLatestTimes = Date(timeIntervalSince1970: 0)
        //: TalkingPrivateChatManager.share.readReceiptDict[targetId] = self.userWrap.readReceiptTime
        FrameLevelThen.share.readReceiptDict[targetId] = self.userWrap.readReceiptTime
        //: self.isFrom = isFrom
        self.isFrom = isFrom
        //: if targetId == MaleMacroDefine.getXiaoMiID() {
        if targetId == MaleMacroDefine.dataBar() { // 系统消息
            //: conversationType = .system
            conversationType = .system
            //: hideNavi = false
            hideNavi = false
            //: } else if targetId == MaleMacroDefine.getCustomerServiceID() {
        } else if targetId == MaleMacroDefine.previousId() { // 官方客服
            //: conversationType = .service
            conversationType = .service
            //: hideNavi = true
            hideNavi = true

            //: } else {
        } else {
            //: hideNavi = true
            hideNavi = true
        }
        //: msgView.targetID = targetId
        msgView.targetID = targetId
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: appColorMsg.map{$0^162}, encoding: .utf8)!)
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if IconContainerAppManager.share.appConfigMode.disableShootScreen {
            if IconContainerAppManager.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = AtThen(frame: CGRect(x: 0, y: 0, width: main_colorData, height: constRecordServerKey))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: half_showViewAnimation()
        random()
        //: createUI()
        startUi()
        //: createUIConstraints()
        diagonal()
        //: bindInteraction()
        isoclinicQuantityeraction()
        //: addTapGestureRecognizer()
        noPathRecognizer()
        //: func__requestChatInfo()
        receive()
        //: func__getFillDraftText()
        electronicText()
        //: TalkingVoiceMsgPlayManager.shared.addDaskManagerDelegate()
        EnterPlayManager.shared.mark()
        //: func__addVoiceNotification()
        store()
        //: addReadPeceiptNotification()
        nowTag()

        //: if conversationType == .private {
        if conversationType == .private {
            //: initCachEffectData()
            labIn()
            //: func__TipsNotification()
            behindMode()
            //: if IconContainerAppManager.share.loginUserMode.sex == Gender.male.rawValue {
            if IconContainerAppManager.share.loginUserMode.sex == ThemeTitleScalarLiteral.male.rawValue {
                //: func__setReplyTipsView()
                clean()
            }
        }
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
        downAdd(isAdd: true)
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: view.endEditing(true)
        view.endEditing(true)
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
        downAdd(isAdd: false)
        //: if conversationType == .private {
        if conversationType == .private {
            //: func__saveFillDraftText()
            textCancel()
        }
    }

    //: override func viewDidDisappear(_ animated: Bool) {
    override func viewDidDisappear(_ animated: Bool) {
        //: super.viewDidDisappear(animated)
        super.viewDidDisappear(animated)
        // 展示App Store评分
        //: if needShowRating {
        if needShowRating {
            //: AppStoreRatingManager.shared.requestQuickRating()
            AdministratorRatingManager.shared.managerRating()
        }
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    // MARK: - Lazy Load

    //: private lazy var contentView: UIView = {
    private lazy var contentView: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = .white
        v.backgroundColor = .white
        //: return v
        return v
        //: }()
    }()

    //: lazy var msgView: TUIMessageController = {
    lazy var msgView: ConstraintToViewController = {
        //: let msgVC = TUIMessageController.init()
        let msgVC = ConstraintToViewController()
        //: msgVC.delegate = self
        msgVC.delegate = self
        //: if conversationType == .private {
        if conversationType == .private {
            //: msgVC.view.backgroundColor = UIColor.clear
            msgVC.view.backgroundColor = UIColor.clear
            //: msgVC.tableView.backgroundColor = UIColor.clear
            msgVC.tableView.backgroundColor = UIColor.clear
            //: } else {
        } else {
            //: msgVC.view.backgroundColor = UIColor.clear
            msgVC.view.backgroundColor = UIColor.clear
            //: msgVC.tableView.backgroundColor = UIColor(hex: "F5F5F5")
            msgVC.tableView.backgroundColor = UIColor(hex: (String(kCenterFormat) + String(noti_errorKey.suffix(5))))
        }
        //: msgVC.tableView.register(TalkingChatTextMsgCell.self, forCellReuseIdentifier: TalkingChatTextMsgCell.className())
        msgVC.tableView.register(ActualView.self, forCellReuseIdentifier: ActualView.className())
        //: msgVC.tableView.register(TalkingChatImageMsgCell.self, forCellReuseIdentifier: TalkingChatImageMsgCell.className())
        msgVC.tableView.register(VideoView.self, forCellReuseIdentifier: VideoView.className())
        //: msgVC.tableView.register(TalkingChatVideoMsgCell.self, forCellReuseIdentifier: TalkingChatVideoMsgCell.className())
        msgVC.tableView.register(NameEqualMessageCell.self, forCellReuseIdentifier: NameEqualMessageCell.className())
        //: msgVC.tableView.register(TalkingChatGiftMsgCell.self, forCellReuseIdentifier: TalkingChatGiftMsgCell.className())
        msgVC.tableView.register(AtViewCell.self, forCellReuseIdentifier: AtViewCell.className())
        //: msgVC.tableView.register(TalkingChatAskForGiftMsgCell.self, forCellReuseIdentifier: TalkingChatAskForGiftMsgCell.className())
        msgVC.tableView.register(SurvivalMsgCell.self, forCellReuseIdentifier: SurvivalMsgCell.className())
        //: msgVC.tableView.register(TalkingChatTipsMsgCell.self, forCellReuseIdentifier: TalkingChatTipsMsgCell.className())
        msgVC.tableView.register(BlockMaxMessageCell.self, forCellReuseIdentifier: BlockMaxMessageCell.className())
        //: msgVC.tableView.register(TalkingChatMatchMsgCell.self, forCellReuseIdentifier: TalkingChatMatchMsgCell.className())
        msgVC.tableView.register(CompartmentChatAnswerMsgCell.self, forCellReuseIdentifier: CompartmentChatAnswerMsgCell.className())
        //: msgVC.tableView.register(TalkingChatAudioMsgCell.self, forCellReuseIdentifier: TalkingChatAudioMsgCell.className())
        msgVC.tableView.register(EventMsgCell.self, forCellReuseIdentifier: EventMsgCell.className())
        //: msgVC.tableView.register(TalkingChatSystemNotifJumpMsgCell.self, forCellReuseIdentifier: TalkingChatSystemNotifJumpMsgCell.className())
        msgVC.tableView.register(AppViewCell.self, forCellReuseIdentifier: AppViewCell.className())
        //: msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: DigitiserReactiveCompatible.TSystemMessageCell_ReuseId)
        msgVC.tableView.register(IconUserViewCell.self, forCellReuseIdentifier: DigitiserReactiveCompatible.TSystemMessageCell_ReuseId)
        //: msgVC.currentChatType = conversationType
        msgVC.currentChatType = conversationType

        //: return msgVC
        return msgVC
        //: }()
    }()

    //: lazy var bgImgeView: UIImageView = {
    lazy var bgImgeView: UIImageView = {
        //: let imgView = UIImageView.init()
        let imgView = UIImageView()
        //: imgView.backgroundColor = .clear
        imgView.backgroundColor = .clear
        //: imgView.contentMode = .scaleAspectFill
        imgView.contentMode = .scaleAspectFill
        //: imgView.clipsToBounds = true
        imgView.clipsToBounds = true
        //: return imgView
        return imgView
        //: }()
    }()

    //: lazy var coverView: UIVisualEffectView = {
    lazy var coverView: UIVisualEffectView = {
        //: let effect = UIBlurEffect.init(style: .dark)
        let effect = UIBlurEffect(style: .dark)
        //: let effectView = UIVisualEffectView.init(effect: effect)
        let effectView = UIVisualEffectView(effect: effect)
        //: effectView.alpha = 0.45
        effectView.alpha = 0.45
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var bgView: UIView = {
    lazy var bgView: UIView = {
        //: let view = UIImageView.init()
        let view = UIImageView()
        //: view.backgroundColor = UIColor.black.withAlphaComponent(0.75)
        view.backgroundColor = UIColor.black.withAlphaComponent(0.75)
        //: return view
        return view
        //: }()
    }()

    //: private lazy var videoCallManager: TalkingCallMenuManager = {
    private lazy var videoCallManager: SameObjectProtocol = {
        //: let m = TalkingCallMenuManager()
        let m = SameObjectProtocol()
        //: m.uid = targetId
        m.uid = targetId
        //: m.onReqestCallErrorBlock = { [weak self] errNo in
        m.onReqestCallErrorBlock = { [weak self] errNo in
            //: guard let self = self,errNo == 101 else { return }
            guard let self = self, errNo == 101 else { return }
            //: self.msgInputView.setHidenFreeMsgTimesView()
            self.msgInputView.getWeaving()
        }
        //: return m
        return m
        //: }()
    }()

    //: lazy var msgInputView: TalkingChatInputView = {
    lazy var msgInputView: TextViewDelegate = {
        //: var type: InputViewType = .PrivateChat
        var type: LevelViewType = .PrivateChat
        //: if conversationType == .service {
        if conversationType == .service {
            //: type = .CustomerChat
            type = .CustomerChat
            //: } else if self.isFrom == .LiveRoom {
        } else if self.isFrom == .LiveRoom {
            //: type = .LiveRoom
            type = .LiveRoom
        }

        //: let view = TalkingChatInputView(type: type)
        let view = TextViewDelegate(type: type)
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: lazy var feedView: UIView = {
    lazy var feedView: UIView = {
        //: let view = UIView.init()
        let view = UIView()
        //: view.backgroundColor = .white
        view.backgroundColor = .white
        //: return view
        return view
        //: }()
    }()

    //: lazy var feedbackBtn: UIButton = {
    lazy var feedbackBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: let attributes = [.foregroundColor: UIColor(hex: "#128CFF")!, .font: UIFont.pingfangFont(type: .Medium, fontSize: 16), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        let attributes = [.foregroundColor: UIColor(hex: (String(appFrameName.prefix(7))))!, .font: UIFont.replyName(type: .Medium, fontSize: 16), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        //: let attributeString = NSAttributedString(string: "Feedback".localized, attributes: attributes)
        let attributeString = NSAttributedString(string: (String(kStatusFormat)).localized, attributes: attributes)
        //: btn.setAttributedTitle(attributeString, for: .normal)
        btn.setAttributedTitle(attributeString, for: .normal)
        //: btn.addTarget(self, action: #selector(feedbackBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(buildForOn), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    lazy var giftEffectView: ThemeAnimatView = {
        //: let effectView = TalkingPrivateChatAnimatView.init()
        let effectView = ThemeAnimatView()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var navView: TalkingChatNavTitleView = {
    lazy var navView: FrameThirdThen = {
        //: let navView = TalkingChatNavTitleView(type: conversationType)
        let navView = FrameThirdThen(type: conversationType)
        //: return navView
        return navView
        //: }()
    }()

    //: lazy var sendGreetBtn: UIButton = {
    lazy var sendGreetBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.replyName(type: .Medium, fontSize: 15)
        //: btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        btn.setTitleColor(UIColor(hex: (noti_formalTitle.capitalized + String(user_giftId))), for: .normal)
        //: btn.setTitle("Quick Greeting".localized, for: .normal)
        btn.setTitle((String(showAtActualMsg.prefix(4)) + "k Gree" + user_indexPath.replacingOccurrences(of: "view", with: "i")).localized, for: .normal)
        //: btn.layer.cornerRadius = 15
        btn.layer.cornerRadius = 15
        //: btn.layer.borderColor = UIColor.appThemeColor().cgColor
        btn.layer.borderColor = UIColor.actionLive().cgColor
        //: btn.layer.borderWidth = 1
        btn.layer.borderWidth = 1
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(sendGreetBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(snapText), for: .touchUpInside)
        //: btn.isHidden = !IconContainerAppManager.share.appUserConfigMode.showDirectGreet || IconContainerAppManager.share.loginUserMode.sex == Gender.male.rawValue || IconContainerAppManager.share.appStatus == AppSkinStatus.special.rawValue
        btn.isHidden = !IconContainerAppManager.share.appUserConfigMode.showDirectGreet || IconContainerAppManager.share.loginUserMode.sex == ThemeTitleScalarLiteral.male.rawValue || IconContainerAppManager.share.appStatus == MinExpressionConvertible.special.rawValue
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var replyTipsView: TalkingPrivateChatReplyTipsView? = {
    private lazy var replyTipsView: BlockSessionTipsView? = {
        //: let view = TalkingPrivateChatReplyTipsView.init()
        let view = BlockSessionTipsView()
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    // 文本输入翻译检测
    //: private lazy var selectTranView: TalkingSelectTranslationView = {
    private lazy var selectTranView: RemarkThen = {
        //: let v = TalkingSelectTranslationView()
        let v = RemarkThen()
        //: v.isHidden = true
        v.isHidden = true
        //: v.transBlock = { [weak self] succeed, text in
        v.transBlock = { [weak self] succeed, text in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            // 展示翻译结果视图
            //: self.view.endEditing(true)
            self.view.endEditing(true)
            //: self.transResultView.showTranslationResultView(text)
            self.transResultView.streetwise(text)
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var transResultView: TalkingTranslationResultView = {
    private lazy var transResultView: EffectResultView = {
        //: let v = TalkingTranslationResultView()
        let v = EffectResultView()
        //: v.isHidden = true
        v.isHidden = true
        //: v.sendBlock = { [weak self] text in
        v.sendBlock = { [weak self] text in
            //: guard let self = self else { return }
            guard let self = self else { return }
            // 发送翻译内容, 清空输入框
            //: self.func__sendTextMsg(msgStr: text, quoteModel: self.msgInputView.quoteModel)
            self.sire(msgStr: text, quoteModel: self.msgInputView.quoteModel)
            //: self.msgInputView.inputTextView.text = ""
            self.msgInputView.inputTextView.text = ""
            //: self.msgInputView.quoteModel = nil
            self.msgInputView.quoteModel = nil
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var beautyPhotosBtn: UIButton = {
    private lazy var beautyPhotosBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.replyName(type: .Medium, fontSize: 15)
        //: btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        btn.setTitleColor(UIColor(hex: (noti_formalTitle.capitalized + String(user_giftId))), for: .normal)
        //: btn.setTitle("Beauty Photos".localized, for: .normal)
        btn.setTitle((String(user_dateKeyValue) + show_itemKey.replacingOccurrences(of: "male", with: "s")).localized, for: .normal)
        //: btn.layer.cornerRadius = 15
        btn.layer.cornerRadius = 15
        //: btn.layer.borderColor = UIColor.appThemeColor().cgColor
        btn.layer.borderColor = UIColor.actionLive().cgColor
        //: btn.layer.borderWidth = 1
        btn.layer.borderWidth = 1
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(beautyPhotosBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(addWith), for: .touchUpInside)
        //: btn.isHidden = IconContainerAppManager.share.loginUserMode.sex == Gender.male.rawValue || IconContainerAppManager.share.appStatus == AppSkinStatus.special.rawValue
        btn.isHidden = IconContainerAppManager.share.loginUserMode.sex == ThemeTitleScalarLiteral.male.rawValue || IconContainerAppManager.share.appStatus == MinExpressionConvertible.special.rawValue
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var gamesView: TalkingLiveRoomGamesView = {
    private lazy var gamesView: AppGamesView = {
        //: let v = TalkingLiveRoomGamesView()
        let v = AppGamesView()
        //: return v
        return v
        //: }()
    }()

    //: private lazy var bannerView: TalkingPrivateBannerView = {
    private lazy var bannerView: UserReactiveCompatible = {
        //: let view = TalkingPrivateBannerView.init()
        let view = UserReactiveCompatible()
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: private lazy var floatScreenView: TalkingGameFloatingScreenView = {
    private lazy var floatScreenView: SundayGoToMeetingScreenView = { // 飘屏
        //: let v = TalkingGameFloatingScreenView()
        let v = SundayGoToMeetingScreenView()
        //: v.isHidden = (IconContainerAppManager.share.appStatus == AppSkinStatus.special.rawValue)
        v.isHidden = (IconContainerAppManager.share.appStatus == MinExpressionConvertible.special.rawValue)
        //: v.isUserInteractionEnabled = false
        v.isUserInteractionEnabled = false
        //: v.endPlayBlock = { [weak self] in
        v.endPlayBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: v.isUserInteractionEnabled = false
            v.isUserInteractionEnabled = false
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var notesTopView: PrivateChatNotesTopView = {
    private lazy var notesTopView: ChatTopView = { // 备注置顶
        //: let v = PrivateChatNotesTopView()
        let v = ChatTopView()
        //: v.tapBlock = { [weak self] in
        v.tapBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.jumpToNotesView()
            self.changeShared()
        }
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()
}

// MARK: - banner

//: extension TalkingPrivateChatController: PrivateBannerDelegate {
extension MakeReactiveCompatible: DirectionBannerDelegate {
    //: func changeBannerViewFream(isMiniView: Bool) {
    func pastFinish(isMiniView: Bool) {
        //: guard self.isFrom == .Normal else { return }
        guard self.isFrom == .Normal else { return }
        //: UIView.animate(withDuration: 0.3) {
        UIView.animate(withDuration: 0.3) {
            //: if isMiniView {
            if isMiniView {
                //: if IconContainerAppManager.share.loginUserMode.sex == Gender.male.rawValue {
                if IconContainerAppManager.share.loginUserMode.sex == ThemeTitleScalarLiteral.male.rawValue {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: if self.replyTipsView != nil {
                        if self.replyTipsView != nil {
                            //: make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            //: } else {
                        } else {
                            //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                            make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        }
                        //: make.trailing.equalTo(-4)
                        make.trailing.equalTo(-4)
                        //: make.height.equalTo(36)
                        make.height.equalTo(36)
                        //: make.width.equalTo(89)
                        make.width.equalTo(89)
                    }

                    //: } else {
                } else {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        //: make.width.equalTo(89)
                        make.width.equalTo(89)
                        //: make.trailing.equalTo(-4)
                        make.trailing.equalTo(-4)
                        //: make.height.equalTo(36)
                        make.height.equalTo(36)
                    }
                }
                //: } else {
            } else {
                //: if IconContainerAppManager.share.loginUserMode.sex == Gender.male.rawValue {
                if IconContainerAppManager.share.loginUserMode.sex == ThemeTitleScalarLiteral.male.rawValue {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.trailing.equalToSuperview()
                        make.trailing.equalToSuperview()
                        //: if self.replyTipsView != nil {
                        if self.replyTipsView != nil {
                            //: make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            //: } else {
                        } else {
                            //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                            make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        }
                        //: make.height.equalTo(106)
                        make.height.equalTo(106)
                        //: make.width.equalTo(93)
                        make.width.equalTo(93)
                    }
                    //: } else {
                } else {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.trailing.equalToSuperview()
                        make.trailing.equalToSuperview()
                        //: make.bottom.equalTo(self.beautyPhotosBtn.snp.top)
                        make.bottom.equalTo(self.beautyPhotosBtn.snp.top)
                        //: make.height.equalTo(106)
                        make.height.equalTo(106)
                        //: make.width.equalTo(93)
                        make.width.equalTo(93)
                    }
                }
            }
            //: self.bannerView.layoutIfNeeded()
            self.bannerView.layoutIfNeeded()
            //: self.bannerView.resetView(isMiniView: isMiniView)
            self.bannerView.miniView(isMiniView: isMiniView)
        }
    }
}

// MARK: - 聊天相关

//: extension TalkingPrivateChatController: TMessageControllerDelegate {
extension MakeReactiveCompatible: RestoreDelegate {
    /// 一键打招呼
    //: @objc func sendGreetBtnClick() {
    @objc func snapText() {
        //: TalkingPrivateChatManager.chat_sendQuickGreeting(toUserId: self.targetId) { errorCode, errorMsg, textDict, audioDict in
        FrameLevelThen.response(toUserId: self.targetId) { _, errorMsg, textDict, audioDict in
            //: guard (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) else {
            guard (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) else {
                //: if errorMsg?.count ?? 0 > 0 {
                if errorMsg?.count ?? 0 > 0 {
                    //: self.func__showStatusBarErrorMsg(showMsg: errorMsg!)
                    self.explain(showMsg: errorMsg!)
                }
                //: return
                return
            }

            // 发送一键打招呼消息
            //: self.func_sendQuickGreetingMsg(textDict: textDict, audioDict: audioDict)
            self.tingManager(textDict: textDict, audioDict: audioDict)
        }
    }

    /// 图片打招呼
    //: @objc func beautyPhotosBtnClick() {
    @objc func addWith() {
        //: TalkingPrivateChatManager.chat_sendBeautyPhotos(toUserId: self.targetId) { isPushSet, dict in
        FrameLevelThen.paradigmEvent(toUserId: self.targetId) { isPushSet, dict in
            //: if isPushSet {
            if isPushSet {
                //: let vc = TalkingGreetPhotoVC.init()
                let vc = MakeDataSource()
                //: self.navigationController?.pushViewController(vc, animated: true)
                self.navigationController?.pushViewController(vc, animated: true)
                //: return
                return
            }
            //: if dict != nil {
            if dict != nil {
                //: UIImageView().setUrlImage(urlStr: dict?["url"] as? String ?? "") {[weak self] _, image in
                UIImageView().tillFirstBar(urlStr: dict?["url"] as? String ?? "") { [weak self] _, image in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: if image != nil {
                    if image != nil {
                        //: let unlockGift = dict?["unlockGift"] as? Dictionary<String, Any>
                        let unlockGift = dict?[(String(constSizeData) + String(dataFeeText.suffix(6)))] as? [String: Any]
                        //: let sendModel = AbTalkingChatMsgCustomSendModel()
                        let sendModel = PlayerChatModel()
                        //: sendModel.isBurn = String.init(format: "%d", dict?["isBurn"] as? Int ?? 0)
                        sendModel.isBurn = String(format: "%d", dict?[String(bytes: kFemaleContent.map{managingDirector(interaction: $0)}, encoding: .utf8)!] as? Int ?? 0)
                        //: sendModel.giftId = unlockGift?["giftId"] as? String ?? "0"
                        sendModel.giftId = unlockGift?[(String(constToTitle))] as? String ?? "0"
                        //: self.func__sendPictureMsg(img: image!, sendModel: sendModel)
                        self.behindSendFrom(img: image!, sendModel: sendModel)
                        //: return
                        return
                    }
                }
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> AddCellData! {
    func model(_: ConstraintToViewController!, currentSend data: V2TIMMessage!) -> AddCellData! {
        //: let dic = ThemeChatReactiveCompatible.parseTXMessageData(data: data.customElem.data)
        let dic = ThemeChatReactiveCompatible.playChange(data: data.customElem.data)
        //: var cellData = TUISocialChatManager.handleTXReceiveMessage(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        var cellData = DirectionReactiveCompatible.toViewModel(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        //: if data.isKind(of: TalkingChatMatchMsgCellData.self), self.userWrap.uid.count > 0 {
        if data.isKind(of: MaterializationReactiveCompatible.self), self.userWrap.uid.count > 0 {
            //: cellData?.msgModel.userInfoModel = self.userWrap
            cellData?.msgModel.userInfoModel = self.userWrap
        }

        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: if jsonDic["rYMsgType"].stringValue == "GJ:CallCustom" { // 音视频通话消息
        if jsonDic[(String(main_viewExampleFormat) + String(user_labelValue))].stringValue == (String(k_infoShowContent) + String(app_viewNowFillData.prefix(6))) { // 音视频通话消息
            //: let isSelf = jsonDic["changeDirection"].boolValue
            let isSelf = jsonDic[(String(appInputContent) + String(const_toolId))].boolValue
            //: if isSelf == true {
            if isSelf == true { // 消息需要翻转方向
                // 更正当前用户uid
                //: cellData?.msgModel.user.uid = jsonDic["extra"]["msgInfo"]["toUid"].intValue
                cellData?.msgModel.user.uid = jsonDic[(String(appViewData))][(String(app_squareProMsg) + String(showErrorId.suffix(5)))][(String(kUpName))].intValue
                // 更正消息展示方向
                //: let msgModel = cellData!.msgModel
                let msgModel = cellData!.msgModel
                //: cellData = MaxCellData(direction: isSelf ? .MsgDirectionOutgoing:.MsgDirectionIncoming)
                cellData = MaxCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: cellData?.msgModel = msgModel
                cellData?.msgModel = msgModel
            }
        }
        //: if cellData?.isKind(of: ValueCustomCellData.self) == true ||
        if cellData?.isKind(of: ValueCustomCellData.self) == true ||
            //: cellData?.isKind(of: FountainheadCellData.self) == true ||
            cellData?.isKind(of: FountainheadCellData.self) == true ||
            //: cellData?.isKind(of: MaxCellData.self) == true ||
            cellData?.isKind(of: MaxCellData.self) == true ||
            //: cellData?.isKind(of: EigenvalueOfAMatrixReactiveCompatible.self) == true {
            cellData?.isKind(of: EigenvalueOfAMatrixReactiveCompatible.self) == true
        {
            //: self.userWrap.existSess = true
            self.userWrap.existSess = true
        }

        //: if let cellData = cellData {
        if let cellData = cellData {
            // 接收解锁 私密消息（照片、视频）礼物，刷新本地消息状态
            //: TalkingPrivateChatManager.chat_onNewMessage_refreshLocalIntimateMsgStatus(cellData: cellData, msgView: self.msgView)
            FrameLevelThen.connectImageView(cellData: cellData, msgView: self.msgView)
            // 女性记录最新回复时间, 刷新表格
            //: TalkingPrivateChatManager.updateReplyLatestTimeAndReload(cellData.msgIncome, data.timestamp, msgView)
            FrameLevelThen.tillName(cellData.msgIncome, data.timestamp, msgView)
        }

        //: return cellData
        return cellData
    }

    //: func messageController(_ controller: TUIMessageController!, onShowMessageData data: AddCellData!) -> DigitiserReactiveCompatible! {
    func bringHomeData(_: ConstraintToViewController!, model data: AddCellData!) -> DigitiserReactiveCompatible! {
        // 更新聊天头像，不会更新腾讯数据库
        //: let data = changeChatHeadPic(data: data)
        let data = reportAddTitle(data: data)

        //: if data.isKind(of: MaxCellData.self) {
        if data.isKind(of: MaxCellData.self) {
            //: let cell = TalkingChatTextMsgCell.init(style: .default, reuseIdentifier: TalkingChatTextMsgCell.className())
            let cell = ActualView(style: .default, reuseIdentifier: ActualView.className())
            //: let cellData = data as! MaxCellData
            let cellData = data as! MaxCellData

            //: if cellData.msgModel.quoteMsgInfo.isQuoteMsg() {
            if cellData.msgModel.quoteMsgInfo.messageTo() {
                //: if cellData.msgModel.quoteMsgInfo.uid == IconContainerAppManager.share.loginUserMode.userID {
                if cellData.msgModel.quoteMsgInfo.uid == IconContainerAppManager.share.loginUserMode.userID {
                    //: cellData.msgModel.quoteMsgInfo.sendName = IconContainerAppManager.share.loginUserMode.nickname ?? ""
                    cellData.msgModel.quoteMsgInfo.sendName = IconContainerAppManager.share.loginUserMode.nickname ?? ""
                    //: }else{
                } else {
                    //: cellData.msgModel.quoteMsgInfo.sendName = self.userWrap.nickname
                    cellData.msgModel.quoteMsgInfo.sendName = self.userWrap.nickname
                }
            }
            //: cell.fill(with: cellData)
            cell.padWith(with: cellData)
//            cell.transBlock = { [weak self] in
//                guard let self = self else { return }
//                self.msgView.translateMsg(with: cellData)
//            }
            //: return cell
            return cell
        }

        //: if data.isKind(of: FountainheadCellData.self) {
        if data.isKind(of: FountainheadCellData.self) {
            //: let cell = TalkingChatImageMsgCell.init(style: .default, reuseIdentifier: TalkingChatImageMsgCell.className())
            let cell = VideoView(style: .default, reuseIdentifier: VideoView.className())
            //: let cellData = data as! FountainheadCellData
            let cellData = data as! FountainheadCellData
            //: cell.fill(with: cellData)
            cell.padWith(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: EigenvalueOfAMatrixReactiveCompatible.self) {
        if data.isKind(of: EigenvalueOfAMatrixReactiveCompatible.self) {
            //: let cell = TalkingChatVideoMsgCell.init(style: .default, reuseIdentifier: TalkingChatVideoMsgCell.className())
            let cell = NameEqualMessageCell(style: .default, reuseIdentifier: NameEqualMessageCell.className())
            //: let cellData = data as! EigenvalueOfAMatrixReactiveCompatible
            let cellData = data as! EigenvalueOfAMatrixReactiveCompatible
            //: cell.fill(with: cellData)
            cell.padWith(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: GiftEffectThen.self) {
        if data.isKind(of: GiftEffectThen.self) {
            //: let cell = TalkingChatGiftMsgCell.init(style: .default, reuseIdentifier: TalkingChatGiftMsgCell.className())
            let cell = AtViewCell(style: .default, reuseIdentifier: AtViewCell.className())
            //: let cellData = data as! GiftEffectThen
            let cellData = data as! GiftEffectThen
            //: cell.fill(with: cellData)
            cell.padWith(with: cellData)
            //: return cell
            return cell
        }
        //: if data.isKind(of:  MakeCellData.self) {
        if data.isKind(of: MakeCellData.self) {
            //: let cell = TalkingChatAskForGiftMsgCell.init(style: .default, reuseIdentifier: TalkingChatAskForGiftMsgCell.className())
            let cell = SurvivalMsgCell(style: .default, reuseIdentifier: SurvivalMsgCell.className())
            //: let cellData = data as!  MakeCellData
            let cellData = data as! MakeCellData
            //: cell.fill(with: cellData)
            cell.padWith(with: cellData)
            //: cell.sendBlock = { [weak self] in
            cell.sendBlock = { [weak self] in
                //: guard let self = self else {
                guard let self = self else {
                    //: return
                    return
                }
                //: var model = TalkingRoomGiftModel.init()
                var model = PointHandyJSON()
                //: model.gid = cellData.msgModel.msgInfo.wantGift.giftId
                model.gid = cellData.msgModel.msgInfo.wantGift.giftId
                //: self.func__sendGiftMsg(giftModel: model, giftNum: "1", cellData: cellData, fromStr: "wantGift")
                self.metronymic(giftModel: model, giftNum: "1", cellData: cellData, fromStr: (String(mainPointStr.suffix(5)) + String(mainErrorMsg)))
            }
            //: return cell
            return cell
        }
        //: if data.isKind(of: ValueCustomCellData.self) {
        if data.isKind(of: ValueCustomCellData.self) {
            //: let cell = TalkingChatAudioMsgCell.init(style: .default, reuseIdentifier: TalkingChatAudioMsgCell.className())
            let cell = EventMsgCell(style: .default, reuseIdentifier: EventMsgCell.className())
            //: let cellData = data as! ValueCustomCellData
            let cellData = data as! ValueCustomCellData
            //: cell.fill(with: cellData)
            cell.padWith(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatMatchMsgCellData.self) {
        if data.isKind(of: MaterializationReactiveCompatible.self) {
            //: let cell = TalkingChatMatchMsgCell.init(style: .default, reuseIdentifier: TalkingChatMatchMsgCell.className())
            let cell = CompartmentChatAnswerMsgCell(style: .default, reuseIdentifier: CompartmentChatAnswerMsgCell.className())
            //: let cellData = data as! TalkingChatMatchMsgCellData
            let cellData = data as! MaterializationReactiveCompatible
            //: if self.userWrap.uid.count > 0 {
            if self.userWrap.uid.count > 0 {
                //: cellData.msgModel.userInfoModel = self.userWrap
                cellData.msgModel.userInfoModel = self.userWrap
            }
            //: cell.fill(with: cellData)
            cell.padWith(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatTipsMsgCellData.self) {
        if data.isKind(of: SuccessAppThen.self) {
            //: let cell = TalkingChatTipsMsgCell.init(style: .default, reuseIdentifier: TalkingChatTipsMsgCell.className())
            let cell = BlockMaxMessageCell(style: .default, reuseIdentifier: BlockMaxMessageCell.className())
            //: let cellData = data as! TalkingChatTipsMsgCellData
            let cellData = data as! SuccessAppThen
            //: cell.fill(with: cellData)
            cell.padWith(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatSystemNotifJumpMsgCellData.self) {
        if data.isKind(of: OptionCellData.self) {
            //: let cell = TalkingChatSystemNotifJumpMsgCell.init(style: .default, reuseIdentifier: TalkingChatSystemNotifJumpMsgCell.className())
            let cell = AppViewCell(style: .default, reuseIdentifier: AppViewCell.className())
            //: let cellData = data as! TalkingChatSystemNotifJumpMsgCellData
            let cellData = data as! OptionCellData
            //: cell.fill(with: cellData)
            cell.padWith(with: cellData)
            //: return cell
            return cell
        }

        //: return nil
        return nil
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageAvatar cell: DigitiserReactiveCompatible!) {
    func groupDiscussion(_: ConstraintToViewController!, halogen cell: DigitiserReactiveCompatible!) {
        //: guard self.isFrom != .LiveRoom,
        guard self.isFrom != .LiveRoom,
              //: conversationType != .system,
              conversationType != .system,
              //: String(cell.messageData?.msgModel.user.uid ?? 0) != MaleMacroDefine.getCustomerServiceID() else { return }
              String(cell.messageData?.msgModel.user.uid ?? 0) != MaleMacroDefine.previousId() else { return }

        //: if let uid = cell.messageData?.msgModel.user.uid {
        if let uid = cell.messageData?.msgModel.user.uid {
            //: CombineAccountEnclaveDecisionMakerThen.share.func__pushToUserDetailVC(uid: String(uid))
            CombineAccountEnclaveDecisionMakerThen.share.windowBy(uid: String(uid))
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageContent cell: DigitiserReactiveCompatible!) {
    func with(_: ConstraintToViewController!, userDevice cell: DigitiserReactiveCompatible!) {
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: VideoView.self) { // 图片
            //: let imageCellData = cell.messageData as! FountainheadCellData
            let imageCellData = cell.messageData as! FountainheadCellData
            //: if imageCellData.msgModel.msgInfo.intimatePhoto.previewUrl.count > 0 {
            if imageCellData.msgModel.msgInfo.intimatePhoto.previewUrl.count > 0 { // 是私密照片
                // 已销毁的图片不能点击
                //: guard imageCellData.msgModel.msgInfo.intimatePhoto.burnStatus != 1 else { return }
                guard imageCellData.msgModel.msgInfo.intimatePhoto.burnStatus != 1 else { return }
                // 展示私密照片视图
                //: let vc = TalkingIntimatePhotoPreviewVC(imageCellData: imageCellData, toUid: self.targetId)
                let vc = GestureTitleViewController(imageCellData: imageCellData, toUid: self.targetId)
                // 更改本地私密照片状态
                //: vc.statusBlock = { (model) in
                vc.statusBlock = { model in
                    //: self.msgView.changeIntimatePhotoMsg(imageCellData, photoModel: model)
                    self.msgView.filterDetect(imageCellData, center: model)
                }
                // 送礼效果在当前页面处理
                //: vc.sendGiftResultBlock = { succeed, result, errorModel in
                vc.sendGiftResultBlock = { succeed, result, errorModel in
                    //: guard succeed else {
                    guard succeed else {
                        //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickUnlockButton)
                        self.back(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: kSucceedAgentName)
                        //: return
                        return
                    }

                    //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: false)
                    self.oeilDeBoeuf(extral: result as! [String: Any?], isFromInputView: false)
                    //: let dataDict = result as! Dictionary<String, Any>
                    let dataDict = result as! [String: Any]
                    //: self.unlockPhotoOrVideoTool(dataDict: dataDict)
                    self.overEqualDict(dataDict: dataDict)
                }
                //: getNavigationController()?.pushViewController(vc, animated: true)
                characteristicRootOfASquareMatrix()?.pushViewController(vc, animated: true)

                //: } else {
            } else { // 普通图片
                //: let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
                let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!

                //: var filePath = (libraryPath as NSString).appendingPathComponent(imageFileName)
                var filePath = (libraryPath as NSString).appendingPathComponent(k_keyText)
                //: filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)
                filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)

                //: var model = TalkingMomentPhotoModel.init()
                var model = TextToModelType()
                //: if FileManager.default.fileExists(atPath: filePath) {
                if FileManager.default.fileExists(atPath: filePath) {
                    //: let data = NSData.init(contentsOfFile: filePath)
                    let data = NSData(contentsOfFile: filePath)
                    //: model.imagePic = UIImage(data: data! as Data)
                    model.imagePic = UIImage(data: data! as Data)
                    //: } else {
                } else {
                    //: if imageCellData.imageData != nil {
                    if imageCellData.imageData != nil {
                        //: model.imagePic = imageCellData.imageData
                        model.imagePic = imageCellData.imageData
                        //: } else {
                    } else {
                        //: model.imgUrl =  imageCellData.imageUrl
                        model.imgUrl = imageCellData.imageUrl
                    }
                }
                //: var currenModel = TalkingMomentModel.init()
                var currenModel = AtMeasurable()
                //: var tempArr: [TalkingMomentPhotoModel] = []
                var tempArr: [TextToModelType] = []
                //: tempArr.append(model)
                tempArr.append(model)
                //: currenModel.pic = tempArr
                currenModel.pic = tempArr
                //: let vc = TalkingMomentPhotosVC.init(momentModel: currenModel, index: 0, type: .normal)
                let vc = VideoImageReactiveCompatible(momentModel: currenModel, index: 0, type: .normal)
                //: getNavigationController()?.pushViewController(vc, animated: true)
                characteristicRootOfASquareMatrix()?.pushViewController(vc, animated: true)
            }

            //: } else if cell.isKind(of: TalkingChatVideoMsgCell.self) {
        } else if cell.isKind(of: NameEqualMessageCell.self) { // 视频
            //: let videoCellData = cell.messageData as! EigenvalueOfAMatrixReactiveCompatible
            let videoCellData = cell.messageData as! EigenvalueOfAMatrixReactiveCompatible
            //: let videoModel = videoCellData.msgModel.msgInfo.video
            let videoModel = videoCellData.msgModel.msgInfo.video
            //: if (videoModel.burnStatus == -1 && videoModel.lockStatus == -1 && videoModel.giftId == "0") { // 普通视频
            if videoModel.burnStatus == -1 && videoModel.lockStatus == -1 && videoModel.giftId == "0" { // 普通视频
                // 优先使用本地视频，没有再使用网络
                //: var videoPath = videoCellData.msgModel.msgInfo.customSend.localVideoUrl
                var videoPath = videoCellData.msgModel.msgInfo.customSend.localVideoUrl
                //: if videoPath.count == 0 || !FileManager.default.fileExists(atPath: videoPath) {
                if videoPath.count == 0 || !FileManager.default.fileExists(atPath: videoPath) {
                    //: videoPath = videoModel.videoUrl
                    videoPath = videoModel.videoUrl
                }

                //: let vc = TalkingMomentVideoVC.init(videoPath: videoPath)
                let vc = ImagePlayerDelegate(videoPath: videoPath)
                //: getNavigationController()?.pushViewController(vc, animated: true)
                characteristicRootOfASquareMatrix()?.pushViewController(vc, animated: true)

                //: } else {
            } else { // 私密视频
                // 已销毁的视频不能点击
                //: guard videoModel.burnStatus != 1 else { return }
                guard videoModel.burnStatus != 1 else { return }
                // 预览私密视频
                //: let vc = TalkingIntimateVideoPreviewVC(videoCellData: videoCellData, toUid: self.targetId)
                let vc = PreviewVc(videoCellData: videoCellData, toUid: self.targetId)
                // 更改本地私密视频状态
                //: vc.statusBlock = { (model) in
                vc.statusBlock = { model in
                    //: self.msgView.changeIntimateVideoMsg(videoCellData, videoModel: model)
                    self.msgView.contextPhoto(videoCellData, with: model)
                }
                // 送礼效果在当前页面处理
                //: vc.sendGiftResultBlock = { succeed, result, errorModel in
                vc.sendGiftResultBlock = { succeed, result, errorModel in
                    //: guard succeed else {
                    guard succeed else {
                        //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickUnlockButton)
                        self.back(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: kSucceedAgentName)
                        //: return
                        return
                    }

                    //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: false)
                    self.oeilDeBoeuf(extral: result as! [String: Any?], isFromInputView: false)
                    //: let dataDict = result as! Dictionary<String, Any>
                    let dataDict = result as! [String: Any]
                    //: self.unlockPhotoOrVideoTool(dataDict: dataDict)
                    self.overEqualDict(dataDict: dataDict)
                }
                //: getNavigationController()?.pushViewController(vc, animated: true)
                characteristicRootOfASquareMatrix()?.pushViewController(vc, animated: true)
            }

            //: } else if cell.isKind(of: TalkingChatAudioMsgCell.self) {
        } else if cell.isKind(of: EventMsgCell.self) {
            //: let audioCell = cell as! TalkingChatAudioMsgCell
            let audioCell = cell as! EventMsgCell
            //: func__DidTouchVoiceCellNeedPlayWithCurrentModel(cellData: audioCell.cellData as! ValueCustomCellData, cellView: audioCell)
            stateView(cellData: audioCell.cellData as! ValueCustomCellData, cellView: audioCell)

            //: } else if cell.isKind(of: TalkingChatTextMsgCell.self) {
        } else if cell.isKind(of: ActualView.self) {
            //: let textCell = cell as! TalkingChatTextMsgCell
            let textCell = cell as! ActualView
            //: let textData = textCell.messageData
            let textData = textCell.messageData
            //: let dic = ThemeChatReactiveCompatible.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            let dic = ThemeChatReactiveCompatible.playChange(data: (textData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(String(appViewData))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains((String(const_viewPath.prefix(7)))) {
                //: let truePersonAuth = extraDic["jumpKey"] as? String ?? ""
                let truePersonAuth = extraDic[(String(const_viewPath.prefix(7)))] as? String ?? ""
                //: if truePersonAuth == "truePersonAuth" {
                if truePersonAuth == (String(app_withMsg) + String(app_makeId)) {
                    //: CombineAccountEnclaveDecisionMakerThen.share.func__pushUserVerifyController(toast: nil)
                    CombineAccountEnclaveDecisionMakerThen.share.funcToast(toast: nil)
                    //: return
                    return
                }
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<HomeMeasurable>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: CombineAccountEnclaveDecisionMakerThen.share.func__actionPushForMessage(jumpModel: jumpModel)
                    CombineAccountEnclaveDecisionMakerThen.share.excess(jumpModel: jumpModel)
                }
            }

            //: let extraJson = JSON(extraDic)
            let extraJson = JSON(extraDic)
            //: if  dic["rYMsgType"] as? String == "GJ:CallCustom" {  // 音视频通话消息
            if dic[(String(main_viewExampleFormat) + String(user_labelValue))] as? String == (String(k_infoShowContent) + String(app_viewNowFillData.prefix(6))) { // 音视频通话消息
                //: if extraJson["msgInfo"]["logType"].intValue == 1 {
                if extraJson[(String(app_squareProMsg) + String(showErrorId.suffix(5)))][(String(k_nameDropFormat.prefix(7)))].intValue == 1 {
                    //: checkAndCallVoice()
                    baptise()
                    //: } else {
                } else {
                    //: checkAndCallVideo()
                    sussOut()
                }
            }

            //: } else if cell.isKind(of: TalkingChatTipsMsgCell.self) {
        } else if cell.isKind(of: BlockMaxMessageCell.self) {
            //: let tipCell = cell as! TalkingChatTipsMsgCell
            let tipCell = cell as! BlockMaxMessageCell
            //: let tipData = tipCell.messageData
            let tipData = tipCell.messageData
            //: let dic = ThemeChatReactiveCompatible.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            let dic = ThemeChatReactiveCompatible.playChange(data: (tipData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(String(appViewData))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains((String(const_viewPath.prefix(7)))) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<HomeMeasurable>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: CombineAccountEnclaveDecisionMakerThen.share.func__actionPushForMessage(jumpModel: jumpModel)
                    CombineAccountEnclaveDecisionMakerThen.share.excess(jumpModel: jumpModel)
                }
            }
            //: } else if cell.isKind(of: TalkingChatMatchMsgCell.self) {
        } else if cell.isKind(of: CompartmentChatAnswerMsgCell.self) {
            //: CombineAccountEnclaveDecisionMakerThen.share.func__pushToUserDetailVC(uid: targetId)
            CombineAccountEnclaveDecisionMakerThen.share.windowBy(uid: targetId)
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onRetryMessageContent cell: DigitiserReactiveCompatible!) {
    func card(_: ConstraintToViewController!, radiogramDeviceAccumulationPlace cell: DigitiserReactiveCompatible!) {
        //: var cellData: TalkingChatMsgBaseCellData?
        var cellData: ResponseGiftCellData?
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: VideoView.self) {
            //: let imageCell = cell as! TalkingChatImageMsgCell
            let imageCell = cell as! VideoView
            //: cellData = imageCell.cellData as! FountainheadCellData
            cellData = imageCell.cellData as! FountainheadCellData
            //: } else if cell.isKind(of: TalkingChatVideoMsgCell.self) {
        } else if cell.isKind(of: NameEqualMessageCell.self) {
            //: let videoCell = cell as! TalkingChatVideoMsgCell
            let videoCell = cell as! NameEqualMessageCell
            //: cellData = videoCell.cellData as! EigenvalueOfAMatrixReactiveCompatible
            cellData = videoCell.cellData as! EigenvalueOfAMatrixReactiveCompatible
            //: } else if cell.isKind(of: TalkingChatAudioMsgCell.self) {
        } else if cell.isKind(of: EventMsgCell.self) {
            //: let audioCell = cell as! TalkingChatAudioMsgCell
            let audioCell = cell as! EventMsgCell
            //: cellData = audioCell.cellData as! ValueCustomCellData
            cellData = audioCell.cellData as! ValueCustomCellData
            //: } else if cell.isKind(of: TalkingChatTextMsgCell.self) {
        } else if cell.isKind(of: ActualView.self) {
            //: let textCell = cell as! TalkingChatTextMsgCell
            let textCell = cell as! ActualView
            //: cellData = textCell.cellData as! MaxCellData
            cellData = textCell.cellData as! MaxCellData
        }
        //: if cellData != nil {
        if cellData != nil {
            //: let reCellData: AddCellData? = TUISocialChatManager.handleTXMessageReSend(withData: cellData!)
            let reCellData: AddCellData? = DirectionReactiveCompatible.addColor(withData: cellData!)
            //: if reCellData != nil {
            if reCellData != nil {
                //: cell.messageData?.msgModel = reCellData!.msgModel
                cell.messageData?.msgModel = reCellData!.msgModel
                //: cell.messageData?.status = .Msg_Status_Fail
                cell.messageData?.status = .Msg_Status_Fail
                //: if reCellData!.isKind(of: EigenvalueOfAMatrixReactiveCompatible.self) {
                if reCellData!.isKind(of: EigenvalueOfAMatrixReactiveCompatible.self) { // 视频重发
                    //: func__sendVideoMsg(msgData: cell.messageData, sendModel: cell.messageData!.msgModel.msgInfo.customSend)
                    makeModel(msgData: cell.messageData, sendModel: cell.messageData!.msgModel.msgInfo.customSend)
                    //: } else {
                } else {
                    //: func__sendPriveteMsg(msgData: cell.messageData!, reSend: true)
                    total(msgData: cell.messageData!, reSend: true)
                }
            }
        }
    }

    /// 回复消息
    //: func messageController(_ controller: TUIMessageController!, onReplyMessage cellData: AddCellData!) {
    func statusMessage(_: ConstraintToViewController!, contextMessage cellData: AddCellData!) {
        //: msgInputView.quoteModel = TalkingPrivateChatManager.handleReplyMsg(cellData: cellData, targetId: userWrap.uid)
        msgInputView.quoteModel = FrameLevelThen.script(cellData: cellData, targetId: userWrap.uid)
    }

    /// 点击“引用”消息
    //: func messageController(_ controller: TUIMessageController!, onClickReplyMessage cellData: AddCellData!) {
    func white(_: ConstraintToViewController!, strengthData cellData: AddCellData!) {
        //: TalkingPrivateChatManager.handleClickReplyMsg(cellData: cellData, targetId: userWrap.uid)
        FrameLevelThen.informationGreet(cellData: cellData, targetId: userWrap.uid)
    }

    /// 实时更新聊天双方头像
    /// - Parameter data: 腾讯数据
    /// - Returns: 腾讯数据
    //: private func changeChatHeadPic(data: AddCellData) -> AddCellData {
    private func reportAddTitle(data: AddCellData) -> AddCellData {
        //: let isSelf = data.msgModel.user.uid == Int(IconContainerAppManager.share.loginUserMode.userID)
        let isSelf = data.msgModel.user.uid == Int(IconContainerAppManager.share.loginUserMode.userID)
        //: if isSelf {
        if isSelf {
            //: if IconContainerAppManager.share.loginUserMode.headPic?.count ?? 0 > 0 {
            if IconContainerAppManager.share.loginUserMode.headPic?.count ?? 0 > 0 {
                //: data.msgModel.user.headPic = IconContainerAppManager.share.loginUserMode.headPic!
                data.msgModel.user.headPic = IconContainerAppManager.share.loginUserMode.headPic!
                //: data.msgModel.user.headPicFrame = IconContainerAppManager.share.loginUserMode.headPicFrame
                data.msgModel.user.headPicFrame = IconContainerAppManager.share.loginUserMode.headPicFrame
            }
            //: } else {
        } else {
            //: if self.userWrap.headPic.count > 0 {
            if self.userWrap.headPic.count > 0 {
                //: data.msgModel.user.headPic = self.userWrap.headPic
                data.msgModel.user.headPic = self.userWrap.headPic
                //: data.msgModel.user.headPicFrame = self.userWrap.headPicFrame
                data.msgModel.user.headPicFrame = self.userWrap.headPicFrame
            }
        }
        //: return data
        return data
    }
}

// MARK: - 发送消息

//: extension TalkingPrivateChatController {
extension MakeReactiveCompatible {
    /// 发送快速打招呼消息
    //: func func_sendQuickGreetingMsg() {
    func anyGroup() {
        //: msgView.targetID = self.targetId
        msgView.targetID = self.targetId
        //: let msgInfo = ["msgType": "txt"]
        let msgInfo = [String(bytes: noti_videoTapMessage.reversed(), encoding: .utf8)!: "txt"]
        //: let paramsDic = ["type": "QuickMsg"]
        let paramsDic = [(constColorId.replacingOccurrences(of: "user", with: "e")): (String(noti_signMessage))]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(app_squareProMsg) + String(showErrorId.suffix(5))): msgInfo, String(bytes: user_layerMessage.map{indexLevel(raw: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = DirectionReactiveCompatible.impingeOpen(withExtral: dic, isSender: true, isTip: false) {
            //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
            self.total(msgData: cellData, reSend: false)
        }
    }

    /// 发送一键打招呼消息
    /// - Parameters:
    ///   - textDict: 文本消息
    ///   - audioDict: 语音消息
    //: private func func_sendQuickGreetingMsg(textDict: [String: Any]?, audioDict: [String: Any]?) {
    private func tingManager(textDict: [String: Any]?, audioDict: [String: Any]?) {
        //: if textDict?.count ?? 0 > 0 {
        if textDict?.count ?? 0 > 0 { // 发送文本消息
            //: let content = textDict?["content"]
            let content = textDict?[String(bytes: dataProfileCameraModePath.map{$0^84}, encoding: .utf8)!]
            //: let greetId = textDict?["id"]
            let greetId = textDict?["id"]
            //: let msgInfo = ["content": content, "txtId": greetId, "msgType": "txt"]
            let msgInfo = [String(bytes: dataProfileCameraModePath.map{$0^84}, encoding: .utf8)!: content, (String(main_titleDataText.suffix(5))): greetId, String(bytes: noti_videoTapMessage.reversed(), encoding: .utf8)!: "txt"]
            //: let paramsDic = ["message": content, "txtId": greetId, "type": "TxtMsg"]
            let paramsDic = [String(bytes: constReportStr.map{recordNow(key: $0)}, encoding: .utf8)!: content, (String(main_titleDataText.suffix(5))): greetId, (constColorId.replacingOccurrences(of: "user", with: "e")): (String(dataEditMessage.suffix(6)))]
            //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
            let dic = [(String(app_squareProMsg) + String(showErrorId.suffix(5))): msgInfo, String(bytes: user_layerMessage.map{indexLevel(raw: $0)}, encoding: .utf8)!: paramsDic]
            //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
            if let cellData = DirectionReactiveCompatible.impingeOpen(withExtral: dic, isSender: true, isTip: false) {
                //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
                self.total(msgData: cellData, reSend: false)
            }
        }

        //: if audioDict?.count ?? 0 > 0 {
        if audioDict?.count ?? 0 > 0 { // 发送语音消息
            //: VoiceDownloadTaskManager.shared.addAudioGreetDownLoadTask(audioDict!) { model in
            ViewTaskManager.shared.withAdd(audioDict!) { model in
                //: if model.taskTypeId != .oneKeyGreet {
                if model.taskTypeId != .oneKeyGreet {
                    //: return
                    return
                }
                //: if model.taskStatus != .finish {
                if model.taskStatus != .finish {
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: self.func__showStatusBarErrorMsg(showMsg: "Voice hello download failed. Procedure")
                        self.explain(showMsg: String(bytes: notiViewValue.map{thoughtImage(video: $0)}, encoding: .utf8)!)
                        //: TalkingPrivateChatManager.chat_changeSaveGreetData(toUserId: self.targetId, isHave: false)
                        FrameLevelThen.menu(toUserId: self.targetId, isHave: false)
                    }
                    //: return
                    return
                }
                //: let greetId = audioDict?["id"]
                let greetId = audioDict?["id"]
                //: let length = audioDict?["length"]
                let length = audioDict?[(show_firstMessage.replacingOccurrences(of: "tool", with: "n") + userKeyName.replacingOccurrences(of: "frame", with: "h"))]
                //: let msgInfo = ["msgType": "audio",
                let msgInfo = [String(bytes: noti_videoTapMessage.reversed(), encoding: .utf8)!: (String(main_removeMsg)),
                               //: "audioId": greetId,
                               (mainSucceedUrl.replacingOccurrences(of: "color", with: "au") + String(show_pictureData)): greetId,
                               //: "audioLength": length,
                               String(bytes: mainViewTitle.map{$0^206}, encoding: .utf8)!: length,
                               //: "audioSandbox": model.unZipFilePath,
                               (userTableText.replacingOccurrences(of: "info", with: "o") + "Sandbo" + k_appStr.replacingOccurrences(of: "model", with: "x")): model.unZipFilePath,
                               //: "senderId": IconContainerAppManager.share.loginUserMode.userID]
                               String(bytes: kLiveId.reversed(), encoding: .utf8)!: IconContainerAppManager.share.loginUserMode.userID]

                //: let paramsDic = ["audioId": greetId,
                let paramsDic = [(mainSucceedUrl.replacingOccurrences(of: "color", with: "au") + String(show_pictureData)): greetId,
                                 //: "audioLength": length,
                                 String(bytes: mainViewTitle.map{$0^206}, encoding: .utf8)!: length,
                                 //: "audioSandbox": model.unZipFilePath,
                                 (userTableText.replacingOccurrences(of: "info", with: "o") + "Sandbo" + k_appStr.replacingOccurrences(of: "model", with: "x")): model.unZipFilePath,
                                 //: "type": "AudioMsg"]
                                 (constColorId.replacingOccurrences(of: "user", with: "e")): (String(noti_priceTitle.suffix(2)) + String(kLaunchFormat.suffix(6)))]
                //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
                let dic = [(String(app_squareProMsg) + String(showErrorId.suffix(5))): msgInfo, String(bytes: user_layerMessage.map{indexLevel(raw: $0)}, encoding: .utf8)!: paramsDic]
                //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
                if let cellData = DirectionReactiveCompatible.impingeOpen(withExtral: dic, isSender: true, isTip: false) {
                    //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
                    self.total(msgData: cellData, reSend: false)
                }
            }
        }
    }

    /// 发送文本消息
    /// - Parameter msgStr: 文本
    //: internal func func__sendTextMsg(msgStr: String, quoteModel: AbTalkingChatMsgQuoteModel?) {
    func sire(msgStr: String, quoteModel: MainScoreModel?) {
        //: let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        //: if temStr.count == 0 {
        if temStr.count == 0 {
            //: func__showStatusBarErrorMsg(showMsg: "Can't send blank message".localized)
            explain(showMsg: (String(dataDirectionName) + notiMiniHeadFormat.replacingOccurrences(of: "bottom", with: "e")).localized)
            //: return
            return
        }

        //: let msgInfo = ["content": temStr, "msgType": "txt"]
        let msgInfo = [String(bytes: dataProfileCameraModePath.map{$0^84}, encoding: .utf8)!: temStr, String(bytes: noti_videoTapMessage.reversed(), encoding: .utf8)!: "txt"]
        //: var paramsDic = ["message": temStr, "type": "TxtMsg"]
        var paramsDic = [String(bytes: constReportStr.map{recordNow(key: $0)}, encoding: .utf8)!: temStr, (constColorId.replacingOccurrences(of: "user", with: "e")): (String(dataEditMessage.suffix(6)))]
        //: var dic:[String: Any] = ["msgInfo": msgInfo]
        var dic: [String: Any] = [(String(app_squareProMsg) + String(showErrorId.suffix(5))): msgInfo]

        //: if let tempQuoteModel = quoteModel{
        if let tempQuoteModel = quoteModel {
            //: paramsDic["quoteMsgId"] = tempQuoteModel.msgId
            paramsDic[(String(app_insertData.prefix(6)) + String(constImageEqualStr))] = tempQuoteModel.msgId
            //: dic["quoteMsgInfo"] = tempQuoteModel.toDictionary()
            dic[(String(kPostKey) + String(noti_filterStr.suffix(9)))] = tempQuoteModel.toDictionary()
        }
        //: dic["params"] = paramsDic
        dic[String(bytes: user_layerMessage.map{indexLevel(raw: $0)}, encoding: .utf8)!] = paramsDic

        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = DirectionReactiveCompatible.impingeOpen(withExtral: dic, isSender: true, isTip: false) {
            //: cellData.msgModel.quoteMsgInfo = quoteModel ?? AbTalkingChatMsgQuoteModel()
            cellData.msgModel.quoteMsgInfo = quoteModel ?? MainScoreModel()
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            total(msgData: cellData, reSend: false)
        }
    }

    /// 发送图片消息
    /// - Parameters:
    ///   - img: 图片
    ///   - sendModel: 本地数据模型
    //: private func func__sendPictureMsg(img: UIImage, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func behindSendFrom(img: UIImage, sendModel: PlayerChatModel) {
        //: let imagePath = FountainheadCellData.getChatImagePath(image: img)
        let imagePath = FountainheadCellData.chat(image: img)
        // 缓存本地的消息
        //: let sendDict = sendModel.toDictionary() ?? [:]
        let sendDict = sendModel.toDictionary() ?? [:]
        //: let msgInfo: [String: Any] = ["contentType": "ImgMsg", "msgType": "img", "imageUri": imagePath, "customSend": sendDict]
        let msgInfo: [String: Any] = [String(bytes: userHiddenItemValue.map{imageTarget(of: $0)}, encoding: .utf8)!: (String(show_placeStopUrl.prefix(6))), String(bytes: noti_videoTapMessage.reversed(), encoding: .utf8)!: "img", String(bytes: user_playerSizeTryTitle.map{awakeCellFill(result: $0)}, encoding: .utf8)!: imagePath, String(bytes: main_withFormat.map{contentKey(list: $0)}, encoding: .utf8)!: sendDict]
        // 接口参数
        //: let paramsDic: [String: Any] = ["pic": imagePath,
        let paramsDic: [String: Any] = ["pic": imagePath,
                                        //: "type": "ImgMsg",
                                        (constColorId.replacingOccurrences(of: "user", with: "e")): (String(show_placeStopUrl.prefix(6))),
                                        //: "isBurn": sendModel.isBurn,
                                        String(bytes: kFemaleContent.map{managingDirector(interaction: $0)}, encoding: .utf8)!: sendModel.isBurn,
                                        //: "giftId": sendModel.giftId]
                                        (String(constToTitle)): sendModel.giftId]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(app_squareProMsg) + String(showErrorId.suffix(5))): msgInfo, String(bytes: user_layerMessage.map{indexLevel(raw: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = DirectionReactiveCompatible.impingeOpen(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            total(msgData: cellData, reSend: false)
        }
    }

    /// 发送视频消息
    /// - Parameters:
    ///   - msgData: 发送视频cellData；nil：上传流程，有值 ：发送流程
    ///   - sendModel: 视频消息自定义本地模型
    //: private func func__sendVideoMsg(msgData: AddCellData?, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func makeModel(msgData: AddCellData?, sendModel: PlayerChatModel) {
        // 缓存本地的消息
        //: let videoDict = sendModel.toDictionary() ?? [:]
        let videoDict = sendModel.toDictionary() ?? [:]
        //: let msgInfo: [String: Any] = ["contentType": "VideoMsg", "msgType": "video", "customSend": videoDict]
        let msgInfo: [String: Any] = [String(bytes: userHiddenItemValue.map{imageTarget(of: $0)}, encoding: .utf8)!: (String(userCoverStr.prefix(8))), String(bytes: noti_videoTapMessage.reversed(), encoding: .utf8)!: (String(main_firstObjectMessage)), String(bytes: main_withFormat.map{contentKey(list: $0)}, encoding: .utf8)!: videoDict]
        // 接口参数
        //: let paramsDic: [String: Any] = ["type": "VideoMsg",
        let paramsDic: [String: Any] = [(constColorId.replacingOccurrences(of: "user", with: "e")): (String(userCoverStr.prefix(8))),
                                        //: "coverImg": sendModel.coverImg,
                                        String(bytes: appMakeTitle.map{$0^35}, encoding: .utf8)!: sendModel.coverImg,
                                        //: "videoUrl": sendModel.videoUrl,
                                        String(bytes: showToStr.reversed(), encoding: .utf8)!: sendModel.videoUrl,
                                        //: "duration": sendModel.duration,
                                        String(bytes: showCornerStr.map{statusUp(day: $0)}, encoding: .utf8)!: sendModel.duration,
                                        //: "isBurn": sendModel.isBurn,
                                        String(bytes: kFemaleContent.map{managingDirector(interaction: $0)}, encoding: .utf8)!: sendModel.isBurn,
                                        //: "giftId": sendModel.giftId]
                                        (String(constToTitle)): sendModel.giftId]
        //: let dict = ["msgInfo": msgInfo, "params": paramsDic]
        let dict = [(String(app_squareProMsg) + String(showErrorId.suffix(5))): msgInfo, String(bytes: user_layerMessage.map{indexLevel(raw: $0)}, encoding: .utf8)!: paramsDic]

        // 压缩成功 ---> 创建cellData ---> 上传视频
        //: guard let cellData = msgData else {
        guard let cellData = msgData else {
            //: if let uploadCellData = TUISocialChatManager.handleTXMessageSend(withExtral: dict, isSender: true, isTip: false) {
            if let uploadCellData = DirectionReactiveCompatible.impingeOpen(withExtral: dict, isSender: true, isTip: false) {
                //: self.msgView.sendCustomMessage(uploadCellData)
                self.msgView.instance(uploadCellData)
                //: self.cos_UploadVideo(msgData: uploadCellData, sendModel: sendModel)
                self.upload(msgData: uploadCellData, sendModel: sendModel)
            }
            //: return
            return
        }

        // 失败重试，展示加载动画
        //: if cellData.status != .Msg_Status_Sending_2 {
        if cellData.status != .Msg_Status_Sending_2 {
            //: self.msgView.changeCustomMsg(cellData, status: .Msg_Status_Sending_2)
            self.msgView.message(cellData, gaynessExpiry: .Msg_Status_Sending_2)
        }

        // 失败重试上传视频流程
        //: guard sendModel.videoUrl.count > 0 else {
        guard sendModel.videoUrl.count > 0 else {
            //: self.cos_UploadVideo(msgData: cellData, sendModel: sendModel)
            self.upload(msgData: cellData, sendModel: sendModel)
            //: return
            return
        }

        // 修改本地消息数据，调用发送视频接口
        //: self.msgView.modifyCustomMessageData(withExtral: dict, cellData: cellData)
        self.msgView.center(dict, mode: cellData)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        total(msgData: cellData, reSend: false)
    }

    /// 发送语音
    /// - Parameters:
    ///   - voicePath: 语音路径
    ///   - voiceTime: 时间
    //: func func__sendVoiceMsg(voicePath: String, voiceTime: Int) {
    func buttonDate(voicePath: String, voiceTime: Int) {
        //: let msgInfo = ["msgType": "audio",
        let msgInfo = [String(bytes: noti_videoTapMessage.reversed(), encoding: .utf8)!: (String(main_removeMsg)),
                       //: "audioLength": "\(voiceTime)",
                       String(bytes: mainViewTitle.map{$0^206}, encoding: .utf8)!: "\(voiceTime)",
                       //: "audioSandbox": voicePath,
                       (userTableText.replacingOccurrences(of: "info", with: "o") + "Sandbo" + k_appStr.replacingOccurrences(of: "model", with: "x")): voicePath,
                       //: "senderId": IconContainerAppManager.share.loginUserMode.userID]
                       String(bytes: kLiveId.reversed(), encoding: .utf8)!: IconContainerAppManager.share.loginUserMode.userID]

        //: let paramsDic = ["audioSandbox": voicePath,
        let paramsDic = [(userTableText.replacingOccurrences(of: "info", with: "o") + "Sandbo" + k_appStr.replacingOccurrences(of: "model", with: "x")): voicePath,
                         //: "type": "AudioMsg",
                         (constColorId.replacingOccurrences(of: "user", with: "e")): (String(noti_priceTitle.suffix(2)) + String(kLaunchFormat.suffix(6))),
                         //: "len": "\(voiceTime)"]
                         "len": "\(voiceTime)"]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(app_squareProMsg) + String(showErrorId.suffix(5))): msgInfo, String(bytes: user_layerMessage.map{indexLevel(raw: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = DirectionReactiveCompatible.impingeOpen(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            total(msgData: cellData, reSend: false)
        }
    }

    /// 发送消息
    /// - Parameters:
    ///   - msgData: 消息数据结构
    ///   - reSend: 是否重发【目前未使用该字段】
    //: private func func__sendPriveteMsg(msgData: AddCellData, reSend: Bool) {
    private func total(msgData: AddCellData, reSend _: Bool) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            explain(showMsg: dataStatusTitle)
            //: return
            return
        }

        //: var params: Dictionary<String, Any> = msgData.msgModel.params as! Dictionary<String, Any>
        var params: [String: Any] = msgData.msgModel.params as! [String: Any]
        //: params["toUid"] = self.targetId
        params[(String(kUpName))] = self.targetId

        // 发送视频消息，索要礼物消息，快速打招呼消息，不在此处插入
        //: if msgData.isKind(of: EigenvalueOfAMatrixReactiveCompatible.self) == false && msgData.isKind(of:  MakeCellData.self) == false && params["type"] as? String != "QuickMsg" {
        if msgData.isKind(of: EigenvalueOfAMatrixReactiveCompatible.self) == false, msgData.isKind(of: MakeCellData.self) == false, params[(constColorId.replacingOccurrences(of: "user", with: "e"))] as? String != (String(noti_signMessage)) {
            //: self.msgView.sendCustomMessage(msgData)
            self.msgView.instance(msgData)
        }
        //: let recordTime = ThemeChatReactiveCompatible.getMessageInsertTime()
        let recordTime = ThemeChatReactiveCompatible.timeIn()

        //: if params.keys .contains("pic") { // 图片
        if params.keys.contains("pic") { // 图片
            //: let imageStr: String = params["pic"] as! String
            let imageStr: String = params["pic"] as! String
            //: do {
            do {
                //: let data: Data = try Data.init(contentsOf: URL.init(fileURLWithPath: imageStr))
                let data: Data = try Data(contentsOf: URL(fileURLWithPath: imageStr))
                //: params["pic"] = data
                params["pic"] = data
                //: } catch {
            } catch {
                //: printLog(message: error)
                printLog(message: error)
            }
        }
        //: let type  = params["type"] as! String
        let type = params[(constColorId.replacingOccurrences(of: "user", with: "e"))] as! String
        //: if type == "AudioMsg" { // 语音
        if type == (String(noti_priceTitle.suffix(2)) + String(kLaunchFormat.suffix(6))) { // 语音
            //: if params.keys.contains("audioSandbox") {
            if params.keys.contains((userTableText.replacingOccurrences(of: "info", with: "o") + "Sandbo" + k_appStr.replacingOccurrences(of: "model", with: "x"))) {
                //: var path =  params["audioSandbox"] as! String
                var path = params[(userTableText.replacingOccurrences(of: "info", with: "o") + "Sandbo" + k_appStr.replacingOccurrences(of: "model", with: "x"))] as! String
                //: params.removeValue(forKey: "audioSandbox")
                params.removeValue(forKey: (userTableText.replacingOccurrences(of: "info", with: "o") + "Sandbo" + k_appStr.replacingOccurrences(of: "model", with: "x")))

                //: let theFileName = (path as NSString).lastPathComponent
                let theFileName = (path as NSString).lastPathComponent

                //: if path.contains("Documents/User/voice/") {
                if path.contains((String(appModelValue.suffix(4)) + kTillFormat.replacingOccurrences(of: "value", with: "ts") + "/User" + String(show_screenPathFormat.prefix(7)))) {
                    //: path = Talking_Chat_voice_SandboxPath + theFileName
                    path = app_tagData + theFileName
                    //: } else if(path.contains("Documents/User/record/")) {
                } else if path.contains((String(showThanUrl.suffix(5)) + String(data_videoCellName))) {
                    //: path = Talking_Chat_record_SandboxPath + theFileName
                    path = show_statusKeyName + theFileName
                }

                //: let audioId = params["audioId"] ?? "0"
                let audioId = params[(mainSucceedUrl.replacingOccurrences(of: "color", with: "au") + String(show_pictureData))] ?? "0"
                //: if  (audioId as! NSString).integerValue == 0 {
                if (audioId as! NSString).integerValue == 0 {
                    //: do {
                    do {
                        //: let voiceData = try Data.init(contentsOf: URL.init(fileURLWithPath: path))
                        let voiceData = try Data(contentsOf: URL(fileURLWithPath: path))
                        //: params["audio"] = voiceData
                        params[(String(main_removeMsg))] = voiceData
                        //: } catch {
                    } catch {}
                }
            }
        }

        // 发送消息接口
        //: StreamRequestTool.req_SendPrivete(param: params, completion: { succeed, result, errorModel in
        StreamRequestTool.toteBag(param: params, completion: { succeed, result, errorModel in
            //: if !succeed {
            if !succeed {
                //: if type == "QuickMsg" {
                if type == (String(noti_signMessage)) {
                    //: self.QuickEndBlock?(false)
                    self.QuickEndBlock?(false)
                }
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.auspicate(msgData, take: nil, cut: NSNumber(value: 0), custom: .Msg_Status_Fail)
                
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickMessagesendButton)
                self.back(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: main_buttonTitle)
                //: if params.keys.contains("txtId") {
                if params.keys.contains((String(main_titleDataText.suffix(5)))) {
                    //: TalkingPrivateChatManager.chat_changeSaveGreetData(toUserId: self.targetId, isHave: false)
                    FrameLevelThen.menu(toUserId: self.targetId, isHave: false)
                }
                /// 打招呼达到设定次数失败时
                //: if errorModel!.errorCode == 10015 {
                if errorModel!.errorCode == 10015 {
                    //: self.msgView.deleteCustomMessage(msgData)
                    self.msgView.camera(msgData)
                }
                //: return
                return
            }
            //: var updateMsgData = msgData
            var updateMsgData = msgData
            //: let dataDict = result as! Dictionary<String, Any>
            let dataDict = result as! [String: Any]
            //: var msgInfo = dataDict["msgInfo"] as! Dictionary<String, Any>
            var msgInfo = dataDict[(String(app_squareProMsg) + String(showErrorId.suffix(5)))] as! [String: Any]

            //: var msgIncome: NSNumber = NSNumber.init(value: 0)
            var msgIncome: NSNumber = .init(value: 0)
            //: if msgInfo.keys.contains("msgIncome") {
            if msgInfo.keys.contains((String(kTransformContent))) {
                //: msgIncome = msgInfo["msgIncome"] as! NSNumber
                msgIncome = msgInfo[(String(kTransformContent))] as! NSNumber
            }

            // 直播时回复消息，积分更改
            //: if TalkingLiveManager.shared().isLive && msgIncome.floatValue > 0 {
            if DraftCopyThen.fileForScope().isLive && msgIncome.floatValue > 0 {
                //: let mfBean = Double(truncating: msgIncome)
                let mfBean = Double(truncating: msgIncome)
                //: NotificationCenter.default.post(name: LIVE_USER_POINT_CHANGE, object: nil, userInfo: ["mfBean": mfBean])
                NotificationCenter.default.post(name: notiViewMsg, object: nil, userInfo: [String(bytes: show_sizeStr.map{numerousnessActual(title: $0)}, encoding: .utf8)!: mfBean])
            }
            /// 亲密度
            //: self.sendMsgIntimateTool(msgInfo: msgInfo)
            self.endProduct(msgInfo: msgInfo)

            /// 索要礼物，快速打招呼
            //: let messageType = msgInfo["messageType"] as? Int
            let messageType = msgInfo[(String(main_editMessage) + String(constToValue.prefix(6)))] as? Int
            //: if messageType == 25 || type == "QuickMsg" {
            if messageType == 25 || type == (String(noti_signMessage)) {
                //: if messageType == 25 {
                if messageType == 25 {
                    //: msgInfo["msgType"] = "WantGift"
                    msgInfo[String(bytes: noti_videoTapMessage.reversed(), encoding: .utf8)!] = (String(data_colorPath) + data_bringValueFramePath.replacingOccurrences(of: "up", with: "t"))
                    //: uploadRecord.uploadRecordEvent(eventID: clickMessageDetailGiftPopAskBtnSusType)
                    showVideoPath.referenceStr(eventID: dataMagnitudeervalKey)
                    //: } else if type == "QuickMsg" {
                } else if type == (String(noti_signMessage)) {
                    //: msgInfo["msgType"] = "txt"
                    msgInfo[String(bytes: noti_videoTapMessage.reversed(), encoding: .utf8)!] = "txt"
                    //: self.QuickEndBlock?(true)
                    self.QuickEndBlock?(true)
                }
                //: let dic = ["msgInfo": msgInfo]
                let dic = [(String(app_squareProMsg) + String(showErrorId.suffix(5))): msgInfo]
                //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
                if let cellData = DirectionReactiveCompatible.impingeOpen(withExtral: dic, isSender: true, isTip: false) {
                    //: updateMsgData = cellData
                    updateMsgData = cellData
                    //: self.msgView.sendCustomMessage(updateMsgData)
                    self.msgView.instance(updateMsgData)
                }
            }

            // 更新提示文案
            //: self.userWrap.existSess = true
            self.userWrap.existSess = true
            //: let key =  "\(IconContainerAppManager.share.loginUserMode.userID)_PrivateChatTipsTitle_\(self.targetId)"
            let key = "\(IconContainerAppManager.share.loginUserMode.userID)" + (String(kCornerValue) + String(dataImageLaughText)) + "\(self.targetId)"
            //: UserDefaults.standard.set(true, forKey: key)
            UserDefaults.standard.set(true, forKey: key)
            //: if IconContainerAppManager.share.loginUserMode.sex == Gender.male.rawValue && type != "QuickMsg" {
            if IconContainerAppManager.share.loginUserMode.sex == ThemeTitleScalarLiteral.male.rawValue, type != (String(noti_signMessage)) {
                //: self.removeReplyTipsView()
                self.quoteAction()
            }
            // 更新本地消息信息
            //: self.msgView.changeCustomMsg(updateMsgData, msgId: msgInfo["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
            self.msgView.auspicate(updateMsgData, take: msgInfo[(String(main_willName))] as? String, cut: msgIncome, custom: .Msg_Status_Succ)
            
            // 女性记录最新回复时间, 刷新表格
            //: TalkingPrivateChatManager.updateReplyLatestTimeAndReload(CGFloat(msgIncome.floatValue), updateMsgData.innerMessage.timestamp, self.msgView)
            FrameLevelThen.tillName(CGFloat(msgIncome.floatValue), updateMsgData.innerMessage.timestamp, self.msgView)

            //: if dataDict.keys.contains("totalMfCoin") {
            if dataDict.keys.contains((notiModelId.replacingOccurrences(of: "calendar", with: "o") + String(data_imageMsg.prefix(9)))) {
                //: let totalMfCoin: NSNumber = dataDict["totalMfCoin"] as! NSNumber
                let totalMfCoin: NSNumber = dataDict[(notiModelId.replacingOccurrences(of: "calendar", with: "o") + String(data_imageMsg.prefix(9)))] as! NSNumber
                //: IconContainerAppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
                IconContainerAppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            }

            /// 免费消息提示
            //: if dataDict.keys.contains("attachSysMsg") {
            if dataDict.keys.contains((String(mainOnItemInsideId) + String(show_topMessage.prefix(5)))) {
                //: let attachSysMsg: String = dataDict["attachSysMsg"] as? String ?? ""
                let attachSysMsg: String = dataDict[(String(mainOnItemInsideId) + String(show_topMessage.prefix(5)))] as? String ?? ""
                //: if attachSysMsg.count>0 {
                if attachSysMsg.count > 0 {
                    //: self.func__privateAttachSys_insertTipMsg(str: attachSysMsg)
                    self.logKind(str: attachSysMsg)
                }
            }

            //: if msgInfo.keys.contains("intimatePhoto") { // 发送成功私密照片 => model，更新本地消息
            if msgInfo.keys.contains((user_sourceMsg.replacingOccurrences(of: "selected", with: "nt") + "mateP" + String(constDataDetailMessage))) { // 发送成功私密照片 => model，更新本地消息
                //: let intimatePhotoDict = msgInfo["intimatePhoto"] as! [String: Any]
                let intimatePhotoDict = msgInfo[(user_sourceMsg.replacingOccurrences(of: "selected", with: "nt") + "mateP" + String(constDataDetailMessage))] as! [String: Any]
                //: if let model = try? AbTalkingChatMsgIntimatePhotoModel(dictionary: intimatePhotoDict) {
                if let model = try? BlueprintModel(dictionary: intimatePhotoDict) {
                    //: updateMsgData.msgModel.msgInfo.intimatePhoto = model
                    updateMsgData.msgModel.msgInfo.intimatePhoto = model
                    //: self.msgView.changeIntimatePhotoMsg(updateMsgData, photoModel: model)
                    self.msgView.filterDetect(updateMsgData, center: model)
                }

                //: } else if msgInfo.keys.contains("video") { // 发送成功私密视频 => model，更新本地消息
            } else if msgInfo.keys.contains((String(main_firstObjectMessage))) { // 发送成功私密视频 => model，更新本地消息
                //: let intimateVideoDict = msgInfo["video"] as! [String: Any]
                let intimateVideoDict = msgInfo[(String(main_firstObjectMessage))] as! [String: Any]
                //: if let model = try? AbTalkingChatMsgVideoModel(dictionary: intimateVideoDict) {
                if let model = try? EliteModel(dictionary: intimateVideoDict) {
                    //: updateMsgData.msgModel.msgInfo.video = model
                    updateMsgData.msgModel.msgInfo.video = model
                    //: self.msgView.changeIntimateVideoMsg(updateMsgData, videoModel: model)
                    self.msgView.contextPhoto(updateMsgData, with: model)
                }
            }
            //: })
        })

        // 判断是否需要插入音视频提示消息
        //: func__privateChatVideo_insertTipMsg()
        areaWith()

        //: updateSession()
        impanel()
    }

    /// 建立会话标识，本地更新数据和会话列表
    //: func updateSession() {
    func impanel() {
        //: DBUserInfoManager.cache_updateSessionAndPostNotif(with: targetId)
        DirectionInfoManager.beforeEqual(with: targetId)
    }

    /// 处理发送失败情况
    //: private func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool, clickEvent: String) {
    private func back(errorCode: Int, errorStr: String, isGift _: Bool, isResend _: Bool, clickEvent: String) {
        //: if errorCode == 50203 {
        if errorCode == 50203 { // 余额不足
            //: guard IconContainerAppManager.share.loginUserMode.status != 1 else {
            guard IconContainerAppManager.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    explain(showMsg: errorStr)
                }
                //: return
                return
            }

            //: CombineAccountEnclaveDecisionMakerThen.share.func__jumpToWebRecharge(clickEvent: clickEvent, sufficient: false)
            CombineAccountEnclaveDecisionMakerThen.share.toMake(clickEvent: clickEvent, sufficient: false)
            //: msgInputView.giftView.dismissView()
            msgInputView.giftView.statusComponent()
            //: IconContainerAppManager.share.loginUserMode.freeMsgTimes = ""
            IconContainerAppManager.share.loginUserMode.freeMsgTimes = ""
            //: msgInputView.setHidenFreeMsgTimesView()
            msgInputView.getWeaving()
//            insertTipMsg_Nobalance(tipStr: errorStr)
            //: } else if errorCode == 10888 {
        } else if errorCode == 10888 { // vip订阅弹窗
            //: let view = TalkingLiveVipSubscribePopView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let view = OverdoPopView(frame: CGRect(x: 0, y: 0, width: main_colorData, height: constRecordServerKey))
            //: view.show()
            view.addShow()
            //: } else if errorCode == 60003, !IconContainerAppManager.share.loginUserMode.isNaUser {
        } else if errorCode == 60003, !IconContainerAppManager.share.loginUserMode.isNaUser {
            /// 真人认证提示弹窗
            //: TalkingPopupWindowManager.shared.faceVerificationPopUpWindow()
            ManagerReactiveCompatible.shared.valueAction()
            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                explain(showMsg: errorStr)
            }
        }
    }
}

// MARK: - 亲密度

//: extension TalkingPrivateChatController {
extension MakeReactiveCompatible {
    /// 解锁图片视频送礼亲密度
    //: private func unlockPhotoOrVideoTool(dataDict: Dictionary<String, Any>) {
    private func overEqualDict(dataDict: [String: Any]) {
        //: let msgInfo = dataDict["msgInfo"] as! Dictionary<String, Any>
        let msgInfo = dataDict[(String(app_squareProMsg) + String(showErrorId.suffix(5)))] as! [String: Any]
        /// 亲密度
        //: self.sendMsgIntimateTool(msgInfo: msgInfo)
        self.endProduct(msgInfo: msgInfo)
    }

    /// 初次展示导航栏的亲密度头像
    //: private func showFirstIntimateView() {
    private func sharedView() {
        //: if self.userWrap.intimate >= IconContainerAppManager.share.appUserConfigMode.intimateLimit.chatTitleEffect && conversationType == .private  {
        if self.userWrap.intimate >= IconContainerAppManager.share.appUserConfigMode.intimateLimit.chatTitleEffect, conversationType == .private {
            //: self.isShowIntimateView = true
            self.isShowIntimateView = true
            //: navView.private_updateIntimateNavView(userModel: userWrap)
            navView.phoneMake(userModel: userWrap)
            /// 初次展示动效
            //: let key = "\(IconContainerAppManager.share.loginUserMode.userID)_\(self.targetId)"
            let key = "\(IconContainerAppManager.share.loginUserMode.userID)_\(self.targetId)"
            //: var intimateCacheData = (Defaults.array(forKey: TalkingFirstShowIntimateGuide)) as? [String] ?? []
            var intimateCacheData = (user_formalData.array(forKey: kNeedTitle)) as? [String] ?? []
            //: if !intimateCacheData.contains(key) {
            if !intimateCacheData.contains(key) {
                //: navView.playAnimation()
                navView.toAnimation()
                //: intimateCacheData.append(key)
                intimateCacheData.append(key)
                //: Defaults.set(intimateCacheData, forKey: TalkingFirstShowIntimateGuide)
                user_formalData.set(intimateCacheData, forKey: kNeedTitle)
            }
        }
    }

    /// 收到亲密度消息
    //: @objc func newMsgIntimate(notification: NSNotification) {
    @objc func beyondBrandNew(notification: NSNotification) {
        //: let data = notification.object as! V2TIMMessage
        let data = notification.object as! V2TIMMessage
        //: let dic = ThemeChatReactiveCompatible.parseTXMessageData(data: data.customElem.data)
        let dic = ThemeChatReactiveCompatible.playChange(data: data.customElem.data)
        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: let extra = jsonDic["extra"].dictionary
        let extra = jsonDic[(String(appViewData))].dictionary
        //: let msgInfo = extra?["msgInfo"]?.dictionary
        let msgInfo = extra?[(String(app_squareProMsg) + String(showErrorId.suffix(5)))]?.dictionary
        //: if data.sender == self.targetId {
        if data.sender == self.targetId {
            //: if let totalIntimate = msgInfo?["totalIntimate"]?.int {
            if let totalIntimate = msgInfo?[(String(app_sizeFormat) + String(k_bottomTitle.suffix(8)))]?.int {
                //: if self.conversationType == .private {
                if self.conversationType == .private {
                    //: let time = data.timestamp.timeIntervalSince1970
                    let time = data.timestamp.timeIntervalSince1970
                    //: self.intimateTool(currenIntimate: totalIntimate, time: Int(time))
                    self.sumimateCurrent(currenIntimate: totalIntimate, time: Int(time))
                }
            }
        }
    }

    /// 发送消息时
    //: private func sendMsgIntimateTool(msgInfo: Dictionary<String, Any>) {
    private func endProduct(msgInfo: [String: Any]) {
        //: if msgInfo.keys.contains("totalIntimate") && self.conversationType == .private {
        if msgInfo.keys.contains((String(app_sizeFormat) + String(k_bottomTitle.suffix(8)))), self.conversationType == .private {
            //: let totalIntimacy = msgInfo["totalIntimate"] as? Int ?? 0
            let totalIntimacy = msgInfo[(String(app_sizeFormat) + String(k_bottomTitle.suffix(8)))] as? Int ?? 0
            //: self.intimateTool(currenIntimate: totalIntimacy)
            self.sumimateCurrent(currenIntimate: totalIntimacy)
        }
    }

    /// 亲密度操作
    //: func intimateTool(currenIntimate: Int, time: Int = 0) {
    func sumimateCurrent(currenIntimate: Int, time: Int = 0) {
        //: guard currenIntimate > 0 else { return }
        guard currenIntimate > 0 else { return }
        //: let model = DBUserInfoManager.cache_getCachedUserInfo(targetId: self.targetId)
        let model = DirectionInfoManager.unblock(targetId: self.targetId)
        //: self.userWrap.intimate = currenIntimate
        self.userWrap.intimate = currenIntimate
        /// 满足最低条件，展示页面
        //: if currenIntimate >= IconContainerAppManager.share.appUserConfigMode.intimateLimit.chatTitleEffect {
        if currenIntimate >= IconContainerAppManager.share.appUserConfigMode.intimateLimit.chatTitleEffect {
            /// 亲密度增加
            //: if !self.isShowIntimateView {
            if !self.isShowIntimateView {
                //: self.isShowIntimateView = true
                self.isShowIntimateView = true
                //: navView.private_updateIntimateNavView(userModel: self.userWrap)
                navView.phoneMake(userModel: self.userWrap)
            }
            //: if currenIntimate - (model?.intimate ?? 0) > 0 {
            if currenIntimate - (model?.intimate ?? 0) > 0 {
                //: self.navView.playIntimateAnim(intimate: currenIntimate - (model?.intimate ?? 0))
                self.navView.spaceInGreet(intimate: currenIntimate - (model?.intimate ?? 0))
                /// 播放完毕
                //: self.navView.animEndBlock = { [weak self] in
                self.navView.animEndBlock = { [weak self] in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: self.navView.setAllData(intimate: currenIntimate)
                    self.navView.aLaCarteSumimate(intimate: currenIntimate)
                }
                //: } else {
            } else {
                //: self.navView.setAllData(intimate: currenIntimate)
                self.navView.aLaCarteSumimate(intimate: currenIntimate)
            }
        }

        //: let currTime = time > 0 ? time: Int(Date().timeIntervalSince1970)
        let currTime = time > 0 ? time : Int(Date().timeIntervalSince1970)
        //: DBUserInfoManager.cache_updateIntimate(with: targetId, intimate: currenIntimate, currTime: currTime)
        DirectionInfoManager.collideWith(with: targetId, intimate: currenIntimate, currTime: currTime)
        //: let userInfo = ["uid": self.targetId,
        let userInfo = ["uid": self.targetId,
                        //: "intimate": currenIntimate] as [String : Any]
                        String(bytes: kMainUrl.map{wearTheTrousers(data: $0)}, encoding: .utf8)!: currenIntimate] as [String: Any]
        //: NotificationCenter.default.post(name: MSGLIST_UPDATE_INTIMATE_NOTIFICATION,
        NotificationCenter.default.post(name: k_soundRecordFormat,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: userInfo)
                                        userInfo: userInfo)
    }
}

// MARK: - 输入框

//: extension TalkingPrivateChatController: ChatInputViewDelegate {
extension MakeReactiveCompatible: TipThen {
    /// 检测是否需要展示翻译入口
    //: func func_inputContentIntervalOneSecond() {
    func cardinal() {
        //: guard IconContainerAppManager.share.loginUserMode.sex == Gender.female.rawValue else { return }
        guard IconContainerAppManager.share.loginUserMode.sex == ThemeTitleScalarLiteral.female.rawValue else { return }
        //: let isEnglish = selectTranView.checkInputLanguage(self.msgInputView.inputTextView.text)
        let isEnglish = selectTranView.produceNativeLanguage(self.msgInputView.inputTextView.text)
        //: self.selectTranView.isHidden = isEnglish
        self.selectTranView.isHidden = isEnglish
    }

    //: func func__selectMentionUser(range: NSRange) {}
    func sharedEachRange(range _: NSRange) {}

    //: func func__selectMoreFunAction(actionType: MoreActionType) {
    func kind(actionType: VideoViewMultiplierTarget) {
        //: switch actionType {
        switch actionType {
        //: case .Photo:
        case .Photo:
            //: if self.conversationType == .private {
            if self.conversationType == .private {
                //: if self.userWrap.intimate >= IconContainerAppManager.share.appUserConfigMode.intimateLimit.sendImg {
                if self.userWrap.intimate >= IconContainerAppManager.share.appUserConfigMode.intimateLimit.sendImg {
                    //: func__choicePhotos()
                    starBottomPhotos()
                    //: } else {
                } else {
                    //: showIntimateErrorStr()
                    when()
                }
                //: } else {
            } else {
                //: func__choicePhotos()
                starBottomPhotos()
            }
        //: case .VideoCall:
        case .VideoCall: // 音/视频通话
            //: var videoStr: String?
            var videoStr: String?
            //: var voiceStr: String?
            var voiceStr: String?
            //: if IconContainerAppManager.share.loginUserMode.sex == Gender.female.rawValue {
            if IconContainerAppManager.share.loginUserMode.sex == ThemeTitleScalarLiteral.female.rawValue {
                //: videoStr = self.userWrap.videoBean
                videoStr = self.userWrap.videoBean
                //: voiceStr = self.userWrap.voiceBean
                voiceStr = self.userWrap.voiceBean
                //: } else {
            } else {
                //: if IconContainerAppManager.share.loginUserMode.loungePlus {
                if IconContainerAppManager.share.loginUserMode.loungePlus { // 男性会员
                    //: videoStr = self.userWrap.videoVIPPrice
                    videoStr = self.userWrap.videoVIPPrice
                    //: voiceStr = self.userWrap.voiceVIPPrice
                    voiceStr = self.userWrap.voiceVIPPrice
                    //: } else {
                } else {
                    //: videoStr = self.userWrap.videoPrice
                    videoStr = self.userWrap.videoPrice
                    //: voiceStr = self.userWrap.voicePrice
                    voiceStr = self.userWrap.voicePrice
                }
            }
            //: if self.userWrap.intimate >= IconContainerAppManager.share.appUserConfigMode.intimateLimit.requestCall {
            if self.userWrap.intimate >= IconContainerAppManager.share.appUserConfigMode.intimateLimit.requestCall {
                //: videoCallManager.showVideoCallMenu(videoPrice: videoStr, voicePrice: voiceStr, vipPrompt: self.userWrap.prompt)
                videoCallManager.bullSEyeAction(videoPrice: videoStr, voicePrice: voiceStr, vipPrompt: self.userWrap.prompt)
                //: } else {
            } else {
                //: showIntimateErrorStr()
                when()
            }

        //: case .ShortVideo:
        case .ShortVideo: // 短视频
            //: if self.conversationType == .private {
            if self.conversationType == .private {
                //: if self.userWrap.intimate >= IconContainerAppManager.share.appUserConfigMode.intimateLimit.sendVideo {
                if self.userWrap.intimate >= IconContainerAppManager.share.appUserConfigMode.intimateLimit.sendVideo {
                    //: func__sendShortVideo()
                    toConstraint()
                    //: } else {
                } else {
                    //: showIntimateErrorStr()
                    when()
                }
                //: } else {
            } else {
                //: func__sendShortVideo()
                toConstraint()
            }

        //: case .Gift: break
        case .Gift: break

        //: case .Game:
        case .Game: // 游戏
            //: gamesView.showView(from: .PrivateChat)
            gamesView.orientationFrom(from: .PrivateChat)
        }
    }

    //: private func showIntimateErrorStr() {
    private func when() {
        //: self.func__showStatusBarErrorMsg(showMsg: "Can't use the function due to your lack of intimacy level.".localized)
        self.explain(showMsg: String(bytes: const_makeRangeKey.map{$0^192}, encoding: .utf8)!.localized)
    }

    //: func func__sendChatRoomGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, model: TalkingChatRoomMemberModel) {}
    func sprechgesang(giftModel _: PointHandyJSON, giftNum _: String, model _: ChaseReactiveCompatible) {}

    // 聊天面板送礼
    /// - Parameters:
    ///   - giftModel: 礼物模型
    ///   - giftNum: 礼物数量
    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func possibility(giftModel: PointHandyJSON, giftNum: String) {
        //: func__sendGiftMsg(giftModel: giftModel, giftNum: giftNum, cellData: nil, fromStr: nil)
        metronymic(giftModel: giftModel, giftNum: giftNum, cellData: nil, fromStr: nil)
    }

    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, cellData:  MakeCellData?, fromStr: String?) {
    func metronymic(giftModel: PointHandyJSON, giftNum: String, cellData: MakeCellData?, fromStr: String?) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            explain(showMsg: dataStatusTitle)
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["toUid"] = self.targetId
        params[(String(kUpName))] = self.targetId
        //: params["giftId"] = giftModel.gid
        params[(String(constToTitle))] = giftModel.gid
        //: params["giftNum"] = giftNum
        params[(mainSharePartyMakeMsg.replacingOccurrences(of: "report", with: "i") + String(data_expectedUrl.prefix(5)))] = giftNum
        //: if giftModel.pkgItemsetId != nil {
        if giftModel.pkgItemsetId != nil {
            //: params["pkgItemsetId"] = giftModel.pkgItemsetId
            params[(String(main_attentionName.suffix(4)) + "temse" + String(constColorMsg.prefix(3)))] = giftModel.pkgItemsetId
        }
        //: if fromStr != nil {
        if fromStr != nil {
            //: params["from"] = fromStr
            params[(kBackgroundId.replacingOccurrences(of: "style", with: "r"))] = fromStr
        }
        // 送礼接口
        //: StreamRequestTool.req_SendGiftPrivete(param: params, completion: { succeed, result, errorModel in
        StreamRequestTool.crucify(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickSendingiftsButton)
                self.back(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: noti_beautyVideoStr)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if giftModel.pkgItemsetId != nil {
            if giftModel.pkgItemsetId != nil {
                //: self.msgInputView.giftView.bags_removeGiftPackage(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.msgInputView.giftView.rivetLineToPatronym(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: true)
            self.oeilDeBoeuf(extral: result as! [String: Any?], isFromInputView: true)
            //: self.updateSession()
            self.impanel()
            /// 亲密度
            //: let extralInfo = result as! Dictionary<String, Any?>
            let extralInfo = result as! [String: Any?]
            //: let msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
            let msgInfoDic: [String: Any] = extralInfo[(String(app_squareProMsg) + String(showErrorId.suffix(5)))] as! [String: Any]
            //: if msgInfoDic.keys.contains("totalIntimate") && self.conversationType == .private {
            if msgInfoDic.keys.contains((String(app_sizeFormat) + String(k_bottomTitle.suffix(8)))), self.conversationType == .private {
                //: let totalIntimacy = msgInfoDic["totalIntimate"] as? Int ?? 0
                let totalIntimacy = msgInfoDic[(String(app_sizeFormat) + String(k_bottomTitle.suffix(8)))] as? Int ?? 0
                //: self.intimateTool(currenIntimate: totalIntimacy)
                self.sumimateCurrent(currenIntimate: totalIntimacy)
            }
            /// 索要礼物送礼
            //: if cellData != nil {
            if cellData != nil {
                // 修改状态
                //: self.msgView.changeIntimateGiftMsg(cellData)
                self.msgView.data(cellData)
                //: uploadRecord.uploadRecordEvent(eventID: ClickmessageDetailaskMsgsendBtn)
                showVideoPath.referenceStr(eventID: const_recordValue)
            }
            //: })
        })
    }

    /// 索要礼物
    //: func func__askforGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func valueSize(giftModel: PointHandyJSON, giftNum _: String) {
        //: uploadRecord.uploadRecordEvent(eventID: ClickMessageDetailGiftPopAskBtnAllType)
        showVideoPath.referenceStr(eventID: appSenseUserMessage)
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            explain(showMsg: dataStatusTitle)
            //: return
            return
        }
        //: guard self.userWrap.intimate >= IconContainerAppManager.share.appUserConfigMode.intimateLimit.wantGift else {
        guard self.userWrap.intimate >= IconContainerAppManager.share.appUserConfigMode.intimateLimit.wantGift else {
            //: showIntimateErrorStr()
            when()
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["wantGiftId"] = giftModel.gid
        params[(notiConversationFormat.replacingOccurrences(of: "frame", with: "t") + "Gift" + String(constEndFormat.prefix(2)))] = giftModel.gid
        //: params["type"] = "WantGiftMsg"
        params[(constColorId.replacingOccurrences(of: "user", with: "e"))] = (String(data_countName) + String(data_cellName.prefix(5)))

        //: let msgInfo = ["msgType": "WantGift"]
        let msgInfo = [String(bytes: noti_videoTapMessage.reversed(), encoding: .utf8)!: (String(data_colorPath) + data_bringValueFramePath.replacingOccurrences(of: "up", with: "t"))]
        //: let dic = ["msgInfo": msgInfo, "params": params]
        let dic = [(String(app_squareProMsg) + String(showErrorId.suffix(5))): msgInfo, String(bytes: user_layerMessage.map{indexLevel(raw: $0)}, encoding: .utf8)!: params]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = DirectionReactiveCompatible.impingeOpen(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            total(msgData: cellData, reSend: false)
        }
    }

    //: func heightToBottomChanged(heightToBottom: CGFloat) {
    func toAdd(heightToBottom: CGFloat) {
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: contentView.snp.updateConstraints { make in
            contentView.snp.updateConstraints { make in
                //: make.top.equalTo(HalfViewTopMargin-heightToBottom)
                make.top.equalTo(data_halfTitle - heightToBottom)
            }
        }
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.bottom.equalTo(self.view).offset(-heightToBottom)
            make.bottom.equalTo(self.view).offset(-heightToBottom)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.actionBottom(true)
            self.msgView.actionBottom(true)
        }
    }

    //: func inputViewHeightChanged(height: CGFloat) {
    func black(height: CGFloat) {
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.actionBottom(true)
            self.msgView.actionBottom(true)
        }
    }

    //: @objc override func func__TapGestureRecognizer(_ tap: UITapGestureRecognizer) {
    @objc override func screenEqual(_ tap: UITapGestureRecognizer) {
        //: super.func__TapGestureRecognizer(tap)
        super.screenEqual(tap)
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: let location = tap.location(ofTouch: 0, in: self.contentView)
            let location = tap.location(ofTouch: 0, in: self.contentView)
            //: if !contentView.point(inside: location, with: nil) {
            if !contentView.point(inside: location, with: nil) {
                // 点击列表以外区域，移除视图（包括聊天列表）
                //: half_dismissViewAnimation(true)
                flick(true)
            }
        }
    }

    /// 开始录音操作
    //: func func__starRecordBtnDown() {
    func send() {
        //: if TalkingVoiceMsgPlayManager.shared.playStatus == .Playing {
        if EnterPlayManager.shared.playStatus == .Playing {
            //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
            EnterPlayManager.shared.distant()
        }
    }
}

// MARK: - 动效

//: extension TalkingPrivateChatController {
extension MakeReactiveCompatible {
    /// 插入礼物消息，播放动效
    /// - Parameters:
    ///   - extral: 送礼接口返回结果
    ///   - isFromInputView: 是否来自输入框送礼入口
    //: func func__insertGiftMessageWithExtral(extral: Dictionary<String, Any?>, isFromInputView: Bool) {
    func oeilDeBoeuf(extral: [String: Any?], isFromInputView: Bool) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
        var msgInfoDic: [String: Any] = extralInfo[(String(app_squareProMsg) + String(showErrorId.suffix(5)))] as! [String: Any]
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic[String(bytes: noti_videoTapMessage.reversed(), encoding: .utf8)!] = (const_valueData.replacingOccurrences(of: "normal", with: "f"))
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo[(String(app_squareProMsg) + String(showErrorId.suffix(5)))] = msgInfoDic

        //: let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        let cellData = DirectionReactiveCompatible.impingeOpen(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        //: self.msgView.sendCustomMessage(cellData)
        self.msgView.instance(cellData)
        //: var msgIncome: NSNumber = NSNumber.init(floatLiteral: 0)
        var msgIncome = NSNumber(floatLiteral: 0)
        //: if msgInfoDic.keys.contains("msgIncome") {
        if msgInfoDic.keys.contains((String(kTransformContent))) {
            //: msgIncome = msgInfoDic["msgIncome"] as! NSNumber
            msgIncome = msgInfoDic[(String(kTransformContent))] as! NSNumber
        }
        //: self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        self.msgView.auspicate(cellData, take: msgInfoDic[(String(main_willName))] as? String, cut: msgIncome, custom: .Msg_Status_Succ)
        
        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any])
        byInfo(giftDic: extral as [String: Any])
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((notiModelId.replacingOccurrences(of: "calendar", with: "o") + String(data_imageMsg.prefix(9)))) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(notiModelId.replacingOccurrences(of: "calendar", with: "o") + String(data_imageMsg.prefix(9)))] as! NSNumber
            //: IconContainerAppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            IconContainerAppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: if isFromInputView {
        if isFromInputView {
            //: msgInputView.giftView.updateGiftInfo(needReload: false, mf_coin: IconContainerAppManager.share.loginUserMode.mf_coin)
            msgInputView.giftView.age(needReload: false, mf_coin: IconContainerAppManager.share.loginUserMode.mf_coin)
        }
    }

    //: func initCachEffectData() {
    func labIn() {
        //: giftEffectView.initCachAnimatData(tagetID: targetId)
        giftEffectView.insertImageTo(tagetID: targetId)
    }

    //: func sendGiftByMeToEfffecct(giftDic: Dictionary<String, Any>) {
    func byInfo(giftDic: [String: Any]) {
        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.pastRow(dict: giftDic)
    }

    //: @objc func addGiftEffectModelArr(giftArr: Array<Any>) {
    @objc func randomGiftArr(giftArr: [Any]) {
        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.bigness(modelArr: giftArr)
    }

    //: @objc func getChatVCTargetId() ->String {
    @objc func communication() -> String {
        //: return targetId
        return targetId
    }
}

// MARK: - 音视频通话Event

//: extension TalkingPrivateChatController {
extension MakeReactiveCompatible {
    /// 检查权限后发起音频通话
    //: func checkAndCallVoice() {
    func baptise() {
        //: videoCallManager.checkAndCallVoice {[weak self] succeed in
        videoCallManager.queryStart { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            //: self.updateSession()
            self.impanel()
        }
    }

    /// 检测权限后发起视频通话
    //: func checkAndCallVideo() {
    func sussOut() {
        //: videoCallManager.checkAndCallVideo {[weak self] succeed in
        videoCallManager.infoRemove { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            //: self.updateSession()
            self.impanel()
        }
    }
}

// MARK: - Event

//: extension TalkingPrivateChatController {
extension MakeReactiveCompatible {
    //: @objc private func feedbackBtnClick() {
    @objc private func buildForOn() {
        //: let vc = TalkingFeedbackVC.init()
        let vc = MasterKeyFeedbackVc()
        //: getNavigationController()?.pushViewController(vc, animated: true)
        characteristicRootOfASquareMatrix()?.pushViewController(vc, animated: true)
    }

    /// 更多按钮事件
    //: private func moreBtnClick() {
    private func utter() {
        //: let vc = TalkingMunuPopView.init(frame: self.view.frame)
        let vc = VoicePopView(frame: self.view.frame)
        //: vc.initwithList(cellTitleList: ["View info".localized, "Notes".localized, "Report".localized])
        vc.managerList(cellTitleList: [(String(mainManagerMessage.suffix(6)) + showTableData.replacingOccurrences(of: "character", with: "f")).localized, (String(notiTextCircleKey.suffix(5))).localized, (String(dataActionMsg.prefix(6))).localized])
        //: vc.cancelColor = .appTitleColor()
        vc.cancelColor = .sendTitle()
        //: vc.munuBlock = { [weak self] index, str in
        vc.munuBlock = { [weak self] index, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if index == 0 {
            if index == 0 {
                //: CombineAccountEnclaveDecisionMakerThen.share.func__pushToUserDetailVC(uid: self.targetId)
                CombineAccountEnclaveDecisionMakerThen.share.windowBy(uid: self.targetId)

                //: } else if index == 1 {
            } else if index == 1 { // 备注
                //: jumpToNotesView()
                changeShared()

                //: } else if index == 2 {
            } else if index == 2 { // 举报
                //: self.msgInputView.showOtherInputView = true
                self.msgInputView.showOtherInputView = true
                //: let reportView = TalkingReportAlertView.init(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.targetId)
                let reportView = SuccessDataSource(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.targetId)
                //: reportView.showReportViewIn(view: nil)
                reportView.beyondShow(view: nil)
                //: reportView.reportHideBlock = { _ in
                reportView.reportHideBlock = { _ in
                    //: self.msgInputView.showOtherInputView = false
                    self.msgInputView.showOtherInputView = false
                }
            }
        }
    }

    /// 跳转到备注页面
    //: private func jumpToNotesView() {
    private func changeShared() {
        //: self.msgInputView.showOtherInputView = true
        self.msgInputView.showOtherInputView = true
        //: let view = PrivateChatNotesView()
        let view = AppView()
        //: view.show(self.userWrap.content, isTop: self.userWrap.top, toUid: self.targetId)
        view.demonstrateUid(self.userWrap.content, isTop: self.userWrap.top, toUid: self.targetId)
        //: view.dismissBlock = { content, isTop in
        view.dismissBlock = { content, isTop in
            //: self.msgInputView.showOtherInputView = false
            self.msgInputView.showOtherInputView = false
            //: self.userWrap.content = content
            self.userWrap.content = content
            //: self.userWrap.top = isTop
            self.userWrap.top = isTop
            //: self.notesTopView.refresh(self.userWrap)
            self.notesTopView.textChange(self.userWrap)
        }
    }

    /// 返回按钮点击事件
    //: private func backButtonClick() {
    private func showIn() {
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: half_dismissViewAnimation(false)
            flick(false)
            //: } else {
        } else {
            //: getNavigationController()?.popViewController(animated: true)
            characteristicRootOfASquareMatrix()?.popViewController(animated: true)
        }
    }

    /// 展示飘屏内容
    //: @objc private func showFloatScreen(notification: NSNotification) {
    @objc private func viewPosition(notification: NSNotification) {
        //: if conversationType == .private {
        if conversationType == .private {
            //: let dataInfo = notification.userInfo as! [String: AnyObject]
            let dataInfo = notification.userInfo as! [String: AnyObject]
            //: if let model = TalkingGameFloatingScreenModel.deserialize(from: dataInfo as? Dictionary) {
            if let model = TargetMeasurable.deserialize(from: dataInfo as? Dictionary) {
                //: self.floatScreenView.isUserInteractionEnabled = true
                self.floatScreenView.isUserInteractionEnabled = true
                //: self.floatScreenView.didReceive(model)
                self.floatScreenView.addOn(model)
            }
        }
    }
}

// MARK: - VIP消息已读回执

//: extension TalkingPrivateChatController {
extension MakeReactiveCompatible {
    /// 添加通知
    //: private func addReadPeceiptNotification() {
    private func nowTag() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(func__msgReadReceipt(notification:)),
                                               selector: #selector(toImage(notification:)),
                                               //: name: CHAT_MSG_READ_RECEIPT_NOTIFICATION,
                                               name: appReasonKeyValue,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(func__refreshMsgList),
                                               selector: #selector(equivalent),
                                               //: name: USER_UPDATE_LOUNGEPLUSINFO_NOTIFICATION,
                                               name: userNetPath,
                                               //: object: nil)
                                               object: nil)
    }

    /// 收到对端已读消息回执通知 -> 刷新列表
    //: @objc func func__msgReadReceipt(notification: NSNotification) {
    @objc func toImage(notification: NSNotification) {
        //: let obj = notification.object as! V2TIMMessageReceipt
        let obj = notification.object as! V2TIMMessageReceipt
        //: guard obj.userID == targetId else { return }
        guard obj.userID == targetId else { return }
        //: guard IconContainerAppManager.share.loginUserMode.loungePlus == true else { return }
        guard IconContainerAppManager.share.loginUserMode.loungePlus == true else { return }
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.msgView.tableView.reloadData()
            self.msgView.tableView.reloadData()
        }
    }

    /// 订阅成功 -> 更改订阅状态 -> 刷新列表
    //: @objc private func func__refreshMsgList() {
    @objc private func equivalent() {
        //: IconContainerAppManager.share.loginUserMode.loungePlus = true
        IconContainerAppManager.share.loginUserMode.loungePlus = true
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.msgView.tableView.reloadData()
            self.msgView.tableView.reloadData()
        }
    }

    /// 判断是否插入已读回执引导充值VIP提示消息
    //: func func__privateVIPReadReceipt_insertTipsMsg() {
    func celluloid() {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.4) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.4) {
            //: guard let lastCellData = self.msgView.uiMsgs.lastObject as? AddCellData else { return }
            guard let lastCellData = self.msgView.uiMsgs.lastObject as? AddCellData else { return }
            //: if TalkingPrivateChatManager.needInsertVIPReadReceiptMsg(cellData: lastCellData) {
            if FrameLevelThen.cellPermission(cellData: lastCellData) {
                // 只做内存插入展示
                //: self.insertTipMsg_Nobalance(tipStr: "View read receipts?".localized, needInsertLocal: false)
                self.implantPlayer(tipStr: (String(appCellName.prefix(6)) + "ead r" + String(kResumeName.prefix(8))).localized, needInsertLocal: false)
            }
        }
    }
}

// MARK: - 选择图片

//: extension TalkingPrivateChatController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
extension MakeReactiveCompatible: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
    //: private func func__choicePhotos() {
    private func starBottomPhotos() {
        //: TalkingPermissionTool.func__openAlbumServiceWithBlock(true) { (isOpen: Bool) in
        CellPermissionTool.targetEvent(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: true, allowVideo: false)
                let vc = StateThen.emptyDown(maxCount: 1, allowPhoto: true, allowVideo: false)
                //: vc.modalPresentationStyle = .fullScreen
                vc.modalPresentationStyle = .fullScreen
                //: self.present(vc, animated: true)
                self.present(vc, animated: true)
                //: vc.didFinishPickingPhotosHandle = { [weak self] ( photos: Array<UIImage>?,
                vc.didFinishPickingPhotosHandle = { [weak self] (photos: [UIImage]?,
                                                                 //: assets: Array<Any>?,
                                                                 _: [Any]?,
                                                                 //: isSelectOriginalPhoto: Bool?)  in
                                                                 _: Bool?) in
                        //: guard let self = self else {return}
                        guard let self = self else { return }
                        //: if photos!.count > 0 {
                        if photos!.count > 0 {
                            //: self.showUsePhoto(img: photos![0])
                            self.titleDemonstrate(img: photos![0])
                        }
                }
            }
        }
    }

    //: private func func__camaraPhotos() {
    private func sizeSelect() {
        //: TalkingPermissionTool.func__openCaptureDeviceServiceWithBlock(true) { (isOpen: Bool) in
        CellPermissionTool.viewBlock(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let imagePicker = UIImagePickerController()
                let imagePicker = UIImagePickerController()
                //: imagePicker.sourceType = .camera
                imagePicker.sourceType = .camera
                //: imagePicker.delegate = self
                imagePicker.delegate = self
                //: self.present(imagePicker, animated: true, completion: {})
                self.present(imagePicker, animated: true, completion: {})
            }
        }
    }

    //: func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
    func imagePickerController(_: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
        //: self.dismiss(animated: true) {
        self.dismiss(animated: true) {
            //: if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
            if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
                //: self.showUsePhoto(img: photo)
                self.titleDemonstrate(img: photo)
            }
        }
    }

    //: func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
    func imagePickerControllerDidCancel(_: UIImagePickerController) {
        //: self.dismiss(animated: true, completion: nil)
        self.dismiss(animated: true, completion: nil)
    }

    /// 展示阅后即焚+私密照片选项
    /// - Parameter img: 图片资源
    //: private func showUsePhoto(img: UIImage) {
    private func titleDemonstrate(img: UIImage) {
        //: guard conversationType == .private &&
        guard conversationType == .private &&
            //: IconContainerAppManager.share.appStatus == AppSkinStatus.normal.rawValue else {
            IconContainerAppManager.share.appStatus == MinExpressionConvertible.normal.rawValue
        else { // 非私聊 && 非审核模式
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = PlayerChatModel()
            //: sendModel.isBurn = "0"
            sendModel.isBurn = "0"
            //: sendModel.giftId = "0"
            sendModel.giftId = "0"
            //: self.func__sendPictureMsg(img: img, sendModel: sendModel)
            self.behindSendFrom(img: img, sendModel: sendModel)
            //: return
            return
        }

        //: let vc = TalkingMomentPhotosVC(images: [img], index: 0, type: .sendIntimatePhoto)
        let vc = VideoImageReactiveCompatible(images: [img], index: 0, type: .sendIntimatePhoto)
        //: vc.sendIntimatePhotoBlock = {[weak self] isBurn, giftId in
        vc.sendIntimatePhotoBlock = { [weak self] isBurn, giftId in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = PlayerChatModel()
            //: sendModel.isBurn = isBurn
            sendModel.isBurn = isBurn
            //: sendModel.giftId = giftId
            sendModel.giftId = giftId
            //: self.func__sendPictureMsg(img: img, sendModel: sendModel)
            self.behindSendFrom(img: img, sendModel: sendModel)
        }
        //: getNavigationController()?.pushViewController(vc, animated: true)
        characteristicRootOfASquareMatrix()?.pushViewController(vc, animated: true)
    }
}

// MARK: - 选择视频

//: extension TalkingPrivateChatController {
extension MakeReactiveCompatible {
    /// 发送短视频
    //: private func func__sendShortVideo() {
    private func toConstraint() {
        // 检测用户短视频版本是否可用
        //: guard TalkingPrivateChatManager.checkShortVideo(userVersion: self.userWrap.version) else {
        guard FrameLevelThen.enableTo(userVersion: self.userWrap.version) else {
            //: let gender = self.userWrap.sex == Gender.male.rawValue ? "He".localized:"She".localized
            let gender = self.userWrap.sex == ThemeTitleScalarLiteral.male.rawValue ? "He".localized : "She".localized
            //: self.func__showStatusBarErrorMsg(showMsg: "%@ is using too low a version".localizedArguments(gender))
            self.explain(showMsg: (String(appAppearName.suffix(4)) + "s using" + String(userGlobalMessage.suffix(4)) + " low" + String(noti_lengthTitle.prefix(4)) + String(kAttributeUrl)).cellArguments(gender))
            //: return
            return
        }

        //: self.openVideoPicker()
        self.keyConversation()
    }

    /// 打开视频相册
    //: private func openVideoPicker() {
    private func keyConversation() {
        //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: false, allowVideo: true)
        let vc = StateThen.emptyDown(maxCount: 1, allowPhoto: false, allowVideo: true)
        //: vc.modalPresentationStyle = .fullScreen
        vc.modalPresentationStyle = .fullScreen
        //: self.present(vc, animated: true)
        self.present(vc, animated: true)
        //: vc.didFinishPickingVideoHandle = { [weak self] ( coverImage: UIImage?, assets: PHAsset?)  in
        vc.didFinishPickingVideoHandle = { [weak self] (_: UIImage?, assets: PHAsset?) in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: TalkingImagePickTool.getVideoPath(asset: assets) { filePath in
            StateThen.activityBy(asset: assets) { filePath in
                //: guard filePath != nil else { return }
                guard filePath != nil else { return }
                //: let asset = AVURLAsset.init(url: filePath!)
                let asset = AVURLAsset(url: filePath!)
                //: let time = asset.duration
                let time = asset.duration
                //: let seconds = Int(roundf(Float(CMTimeGetSeconds(time))))
                let seconds = Int(roundf(Float(CMTimeGetSeconds(time))))
                //: if seconds >= 5 {
                if seconds >= 5 { // 压缩视频
                    //: self.CompressedVideo(url: filePath!)
                    self.listEnter(url: filePath!)
                    //: } else {
                } else {
                    //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                    DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) { // 视频压缩取消
                        //: self.func__showStatusBarErrorMsg(showMsg: "Shoot video at least 5 seconds".localized)
                        self.explain(showMsg: (String(noti_partyValue.prefix(6)) + "vide" + String(dataManagerPath.prefix(4)) + " least " + String(main_progressUrl.suffix(9))).localized)
                    }
                }
            }
        }
    }

    /// 视频压缩
    /// - Parameter url: 本地路径
    //: private func CompressedVideo(url: URL) {
    private func listEnter(url: URL) {
        //: let style: TalkingVideoEditorStyle = (conversationType == .private) ? .intimateVideo : .normal
        let style: ScreenEditorStyle = (conversationType == .private) ? .intimateVideo : .normal
        //: let videoVC = TalkingVideoEditorController(style: style, maxDuration: 60.0, videoURL: url) { videoInfo in
        let videoVC = ResponseViewController(style: style, maxDuration: 60.0, videoURL: url) { videoInfo in
            //: guard let videoInfo = videoInfo else { return }
            guard let videoInfo = videoInfo else { return }
            // 压缩成功，实例化模型
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = PlayerChatModel()
            //: sendModel.giftId = videoInfo.giftId
            sendModel.giftId = videoInfo.giftId
            //: sendModel.isBurn = videoInfo.isBurn
            sendModel.isBurn = videoInfo.isBurn
            //: sendModel.localCoverImg = videoInfo.coverPath
            sendModel.localCoverImg = videoInfo.coverPath
            //: sendModel.localVideoUrl = videoInfo.videoPath
            sendModel.localVideoUrl = videoInfo.videoPath
            //: sendModel.duration = Int(roundf(Float(videoInfo.duration)))
            sendModel.duration = Int(roundf(Float(videoInfo.duration)))
            // 发送视频消息
            //: self.func__sendVideoMsg(msgData: nil, sendModel: sendModel)
            self.makeModel(msgData: nil, sendModel: sendModel)
        }

        //: ProgressHUD.dismiss()
        MakeView.presentAddLab()
        //: getNavigationController()?.pushViewController(videoVC, animated: true)
        characteristicRootOfASquareMatrix()?.pushViewController(videoVC, animated: true)
    }

    /// 上传视频
    /// - Parameters:
    ///   - msgData: cellData，此处只数据传递
    ///   - sendModel: 消息自定义本地模型
    //: private func cos_UploadVideo(msgData: AddCellData, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func upload(msgData: AddCellData, sendModel: PlayerChatModel) {
        //: TalkingMomentVideoManager.shared.cos_uploadVideo(type: 3, coverPath: sendModel.localCoverImg, videoPath: sendModel.localVideoUrl) { coverUrl, videoUrl, succeed in
        MaleReactiveCompatible.shared.startSelected(type: 3, coverPath: sendModel.localCoverImg, videoPath: sendModel.localVideoUrl) { coverUrl, videoUrl, succeed in
            //: guard succeed else {
            guard succeed else {
                //: self.func__showStatusBarErrorMsg(showMsg: "Upload failed".localized)
                self.explain(showMsg: (String(userNameData.prefix(2)) + const_replyKey.replacingOccurrences(of: "share", with: "o") + " failed").localized)
                // 更改消息状态为失败
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.auspicate(msgData, take: nil, cut: NSNumber(value: 0), custom: .Msg_Status_Fail)

                //: return
                return
            }

            /// 上传成功，刷新本地视频消息，调用发消息接口
            //: sendModel.coverImg = coverUrl
            sendModel.coverImg = coverUrl
            //: sendModel.videoUrl = videoUrl
            sendModel.videoUrl = videoUrl
            //: self.func__sendVideoMsg(msgData: msgData, sendModel: sendModel)
            self.makeModel(msgData: msgData, sendModel: sendModel)
        }
    }
}

// MARK: - 语音消息

//: extension TalkingPrivateChatController {
extension MakeReactiveCompatible {
    //: func func__addVoiceNotification() {
    func store() {
        //: NotificationCenter.default.addObserver(self, selector: #selector(appDidBecomeActive),
        NotificationCenter.default.addObserver(self, selector: #selector(soberUp),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(appDidEnterBack),
        NotificationCenter.default.addObserver(self, selector: #selector(offStop),
                                               //: name: UIApplication.didEnterBackgroundNotification,
                                               name: UIApplication.didEnterBackgroundNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(audioSessionInterrupted(notification:)),
                                               selector: #selector(joinFrom(notification:)),
                                               //: name: AVAudioSession.interruptionNotification,
                                               name: AVAudioSession.interruptionNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(insertGiftMsgNotif(notification:)),
        NotificationCenter.default.addObserver(self, selector: #selector(nowadays(notification:)),
                                               //: name: CHAT_ADD_CALL_RECORD_MSG_NOTIFICATION,
                                               name: notiBarReadTitle,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(newMsgIntimate(notification:)),
                                               selector: #selector(beyondBrandNew(notification:)),
                                               //: name: CHAT_NEW_MSG_NOTIFICATION,
                                               name: k_tableValue,
                                               //: object: nil)
                                               object: nil)
    }

    //: func func__addOrRrmoveObserverForMsgPlayer(isAdd: Bool) {
    func downAdd(isAdd: Bool) {
        //: if isAdd {
        if isAdd {
            //: TalkingVoiceMsgPlayManager.shared.addDaskManagerDelegate()
            EnterPlayManager.shared.mark()
            //: } else {
        } else {
            //: TalkingVoiceMsgPlayManager.shared.removeDaskManagerDelegate()
            EnterPlayManager.shared.subject()
        }
    }

    //: @objc func appDidBecomeActive() {
    @objc func soberUp() {
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
        downAdd(isAdd: true)
    }

    //: @objc func appDidEnterBack() {
    @objc func offStop() {
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
        downAdd(isAdd: false)
    }

    //: @objc func audioSessionInterrupted(notification: NSNotification) {
    @objc func joinFrom(notification: NSNotification) {
        //: guard let userInfo = notification.userInfo, let reasonValue = userInfo[AVAudioSessionInterruptionTypeKey] as? UInt else { return }
        guard let userInfo = notification.userInfo, let reasonValue = userInfo[AVAudioSessionInterruptionTypeKey] as? UInt else { return }
        //: switch reasonValue {
        switch reasonValue {
        //: case AVAudioSession.InterruptionType.began.rawValue:
        case AVAudioSession.InterruptionType.began.rawValue: // Began
            //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
            downAdd(isAdd: false)
        //: break
        //: case AVAudioSession.InterruptionType.ended.rawValue:
        case AVAudioSession.InterruptionType.ended.rawValue: // End
            //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
            downAdd(isAdd: true)
        //: break
        //: default:
        default:
            //: break
            break
        }
    }

    //: func func__DidTouchVoiceCellNeedPlayWithCurrentModel(cellData: ValueCustomCellData, cellView: TalkingChatAudioMsgCell) {
    func stateView(cellData: ValueCustomCellData, cellView: EventMsgCell) {
        //: guard TalkingSocketManager.shared.isName == false else {
        guard GiftCommentSocketDelegate.shared.isName == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.explain(showMsg: user_topPath)
            //: return
            return
        }

        //: let message = cellData.innerMessage
        let message = cellData.innerMessage

        //: for indexModel in self.msgView.uiMsgs {
        for indexModel in self.msgView.uiMsgs {
            //: if (indexModel as? AddCellData)!.isKind(of: ValueCustomCellData.self) == false {
            if (indexModel as? AddCellData)!.isKind(of: ValueCustomCellData.self) == false {
                //: continue
                continue
            }
            //: let audioModel = indexModel as! ValueCustomCellData
            let audioModel = indexModel as! ValueCustomCellData
            //: audioModel.voiceModel.isPlayingStatus = 2
            audioModel.voiceModel.isPlayingStatus = 2
            //: audioModel.voiceModel.activityShowStatus = 2
            audioModel.voiceModel.activityShowStatus = 2
            // 红点提示隐藏
            //: if (audioModel.isEqual(cellData)) {
            if audioModel.isEqual(cellData) {
                //: cellView.voiceTrackPoint.isHidden = true
                cellView.voiceTrackPoint.isHidden = true
            }
        }
        //: if message != nil {
        if message != nil {
            //: cellData.voiceModel.db_voiceCacheWrap = DBUserInfoManager.cache_getAudioDB(message: message!)
            cellData.voiceModel.db_voiceCacheWrap = DirectionInfoManager.inputObject(message: message!)
        }
        //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
        EnterPlayManager.shared.distant()

        //: if TalkingVoiceMsgPlayManager.shared.clickCellId == cellData.voiceModel.db_voiceCacheWrap.msgId {
        if EnterPlayManager.shared.clickCellId == cellData.voiceModel.db_voiceCacheWrap.msgId {
            //: printLog(message: "播放状态:\(TalkingVoiceMsgPlayManager.shared.playStatus)")
            printLog(message: (showMakeKey.capitalized) + "\(EnterPlayManager.shared.playStatus)")
            //: if TalkingVoiceMsgPlayManager.shared.playStatus != .Unknown {
            if EnterPlayManager.shared.playStatus != .Unknown {
                //: TalkingVoiceMsgPlayManager.shared.playStatus = .Unknown
                EnterPlayManager.shared.playStatus = .Unknown
                //: return
                return
            }
        }
        // 指定播放的cell
        //: TalkingVoiceMsgPlayManager.shared.clickCellId = cellData.voiceModel.db_voiceCacheWrap.msgId
        EnterPlayManager.shared.clickCellId = cellData.voiceModel.db_voiceCacheWrap.msgId
        //: TalkingVoiceMsgPlayManager.shared.playStatus = .Waiting
        EnterPlayManager.shared.playStatus = .Waiting
        //: var waitArray = [cellData.voiceModel]
        var waitArray = [cellData.voiceModel]
        //: if cellData.voiceModel.db_voiceCacheWrap.db_isRead == "0" {
        if cellData.voiceModel.db_voiceCacheWrap.db_isRead == "0" {
            //: let currentIndex = self.msgView.uiMsgs.index(of: cellData)
            let currentIndex = self.msgView.uiMsgs.index(of: cellData)
            //: for index in currentIndex+1 ..< self.msgView.uiMsgs.count {
            for index in currentIndex + 1 ..< self.msgView.uiMsgs.count {
                //: let subModel: AddCellData = self.msgView.uiMsgs.object(at: index) as! AddCellData
                let subModel: AddCellData = self.msgView.uiMsgs.object(at: index) as! AddCellData
                //: let subMessage = subModel.innerMessage
                let subMessage = subModel.innerMessage
                //: if subModel.isKind(of: ValueCustomCellData.self) {
                if subModel.isKind(of: ValueCustomCellData.self) {
                    //: let audioModel = subModel as! ValueCustomCellData
                    let audioModel = subModel as! ValueCustomCellData
                    //: let voiceModel = audioModel.voiceModel
                    let voiceModel = audioModel.voiceModel
                    //: if subMessage != nil {
                    if subMessage != nil {
                        //: voiceModel.db_voiceCacheWrap = DBUserInfoManager.cache_getAudioDB(message: subMessage!)
                        voiceModel.db_voiceCacheWrap = DirectionInfoManager.inputObject(message: subMessage!)
                    }
                    //: if (voiceModel.db_voiceCacheWrap.db_isRead == "0") {
                    if voiceModel.db_voiceCacheWrap.db_isRead == "0" {
                        //: waitArray.append(voiceModel)
                        waitArray.append(voiceModel)
                    }
                    //: cellData.voiceModel.db_voiceCacheWrap.db_isRead = "1"
                    cellData.voiceModel.db_voiceCacheWrap.db_isRead = "1"
                    //: cellView.voiceTrackPoint.isHidden = true
                    cellView.voiceTrackPoint.isHidden = true
                    //: WCDBVoiceMsgTable.db_updateVoiceMsg(cellData.voiceModel.db_voiceCacheWrap)
                    ContainerMsgTable.modifyItem(cellData.voiceModel.db_voiceCacheWrap)
                }
            }
        }
        //: TalkingVoiceMsgPlayManager.shared.starPlayMsg(msgArr: waitArray)
        EnterPlayManager.shared.galaxyPlay(msgArr: waitArray)
    }
}

// MARK: - 用户相关

//: extension TalkingPrivateChatController {
extension MakeReactiveCompatible {
    /// 请求聊天用户信息 V1.8.0 提前到进入页面之前，先判断是否封号
    //: func func__requestChatInfo() {
    func receive() {
        //: guard conversationType == .private || conversationType == .service else { return }
        guard conversationType == .private || conversationType == .service else { return }

        //: self.func__updateUIWithUserWrap()
        self.infoWrap()
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        // 判断是否需要插入一键打招呼消息
        //: if (self.quickGreetingTextDict?.count ?? 0 > 0) || (self.quickGreetingAudioDict?.count ?? 0 > 0) {
        if (self.quickGreetingTextDict?.count ?? 0 > 0) || (self.quickGreetingAudioDict?.count ?? 0 > 0) {
            //: self.func_sendQuickGreetingMsg(textDict: self.quickGreetingTextDict, audioDict: self.quickGreetingAudioDict)
            self.tingManager(textDict: self.quickGreetingTextDict, audioDict: self.quickGreetingAudioDict)
        }
        //: if self.msgView.uiMsgs.count > 0 && self.userWrap.existSess == false {
        if self.msgView.uiMsgs.count > 0, self.userWrap.existSess == false {
            //: for model in self.msgView.uiMsgs {
            for model in self.msgView.uiMsgs {
                //: let indexModel = model as? AddCellData
                let indexModel = model as? AddCellData
                //: if indexModel?.isKind(of: ValueCustomCellData.self) == true || indexModel?.isKind(of: FountainheadCellData.self) == true || indexModel?.isKind(of: MaxCellData.self) == true || indexModel?.isKind(of: EigenvalueOfAMatrixReactiveCompatible.self) == true {
                if indexModel?.isKind(of: ValueCustomCellData.self) == true || indexModel?.isKind(of: FountainheadCellData.self) == true || indexModel?.isKind(of: MaxCellData.self) == true || indexModel?.isKind(of: EigenvalueOfAMatrixReactiveCompatible.self) == true {
                    //: self.userWrap.existSess = true
                    self.userWrap.existSess = true
                    //: break
                    break
                }
            }
        }
        // 判断是否需要插入音视频提示消息
        //: self.func__privateChatVideo_insertTipMsg()
        self.areaWith()
        // 判断是否需要插入官方客服提示消息
        //: self.func__customerServiceCenter_insertTipMsg()
        self.midfieldCamera()
        /// 判断是否插入已读回执引导充值VIP提示消息
        //: self.func__privateVIPReadReceipt_insertTipsMsg()
        self.celluloid()
    }

    /// 刷新UI
    //: func func__updateUIWithUserWrap() {
    func infoWrap() {
        //: guard userWrap.uid.count > 0 else { return }
        guard userWrap.uid.count > 0 else { return }
        //: bgImgeView.setUrlImage(urlStr: userWrap.headPic)
        bgImgeView.pagePush(urlStr: userWrap.headPic)
        //: navView.private_updateNavView(userModel: userWrap, isFrom: self.isFrom)
        navView.contentPicture(userModel: userWrap, isFrom: self.isFrom)
        /// 亲密度头像
        //: self.showFirstIntimateView()
        self.sharedView()

        //: if sendGreetBtn.isHidden == false {
        if sendGreetBtn.isHidden == false {
            //: sendGreetBtn.isHidden = userWrap.sex == IconContainerAppManager.share.loginUserMode.sex
            sendGreetBtn.isHidden = userWrap.sex == IconContainerAppManager.share.loginUserMode.sex
        }
        //: if beautyPhotosBtn.isHidden == false {
        if beautyPhotosBtn.isHidden == false {
            //: beautyPhotosBtn.isHidden = userWrap.sex == IconContainerAppManager.share.loginUserMode.sex
            beautyPhotosBtn.isHidden = userWrap.sex == IconContainerAppManager.share.loginUserMode.sex
        }
        // 备注置顶
        //: notesTopView.refresh(userWrap)
        notesTopView.textChange(userWrap)
    }

    //: func func__getFillDraftText() {
    func electronicText() {
        //: let userID = "c2c_"+targetId
        let userID = "c2c_" + targetId
        //: V2TIMManager.sharedInstance().getConversation(userID) { [weak self] conv in
        V2TIMManager.sharedInstance().getConversation(userID) { [weak self] conv in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                //: let draftText = conv?.draftText ?? ""
                let draftText = conv?.draftText ?? ""
                //: if !draftText.isEmptyString {
                if !draftText.isEmptyString {
                    //: self.msgInputView.inputTextView.text = conv?.draftText
                    self.msgInputView.inputTextView.text = conv?.draftText
                    //: self.msgInputView.inputTextView.becomeFirstResponder()
                    self.msgInputView.inputTextView.becomeFirstResponder()
                    //: self.func_inputContentIntervalOneSecond()
                    self.cardinal()
                }
            }
            //: } fail: { code, desc in
        } fail: { _, _ in
        }
    }

    //: func func__saveFillDraftText() {
    func textCancel() {
        //: let userID = "c2c_"+targetId
        let userID = "c2c_" + targetId
        //: V2TIMManager.sharedInstance().setConversationDraft(userID, draftText: msgInputView.inputTextView.text!) {
        V2TIMManager.sharedInstance().setConversationDraft(userID, draftText: msgInputView.inputTextView.text!) {
            //: } fail: { code, desc in
        } fail: { _, _ in
        }
    }
}

// MARK: - 提示消息

//: extension TalkingPrivateChatController {
extension MakeReactiveCompatible {
    //: func func__TipsNotification() {
    func behindMode() {
        //: NotificationCenter.default.addObserver(self, selector: #selector(insertTipsNotif(notification:)),
        NotificationCenter.default.addObserver(self, selector: #selector(firstYearMore(notification:)),
                                               //: name: CHAT_TIPS_TEXT_NOTIFICATION,
                                               name: kBottomStr,
                                               //: object: nil)
                                               object: nil)
    }

    //: @objc func insertTipsNotif(notification: NSNotification) -> Void {
    @objc func firstYearMore(notification: NSNotification) {
        //: let userinfo = notification.userInfo as! [String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let celldata: TalkingChatTipsMsgCellData  = userinfo["msg"] as! TalkingChatTipsMsgCellData
        let celldata: SuccessAppThen = userinfo["msg"] as! SuccessAppThen

        //: self.msgView.uiMsgs.add(celldata)
        self.msgView.uiMsgs.add(celldata)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.actionBottom(true)
        self.msgView.actionBottom(true)
    }

    /// 插入提示消息
    /// - Parameters:
    ///   - tipStr: 提示消息文本
    ///   - needInsertLocal: 是否需要插入本地数据库（默认：是）
    //: func insertTipMsg_Nobalance(tipStr: String, needInsertLocal: Bool = true) {
    func implantPlayer(tipStr: String, needInsertLocal: Bool = true) {
        //: let msgInfo: [String: Any] = ["content": tipStr,
        let msgInfo: [String: Any] = [String(bytes: dataProfileCameraModePath.map{$0^84}, encoding: .utf8)!: tipStr,
                                      //: "msgType": "tips"]
                                      String(bytes: noti_videoTapMessage.reversed(), encoding: .utf8)!: (String(main_titleMessage))]
        //: var message: AddCellData?
        var message: AddCellData?
        //: if needInsertLocal == true {
        if needInsertLocal == true { // 需要插入本地
            //: message = TUISocialChatManager.insertTXMessage(withExtral: msgInfo, toUid: targetId, isSender: true, isTip: true)
            message = DirectionReactiveCompatible.rangePacketModel(withExtral: msgInfo, toUid: targetId, isSender: true, isTip: true)
            //: } else {
        } else {
            //: message = TUISocialChatManager.handleTXMessageSend(withExtral: msgInfo, isSender: true, isTip: true)
            message = DirectionReactiveCompatible.impingeOpen(withExtral: msgInfo, isSender: true, isTip: true)
        }

        //: guard let message = message else { return }
        guard let message = message else { return }
        //: self.msgView.uiMsgs.add(message)
        self.msgView.uiMsgs.add(message)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.actionBottom(true)
        self.msgView.actionBottom(true)
    }

    /// 官方客服插入系统提示消息
    //: func func__customerServiceCenter_insertTipMsg() {
    func midfieldCamera() {
        //: guard conversationType == .service else { return }
        guard conversationType == .service else { return }
        //: TalkingPrivateChatManager.chat_customerServiceCenter_insertTipMsg { tipsStr in
        FrameLevelThen.sharedStart { tipsStr in
            //: guard tipsStr != nil else { return }
            guard tipsStr != nil else { return }
            // 客服提示消息，只做内存插入展示
            //: self.insertTipMsg_Nobalance(tipStr: tipsStr!, needInsertLocal: false)
            self.implantPlayer(tipStr: tipsStr!, needInsertLocal: false)
        }
    }

    /// 判断是否需要插入免费提示消息
    //: func func__privateAttachSys_insertTipMsg(str: String) {
    func logKind(str: String) {
        //: guard conversationType == .private else { return }
        guard conversationType == .private else { return }
        //: self.insertTipMsg_Nobalance(tipStr: str)
        self.implantPlayer(tipStr: str)
    }

    /// 判断是否需要插入音视频通话提示消息
    //: func func__privateChatVideo_insertTipMsg() {
    func areaWith() {
        //: guard conversationType == .private else { return }
        guard conversationType == .private else { return }
        //: TalkingPrivateChatManager.chat_privateChatVideo_insertTipMsg(toUid: self.userWrap.uid, msgView: self.msgView) {
        FrameLevelThen.windowTo(toUid: self.userWrap.uid, msgView: self.msgView) {
            // 插入音视频提示消息
            //: let str = self.userWrap.nickname
            let str = self.userWrap.nickname
            //: self.insertTipMsg_Nobalance(tipStr: "You can use the video chat feature to give %@ a call".localizedArguments(str))
            self.implantPlayer(tipStr: String(bytes: kGiftName.map{equalAdd(thumb: $0)}, encoding: .utf8)!.cellArguments(str))
        }
    }

    /// 通话礼物消息
    //: @objc func insertGiftMsgNotif(notification: NSNotification) {
    @objc func nowadays(notification: NSNotification) {
        //: let dict = notification.userInfo
        let dict = notification.userInfo
        //: let cellData: AddCellData = dict?["cellData"] as! AddCellData
        let cellData: AddCellData = dict?[(String(kVideoUrl.suffix(7)) + String(noti_objectStr))] as! AddCellData
        //: self.msgView.uiMsgs.add(cellData)
        self.msgView.uiMsgs.add(cellData)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.actionBottom(true)
        self.msgView.actionBottom(true)
    }
}

// MARK: - 回复文案

//: extension TalkingPrivateChatController: PrivateChatReplyTipsProtocol {
extension MakeReactiveCompatible: ThemeThen {
    //: func func__setReplyTipsView() {
    func clean() {
        //: let key =  "\(IconContainerAppManager.share.loginUserMode.userID)_PrivateChatTipsTitle_\(self.targetId)"
        let key = "\(IconContainerAppManager.share.loginUserMode.userID)" + (String(kCornerValue) + String(dataImageLaughText)) + "\(self.targetId)"
        //: let isShow: Bool = UserDefaults.standard.object(forKey: key) as? Bool ?? false
        let isShow: Bool = UserDefaults.standard.object(forKey: key) as? Bool ?? false
        //: if !isShow && IconContainerAppManager.share.appStatus == AppSkinStatus.normal.rawValue {
        if !isShow && IconContainerAppManager.share.appStatus == MinExpressionConvertible.normal.rawValue {
            //: view.addSubview(replyTipsView!)
            view.addSubview(replyTipsView!)
            //: replyTipsView?.snp.makeConstraints { make in
            replyTipsView?.snp.makeConstraints { make in
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(50)
                make.height.equalTo(50)
            }
            //: msgView.view.snp.remakeConstraints { make in
            msgView.view.snp.remakeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(replyTipsView!.snp.top)
                make.bottom.equalTo(replyTipsView!.snp.top)
            }
            //: self.replyTipsView?.setTipsStrView()
            self.replyTipsView?.setRandom()
            // 修改banner位置
            //: changeBannerViewFream(isMiniView: self.bannerView.isMiniView)
            pastFinish(isMiniView: self.bannerView.isMiniView)
            //: } else {
        } else {
            //: removeReplyTipsView()
            quoteAction()
        }
    }

    //: func seleteReplyTipsMessage(str: String) {
    func sharedColor(str: String) {
        //: self.userWrap.existSess = true
        self.userWrap.existSess = true
        //: func__sendTextMsg(msgStr: str, quoteModel: nil)
        sire(msgStr: str, quoteModel: nil)
        //: removeReplyTipsView()
        quoteAction()
    }

    //: func removeReplyTipsView() {
    func quoteAction() {
        //: if conversationType == .private {
        if conversationType == .private {
            //: if self.replyTipsView != nil {
            if self.replyTipsView != nil {
                //: self.replyTipsView?.removeFromSuperview()
                self.replyTipsView?.removeFromSuperview()
                //: self.replyTipsView = nil
                self.replyTipsView = nil
            }
            //: msgView.view.snp.remakeConstraints { make in
            msgView.view.snp.remakeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
            }
            //: changeBannerViewFream(isMiniView: self.bannerView.isMiniView)
            pastFinish(isMiniView: self.bannerView.isMiniView)
        }
    }
}

// MARK: - 半屏页面展示/隐藏动画

//: extension TalkingPrivateChatController {
extension MakeReactiveCompatible {
    /// 半屏展示动画
    //: private func half_showViewAnimation() {
    private func random() {
        //: guard self.isFrom == .LiveRoom else { return }
        guard self.isFrom == .LiveRoom else { return }
        //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_SHOW, object: nil, userInfo: nil)
        NotificationCenter.default.post(name: user_informationPath, object: nil, userInfo: nil)
        //: self.view.frame = CGRect(x: 0, y: ScreenHeight-HalfViewTopMargin, width: ScreenWidth, height: ScreenHeight)
        self.view.frame = CGRect(x: 0, y: constRecordServerKey - data_halfTitle, width: main_colorData, height: constRecordServerKey)
        //: UIView.animate(withDuration: 0.25) {
        UIView.animate(withDuration: 0.25) {
            //: self.view.frame = CGRect(x: 0,
            self.view.frame = CGRect(x: 0,
                                     //: y: 0,
                                     y: 0,
                                     //: width: ScreenWidth,
                                     width: main_colorData,
                                     //: height: ScreenHeight)
                                     height: constRecordServerKey)
        }
    }

    /// 半屏页消失动画
    /// - Parameter removeChatList: 是否需要移除聊天列表
    //: private func half_dismissViewAnimation(_ removeChatList: Bool) {
    private func flick(_ removeChatList: Bool) {
        //: guard self.isFrom == .LiveRoom else { return }
        guard self.isFrom == .LiveRoom else { return }

        //: self.view.endEditing(true)
        self.view.endEditing(true)
        // 移除聊天列表
        //: if removeChatList == true, let currentVC = self.currentViewController() {
        if removeChatList == true, let currentVC = self.actionLatest() {
            //: if TalkingVoiceRoomManager.shared().isParty == true {
            if PopEventHandler.addMonth().isParty == true {
                //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_DISMISS, object: nil, userInfo: nil)
                NotificationCenter.default.post(name: appTagValue, object: nil, userInfo: nil)
            }
            //: for vc in currentVC.children {
            for vc in currentVC.children {
                //: if vc.isKind(of: TalkingChatListViewController.self) {
                if vc.isKind(of: DataSizeCrossingViewDelegate.self) {
                    //: vc.removeFromParent()
                    vc.removeFromParent()
                    //: vc.view.removeFromSuperview()
                    vc.view.removeFromSuperview()
                    //: break
                    break
                }
            }
        }

        // 移除私聊页
        //: UIView.animate(withDuration: 0.25) {
        UIView.animate(withDuration: 0.25) {
            //: self.view.frame = CGRect(x: 0,
            self.view.frame = CGRect(x: 0,
                                     //: y: ScreenHeight-HalfViewTopMargin,
                                     y: constRecordServerKey - data_halfTitle,
                                     //: width: ScreenWidth,
                                     width: main_colorData,
                                     //: height: ScreenHeight)
                                     height: constRecordServerKey)
            //: } completion: { finish in
        } completion: { _ in
            //: self.removeFromParent()
            self.removeFromParent()
            //: self.view.removeFromSuperview()
            self.view.removeFromSuperview()
            //: if TalkingVoiceRoomManager.shared().isParty == false {
            if PopEventHandler.addMonth().isParty == false {
                //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_DISMISS, object: nil, userInfo: nil)
                NotificationCenter.default.post(name: appTagValue, object: nil, userInfo: nil)
            }
        }
    }
}

// MARK: - Layout

//: extension TalkingPrivateChatController {
extension MakeReactiveCompatible {
    /// 创建UI
    //: private func createUI() {
    private func startUi() {
        //: view.backgroundColor = (self.isFrom == .LiveRoom) ? UIColor.clear:UIColor.white
        view.backgroundColor = (self.isFrom == .LiveRoom) ? UIColor.clear : UIColor.white
        //: view.addSubview(contentView)
        view.addSubview(contentView)

        //: if conversationType == .private {
        if conversationType == .private {
            //: contentView.addSubview(bgImgeView)
            contentView.addSubview(bgImgeView)
            //: contentView.addSubview(bgView)
            contentView.addSubview(bgView)
            //: contentView.addSubview(coverView)
            contentView.addSubview(coverView)
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
            //: contentView.addSubview(beautyPhotosBtn)
            contentView.addSubview(beautyPhotosBtn)
            //: contentView.addSubview(sendGreetBtn)
            contentView.addSubview(sendGreetBtn)
            //: if self.isFrom == .Normal {
            if self.isFrom == .Normal {
                //: contentView.addSubview(bannerView)
                contentView.addSubview(bannerView)
            }
            //: contentView.addSubview(floatScreenView)
            contentView.addSubview(floatScreenView)
            //: contentView.addSubview(giftEffectView)
            contentView.addSubview(giftEffectView)
            //: contentView.addSubview(navView)
            contentView.addSubview(navView)
            //: contentView.addSubview(notesTopView)
            contentView.addSubview(notesTopView)
            //: view.addSubview(msgInputView)
            view.addSubview(msgInputView)
            //: view.addSubview(selectTranView)
            view.addSubview(selectTranView)
            //: view.addSubview(transResultView)
            view.addSubview(transResultView)

            //: } else if conversationType == .service {
        } else if conversationType == .service {
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
            //: contentView.addSubview(navView)
            contentView.addSubview(navView)
            //: view.addSubview(msgInputView)
            view.addSubview(msgInputView)
            //: view.addSubview(selectTranView)
            view.addSubview(selectTranView)
            //: view.addSubview(transResultView)
            view.addSubview(transResultView)

            //: } else {
        } else {
            //: title = "System notification".localized
            title = (String(appCellDirectionName.suffix(5)) + String(dataCurName.prefix(4)) + app_shareMessage.replacingOccurrences(of: "zone", with: "f") + "tion").localized
            //: contentView.addSubview(feedView)
            contentView.addSubview(feedView)
            //: feedView.addSubview(feedbackBtn)
            feedView.addSubview(feedbackBtn)
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
        }
    }

    /// 约束
    //: private func createUIConstraints() {
    private func diagonal() {
        //: contentView.snp.makeConstraints { make in
        contentView.snp.makeConstraints { make in
            //: make.bottom.leading.trailing.equalToSuperview()
            make.bottom.leading.trailing.equalToSuperview()
            //: make.top.equalTo((self.isFrom == .LiveRoom) ? HalfViewTopMargin:0)
            make.top.equalTo((self.isFrom == .LiveRoom) ? data_halfTitle : 0)
        }

        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: contentView.layoutIfNeeded()
            contentView.layoutIfNeeded()
            //: contentView.Corner(width: contentView.width,
            contentView.meanSunRadii(width: contentView.width,
                                     //: height: contentView.height,
                                     height: contentView.height,
                                     //: corners: [.topLeft, .topRight],
                                     corners: [.topLeft, .topRight],
                                     //: cornerRadii: .init(width: 8, height: 8))
                                     cornerRadii: .init(width: 8, height: 8))
        }

        //: if conversationType == .private {
        if conversationType == .private {
            //: bgView.snp.makeConstraints { make in
            bgView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: bgImgeView.snp.makeConstraints { make in
            bgImgeView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: coverView.snp.makeConstraints { make in
            coverView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: navView.snp.makeConstraints { make in
            navView.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalToSuperview()
                make.leading.trailing.top.equalToSuperview()
                //: make.height.equalTo((self.isFrom == .LiveRoom) ? NavigationBarHeight:StatusBarNavigationBarHeight)
                make.height.equalTo((self.isFrom == .LiveRoom) ? user_ofMsg : data_halfMsg)
            }
            //: notesTopView.snp.makeConstraints { make in
            notesTopView.snp.makeConstraints { make in
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.leading.equalTo(10)
                make.leading.equalTo(10)
                //: make.trailing.equalTo(-10)
                make.trailing.equalTo(-10)
                //: make.height.equalTo(36)
                make.height.equalTo(36)
            }
            //: msgInputView.snp.makeConstraints { make in
            msgInputView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo(view)
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(msgInputView.BackViewHeight)
                make.height.equalTo(msgInputView.BackViewHeight)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: if beautyPhotosBtn.isHidden {
                if beautyPhotosBtn.isHidden {
                    //: make.bottom.equalTo(msgInputView.snp.top)
                    make.bottom.equalTo(msgInputView.snp.top)
                    //: } else {
                } else {
                    //: make.bottom.equalTo(beautyPhotosBtn.snp.top).offset(-5)
                    make.bottom.equalTo(beautyPhotosBtn.snp.top).offset(-5)
                }
            }
            //: giftEffectView.snp.makeConstraints { make in
            giftEffectView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: sendGreetBtn.snp.makeConstraints { make in
            sendGreetBtn.snp.makeConstraints { make in
                //: make.bottom.equalTo(beautyPhotosBtn)
                make.bottom.equalTo(beautyPhotosBtn)
                //: make.leading.equalTo(10)
                make.leading.equalTo(10)
                //: make.size.equalTo(beautyPhotosBtn)
                make.size.equalTo(beautyPhotosBtn)
            }
            //: beautyPhotosBtn.snp.makeConstraints { make in
            beautyPhotosBtn.snp.makeConstraints { make in
                //: make.bottom.equalTo(msgInputView.snp.top).offset(-8)
                make.bottom.equalTo(msgInputView.snp.top).offset(-8)
                //: make.leading.equalTo(sendGreetBtn.snp.trailing).offset(9)
                make.leading.equalTo(sendGreetBtn.snp.trailing).offset(9)
                //: make.width.equalTo(134)
                make.width.equalTo(134)
                //: make.height.equalTo(30)
                make.height.equalTo(30)
            }
            //: selectTranView.snp.makeConstraints { make in
            selectTranView.snp.makeConstraints { make in
                //: make.leading.trailing.equalTo(msgInputView)
                make.leading.trailing.equalTo(msgInputView)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.height.equalTo(27)
                make.height.equalTo(27)
            }
            //: transResultView.snp.makeConstraints { make in
            transResultView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalToSuperview()
                make.leading.trailing.bottom.equalToSuperview()
                //: make.height.equalTo(206+kDeviceSafeBottomHeight)
                make.height.equalTo(206 + noti_saveFormat)
            }
            //: floatScreenView.snp.makeConstraints { make in
            floatScreenView.snp.makeConstraints { make in
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(40)
                make.height.equalTo(40)
            }
            //: if self.isFrom == .Normal {
            if self.isFrom == .Normal {
                //: bannerView.snp.makeConstraints { make in
                bannerView.snp.makeConstraints { make in
                    //: make.trailing.equalToSuperview()
                    make.trailing.equalToSuperview()
                    //: make.bottom.equalTo(beautyPhotosBtn.snp.top)
                    make.bottom.equalTo(beautyPhotosBtn.snp.top)
                    //: make.height.equalTo(106)
                    make.height.equalTo(106)
                    //: make.width.equalTo(93)
                    make.width.equalTo(93)
                }
            }

            //: } else if conversationType == .service {
        } else if conversationType == .service {
            //: navView.snp.makeConstraints { make in
            navView.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalToSuperview()
                make.leading.trailing.top.equalToSuperview()
                //: make.height.equalTo((self.isFrom == .LiveRoom) ? NavigationBarHeight:StatusBarNavigationBarHeight)
                make.height.equalTo((self.isFrom == .LiveRoom) ? user_ofMsg : data_halfMsg)
            }
            //: msgInputView.snp.makeConstraints { make in
            msgInputView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo(view)
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(msgInputView.BackViewHeight)
                make.height.equalTo(msgInputView.BackViewHeight)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
            }
            //: selectTranView.snp.makeConstraints { make in
            selectTranView.snp.makeConstraints { make in
                //: make.leading.trailing.equalTo(msgInputView)
                make.leading.trailing.equalTo(msgInputView)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.height.equalTo(27)
                make.height.equalTo(27)
            }
            //: transResultView.snp.makeConstraints { make in
            transResultView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalToSuperview()
                make.leading.trailing.bottom.equalToSuperview()
                //: make.height.equalTo(206+kDeviceSafeBottomHeight)
                make.height.equalTo(206 + noti_saveFormat)
            }

            //: } else {
        } else {
            //: feedView.snp.makeConstraints { make in
            feedView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo((view))
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(56+kDeviceSafeBottomHeight)
                make.height.equalTo(56 + noti_saveFormat)
            }
            //: feedbackBtn.snp.makeConstraints { make in
            feedbackBtn.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalTo(feedView)
                make.leading.trailing.top.equalTo(feedView)
                //: make.height.equalTo(56)
                make.height.equalTo(56)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalTo((view))
                make.leading.trailing.top.equalTo(view)
                //: make.bottom.equalTo(feedView.snp.top)
                make.bottom.equalTo(feedView.snp.top)
            }
        }
    }

    /// 事件绑定
    //: func bindInteraction() {
    func isoclinicQuantityeraction() {
        //: navView.backButton.rx.tap
        navView.backButton.rx.tap
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.backButtonClick()
                self.showIn()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: navView.moreButton.rx.controlEvent(.touchUpInside)
        navView.moreButton.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.view.endEditing(true)
                self.view.endEditing(true)
                //: self.moreBtnClick()
                self.utter()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: navView.videoCallBtn.rx.controlEvent(.touchUpInside)
        navView.videoCallBtn.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.view.endEditing(true)
                self.view.endEditing(true)
                //: self.func__selectMoreFunAction(actionType: .VideoCall)
                self.kind(actionType: .VideoCall)
                //: uploadRecord.uploadRecordEvent(eventID: ClickChatVideoButton, toUid: self.userWrap.uid )
                showVideoPath.referenceStr(eventID: show_videoData, toUid: self.userWrap.uid)
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
    }
}
