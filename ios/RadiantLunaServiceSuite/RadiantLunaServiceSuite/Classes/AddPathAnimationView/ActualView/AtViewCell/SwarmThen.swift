
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let show_sizeTitleBlockValue:[UInt8] = [0x7,0x0,0x7,0x1a,0x46,0xd,0x1,0xa,0xb,0x1c,0x54,0x47,0x4e,0x6,0xf,0x1d,0x4e,0x0,0x1,0x1a,0x4e,0xc,0xb,0xb,0x0,0x4e,0x7,0x3,0x1e,0x2,0xb,0x3,0xb,0x0,0x1a,0xb,0xa]

				private func modelTo(rank num: UInt8) -> UInt8 {
					return num ^ 110
				}

/*: "roomId" :*/
fileprivate let show_dataId:String = "full"
fileprivate let data_effectMessage:String = "line background margin model corneroomId"

/*: "type" :*/
fileprivate let data_sizeId:[UInt8] = [0xfa,0xf7,0xfe,0xeb]

/*: "extra" :*/
fileprivate let show_playData:String = "eclicktra"

/*: "jumpKey" :*/
fileprivate let noti_pathTitle:[UInt8] = [0x4,0xf,0x7,0xa,0xe5,0xff,0x13]

				fileprivate func modeBottom(next num: UInt8) -> UInt8 {
					let value = Int(num) + 102
					if value > 255 {
						return UInt8(value - 256)
					} else {
						return UInt8(value)
					}
				}

/*: "Send Gift" :*/
fileprivate let show_viewName:[Character] = ["S"]
fileprivate let mainCollectionFormat:[Character] = ["e","n","d"," ","G","i","f","t"]

/*: "View info" :*/
fileprivate let constSharedHiddenValue:[Character] = ["V","i","e","w"," ","i","n","f","o"]

/*: "Report" :*/
fileprivate let app_countStr:String = "Reportfor equal lab"

/*: "name" :*/
fileprivate let noti_mainButtonValue:[UInt8] = [0xd1,0xde,0xd2,0xda]

/*: "Can't send blank message" :*/
fileprivate let noti_tableData:String = "Can\'t se"
fileprivate let noti_replyTurnPath:String = "colord"
fileprivate let k_postFormat:[Character] = [" ","b","l","a","n","k"," ","m","e","s","s","a","g","e"]

/*: "user" :*/
fileprivate let dataIndexValue:String = "usbuttonr"

/*: "content" :*/
fileprivate let main_sharedPath:[UInt8] = [0x95,0x99,0x98,0x82,0x93,0x98,0x82]

				private func translationClear(status num: UInt8) -> UInt8 {
					return num ^ 246
				}

/*: "msgType" :*/
fileprivate let appViewUserFormat:String = "msgTyrow make model layer"
fileprivate let dataBagName:[Character] = ["p","e"]

/*: "isGroup" :*/
fileprivate let kGreetMsg:[Character] = ["i","s","G","r","o","u","p"]

/*: "jumps" :*/
fileprivate let show_windowModelKey:[Character] = ["j","u","m","p","s"]

/*: "message" :*/
fileprivate let show_labUrl:[UInt8] = [0xfc,0xf4,0xe2,0xe2,0xf0,0xf6,0xf4]

/*: "TxtMsg" :*/
fileprivate let kTheContent:String = "t"
fileprivate let showTableContent:String = "xtMsgcell cell error"

/*: "toUid" :*/
fileprivate let const_hiddenId:[Character] = ["t","o","U","i","d"]

/*: "msgInfo" :*/
fileprivate let data_sizeValue:String = "button return data mode livemsgInfo"

/*: "params" :*/
fileprivate let userAddMsg:[UInt8] = [0x73,0x6d,0x61,0x72,0x61,0x70]

/*: "contentType" :*/
fileprivate let show_makeTitle:[UInt8] = [0x4b,0x47,0x46,0x5c,0x4d,0x46,0x5c,0x7c,0x51,0x58,0x4d]

				private func toPath(manager num: UInt8) -> UInt8 {
					return num ^ 40
				}

/*: "ImgMsg" :*/
fileprivate let data_colorPlayerValue:String = "ImgMsgelse action to"

/*: "imageUri" :*/
fileprivate let k_tempVoicePath:[UInt8] = [0x37,0x33,0x3f,0x39,0x3b,0xb,0x2c,0x37]

				private func analogDigitalConverter(key num: UInt8) -> UInt8 {
					return num ^ 94
				}

/*: "msgId" :*/
fileprivate let mainGenderName:[Character] = ["m","s","g","I","d"]

/*: "totalMfCoin" :*/
fileprivate let kStyleData:String = "user style equaltotalMf"
fileprivate let show_nameMsg:String = "true return model index userCoin"

/*: "giftId" :*/
fileprivate let const_barKey:String = "giftIdmake return tick type"

/*: "giftNum" :*/
fileprivate let userDismissPath:String = "giftNumself scale make path voice"

/*: "pkgItemsetId" :*/
fileprivate let notiCenterValue:String = "name open giftpkgItem"
fileprivate let notiSignPath:String = "setIdempty permit false text self"

/*: "tips" :*/
fileprivate let userToUrl:[Character] = ["t","i","p","s"]

/*: "gift" :*/
fileprivate let show_makeHiddenUrl:String = "gusert"

/*: "nickname" :*/
fileprivate let showFillPath:String = "nclearckna"
fileprivate let appFinishStr:[Character] = ["m","e"]

/*: "headPic" :*/
fileprivate let k_layerFormat:[UInt8] = [0x7a,0x77,0x73,0x76,0x62,0x7b,0x75]

				fileprivate func quantitativeRelation(position num: UInt8) -> UInt8 {
					let value = Int(num) - 18
					if value < 0 {
						return UInt8(value + 256)
					} else {
						return UInt8(value)
					}
				}

/*: "All Numbers" :*/
fileprivate let appFacePath:[Character] = ["A","l","l"," ","N","u","m"]
fileprivate let constHalfScreenPath:[Character] = ["b","e","r","s"]

/*: "sendNum" :*/
fileprivate let app_cleanShowCenterId:[Character] = ["s","e","n","d","N"]
fileprivate let notiListKey:[Character] = ["u","m"]

/*: "toUser" :*/
fileprivate let showByNameUrl:String = "toUsepath port lab false data"
fileprivate let noti_formatName:String = "table"

/*: "msgIncome" :*/
fileprivate let kReValue:String = "msgIin let earth if"
fileprivate let notiFileData:String = "sharecome"

/*: "isConnection" :*/
fileprivate let k_endMsg:String = "name for let open varisConne"
fileprivate let showLabelFormat:String = "ctibottom"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  SwarmThen.swift
//  AbroadTalking
//
//  Created by young on 2023/4/4.
//

//: import UIKit
import UIKit

//: class TalkingGroupChatController: TalkingBaseViewController {
class SwarmThen: LightThen {
    //: var infoModel = TalkingChatRoomInfoModel()
    var infoModel = UserMeasurable() // 群聊信息
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var tipsTimer: Timer?
    private var tipsTimer: Timer? // 提示消息定时器
    //: private var conversationData = TalkingConversationModel()
    private var conversationData = ConferenceConversationModel() // 会话cellData
    //: private var conversationType: TalkingIMChatType = .group
    private var conversationType: TalkingIMChatType = .group // 会话类型
    //: private var atFlag = false
    private var atFlag = false // 记录是否@

    //: init(groupType: String, infoData: [String: Any]) {
    init(groupType: String, infoData: [String: Any]) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        //: hideNavi = true
        hideNavi = true
        //: if let model = TalkingChatRoomInfoModel.deserialize(from: infoData) {
        if let model = UserMeasurable.deserialize(from: infoData) {
            //: infoModel = model
            infoModel = model
        }
        //: conversationData.groupID = infoModel.roomId
        conversationData.groupID = infoModel.roomId
        //: conversationData.groupType = groupType
        conversationData.groupType = groupType
        //: if groupType == GroupType_Meeting {
        if groupType == GroupType_Meeting {
            //: conversationType = .meeting
            conversationType = .meeting
        }
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: show_sizeTitleBlockValue.map{modelTo(rank: $0)}, encoding: .utf8)!)
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if IconContainerAppManager.share.appConfigMode.disableShootScreen {
            if IconContainerAppManager.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = AtThen(frame: CGRect(x: 0, y: 0, width: main_colorData, height: constRecordServerKey))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: createUI()
        activeAgent()
        //: createUIConstraints()
        addUp()
        //: bindInteraction()
        sumeractionRecord()
        //: addTapGestureRecognizer()
        noPathRecognizer()
        //: func__chatRoom_insertTipMsg()
        duringShared()
        //: chatSettings()
        addAppearSettings()

        //: if conversationType == .private {
        if conversationType == .private {
            //: initCachEffectData()
            cach()
        }
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: if self.atFlag {
        if self.atFlag {
            //: self.msgInputView.inputTextView.becomeFirstResponder()
            self.msgInputView.inputTextView.becomeFirstResponder()
            //: self.atFlag = false
            self.atFlag = false
        }
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: view.endEditing(true)
        view.endEditing(true)
    }

    //: deinit {
    deinit {
        //: if self.conversationData.groupType == GroupType_Meeting {
        if self.conversationData.groupType == GroupType_Meeting {
            //: V2TIMManager.sharedInstance().quitGroup(self.conversationData.groupID, succ: nil, fail: nil)
            V2TIMManager.sharedInstance().quitGroup(self.conversationData.groupID, succ: nil, fail: nil)
        }
        //: self.func__joinOrQuitRoom(join: false)
        self.viewUnionize(join: false)
        //: self.destroyTipsTimer()
        self.visualImage()
    }

    // MARK: - Lazy Load

    //: private lazy var msgView: TUIMessageController = {
    private lazy var msgView: ConstraintToViewController = {
        //: let msgVC = TUIMessageController.init()
        let msgVC = ConstraintToViewController()
        //: msgVC.delegate = self
        msgVC.delegate = self
        //: msgVC.view.backgroundColor = UIColor.clear
        msgVC.view.backgroundColor = UIColor.clear
        //: msgVC.tableView.backgroundColor = .clear
        msgVC.tableView.backgroundColor = .clear
        //: msgVC.tableView.register(TalkingGroupChatTextMsgCell.self, forCellReuseIdentifier: TalkingGroupChatTextMsgCell.className())
        msgVC.tableView.register(MudraViewCell.self, forCellReuseIdentifier: MudraViewCell.className())
        //: msgVC.tableView.register(TalkingChatImageMsgCell.self, forCellReuseIdentifier: TalkingChatImageMsgCell.className())
        msgVC.tableView.register(VideoView.self, forCellReuseIdentifier: VideoView.className())
        //: msgVC.tableView.register(TalkingGroupChatGiftMsgCell.self, forCellReuseIdentifier: TalkingGroupChatGiftMsgCell.className())
        msgVC.tableView.register(ViewMsgCell.self, forCellReuseIdentifier: ViewMsgCell.className())
        //: msgVC.tableView.register(TalkingChatTipsMsgCell.self, forCellReuseIdentifier: TalkingChatTipsMsgCell.className())
        msgVC.tableView.register(BlockMaxMessageCell.self, forCellReuseIdentifier: BlockMaxMessageCell.className())
        //: msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: DigitiserReactiveCompatible.TSystemMessageCell_ReuseId)
        msgVC.tableView.register(IconUserViewCell.self, forCellReuseIdentifier: DigitiserReactiveCompatible.TSystemMessageCell_ReuseId)
        //: msgVC.currentChatType = conversationType
        msgVC.currentChatType = conversationType
        //: return msgVC
        return msgVC
        //: }()
    }()

    //: private lazy var msgInputView: TalkingChatInputView = {
    private lazy var msgInputView: TextViewDelegate = {
        //: let view = TalkingChatInputView(type: .GroupChat)
        let view = TextViewDelegate(type: .GroupChat)
        //: view.chatRoomID = infoModel.roomId
        view.chatRoomID = infoModel.roomId
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: private lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    private lazy var giftEffectView: ThemeAnimatView = {
        //: let effectView = TalkingPrivateChatAnimatView.init()
        let effectView = ThemeAnimatView()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: private lazy var giftTrackView: TalkingGiftTrackView = {
    private lazy var giftTrackView: ImageIconThen = {
        //: let trackView = TalkingGiftTrackView.init()
        let trackView = ImageIconThen()
        //: trackView.isUserInteractionEnabled = false
        trackView.isUserInteractionEnabled = false
        //: return trackView
        return trackView
        //: }()
    }()

    //: private lazy var navView: TalkingChatNavTitleView = {
    private lazy var navView: FrameThirdThen = {
        //: let navView = TalkingChatNavTitleView(type: conversationType)
        let navView = FrameThirdThen(type: conversationType)
        //: navView.titleLabel.text = infoModel.name
        navView.titleLabel.text = infoModel.name
        //: return navView
        return navView
        //: }()
    }()

    //: private lazy var someoneView: TalkingSomeoneAtMeView = {
    private lazy var someoneView: DigitizerReactiveCompatible = {
        //: let v = TalkingSomeoneAtMeView()
        let v = DigitizerReactiveCompatible()
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()

    //: private lazy var mentionInfoArr: [[String: Any]] = {
    private lazy var mentionInfoArr: [[String: Any]] = //: return [[String: Any]]()
        .init()
    //: }()
}

// MARK: - Load Data

//: extension TalkingGroupChatController {
extension SwarmThen {
    /// 群聊设置
    //: private func chatSettings() {
    private func addAppearSettings() {
        //: TalkingGroupChatManager.share.chatRoomVC = self
        SessionChatManager.share.chatRoomVC = self

        //: func__joinOrQuitRoom(join: true)
        viewUnionize(join: true)
    }

    /// 加入或退出聊天室
    /// - Parameter join: 是否加入
    //: private func func__joinOrQuitRoom(join: Bool) {
    private func viewUnionize(join: Bool) {
        //: let params = ["roomId": self.conversationData.groupID, "type": (join == true ? 1:2)] as [String: Any]
        let params = [(show_dataId.replacingOccurrences(of: "full", with: "r") + String(data_effectMessage.suffix(5))): self.conversationData.groupID, String(bytes: data_sizeId.map{$0^142}, encoding: .utf8)!: join == true ? 1 : 2] as [String: Any]
        //: StreamRequestTool.req_chatRoomJoinOrQuit(params: params) { succeed, result, errorModel in
        StreamRequestTool.bySegment(params: params) { succeed, _, _ in
            //: guard succeed else { return }
            guard succeed else { return }
//            guard let data = result as? [String: Any] else {
//                return
//            }
//
//            if join {
//
//            }
        }
    }
}

// MARK: - RestoreDelegate

//: extension TalkingGroupChatController: TMessageControllerDelegate {
extension SwarmThen: RestoreDelegate {
    //: func messageController(_ controller: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> AddCellData! {
    func model(_: ConstraintToViewController!, currentSend data: V2TIMMessage!) -> AddCellData! {
        //: let dic = ThemeChatReactiveCompatible.parseTXMessageData(data: data.customElem.data)
        let dic = ThemeChatReactiveCompatible.playChange(data: data.customElem.data)
        //: let cellData = TUISocialChatManager.handleTXReceiveMessage(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        let cellData = DirectionReactiveCompatible.toViewModel(withExtral: dic, cusData: data.localCustomData, tempModel: data)

        //: return cellData
        return cellData
    }

    //: func messageController(_ controller: TUIMessageController!, onShowMessageData data: AddCellData!) -> DigitiserReactiveCompatible! {
    func bringHomeData(_: ConstraintToViewController!, model data: AddCellData!) -> DigitiserReactiveCompatible! {
        //: if data.isKind(of: ScreenThen.self) {
        if data.isKind(of: ScreenThen.self) {
            //: let cell = TalkingGroupChatTextMsgCell.init(style: .default, reuseIdentifier: TalkingGroupChatTextMsgCell.className())
            let cell = MudraViewCell(style: .default, reuseIdentifier: MudraViewCell.className())
            //: let cellData = data as! ScreenThen
            let cellData = data as! ScreenThen
            //: cell.fill(with: cellData)
            cell.padWith(with: cellData)
            //: cellData.atBlock = { [weak self] uid in
            cellData.atBlock = { [weak self] uid in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.menu_goToUserDetailController(uid: uid)
                self.addUser(uid: uid)
            }
            //: cell.transBlock = { [weak self] in
            cell.transBlock = { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.msgView.translateMsg(with: cellData)
                self.msgView.contriveData(cellData)
            }

            //: return cell
            return cell
        }

        //: if data.isKind(of: FountainheadCellData.self) {
        if data.isKind(of: FountainheadCellData.self) {
            //: let cell = TalkingChatImageMsgCell.init(style: .default, reuseIdentifier: TalkingChatImageMsgCell.className())
            let cell = VideoView(style: .default, reuseIdentifier: VideoView.className())
            //: let cellData = data as! FountainheadCellData
            let cellData = data as! FountainheadCellData
            //: cell.fill(with: cellData)
            cell.padWith(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingGroupChatGiftMsgCellData.self) {
        if data.isKind(of: GiveawayEthnicGroupChatValueCompartmentMetadataCellData.self) {
            //: let cell = TalkingGroupChatGiftMsgCell.init(style: .default, reuseIdentifier: TalkingGroupChatGiftMsgCell.className())
            let cell = ViewMsgCell(style: .default, reuseIdentifier: ViewMsgCell.className())
            //: let cellData = data as! TalkingGroupChatGiftMsgCellData
            let cellData = data as! GiveawayEthnicGroupChatValueCompartmentMetadataCellData
            //: cell.fill(with: cellData)
            cell.padWith(with: cellData)

            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatTipsMsgCellData.self) {
        if data.isKind(of: SuccessAppThen.self) {
            //: let cell = TalkingChatTipsMsgCell.init(style: .default, reuseIdentifier: TalkingChatTipsMsgCell.className())
            let cell = BlockMaxMessageCell(style: .default, reuseIdentifier: BlockMaxMessageCell.className())
            //: let cellData = data as! TalkingChatTipsMsgCellData
            let cellData = data as! SuccessAppThen
            //: cell.fill(with: cellData)
            cell.padWith(with: cellData)
            //: return cell
            return cell
        }

        //: return nil
        return nil
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageAvatar cell: DigitiserReactiveCompatible!) {
    func groupDiscussion(_: ConstraintToViewController!, halogen cell: DigitiserReactiveCompatible!) {
        //: if let userModel = cell.messageData?.msgModel.user {
        if let userModel = cell.messageData?.msgModel.user {
            //: if userModel.uid == Int(IconContainerAppManager.share.loginUid) {
            if userModel.uid == Int(IconContainerAppManager.share.loginUid) {
                //: menu_goToUserDetailController(uid: userModel.uid )
                addUser(uid: userModel.uid)
                //: }else{
            } else {
                //: menu_showMunuPopView(user: userModel)
                appPin(user: userModel)
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageContent cell: DigitiserReactiveCompatible!) {
    func with(_: ConstraintToViewController!, userDevice cell: DigitiserReactiveCompatible!) {
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: VideoView.self) { // 图片
            //: let imageCellData = cell.messageData as! FountainheadCellData
            let imageCellData = cell.messageData as! FountainheadCellData
            //: let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
            let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
            //: var filePath = (libraryPath as NSString).appendingPathComponent(imageFileName)
            var filePath = (libraryPath as NSString).appendingPathComponent(k_keyText)
            //: filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)
            filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)

            //: var model = TalkingMomentPhotoModel.init()
            var model = TextToModelType()
            //: if FileManager.default.fileExists(atPath: filePath) {
            if FileManager.default.fileExists(atPath: filePath) {
                //: let data = NSData.init(contentsOfFile: filePath)
                let data = NSData(contentsOfFile: filePath)
                //: model.imagePic = UIImage(data: data! as Data)
                model.imagePic = UIImage(data: data! as Data)
                //: } else {
            } else {
                //: if imageCellData.imageData != nil {
                if imageCellData.imageData != nil {
                    //: model.imagePic = imageCellData.imageData
                    model.imagePic = imageCellData.imageData
                    //: } else {
                } else {
                    //: model.imgUrl =  imageCellData.imageUrl
                    model.imgUrl = imageCellData.imageUrl
                }
            }
            //: var currenModel = TalkingMomentModel.init()
            var currenModel = AtMeasurable()
            //: var tempArr: [TalkingMomentPhotoModel] = []
            var tempArr: [TextToModelType] = []
            //: tempArr.append(model)
            tempArr.append(model)
            //: currenModel.pic = tempArr
            currenModel.pic = tempArr
            //: let vc = TalkingMomentPhotosVC.init(momentModel: currenModel, index: 0, type: .normal)
            let vc = VideoImageReactiveCompatible(momentModel: currenModel, index: 0, type: .normal)
            //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
            self.actionLatest()?.navigationController?.pushViewController(vc, animated: true)

            //: } else if cell.isKind(of: TalkingGroupChatTextMsgCell.self) {
        } else if cell.isKind(of: MudraViewCell.self) {
            //: let textCell = cell as! TalkingGroupChatTextMsgCell
            let textCell = cell as! MudraViewCell
            //: let textData = textCell.messageData
            let textData = textCell.messageData
            //: let dic = ThemeChatReactiveCompatible.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            let dic = ThemeChatReactiveCompatible.playChange(data: (textData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(show_playData.replacingOccurrences(of: "click", with: "x"))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains(String(bytes: noti_pathTitle.map{modeBottom(next: $0)}, encoding: .utf8)!) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<HomeMeasurable>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: CombineAccountEnclaveDecisionMakerThen.share.func__actionPushForMessage(jumpModel: jumpModel)
                    CombineAccountEnclaveDecisionMakerThen.share.excess(jumpModel: jumpModel)
                }
            }

            //: } else if cell.isKind(of: TalkingChatTipsMsgCell.self) {
        } else if cell.isKind(of: BlockMaxMessageCell.self) {
            //: let tipCell = cell as! TalkingChatTipsMsgCell
            let tipCell = cell as! BlockMaxMessageCell
            //: let tipData = tipCell.messageData
            let tipData = tipCell.messageData
            //: let dic = ThemeChatReactiveCompatible.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            let dic = ThemeChatReactiveCompatible.playChange(data: (tipData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(show_playData.replacingOccurrences(of: "click", with: "x"))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains(String(bytes: noti_pathTitle.map{modeBottom(next: $0)}, encoding: .utf8)!) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<HomeMeasurable>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: CombineAccountEnclaveDecisionMakerThen.share.func__actionPushForMessage(jumpModel: jumpModel)
                    CombineAccountEnclaveDecisionMakerThen.share.excess(jumpModel: jumpModel)
                }
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onRetryMessageContent cell: DigitiserReactiveCompatible!) {
    func card(_: ConstraintToViewController!, radiogramDeviceAccumulationPlace cell: DigitiserReactiveCompatible!) {
        //: func__sendPriveteMsg(msgData: cell.messageData!, reSend: true)
        videoSend(msgData: cell.messageData!, reSend: true)
    }
}

// MARK: - Event

//: extension TalkingGroupChatController {
extension SwarmThen {
    /// 销毁倒计时
    //: private func destroyTipsTimer() {
    private func visualImage() {
        //: if tipsTimer != nil {
        if tipsTimer != nil {
            //: tipsTimer?.invalidate()
            tipsTimer?.invalidate()
            //: tipsTimer = nil
            tipsTimer = nil
        }
    }

    /// 点击用户头像，展示操作面板
    /// - Parameter user: 用户模型
    //: private func menu_showMunuPopView(user: AbTalkingChatMsgUserModel) {
    private func appPin(user: ResponseModel) {
        //: view.endEditing(true)
        view.endEditing(true)
        //: let vc = TalkingMunuPopView.init(frame: self.view.frame)
        let vc = VoicePopView(frame: self.view.frame)
        //: vc.title = user.nickname
        vc.title = user.nickname
        //: vc.initwithList(cellTitleList: ["Send Gift".localized, "@", "View info".localized, "Report".localized])
        vc.managerList(cellTitleList: [(String(show_viewName) + String(mainCollectionFormat)).localized, "@", (String(constSharedHiddenValue)).localized, (String(app_countStr.prefix(6))).localized])
        //: vc.munuBlock = { [weak self] index, str in
        vc.munuBlock = { [weak self] index, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if index == 0 {
            if index == 0 {
                //: self.menu_sentGiftAction(user: user)
                self.sent(user: user)
                //: } else if index == 1 {
            } else if index == 1 {
                //: self.menu_atTa(user: user)
                self.rankUser(user: user)
                //: } else if index == 2 {
            } else if index == 2 {
                //: self.menu_goToUserDetailController(uid: user.uid)
                self.addUser(uid: user.uid)
                //: } else if index == 3 {
            } else if index == 3 {
                //: self.menu_reportAction(uid: String(user.uid))
                self.deed(uid: String(user.uid))
            }
        }
    }

    /// 送礼
    /// - Parameter user: 用户模型
    //: private func menu_sentGiftAction(user: AbTalkingChatMsgUserModel) {
    private func sent(user: ResponseModel) {
        //: self.msgInputView.clickgiftBtn()
        self.msgInputView.finish()
        //: let model = TalkingChatRoomMemberModel()
        let model = ChaseReactiveCompatible()
        //: model.uid = "\(user.uid)"
        model.uid = "\(user.uid)"
        //: model.nickname = user.nickname
        model.nickname = user.nickname
        //: model.headPic = user.headPic
        model.headPic = user.headPic
        //: model.sex = user.sex
        model.sex = user.sex
        //: model.age = Int(user.age) ?? 0
        model.age = Int(user.age) ?? 0
        //: self.msgInputView.giftView.didClickSelectedRow(model: model)
        self.msgInputView.giftView.bestowal(model: model)
    }

    /// @Ta
    /// - Parameter user: 用户模型
    //: private func menu_atTa(user: AbTalkingChatMsgUserModel) {
    private func rankUser(user: ResponseModel) {
        //: let dict: [String: Any] = ["uid": String(user.uid), "name": user.nickname]
        let dict: [String: Any] = ["uid": String(user.uid), String(bytes: noti_mainButtonValue.map{$0^191}, encoding: .utf8)!: user.nickname]
        //: self.msgInputView.receiveAtInfo(range: nil, dict: dict)
        self.msgInputView.constraintView(range: nil, dict: dict)
        //: self.msgInputView.inputTextView.becomeFirstResponder()
        self.msgInputView.inputTextView.becomeFirstResponder()
    }

    /// 访问用户详情
    /// - Parameter uid: 用户Id
    //: private func menu_goToUserDetailController(uid: Int) {
    private func addUser(uid: Int) {
        //: CombineAccountEnclaveDecisionMakerThen.share.func__pushToUserDetailVC(uid: String(uid))
        CombineAccountEnclaveDecisionMakerThen.share.windowBy(uid: String(uid))
    }

    /// 举报
    /// - Parameter uid: 用户Id
    //: private func menu_reportAction(uid: String) {
    private func deed(uid: String) {
        //: self.msgInputView.showOtherInputView = true
        self.msgInputView.showOtherInputView = true
        //: let reportView = TalkingReportAlertView.init(frame: UIScreen.main.bounds, type: .reportUserType, rId: uid)
        let reportView = SuccessDataSource(frame: UIScreen.main.bounds, type: .reportUserType, rId: uid)
        //: reportView.showReportViewIn(view: nil)
        reportView.beyondShow(view: nil)
        //: reportView.reportHideBlock = { [weak self] _ in
        reportView.reportHideBlock = { [weak self] _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.msgInputView.showOtherInputView = false
            self.msgInputView.showOtherInputView = false
            //: return
        }
    }
}

// MARK: - 发送消息

//: extension TalkingGroupChatController {
extension SwarmThen {
    /// 发送文本消息
    /// - Parameter msgStr: 文本

    //: internal func func__sendTextMsg(msgStr: String, quoteModel: AbTalkingChatMsgQuoteModel?) {
    func sire(msgStr: String, quoteModel _: MainScoreModel?) {
        //: let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        //: if temStr.count == 0 {
        if temStr.count == 0 {
            //: func__showStatusBarErrorMsg(showMsg: "Can't send blank message".localized)
            explain(showMsg: (noti_tableData + noti_replyTurnPath.replacingOccurrences(of: "color", with: "n") + String(k_postFormat)).localized)
            //: return
            return
        }

        //: let uidStr = NSMutableString()
        let uidStr = NSMutableString()
        //: var jumps = [[String: String]]()
        var jumps = [[String: String]]()
        //: if msgInputView.atArr.count > 0 {
        if msgInputView.atArr.count > 0 { // 有@ 消息
            //: msgInputView.atArr.forEach { uid in
            msgInputView.atArr.forEach { uid in
                //: let dict = ["jumpKey": "user", "uid": uid]
                let dict = [String(bytes: noti_pathTitle.map{modeBottom(next: $0)}, encoding: .utf8)!: (dataIndexValue.replacingOccurrences(of: "button", with: "e")), "uid": uid]
                //: uidStr.append(uid+",")
                uidStr.append(uid + ",")
                //: jumps.append(dict)
                jumps.append(dict)
            }
            //: uidStr.deleteCharacters(in: NSRange(location: uidStr.length-1, length: 1))
            uidStr.deleteCharacters(in: NSRange(location: uidStr.length - 1, length: 1))
        }

        //: var msgInfo: [String: Any] = ["content": temStr, "msgType": "txt", "isGroup": "1"]
        var msgInfo: [String: Any] = [String(bytes: main_sharedPath.map{translationClear(status: $0)}, encoding: .utf8)!: temStr, (String(appViewUserFormat.prefix(5)) + String(dataBagName)): "txt", (String(kGreetMsg)): "1"]
        //: if jumps.count > 0 {
        if jumps.count > 0 {
            //: msgInfo["jumps"] = jumps
            msgInfo[(String(show_windowModelKey))] = jumps
        }

        // 接口参数
        //: var paramsDic: [String: Any] = ["message": temStr,
        var paramsDic: [String: Any] = [String(bytes: show_labUrl.map{$0^145}, encoding: .utf8)!: temStr,
                                        //: "type": "TxtMsg",
                                        String(bytes: data_sizeId.map{$0^142}, encoding: .utf8)!: (kTheContent.uppercased() + String(showTableContent.prefix(5))),
                                        //: "roomId": self.infoModel.roomId]
                                        (show_dataId.replacingOccurrences(of: "full", with: "r") + String(data_effectMessage.suffix(5))): self.infoModel.roomId]
        //: if uidStr.length > 0 {
        if uidStr.length > 0 {
            //: paramsDic["toUid"] = uidStr
            paramsDic[(String(const_hiddenId))] = uidStr
        }

        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(data_sizeValue.suffix(7))): msgInfo, String(bytes: userAddMsg.reversed(), encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = DirectionReactiveCompatible.impingeOpen(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            videoSend(msgData: cellData, reSend: false)
        }
    }

    /// 发送图片消息
    /// - Parameter img: 图片
    //: private func func__sendPictureMsg(img: UIImage) {
    private func directBetween(img: UIImage) {
        //: let imagePath = FountainheadCellData.getChatImagePath(image: img)
        let imagePath = FountainheadCellData.chat(image: img)
        //: let msgInfo: [String: Any] = ["contentType": "ImgMsg", "msgType": "img", "imageUri": imagePath]
        let msgInfo: [String: Any] = [String(bytes: show_makeTitle.map{toPath(manager: $0)}, encoding: .utf8)!: (String(data_colorPlayerValue.prefix(6))), (String(appViewUserFormat.prefix(5)) + String(dataBagName)): "img", String(bytes: k_tempVoicePath.map{analogDigitalConverter(key: $0)}, encoding: .utf8)!: imagePath]
        // 接口参数
        //: let paramsDic: [String: Any] = ["pic": imagePath,
        let paramsDic: [String: Any] = ["pic": imagePath,
                                        //: "type": "ImgMsg",
                                        String(bytes: data_sizeId.map{$0^142}, encoding: .utf8)!: (String(data_colorPlayerValue.prefix(6))),
                                        //: "roomId": self.infoModel.roomId]
                                        (show_dataId.replacingOccurrences(of: "full", with: "r") + String(data_effectMessage.suffix(5))): self.infoModel.roomId]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(data_sizeValue.suffix(7))): msgInfo, String(bytes: userAddMsg.reversed(), encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = DirectionReactiveCompatible.impingeOpen(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            videoSend(msgData: cellData, reSend: false)
        }
    }

    /// 发送消息
    /// - Parameters:
    ///   - msgData: 消息数据结构
    ///   - reSend: 是否重发【目前未使用该字段】
    //: private func func__sendPriveteMsg(msgData: AddCellData, reSend: Bool) {
    private func videoSend(msgData: AddCellData, reSend _: Bool) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            explain(showMsg: dataStatusTitle)
            //: return
            return
        }
        // 插入本地
        //: self.msgView.sendCustomMessage(msgData)
        self.msgView.instance(msgData)
        // 处理请求参数
        //: var params: Dictionary<String, Any> = msgData.msgModel.params as! Dictionary<String, Any>
        var params: [String: Any] = msgData.msgModel.params as! [String: Any]
//        params["toUid"] = self.targetId
        //: if params.keys.contains("pic") { // 图片
        if params.keys.contains("pic") { // 图片
            //: let imageStr: String = params["pic"] as! String
            let imageStr: String = params["pic"] as! String
            //: do {
            do {
                //: let data: Data = try Data.init(contentsOf: URL.init(fileURLWithPath: imageStr))
                let data: Data = try Data(contentsOf: URL(fileURLWithPath: imageStr))
                //: params["pic"] = data
                params["pic"] = data
                //: } catch {
            } catch {
                //: printLog(message: error)
                printLog(message: error)
            }
        }

        // 发送消息接口
        //: StreamRequestTool.req_SendRoomMsg(param: params, completion: { succeed, result, errorModel in
        StreamRequestTool.contriveFromBe(param: params, completion: { succeed, result, errorModel in
            //: if !succeed {
            if !succeed {
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.auspicate(msgData, take: nil, cut: NSNumber(value: 0), custom: .Msg_Status_Fail)
            
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                self.anyScreen(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                //: return
                return
            }

            //: let dataDict = result as! [String: Any]
            let dataDict = result as! [String: Any]
            //: let msgInfo = dataDict["msgInfo"] as! [String: Any]
            let msgInfo = dataDict[(String(data_sizeValue.suffix(7)))] as! [String: Any]

            // 更新本地消息信息
            //: self.msgView.changeCustomMsg(msgData, msgId: msgInfo["msgId"] as? String, msgIncome: NSNumber(value: 0), status: .Msg_Status_Succ)
            self.msgView.auspicate(msgData, take: msgInfo[(String(mainGenderName))] as? String, cut: NSNumber(value: 0), custom: .Msg_Status_Succ)

            //: if dataDict.keys.contains("totalMfCoin") {
            if dataDict.keys.contains((String(kStyleData.suffix(7)) + String(show_nameMsg.suffix(4)))) {
                //: let totalMfCoin: NSNumber = dataDict["totalMfCoin"] as! NSNumber
                let totalMfCoin: NSNumber = dataDict[(String(kStyleData.suffix(7)) + String(show_nameMsg.suffix(4)))] as! NSNumber
                //: IconContainerAppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
                IconContainerAppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            }
            //: })
        })
    }

    /// 处理发送失败情况
    //: private func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool) {
    private func anyScreen(errorCode: Int, errorStr: String, isGift _: Bool, isResend _: Bool) {
        //: if errorCode == 50203 {
        if errorCode == 50203 {
            //: guard IconContainerAppManager.share.loginUserMode.status != 1 else {
            guard IconContainerAppManager.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    explain(showMsg: errorStr)
                }
                //: return
                return
            }

            //: CombineAccountEnclaveDecisionMakerThen.share.func__jumpToWebRecharge(clickEvent: clickSendingiftsButton, sufficient: false)
            CombineAccountEnclaveDecisionMakerThen.share.toMake(clickEvent: noti_beautyVideoStr, sufficient: false)
            //: msgInputView.giftView.dismissView()
            msgInputView.giftView.statusComponent()
            //: insertTipMsg_Nobalance(tipStr: errorStr)
            eventTitleCell(tipStr: errorStr)
            //: }else if errorCode == 10888 {
        } else if errorCode == 10888 {
            //: let view = TalkingLiveVipSubscribePopView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let view = OverdoPopView(frame: CGRect(x: 0, y: 0, width: main_colorData, height: constRecordServerKey))
            //: view.show()
            view.addShow()
            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                explain(showMsg: errorStr)
            }
        }
    }
}

// MARK: - 输入框

//: extension TalkingGroupChatController: ChatInputViewDelegate {
extension SwarmThen: TipThen {
    //: func func__askforGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func valueSize(giftModel _: PointHandyJSON, giftNum _: String) {}

    /// 输入框输入内容停顿超过1秒
    //: func func_inputContentIntervalOneSecond() {}
    func cardinal() {}

    /// 输入框@ 在线列表用户
    //: func func__selectMentionUser(range: NSRange) {
    func sharedEachRange(range: NSRange) {
        //: let vc = TalkingChatRoomOnlineListController()
        let vc = IconViewDelegate()
        //: vc.roomID = self.infoModel.roomId
        vc.roomID = self.infoModel.roomId
        //: vc.titleType = .callNumber
        vc.titleType = .callNumber
        //: vc.selectedBlock = {[weak self] dict in
        vc.selectedBlock = { [weak self] dict in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.atFlag = true
            self.atFlag = true
            //: self.msgInputView.receiveAtInfo(range: range, dict: dict)
            self.msgInputView.constraintView(range: range, dict: dict)
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
        }
        //: self.navigationController?.pushViewController(vc, animated: true)
        self.navigationController?.pushViewController(vc, animated: true)
    }

    //: func func__selectMoreFunAction(actionType: MoreActionType) {
    func kind(actionType: VideoViewMultiplierTarget) {
        //: switch actionType {
        switch actionType {
        //: case .Photo:
        case .Photo:
            //: func__choicePhotos()
            indexStrength()

        //: default:
        default:
            //: break
            break
        }
    }

    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {}
    func possibility(giftModel _: PointHandyJSON, giftNum _: String) {}

    //: func func__sendChatRoomGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, model: TalkingChatRoomMemberModel) {
    func sprechgesang(giftModel: PointHandyJSON, giftNum: String, model: ChaseReactiveCompatible) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            explain(showMsg: dataStatusTitle)
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["toUid"] =  model.uid
        params[(String(const_hiddenId))] = model.uid
        //: params["giftId"] = giftModel.gid
        params[(String(const_barKey.prefix(6)))] = giftModel.gid
        //: params["giftNum"] = giftNum
        params[(String(userDismissPath.prefix(7)))] = giftNum
        //: params["roomId"] = infoModel.roomId
        params[(show_dataId.replacingOccurrences(of: "full", with: "r") + String(data_effectMessage.suffix(5)))] = infoModel.roomId
        //: if giftModel.pkgItemsetId != nil {
        if giftModel.pkgItemsetId != nil {
            //: params["pkgItemsetId"] = giftModel.pkgItemsetId
            params[(String(notiCenterValue.suffix(7)) + String(notiSignPath.prefix(5)))] = giftModel.pkgItemsetId
        }

        // 送礼接口
        //: StreamRequestTool.req_SendGiftChatRoom(param: params, completion: { succeed, result, errorModel in
        StreamRequestTool.addCompletion(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                self.anyScreen(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if giftModel.pkgItemsetId != nil {
            if giftModel.pkgItemsetId != nil {
                //: self.msgInputView.giftView.bags_removeGiftPackage(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.msgInputView.giftView.rivetLineToPatronym(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, model: model, isFromInputView: true)
            self.equilateral(extral: result as! [String: Any?], model: model, isFromInputView: true)
            //: })
        })
    }

    //: func heightToBottomChanged(heightToBottom: CGFloat) {
    func toAdd(heightToBottom: CGFloat) {
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.bottom.equalTo(self.view).offset(-heightToBottom)
            make.bottom.equalTo(self.view).offset(-heightToBottom)
        }
        //: someoneView.snp.updateConstraints { make in
        someoneView.snp.updateConstraints { make in
            //: make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            make.bottom.equalTo(msgInputView.snp.top).offset(-103)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.actionBottom(true)
            self.msgView.actionBottom(true)
        }
    }

    //: func inputViewHeightChanged(height: CGFloat) {
    func black(height: CGFloat) {
        //: self.msgInputView.snp.updateConstraints { make in
        self.msgInputView.snp.updateConstraints { make in
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: self.view.needsUpdateConstraints()
        self.view.needsUpdateConstraints()
        //: self.view.updateConstraintsIfNeeded()
        self.view.updateConstraintsIfNeeded()
        //: UIView.animate(withDuration: 0.3, animations: {
        UIView.animate(withDuration: 0.3, animations: {
            //: self.view.layoutIfNeeded()
            self.view.layoutIfNeeded()
            //: })
        })
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.actionBottom(true)
            self.msgView.actionBottom(true)
        }
    }

    //: @objc override func func__TapGestureRecognizer(_ tap: UITapGestureRecognizer) {
    @objc override func screenEqual(_ tap: UITapGestureRecognizer) {
        //: super.func__TapGestureRecognizer(tap)
        super.screenEqual(tap)
    }

    //: func func__starRecordBtnDown() {}
    func send() {}

    //: func func__sendVoiceMsg(voicePath: String, voiceTime: Int) {}
    func buttonDate(voicePath _: String, voiceTime _: Int) {}
}

// MARK: - 选择图片

//: extension TalkingGroupChatController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
extension SwarmThen: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
    //: private func func__choicePhotos() {
    private func indexStrength() {
        //: TalkingPermissionTool.func__openAlbumServiceWithBlock(true) { (isOpen: Bool) in
        CellPermissionTool.targetEvent(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: true, allowVideo: false)
                let vc = StateThen.emptyDown(maxCount: 1, allowPhoto: true, allowVideo: false)
                //: vc.modalPresentationStyle = .fullScreen
                vc.modalPresentationStyle = .fullScreen
                //: CombineAccountEnclaveDecisionMakerThen.share.func__getCurrentActivityVC()?.present(vc, animated: true)
                CombineAccountEnclaveDecisionMakerThen.share.towardMagnitudeVc()?.present(vc, animated: true)
                //: vc.didFinishPickingPhotosHandle = { [weak self] ( photos: Array<UIImage>?,
                vc.didFinishPickingPhotosHandle = { [weak self] (photos: [UIImage]?,
                                                                 //: assets: Array<Any>?,
                                                                 _: [Any]?,
                                                                 //: isSelectOriginalPhoto: Bool?)  in
                                                                 _: Bool?) in
                        //: guard let self = self else {return}
                        guard let self = self else { return }
                        //: if photos!.count > 0 {
                        if photos!.count > 0 {
                            //: self.func__sendPictureMsg(img: photos![0])
                            self.directBetween(img: photos![0])
                        }
                }
            }
        }
    }

    //: private func func__camaraPhotos() {
    private func sum() {
        //: TalkingPermissionTool.func__openCaptureDeviceServiceWithBlock(true) { (isOpen: Bool) in
        CellPermissionTool.viewBlock(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let imagePicker = UIImagePickerController()
                let imagePicker = UIImagePickerController()
                //: imagePicker.sourceType = .camera
                imagePicker.sourceType = .camera
                //: imagePicker.delegate = self
                imagePicker.delegate = self
                //: self.present(imagePicker, animated: true, completion: {})
                self.present(imagePicker, animated: true, completion: {})
            }
        }
    }

    //: func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
    func imagePickerController(_: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
        //: self.dismiss(animated: true) {
        self.dismiss(animated: true) {
            //: if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
            if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
                //: self.func__sendPictureMsg(img: photo)
                self.directBetween(img: photo)
            }
        }
    }

    //: func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
    func imagePickerControllerDidCancel(_: UIImagePickerController) {
        //: self.dismiss(animated: true, completion: nil)
        self.dismiss(animated: true, completion: nil)
    }
}

// MARK: - 处理@ 消息

//: extension TalkingGroupChatController {
extension SwarmThen {
    /// 接收到群聊@ 消息
    /// - Parameter info: @消息数据
    //: func handleMentionMsg(info: [String: Any]) {
    func appInfo(info: [String: Any]) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: guard info["roomId"] as! String == self.conversationData.groupID else { return }
            guard info[(show_dataId.replacingOccurrences(of: "full", with: "r") + String(data_effectMessage.suffix(5)))] as! String == self.conversationData.groupID else { return }
            //: if self.mentionInfoArr.count >= 5 {
            if self.mentionInfoArr.count >= 5 { // 只保留最新5条@ 消息
                //: self.mentionInfoArr.removeFirst()
                self.mentionInfoArr.removeFirst()
            }
            //: self.mentionInfoArr.append(info)
            self.mentionInfoArr.append(info)
            //: self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false:true
            self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false : true
        }
    }
}

// MARK: - 提示消息

//: extension TalkingGroupChatController {
extension SwarmThen {
    //: func func__TipsNotification() {
    func viewShared() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(insertTipsNotif(notification:)),
                                               selector: #selector(example(notification:)),
                                               //: name: CHAT_TIPS_TEXT_NOTIFICATION,
                                               name: kBottomStr,
                                               //: object: nil)
                                               object: nil)
    }

    //: @objc func insertTipsNotif(notification: NSNotification) {
    @objc func example(notification: NSNotification) {
        //: let userinfo = notification.userInfo as! [String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let celldata: TalkingChatTipsMsgCellData  = userinfo["msg"] as! TalkingChatTipsMsgCellData
        let celldata: SuccessAppThen = userinfo["msg"] as! SuccessAppThen

        //: self.msgView.uiMsgs.add(celldata)
        self.msgView.uiMsgs.add(celldata)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.actionBottom(true)
        self.msgView.actionBottom(true)
    }

    /// 插入提示消息
    /// - Parameters:
    ///   - tipStr: 提示消息文本
    ///   - needInsertLocal: 是否需要插入本地数据库（默认：是）
    //: func insertTipMsg_Nobalance(tipStr: String, needInsertLocal: Bool = true) {
    func eventTitleCell(tipStr: String, needInsertLocal: Bool = true) {
        //: let msgInfo = ["content": tipStr, "msgType": "tips"]
        let msgInfo = [String(bytes: main_sharedPath.map{translationClear(status: $0)}, encoding: .utf8)!: tipStr, (String(appViewUserFormat.prefix(5)) + String(dataBagName)): (String(userToUrl))]
        //: var message: AddCellData?
        var message: AddCellData?
        //: if needInsertLocal == true {
        if needInsertLocal == true { // 需要插入本地
            //: } else {
        } else {
            //: message = TUISocialChatManager.handleTXMessageSend(withExtral: msgInfo, isSender: true, isTip: true)
            message = DirectionReactiveCompatible.impingeOpen(withExtral: msgInfo, isSender: true, isTip: true)
        }

        //: guard let message = message else { return }
        guard let message = message else { return }
        //: self.msgView.uiMsgs.add(message)
        self.msgView.uiMsgs.add(message)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.actionBottom(true)
        self.msgView.actionBottom(true)
    }

    /// 公共聊天室插入系统提示消息
    //: func func__chatRoom_insertTipMsg() {
    func duringShared() {
        // 延时，内存插入
        //: tipsTimer = Timer.scheduledTimer(withTimeInterval: 5, repeats: false, block: {[weak self] time in
        tipsTimer = Timer.scheduledTimer(withTimeInterval: 5, repeats: false, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard IconContainerAppManager.share.appUserConfigMode.rTips.count > 0 else { return }
            guard IconContainerAppManager.share.appUserConfigMode.rTips.count > 0 else { return }
            //: self.insertTipMsg_Nobalance(tipStr: IconContainerAppManager.share.appUserConfigMode.rTips, needInsertLocal: false)
            self.eventTitleCell(tipStr: IconContainerAppManager.share.appUserConfigMode.rTips, needInsertLocal: false)
            //: })
        })
    }
}

// MARK: - 动效

//: extension TalkingGroupChatController {
extension SwarmThen {
    /// 插入礼物消息，播放动效
    /// - Parameters:
    ///   - extral: 送礼接口返回结果
    ///   - isFromInputView: 是否来自输入框送礼入口
    //: func func__insertGiftMessageWithExtral(extral: Dictionary<String, Any?>, model: TalkingChatRoomMemberModel, isFromInputView: Bool) {
    func equilateral(extral: [String: Any?], model: ChaseReactiveCompatible, isFromInputView: Bool) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
        var msgInfoDic: [String: Any] = extralInfo[(String(data_sizeValue.suffix(7)))] as! [String: Any]
        /// 用于本地判断celldata类型
        //: msgInfoDic["isGroup"] = "1"
        msgInfoDic[(String(kGreetMsg))] = "1"
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic[(String(appViewUserFormat.prefix(5)) + String(dataBagName))] = (show_makeHiddenUrl.replacingOccurrences(of: "user", with: "if"))
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo[(String(data_sizeValue.suffix(7)))] = msgInfoDic

        //: var toUser = ["uid": model.uid,
        var toUser = ["uid": model.uid,
                      //: "nickname": model.nickname,
                      (showFillPath.replacingOccurrences(of: "clear", with: "i") + String(appFinishStr)): model.nickname,
                      //: "headPic": model.headPic]
                      String(bytes: k_layerFormat.map{quantitativeRelation(position: $0)}, encoding: .utf8)!: model.headPic]

        //: if model.nickname == "All Numbers".localized {
        if model.nickname == (String(appFacePath) + String(constHalfScreenPath)).localized {
            //: if extralInfo.keys.contains("gift") != false {
            if extralInfo.keys.contains((show_makeHiddenUrl.replacingOccurrences(of: "user", with: "if"))) != false {
                //: let giftdic = extralInfo["gift"] as! Dictionary<String, Any>
                let giftdic = extralInfo[(show_makeHiddenUrl.replacingOccurrences(of: "user", with: "if"))] as! [String: Any]
                //: toUser["nickname"] = "\(giftdic["sendNum"] ?? "1")"
                toUser[(showFillPath.replacingOccurrences(of: "clear", with: "i") + String(appFinishStr))] = "\(giftdic[(String(app_cleanShowCenterId) + String(notiListKey))] ?? "1")"
            }
        }
        //: extralInfo["toUser"] = toUser
        extralInfo[(String(showByNameUrl.prefix(5)) + noti_formatName.replacingOccurrences(of: "table", with: "r"))] = toUser

        //: let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        let cellData = DirectionReactiveCompatible.impingeOpen(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        //: self.msgView.sendCustomMessage(cellData)
        self.msgView.instance(cellData)
        //: var msgIncome: NSNumber = NSNumber.init(floatLiteral: 0)
        var msgIncome = NSNumber(floatLiteral: 0)
        //: if msgInfoDic.keys.contains("msgIncome") {
        if msgInfoDic.keys.contains((String(kReValue.prefix(4)) + notiFileData.replacingOccurrences(of: "share", with: "n"))) {
            //: msgIncome = msgInfoDic["msgIncome"] as! NSNumber
            msgIncome = msgInfoDic[(String(kReValue.prefix(4)) + notiFileData.replacingOccurrences(of: "share", with: "n"))] as! NSNumber
        }
        //: self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        self.msgView.auspicate(cellData, take: msgInfoDic[(String(mainGenderName))] as? String, cut: msgIncome, custom: .Msg_Status_Succ)
        
        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any], model: model)
        prolusion(giftDic: extral as [String: Any], model: model)
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((String(kStyleData.suffix(7)) + String(show_nameMsg.suffix(4)))) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(String(kStyleData.suffix(7)) + String(show_nameMsg.suffix(4)))] as! NSNumber
            //: IconContainerAppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            IconContainerAppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: if isFromInputView {
        if isFromInputView {
            //: msgInputView.giftView.updateGiftInfo(needReload: false, mf_coin: IconContainerAppManager.share.loginUserMode.mf_coin)
            msgInputView.giftView.age(needReload: false, mf_coin: IconContainerAppManager.share.loginUserMode.mf_coin)
        }
        //: msgInputView.giftView.saveRecentMembers()
        msgInputView.giftView.addTap()
    }

    //: func initCachEffectData() {
    func cach() {
        //: giftEffectView.initCachAnimatData(tagetID: infoModel.roomId)
        giftEffectView.insertImageTo(tagetID: infoModel.roomId)
    }

    //: func sendGiftByMeToEfffecct(giftDic: Dictionary<String, Any>, model: TalkingChatRoomMemberModel) {
    func prolusion(giftDic: [String: Any], model: ChaseReactiveCompatible) {
        //: guard let giftModel = giftEffectView.chatRoomDisposeReceiveGiftMsg(giftMessageDic: (giftDic as NSDictionary) as! Dictionary<String, Any>, model: model) else { return }
        guard let giftModel = giftEffectView.thenAddModel(giftMessageDic: (giftDic as NSDictionary) as! [String: Any], model: model) else { return }
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray.init()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in ObtrudeUponThen.share.front() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [SessionMeasurable]
        }
        //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel)
        giftTrackView.menuAcross(model: giftModel)

        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.pastRow(dict: giftDic)
    }

    //: @objc func addGiftEffectModelArr(giftArr: Array<Any>) {
    @objc func statusArr(giftArr: [Any]) {
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray.init()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in ObtrudeUponThen.share.front() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [SessionMeasurable]
        }
        //: for i in 0..<giftArr.count {
        for i in 0 ..< giftArr.count {
            //: let giftModel = giftArr[i]
            let giftModel = giftArr[i]
            //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel as! TalkingGiftMsgAnimatModel)
            giftTrackView.menuAcross(model: giftModel as! ViewModelType)
        }

        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.bigness(modelArr: giftArr)
    }
}

// MARK: - Layout

//: extension TalkingGroupChatController {
extension SwarmThen {
    /// 创建UI
    //: private func createUI() {
    private func activeAgent() {
        //: self.addChild(msgView)
        self.addChild(msgView)
        //: view.addSubview(msgView.view)
        view.addSubview(msgView.view)
        //: view.addSubview(someoneView)
        view.addSubview(someoneView)
        //: view.addSubview(giftEffectView)
        view.addSubview(giftEffectView)
        //: view.insertSubview(giftTrackView, belowSubview: giftEffectView)
        view.insertSubview(giftTrackView, belowSubview: giftEffectView)
        //: view.addSubview(msgInputView)
        view.addSubview(msgInputView)
        //: view.addSubview(navView)
        view.addSubview(navView)
        //: msgView.targetID = self.conversationData.groupID
        msgView.targetID = self.conversationData.groupID
    }

    /// 约束
    //: private func createUIConstraints() {
    private func addUp() {
        //: navView.snp.makeConstraints { make in
        navView.snp.makeConstraints { make in
            //: make.leading.trailing.top.equalTo(view)
            make.leading.trailing.top.equalTo(view)
            //: make.height.equalTo(StatusBarNavigationBarHeight)
            make.height.equalTo(data_halfMsg)
        }
        //: msgInputView.snp.makeConstraints { make in
        msgInputView.snp.makeConstraints { make in
            //: make.leading.trailing.bottom.equalTo(view)
            make.leading.trailing.bottom.equalTo(view)
            //: make.height.equalTo(msgInputView.BackViewHeight)
            make.height.equalTo(msgInputView.BackViewHeight)
        }
        //: msgView.view.snp.makeConstraints { make in
        msgView.view.snp.makeConstraints { make in
            //: make.leading.trailing.equalTo(view)
            make.leading.trailing.equalTo(view)
            //: make.top.equalTo(StatusBarNavigationBarHeight)
            make.top.equalTo(data_halfMsg)
            //: make.bottom.equalTo(msgInputView.snp.top)
            make.bottom.equalTo(msgInputView.snp.top)
        }
        //: someoneView.snp.makeConstraints { make in
        someoneView.snp.makeConstraints { make in
            //: make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            //: make.trailing.equalToSuperview()
            make.trailing.equalToSuperview()
            //: make.size.equalTo(CGSize(width: 130, height: 37))
            make.size.equalTo(CGSize(width: 130, height: 37))
        }
        //: giftEffectView.snp.makeConstraints { make in
        giftEffectView.snp.makeConstraints { make in
            //: make.leading.trailing.top.bottom.equalTo(view)
            make.leading.trailing.top.bottom.equalTo(view)
        }
        //: giftTrackView.snp.makeConstraints { make in
        giftTrackView.snp.makeConstraints { make in
            //: make.leading.equalTo(view)
            make.leading.equalTo(view)
            //: make.height.equalTo(180)
            make.height.equalTo(180)
            //: make.bottom.equalTo(-actualHeight(h: 220)-kDeviceSafeBottomHeight)
            make.bottom.equalTo(-actualHeight(h: 220) - noti_saveFormat)
        }
    }

    /// 事件绑定
    //: func bindInteraction() {
    func sumeractionRecord() {
        // 返回
        //: navView.backButton.rx.tap.subscribe(onNext: { [weak self] in
        navView.backButton.rx.tap.subscribe(onNext: { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
            //: }).disposed(by: disposeBag)
        }).disposed(by: disposeBag)

        // 在线列表
        //: navView.onlineListBtn.rx.tap.subscribe(onNext: { [weak self] in
        navView.onlineListBtn.rx.tap.subscribe(onNext: { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let vc = TalkingChatRoomOnlineListController()
            let vc = IconViewDelegate()
            //: vc.roomID = self.infoModel.roomId
            vc.roomID = self.infoModel.roomId
            //: vc.titleType = .normal
            vc.titleType = .normal
            //: self.navigationController?.pushViewController(vc, animated: true)
            self.navigationController?.pushViewController(vc, animated: true)
            //: }).disposed(by: disposeBag)
        }).disposed(by: disposeBag)

        // 群聊someone@ me 点击事件
        //: someoneView.mentionBtn.rx.tap
        someoneView.mentionBtn.rx.tap
            //: .throttle(.milliseconds(1000), scheduler: MainScheduler.instance)
            .throttle(.milliseconds(1000), scheduler: MainScheduler.instance)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                // 找到当前消息，滚动到指定位置
                //: guard let dict = self.mentionInfoArr.first else { return }
                guard let dict = self.mentionInfoArr.first else { return }
                //: for (index, cellData) in self.msgView.uiMsgs.enumerated() {
                for (index, cellData) in self.msgView.uiMsgs.enumerated() {
                    //: if (cellData as! AddCellData).isKind(of: ScreenThen.self) {
                    if (cellData as! AddCellData).isKind(of: ScreenThen.self) {
                        //: let groupCellData = cellData as! ScreenThen
                        let groupCellData = cellData as! ScreenThen
                        //: if groupCellData.msgID == dict["msgId"] as! String {
                        if groupCellData.msgID == dict[(String(mainGenderName))] as! String {
                            //: self.msgView.tableView.scroll(toRow: UInt(index), inSection: 0, at: .bottom, animated: true)
                            self.msgView.tableView.scroll(toRow: UInt(index), inSection: 0, at: .bottom, animated: true)
                            //: self.mentionInfoArr.removeFirst()
                            self.mentionInfoArr.removeFirst()
                            //: self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false:true
                            self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false : true
                        }
                    }
                }
                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)

        // 被挤下线时，退出群聊
        //: EditManageressV2Listener.shared.rx
        EditManageressV2Listener.shared.rx
            //: .observeWeakly(Bool.self, "isConnection")
            .observeWeakly(Bool.self, (String(k_endMsg.suffix(7)) + showLabelFormat.replacingOccurrences(of: "bottom", with: "on")))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: if value == false {
                if value == false {
                    //: self.navigationController?.popViewController(animated: true)
                    self.navigationController?.popViewController(animated: true)
                }
                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)
    }
}
