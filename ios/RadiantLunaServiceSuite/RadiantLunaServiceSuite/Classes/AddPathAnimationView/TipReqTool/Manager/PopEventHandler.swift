
//: Declare String Begin

/*: "PARTY_STATUS_CHANGE_NOTIFICATION" :*/
fileprivate let showModelMsg:[UInt8] = [0xa4,0xb5,0xa6,0xa0,0xad,0xab,0xa7,0xa0,0xb5,0xa0,0xa1,0xa7,0xab,0xb7,0xbc,0xb5,0xba,0xb3,0xb1,0xab,0xba,0xbb,0xa0,0xbd,0xb2,0xbd,0xb7,0xb5,0xa0,0xbd,0xbb,0xba]

				private func nookAndCranny(view num: UInt8) -> UInt8 {
					return num ^ 244
				}

/*: "redirectUrl" :*/
fileprivate let dataManagerStr:[Character] = ["r","e","d"]
fileprivate let appLineColorKey:String = "edit for stringirectUrl"

/*: "VOICEROOM: 进房模型解析失败。" :*/
fileprivate let data_contentBarPath:[Character] = ["V","O","I","C","E","R","O","O","M",":"," ","\u{8fdb}","房"]
fileprivate let appAwakeKey:String = "模型\u{89e3}析失败。"

/*: "Party room exception" :*/
fileprivate let user_playerName:String = "false import makeParty "
fileprivate let main_clickTitle:String = "xuserption"

/*: "male" :*/
fileprivate let noti_dropKey:[UInt8] = [0x49,0x45,0x48,0x41]

				private func enableMake(cell num: UInt8) -> UInt8 {
					return num ^ 36
				}

/*: "female" :*/
fileprivate let noti_mediumUrl:String = "FEMALE"

/*: "VOICEROOM: 群聊进房失败。groupId:  :*/
fileprivate let constScriptPath:[Character] = ["V","O","I","C","E","R","O","O","M",":"," ","群","聊","进","房"]
fileprivate let noti_sendKey:String = "失败。grostring event extension else main"

/*: "VOICEROOM: 切房模型解析失败。" :*/
fileprivate let userMethodImageValue:[Character] = ["V","O","I","C","E","R","O"]
fileprivate let const_managerVoiceUrl:[Character] = ["O","M",":"," ","切","房"]
fileprivate let noti_inputName:String = "模型解\u{6790}\u{5931}\u{8d25}\u{3002}"

/*: "VOICEROOM: 群聊切房失败。groupId:  :*/
fileprivate let user_rowValue:String = "VtoICE"
fileprivate let appTitleMsg:String = ": 群app切"
fileprivate let const_clearMessage:String = "roupId: view aspect"

/*: "errCode" :*/
fileprivate let data_giftUrl:[UInt8] = [0xc,0x1b,0x1b,0x2a,0x6,0xd,0xc]

				private func backButton(message num: UInt8) -> UInt8 {
					return num ^ 105
				}

/*: "custom_kickout_message" :*/
fileprivate let app_inputMessage:[Character] = ["c","u","s","t","o","m","_","k","i","c"]
fileprivate let constPopularTitle:[Character] = ["k","o","u","t","_","m","e","s","s","a","g","e"]

/*: "closeRoom" :*/
fileprivate let app_playMessage:String = "range bottom view buttoncloseRoom"

/*: "adminCloseRoom" :*/
fileprivate let mainIndexPath:[Character] = ["a","d","m","i","n","C","l","o"]
fileprivate let dataSpecialName:String = "seRoomflow index view array share"

/*: "The room is closed, please go to another room" :*/
fileprivate let userViewTitle:[UInt8] = [0xec,0x0,0xfd,0xb8,0xa,0x7,0x7,0x5,0xb8,0x1,0xb,0xb8,0xfb,0x4,0x7,0xb,0xfd,0xfc,0xc4,0xb8,0x8,0x4,0xfd,0xf9,0xb,0xfd,0xb8,0xff,0x7,0xb8,0xc,0x7,0xb8,0xf9,0x6,0x7,0xc,0x0,0xfd,0xa,0xb8,0xa,0x7,0x7,0x5]

				fileprivate func colorContent(center num: UInt8) -> UInt8 {
					let value = Int(num) - 152
					if value < 0 {
						return UInt8(value + 256)
					} else {
						return UInt8(value)
					}
				}

/*: "You were kicked out of the room" :*/
fileprivate let showOutsideKey:[UInt8] = [0x91,0xa7,0xbd,0xe8,0xbf,0xad,0xba,0xad,0xe8,0xa3,0xa1,0xab,0xa3,0xad,0xac,0xe8,0xa7,0xbd,0xbc,0xe8,0xa7,0xae,0xe8,0xbc,0xa0,0xad,0xe8,0xba,0xa7,0xa7,0xa5]

				private func labAction(model num: UInt8) -> UInt8 {
					return num ^ 200
				}

/*: "VOICEROOM：进房失败，roomID:  :*/
fileprivate let k_voiceText:String = "statusIC"
fileprivate let appChangeMsg:String = "M：进房\u{5931}败，"
fileprivate let appProgressVideoId:String = "to liveroomID: "

/*: , error:  :*/
fileprivate let show_playerStylePath:String = "chemistry let hide title, error"
fileprivate let kContentViewLetData:[Character] = [":"," "]

/*: , reason:  :*/
fileprivate let appOfName:[Character] = [","," ","r","e","a","s"]
fileprivate let userQuantityimateData:[Character] = ["o","n",":"," "]

/*: , extendedData:  :*/
fileprivate let data_tabValue:[Character] = [","," ","e","x","t","e","n","d","e","d","D","a","t"]
fileprivate let main_bottomStr:[Character] = ["a",":"," "]

/*: "VOICEROOM: 当前用户推流失败，roomID:  :*/
fileprivate let kCenterMsg:[Character] = ["V","O","I","C","E","R","O","O","M",":"," ","当","前","\u{7528}","户","推","流","失","\u{8d25}","，"]
fileprivate let data_playerKey:[Character] = ["r","o","o","m","I","D",":"," "]

/*: , errorCode:  :*/
fileprivate let const_bubbleName:String = "model gift normal old, erro"

/*: , streamID:  :*/
fileprivate let main_replyName:String = "self horse lab var, st"
fileprivate let constEnterTitle:[Character] = [" "]

/*: "An error occurred, please try again" :*/
fileprivate let appListSizeId:[UInt8] = [0x5d,0x8a,0x3c,0x81,0x8e,0x8e,0x8b,0x8e,0x3c,0x8b,0x7f,0x7f,0x91,0x8e,0x8e,0x81,0x80,0x48,0x3c,0x8c,0x88,0x81,0x7d,0x8f,0x81,0x3c,0x90,0x8e,0x95,0x3c,0x7d,0x83,0x7d,0x85,0x8a]

				fileprivate func inPlayer(shape num: UInt8) -> UInt8 {
					let value = Int(num) - 28
					if value < 0 {
						return UInt8(value + 256)
					} else {
						return UInt8(value)
					}
				}

/*: "VOICEROOM: 拉流失败，roomID:  :*/
fileprivate let showIntervalId:[Character] = ["V","O","I","C","E","R","O","O","M"]
fileprivate let userSaveMsg:[Character] = [":"," ","\u{62c9}"]
fileprivate let k_viewPath:String = "\u{6d41}失败，r"

/*: "VOICEROOM: 信令解析失败，command:  :*/
fileprivate let userAddText:[Character] = ["V","O","I","C","E","R","O","O","M",":"," ","信","令"]
fileprivate let dataImageTextId:[Character] = ["解","析","失","败","，","c","o","m","m","a","n","d",":"," "]

/*: , roomID:  :*/
fileprivate let constGuideName:[Character] = [","," ","r","o","o","m","I","D",":"," "]

/*: "data" :*/
fileprivate let k_valueMessage:[Character] = ["d","a","t","a"]

/*: "position" :*/
fileprivate let mainExtraSectionUrl:String = "frameosition"

/*: "xxxx--  :*/
fileprivate let kLabelFromTitle:String = "XX"
fileprivate let appIndexSendMakeName:String = "xx-- gift let share window model"

/*: "opType" :*/
fileprivate let noti_strengthKey:String = "conversation error var inopType"

/*: "party:changeMike" :*/
fileprivate let user_iconStyleName:String = "party:chfirst comment else"
fileprivate let data_cornerFollowPath:String = "app returnangeMike"

/*: "type" :*/
fileprivate let data_viewSharedId:[Character] = ["t","y","p","e"]

/*: "toPosition" :*/
fileprivate let kPlusData:[Character] = ["t","o","P","o","s","i"]
fileprivate let show_toModelUrl:[Character] = ["t","i","o","n"]

/*: "party:adminChangeMike" :*/
fileprivate let user_makePositionStr:String = "stylert"
fileprivate let noti_startStr:String = "inChanbar if view to invite"
fileprivate let constLogSizeName:[Character] = ["g","e","M","i","k","e"]

/*: "You were kicked off the seat" :*/
fileprivate let k_blockAddVarMsg:[Character] = ["Y","o","u"," ","w","e","r"]
fileprivate let const_indexValue:String = "e kicked live add info"
fileprivate let data_appId:String = "off data of head self"

/*: "party:changeCoin" :*/
fileprivate let user_nameKey:String = "painfoty"
fileprivate let data_actualEqualStr:String = "OIN"

/*: "party:closeStream" :*/
fileprivate let userViewMsg:[Character] = ["p","a","r","t"]
fileprivate let show_mainStr:String = "y:clodata return type video tap"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  PopEventHandler.swift
//  Adjust
//
//  Created by DouXiu on 2024/9/12.
//

//: import ZegoExpressEngine
import ZegoExpressEngine

/// 语聊房状态变更通知
//: public let PARTY_STATUS_CHANGE_NOTIFICATION = NSNotification.Name(rawValue: "PARTY_STATUS_CHANGE_NOTIFICATION")
public let show_buttonPath = NSNotification.Name(rawValue: String(bytes: showModelMsg.map{nookAndCranny(view: $0)}, encoding: .utf8)!)
/// 麦位数
//: public let MIKE_SEAT_COUNT = 8
public let kLiveName = 8

/// 语聊房角色
//: enum VoiceRoomRole: Int {
enum SourceSum: Int {
    //: case host = 1
    case host = 1 // 房主
    //: case audience = 2
    case audience = 2 // 观众
    //: case manager = 4
    case manager = 4 // 管理员
}

//: class TalkingVoiceRoomManager: NSObject {
class PopEventHandler: NSObject {
    /// 语聊房模型
    //: private(set) var partyModel = TalkingVoiceRoomModel()
    private(set) var partyModel = ImageTransformable()
    /// 是否正在语聊房中（状态变更时发送通知）
    //: private(set) var isParty: Bool = false {
    private(set) var isParty: Bool = false {
        //: didSet {
        didSet {
            //: NotificationCenter.default.post(name: PARTY_STATUS_CHANGE_NOTIFICATION, object: self)
            NotificationCenter.default.post(name: show_buttonPath, object: self)
        }
    }

    /// 是否小窗口模式
    //: private(set) var isSmallMode: Bool = false
    private(set) var isSmallMode: Bool = false
    /// singleton
    //: private static var _instance: TalkingVoiceRoomManager?
    private static var _instance: PopEventHandler?
    // 语聊房控制器
    //: private var voiceRoomVC: TalkingVoiceRoomViewController?
    private var voiceRoomVC: UserManagerDelegate?
    /// 麦位信息字典 [麦位 0~7：麦位模型]
    //: private var mikePositionDict = [Int: TalkingMikeListItemModel]()
    private var mikePositionDict = [Int: SupercedeModelType]()
    //: private let mplock = NSLock()
    private let mplock = NSLock()
    /// 正在进房请求中（防止多次调用）
    //: private var isEnterRoomReq = false
    private var isEnterRoomReq = false
    /// 麦克风操作请求中（防止多次调用）
    //: private var isMicReq = false
    private var isMicReq = false

    //: override init() {
    override init() {
        //: super.init()
        super.init()
        //: self.mikePositionDict = initMikePosition()
        self.mikePositionDict = thread()
    }

    //: class func shared() -> TalkingVoiceRoomManager {
    class func addMonth() -> PopEventHandler {
        //: guard _instance != nil else {
        guard _instance != nil else {
            //: _instance = TalkingVoiceRoomManager()
            _instance = PopEventHandler()
            //: return _instance!
            return _instance!
        }
        //: return _instance!
        return _instance!
    }

    //: class func destroy() {
    class func duringHead() {
        //: if TalkingVoiceRoomManager._instance != nil {
        if PopEventHandler._instance != nil {
            //: TalkingVoiceRoomManager._instance = nil
            PopEventHandler._instance = nil
        }
    }

    // MARK: - Lazy Load

    // 语聊房小窗口悬浮窗
    //: private lazy var miniView: TalkingVoiceRoomMiniView = {
    private lazy var miniView: MiniContainerView = {
        //: let mini = TalkingVoiceRoomMiniView.buildLiveMiniView()
        let mini = MiniContainerView.miniskirt()
        //: mini.isHidden = true
        mini.isHidden = true
        //: mini.tapGestureBlock = { [weak self] in
        mini.tapGestureBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.voiceRoom_goback()
            self.transomWindow()
        }
        //: return mini
        return mini
        //: }()
    }()
}

// MARK: - 操作麦位请求

//: extension TalkingVoiceRoomManager {
extension PopEventHandler {
    /// 房主/观众 麦位操作请求
    /// - Parameters:
    ///   - type: 1 上麦，2 下麦，3 切麦，4 开麦，5 闭麦
    ///   - position: 麦位
    ///   - toPosition: 目标麦位（切麦传值）
    //: func voiceRoom_reqChangeMike(type: Int, position: Int, toPosition: Int? = nil) {
    func vocalizationAllOver(type: Int, position: Int, toPosition: Int? = nil) {
        //: guard isMicReq == false else { return }
        guard isMicReq == false else { return }
        //: isMicReq = true
        isMicReq = true
        //: TalkingVoiceRoomReqTool.req_changeMike(roomId: partyModel.roomId,
        TipReqTool.theVoice(roomId: partyModel.roomId,
                                         //: uid: Int(IconContainerAppManager.share.loginUid) ?? 0,
                                         uid: Int(IconContainerAppManager.share.loginUid) ?? 0,
                                         //: type: type,
                                         type: type,
                                         //: position: position,
                                         position: position,
                                         //: toPos: toPosition)
                                         toPos: toPosition)
        //: { succeed, result, errorModel in
        { succeed, result, errorModel in
            //: self.isMicReq = false
            self.isMicReq = false
            //: guard succeed else {
            guard succeed else { // 失败提示
                //: if errorModel?.errorCode == 20001 {
                if errorModel?.errorCode == 20001 { // 已在麦位，开始推流，刷新UI
                    //: ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                    ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                    //: let streamID = self.voiceRoom_getStreamID()
                    let streamID = self.rankRemove()
                    //: ZegoExpressEngine.shared().startPublishingStream(streamID)
                    ZegoExpressEngine.shared().startPublishingStream(streamID)
                    //: guard let dict = result as? [String: Any] else { return }
                    guard let dict = result as? [String: Any] else { return }
                    //: guard let mikeModel = TalkingMikeListItemModel.deserialize(from: dict) else { return }
                    guard let mikeModel = SupercedeModelType.deserialize(from: dict) else { return }
                    //: self.updateMikePosition(position: position, model: mikeModel)
                    self.mikeStatus(position: position, model: mikeModel)
                }
                //: return
                return
            }

            // 成功后只做逻辑处理，在接收信令处统一刷新UI
            //: switch type {
            switch type {
            //: case 1:
            case 1: // 上麦成功
                //: ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                //: let streamID = self.voiceRoom_getStreamID()
                let streamID = self.rankRemove()
                //: ZegoExpressEngine.shared().startPublishingStream(streamID)
                ZegoExpressEngine.shared().startPublishingStream(streamID)

            //: case 2:
            case 2: // 下麦成功
                //: ZegoExpressEngine.shared().stopPublishingStream()
                ZegoExpressEngine.shared().stopPublishingStream()

            //: case 3:
            case 3: // 切麦成功
                //: break
                break

            //: case 4:
            case 4: // 开麦成功
                //: ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                ZegoExpressEngine.shared().mutePublishStreamAudio(false)

            //: case 5:
            case 5: // 闭麦成功
                //: ZegoExpressEngine.shared().mutePublishStreamAudio(true)
                ZegoExpressEngine.shared().mutePublishStreamAudio(true)

            //: default:
            default:
                //: break
                break
            }
        }
    }

    /// 房主对他人操作请求
    /// - Parameters:
    ///   - uid: 用户Id
    ///   - type: 1 锁麦，2 解锁麦，3 踢下麦，4 开麦，5 闭麦
    ///   - position: 麦位
    //: func voiceRoom_reqAdminChangeMike(uid: Int, type: Int, position: Int) {
    func exhibit(uid: Int, type: Int, position: Int) {
        //: guard isMicReq == false else { return }
        guard isMicReq == false else { return }
        //: isMicReq = true
        isMicReq = true
        //: TalkingVoiceRoomReqTool.req_adminChangeMike(roomId: partyModel.roomId,
        TipReqTool.positionButton(roomId: partyModel.roomId,
                                               //: uid: uid,
                                               uid: uid,
                                               //: type: type,
                                               type: type,
                                               //: position: position)
                                               position: position)
        //: { _, _, _ in
        { _, _, _ in
            //: self.isMicReq = false
            self.isMicReq = false
        }
    }

    /// 获取推流Id
    //: private func voiceRoom_getStreamID() -> String {
    private func rankRemove() -> String {
        //: return "p-\(IconContainerAppManager.share.loginUid)-\(partyModel.roomId)"
        return "p-\(IconContainerAppManager.share.loginUid)-\(partyModel.roomId)"
    }
}

// MARK: - 权限检测，开启/进入/切换 语聊房

//: extension TalkingVoiceRoomManager {
extension PopEventHandler {
    /// 检测状态并进入语聊房
    /// - Parameters:
    ///   - roomId: 房间Id（nil：创建房间；有值：进入房间）
    //: func voiceRoom_checkAndTurnOn(roomId: String? = nil) {
    func secret(roomId: String? = nil) {
        //: guard isParty == false else { return }
        guard isParty == false else { return }
        //: guard isEnterRoomReq == false else { return }
        guard isEnterRoomReq == false else { return }
        //: isEnterRoomReq = true
        isEnterRoomReq = true

        // 1. 权限检测
        //: TalkingPermissionTool.func__openRecordServiceWithBlock(true) { isOpen in
        CellPermissionTool.towardAction(true) { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.explain(showMsg: main_deviceName)
                //: self.isEnterRoomReq = false
                self.isEnterRoomReq = false
                //: return
                return
            }

            // 2. 接口是否禁播检测
            //: TalkingVoiceRoomReqTool.req_voiceRoomCheck(roomId: roomId) { succeed, result, errorModel in
            TipReqTool.anteroom(roomId: roomId) { succeed, result, errorModel in
                //: self.isEnterRoomReq = false
                self.isEnterRoomReq = false
                //: guard succeed else {
                guard succeed else {
                    //: if errorModel?.errorCode == 302 {
                    if errorModel?.errorCode == 302 { // 失败，跳转H5
                        //: if let result = result as? [String: Any] {
                        if let result = result as? [String: Any] {
                            //: CombineAccountEnclaveDecisionMakerThen.share.func__pushToWebVC(urlStr: result["redirectUrl"] as? String)
                            CombineAccountEnclaveDecisionMakerThen.share.sizeConfig(urlStr: result[(String(dataManagerStr) + String(appLineColorKey.suffix(8)))] as? String)
                        }
                        //: } else if errorModel?.errorMsg.count ?? 0 > 0 {
                    } else if errorModel?.errorMsg.count ?? 0 > 0 {
                        //: self.func__showStatusBarErrorMsg(showMsg: errorModel!.errorMsg)
                        self.explain(showMsg: errorModel!.errorMsg)
                    }
                    //: return
                    return
                }

                // 3. 是否正在音视频通话中
                //: guard TalkingSocketManager.shared.isName == false else {
                guard GiftCommentSocketDelegate.shared.isName == false else {
                    //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
                    self.explain(showMsg: user_topPath)
                    //: return
                    return
                }

                // 4. 校验成功，解析模型
                //: guard let model = TalkingVoiceRoomModel.deserialize(from: result as? Dictionary) else {
                guard let model = ImageTransformable.deserialize(from: result as? Dictionary) else {
                    //: UploadLogTool.writeLog(msg: "VOICEROOM: 进房模型解析失败。")
                    BuildLogTool.fileView(msg: (String(data_contentBarPath) + appAwakeKey))
                    //: self.func__showStatusBarErrorMsg(showMsg: "Party room exception".localized)
                    self.explain(showMsg: (String(user_playerName.suffix(6)) + "room e" + main_clickTitle.replacingOccurrences(of: "user", with: "ce")).localized)
                    //: return
                    return
                }
                //: self.partyModel = model
                self.partyModel = model

                // 5. 创建语聊房UI，同时调用sdk创建房间
                //: self.voiceRoomVC = TalkingVoiceRoomViewController()
                self.voiceRoomVC = UserManagerDelegate()
                //: self.getNavigationController()?.pushViewController(self.voiceRoomVC!, animated: true)
                self.characteristicRootOfASquareMatrix()?.pushViewController(self.voiceRoomVC!, animated: true)
                //: self.miniView.refreshMiniView()
                self.miniView.miniskirtFuturismReload()
                //: self.miniView.isHidden = true
                self.miniView.isHidden = true
                //: self.voiceRoom_create()
                self.phonation()
                //: UIApplication.shared.isIdleTimerDisabled = true
                UIApplication.shared.isIdleTimerDisabled = true // 防止息屏
                //: self.isParty = true
                self.isParty = true

                // 6. 埋点
                //: uploadRecord.uploadRecordEvent(eventID: "\(click_partyDetail_enter)_\(IconContainerAppManager.share.loginUserMode.sex == Gender.male.rawValue ? "male" : "female")")
                showVideoPath.referenceStr(eventID: "\(userBarDomainKey)_\(IconContainerAppManager.share.loginUserMode.sex == ThemeTitleScalarLiteral.male.rawValue ? String(bytes: noti_dropKey.map{enableMake(cell: $0)}, encoding: .utf8)! : (noti_mediumUrl.lowercased()))")

                // 7. 初始化弹幕群聊
                //: TalkingDanmuManager.joinLiveRoomDanmu(groupId: self.partyModel.chatGroupId, announcementMsg: self.partyModel.notification) { succeed in
                DataDanmuManager.humankindMake(groupId: self.partyModel.chatGroupId, announcementMsg: self.partyModel.notification) { succeed in
                    //: if succeed == false {
                    if succeed == false {
                        //: UploadLogTool.writeLog(msg: "VOICEROOM: 群聊进房失败。groupId: \(self.partyModel.chatGroupId).")
                        BuildLogTool.fileView(msg: (String(constScriptPath) + String(noti_sendKey.prefix(6)) + "upId: ") + "\(self.partyModel.chatGroupId).")
                        //: self.func__showStatusBarErrorMsg(showMsg: "Party room exception".localized)
                        self.explain(showMsg: (String(user_playerName.suffix(6)) + "room e" + main_clickTitle.replacingOccurrences(of: "user", with: "ce")).localized)
                        //: self.voiceRoom_releaseAllResource()
                        self.waysAndMeansResource()
                    }
                }
            }
        }
    }

    /// 切换语聊房
    /// - Parameters:
    ///   - roomId: 当前进入房间Id
    ///   - beforeRoomId: 上一房间Id
    //: func voiceRoom_switch(roomId: String, beforeRoomId: String) {
    func onOf(roomId: String, beforeRoomId: String) {
        //: guard isEnterRoomReq == false else { return }
        guard isEnterRoomReq == false else { return }
        //: isEnterRoomReq = true
        isEnterRoomReq = true
        // 1. 调用切房接口
        //: TalkingVoiceRoomReqTool.req_voiceRoomSwitch(roomId: roomId, beforeRoomId: beforeRoomId) { succeed, result, _ in
        TipReqTool.giftColor(roomId: roomId, beforeRoomId: beforeRoomId) { succeed, result, _ in
            //: self.isEnterRoomReq = false
            self.isEnterRoomReq = false
            //: guard succeed else { return }
            guard succeed else { return }
            // 2. 释放上个房间资源 (使用partyModel旧值)
            //: self.voiceRoomVC?.releaseAllResourceAndPop()
            self.voiceRoomVC?.listAndPop()

            // 3. 主播切房，需要关闭底部tabbar动效 (使用partyModel旧值)
            //: if String(self.partyModel.streamerInfo.uid) == IconContainerAppManager.share.loginUid {
            if String(self.partyModel.streamerInfo.uid) == IconContainerAppManager.share.loginUid {
                //: self.isParty = false
                self.isParty = false
            }

            // 4. 校验成功，解析模型（给partyModel重新赋值）
            //: guard let model = TalkingVoiceRoomModel.deserialize(from: result as? Dictionary) else {
            guard let model = ImageTransformable.deserialize(from: result as? Dictionary) else {
                //: UploadLogTool.writeLog(msg: "VOICEROOM: 切房模型解析失败。")
                BuildLogTool.fileView(msg: (String(userMethodImageValue) + String(const_managerVoiceUrl) + noti_inputName))
                //: self.func__showStatusBarErrorMsg(showMsg: "Party room exception".localized)
                self.explain(showMsg: (String(user_playerName.suffix(6)) + "room e" + main_clickTitle.replacingOccurrences(of: "user", with: "ce")).localized)
                //: return
                return
            }
            //: self.partyModel = model
            self.partyModel = model

            // 5. 创建语聊房UI，同时调用sdk切房
            //: self.voiceRoomVC = TalkingVoiceRoomViewController()
            self.voiceRoomVC = UserManagerDelegate()
            //: self.getNavigationController()?.pushViewController(self.voiceRoomVC!, animated: true)
            self.characteristicRootOfASquareMatrix()?.pushViewController(self.voiceRoomVC!, animated: true)
            //: self.miniView.refreshMiniView()
            self.miniView.miniskirtFuturismReload()
            //: self.miniView.isHidden = true
            self.miniView.isHidden = true
            //: self.voiceRoom_switch(beforeRoomId: beforeRoomId)
            self.outPress(beforeRoomId: beforeRoomId)
            //: self.isParty = true
            self.isParty = true
            //: self.isSmallMode = false
            self.isSmallMode = false

            // 6. 埋点
            //: uploadRecord.uploadRecordEvent(eventID: "\(click_partyDetail_enter)_\(IconContainerAppManager.share.loginUserMode.sex == Gender.male.rawValue ? "male" : "female")")
            showVideoPath.referenceStr(eventID: "\(userBarDomainKey)_\(IconContainerAppManager.share.loginUserMode.sex == ThemeTitleScalarLiteral.male.rawValue ? String(bytes: noti_dropKey.map{enableMake(cell: $0)}, encoding: .utf8)! : (noti_mediumUrl.lowercased()))")

            // 7. 初始化弹幕群聊
            //: TalkingDanmuManager.joinLiveRoomDanmu(groupId: self.partyModel.chatGroupId, announcementMsg: self.partyModel.notification) { succeed in
            DataDanmuManager.humankindMake(groupId: self.partyModel.chatGroupId, announcementMsg: self.partyModel.notification) { succeed in
                //: if succeed == false {
                if succeed == false {
                    //: UploadLogTool.writeLog(msg: "VOICEROOM: 群聊切房失败。groupId: \(self.partyModel.chatGroupId).")
                    BuildLogTool.fileView(msg: (user_rowValue.replacingOccurrences(of: "to", with: "O") + "ROOM" + appTitleMsg.replacingOccurrences(of: "app", with: "聊") + "房\u{5931}败。g" + String(const_clearMessage.prefix(8))) + "\(self.partyModel.chatGroupId).")
                    //: self.func__showStatusBarErrorMsg(showMsg: "Party room exception".localized)
                    self.explain(showMsg: (String(user_playerName.suffix(6)) + "room e" + main_clickTitle.replacingOccurrences(of: "user", with: "ce")).localized)
                    //: self.voiceRoom_releaseAllResource()
                    self.waysAndMeansResource()
                }
            }
        }
    }
}

// MARK: - Public Event

//: extension TalkingVoiceRoomManager {
extension PopEventHandler {
    /// 最小化语聊房
    //: func voiceRoom_mini() {
    func stud() {
        //: isSmallMode = true
        isSmallMode = true
        //: miniView.isHidden = false
        miniView.isHidden = false
        //: voiceRoomVC?.popCurrentViewController()
        voiceRoomVC?.videoStart()
    }

    /// 返回语聊房
    //: func voiceRoom_goback() {
    func transomWindow() {
        //: isSmallMode = false
        isSmallMode = false
        //: miniView.isHidden = true
        miniView.isHidden = true
        //: guard voiceRoomVC != nil else { return }
        guard voiceRoomVC != nil else { return }
        //: guard let currentVC = currentViewController() else { return }
        guard let currentVC = actionLatest() else { return }
        // 如果顶部视图是私信页，先返回列表，避免多个私信页消息同步问题
        //: if currentVC.isKind(of: TalkingPrivateChatController.self) {
        if currentVC.isKind(of: MakeReactiveCompatible.self) {
            //: if var vcArr = currentVC.navigationController?.viewControllers {
            if var vcArr = currentVC.navigationController?.viewControllers {
                //: vcArr.removeLast()
                vcArr.removeLast()
                //: vcArr.append(voiceRoomVC!)
                vcArr.append(voiceRoomVC!)
                //: currentVC.navigationController?.setViewControllers(vcArr, animated: true)
                currentVC.navigationController?.setViewControllers(vcArr, animated: true)
            }
            //: } else {
        } else {
            //: currentVC.navigationController?.pushViewController(voiceRoomVC!, animated: true)
            currentVC.navigationController?.pushViewController(voiceRoomVC!, animated: true)
        }
    }

    /// 销毁语聊房所有资源
    //: func voiceRoom_releaseAllResource() {
    func waysAndMeansResource() {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.isParty = false
            self.isParty = false // 有通知监听，需置为false
            //: UIApplication.shared.isIdleTimerDisabled = false
            UIApplication.shared.isIdleTimerDisabled = false // 恢复息屏
            //: self.miniView.removeFromSuperview()
            self.miniView.removeFromSuperview() // 移除小窗口
            //: self.voiceRoomVC?.releaseAllResourceAndPop()
            self.voiceRoomVC?.listAndPop() // 将语聊房视图从栈中移除并释放资源
            //: if self.partyModel.position >= 0 {
            if self.partyModel.position >= 0 { // 退房下麦
                //: self.voiceRoom_reqChangeMike(type: 2, position: self.partyModel.position)
                self.vocalizationAllOver(type: 2, position: self.partyModel.position)
            }
            //: ZegoExpressEngine.shared().logoutRoom()
            ZegoExpressEngine.shared().logoutRoom() // 退出房间
            //: ZegoExpressEngine.destroy(nil)
            ZegoExpressEngine.destroy(nil) // 销毁引擎
            //: TalkingVoiceRoomManager.destroy()
            PopEventHandler.duringHead() // 销毁当前单例
        }
    }
}

// MARK: - Private Event

//: extension TalkingVoiceRoomManager {
extension PopEventHandler {
    /// 创建语聊房
    //: private func voiceRoom_create() {
    private func phonation() {
        // 创建引擎
        //: let profile = ZegoEngineProfile()
        let profile = ZegoEngineProfile()
        //: profile.appID = MaleMacroDefine.getVoiceRoomAppId()
        profile.appID = MaleMacroDefine.schoolroomOmitId()
        //: profile.scenario = .standardChatroom
        profile.scenario = .standardChatroom
        //: ZegoExpressEngine.createEngine(with: profile, eventHandler: self)
        ZegoExpressEngine.createEngine(with: profile, eventHandler: self)
        // 进阶配置，进入房间
        //: let config = ZegoRoomConfig()
        let config = ZegoRoomConfig()
        //: config.token = partyModel.token
        config.token = partyModel.token
        //: let user = ZegoUser(userID: IconContainerAppManager.share.loginUid)
        let user = ZegoUser(userID: IconContainerAppManager.share.loginUid)
        //: ZegoExpressEngine.shared().loginRoom(partyModel.roomId, user: user, config: config)
        ZegoExpressEngine.shared().loginRoom(partyModel.roomId, user: user, config: config)
    }

    /// 切换语聊房
    /// - Parameter beforeRoomId: 上一房间Id
    //: private func voiceRoom_switch(beforeRoomId: String) {
    private func outPress(beforeRoomId: String) {
        //: let config = ZegoRoomConfig()
        let config = ZegoRoomConfig()
        //: config.token = partyModel.token
        config.token = partyModel.token
        //: ZegoExpressEngine.shared().switchRoom(beforeRoomId, toRoomID: partyModel.roomId, config: config)
        ZegoExpressEngine.shared().switchRoom(beforeRoomId, toRoomID: partyModel.roomId, config: config)
    }
}

// MARK: - ZegoEventHandler

//: extension TalkingVoiceRoomManager: ZegoEventHandler {
extension PopEventHandler: ZegoEventHandler {
    // MARK: - 进房/切房 监听回调

    /// 进房/切房 回调
    //: func onRoomStateChanged(_ reason: ZegoRoomStateChangedReason, errorCode: Int32, extendedData: [AnyHashable: Any], roomID: String) {
    func onRoomStateChanged(_ reason: ZegoRoomStateChangedReason, errorCode: Int32, extendedData: [AnyHashable: Any], roomID: String) {
        //: guard roomID == partyModel.roomId else { return }
        guard roomID == partyModel.roomId else { return }
        //: if errorCode == 0, reason == .logined {
        if errorCode == 0, reason == .logined { // 进房成功
            // 启动声浪等级监控
            //: ZegoExpressEngine.shared().startSoundLevelMonitor()
            ZegoExpressEngine.shared().startSoundLevelMonitor()
            // 开启音量稳定控制
            //: ZegoExpressEngine.shared().enableAEC(true)
            ZegoExpressEngine.shared().enableAEC(true)
            // 获取嘉宾列表接口
            //: TalkingVoiceRoomReqTool.req_getMikeList(roomId: partyModel.roomId) { list in
            TipReqTool.translation(roomId: partyModel.roomId) { list in
                // 保存麦位列表信息
                //: for (key, value) in list.enumerated() {
                for (key, value) in list.enumerated() {
                    //: self.modifyMikePosition(key: key, model: value)
                    self.modelPosition(key: key, model: value)
                }
                // 刷新麦位列表UI
                //: self.voiceRoomVC?.mikePositionView.refreshAllMikeView()
                self.voiceRoomVC?.mikePositionView.towardPlayer()
                // 房主首次创建房间，主动上麦
                //: if self.partyModel.userRole == VoiceRoomRole.host.rawValue, self.partyModel.isCreate == true {
                if self.partyModel.userRole == SourceSum.host.rawValue, self.partyModel.isCreate == true {
                    //: self.voiceRoom_reqChangeMike(type: 1, position: 0)
                    self.vocalizationAllOver(type: 1, position: 0)
                }
            }

            //: } else if reason == .logining || reason == .reconnectFailed || reason == .reconnecting || reason == .reconnected {
        } else if reason == .logining || reason == .reconnectFailed || reason == .reconnecting || reason == .reconnected {
            //: if errorCode == 0, reason == .reconnected {
            if errorCode == 0, reason == .reconnected { // 重连成功
                //: TalkingVoiceRoomReqTool.req_getMikeList(roomId: partyModel.roomId) { list in
                TipReqTool.translation(roomId: partyModel.roomId) { list in
                    // 更新麦位列表信息
                    //: for (key, value) in list.enumerated() {
                    for (key, value) in list.enumerated() {
                        //: self.modifyMikePosition(key: key, model: value)
                        self.modelPosition(key: key, model: value)
                        //: if String(value.uid) == IconContainerAppManager.share.loginUid {
                        if String(value.uid) == IconContainerAppManager.share.loginUid {
                            //: if value.mikeStatus == 1 {
                            if value.mikeStatus == 1 { // 房主闭麦
                                //: ZegoExpressEngine.shared().stopPublishingStream()
                                ZegoExpressEngine.shared().stopPublishingStream()
                                //: } else {
                            } else {
                                //: let streamID = self.voiceRoom_getStreamID()
                                let streamID = self.rankRemove()
                                //: ZegoExpressEngine.shared().startPublishingStream(streamID)
                                ZegoExpressEngine.shared().startPublishingStream(streamID)
                                //: let isMute = value.mikeStatus == 0 ? true : false
                                let isMute = value.mikeStatus == 0 ? true : false
                                //: ZegoExpressEngine.shared().mutePublishStreamAudio(isMute)
                                ZegoExpressEngine.shared().mutePublishStreamAudio(isMute)
                            }
                        }
                    }
                    // 刷新麦位列表UI
                    //: self.voiceRoomVC?.mikePositionView.refreshAllMikeView()
                    self.voiceRoomVC?.mikePositionView.towardPlayer()
                }
                //: } else if errorCode != 0 {
            } else if errorCode != 0 {
                // 进房失败埋点
                //: uploadRecord.uploadRecordEvent(eventID: click_partyTab_creParty_fail, parameterStr: ["errCode": String(errorCode)])
                showVideoPath.iconStr(eventID: showRateValue, parameterStr: [String(bytes: data_giftUrl.map{backButton(message: $0)}, encoding: .utf8)!: String(errorCode)])
            }

            //: } else {
        } else { // 进房失败
            //: if errorCode == 1002055 || errorCode == 1002050 {
            if errorCode == 1_002_055 || errorCode == 1_002_050 { // 用户退出 || 被踢出房间
                //: let json = JSON(extendedData)
                let json = JSON(extendedData)
                //: let value = json["custom_kickout_message"].stringValue
                let value = json[(String(app_inputMessage) + String(constPopularTitle))].stringValue
                //: if value == "closeRoom" || value == "adminCloseRoom" { // 关闭房间
                if value == (String(app_playMessage.suffix(9))) || value == (String(mainIndexPath) + String(dataSpecialName.prefix(6))) { // 关闭房间
                    //: voiceRoom_releaseAllResource()
                    waysAndMeansResource()
                    //: let config = ShowAlertConfig()
                    let config = InsertAlertConfig()
                    //: config.alignment = .center
                    config.alignment = .center
                    //: TalkingAlertShow.customAlert(message: "The room is closed, please go to another room".localized, rightBtnTitle: "OK".localized, rightBlock: {
                    CommentSourceThen.lockComponent(message: String(bytes: userViewTitle.map{colorContent(center: $0)}, encoding: .utf8)!.localized, rightBtnTitle: "OK".localized, rightBlock: {
                        // 跳转到首页tab-party
                        //: NotificationCenter.default.post(name: SWITCH_TABBAR_HOME_PARTY_NOTIFICATION, object: nil, userInfo: nil)
                        NotificationCenter.default.post(name: noti_domainUrl, object: nil, userInfo: nil)
                        //: }, config: config)
                    }, config: config)

                    //: } else {
                } else { // 被踢出房间
                    //: func__showStatusBarErrorMsg(showMsg: "You were kicked out of the room".localized)
                    explain(showMsg: String(bytes: showOutsideKey.map{labAction(model: $0)}, encoding: .utf8)!.localized)
                }

                //: } else {
            } else {
                //: UploadLogTool.writeLog(msg: "VOICEROOM：进房失败，roomID: \(roomID), error: \(errorCode), reason: \(reason), extendedData: \(extendedData).")
                BuildLogTool.fileView(msg: (k_voiceText.replacingOccurrences(of: "status", with: "VO") + "EROO" + appChangeMsg + String(appProgressVideoId.suffix(8))) + "\(roomID)" + (String(show_playerStylePath.suffix(7)) + String(kContentViewLetData)) + "\(errorCode)" + (String(appOfName) + String(userQuantityimateData)) + "\(reason)" + (String(data_tabValue) + String(main_bottomStr)) + "\(extendedData).")
                //: func__showStatusBarErrorMsg(showMsg: "Party room exception".localized)
                explain(showMsg: (String(user_playerName.suffix(6)) + "room e" + main_clickTitle.replacingOccurrences(of: "user", with: "ce")).localized)
                // 进房失败埋点
                //: uploadRecord.uploadRecordEvent(eventID: click_partyTab_creParty_fail, parameterStr: ["errCode": String(errorCode)])
                showVideoPath.iconStr(eventID: showRateValue, parameterStr: [String(bytes: data_giftUrl.map{backButton(message: $0)}, encoding: .utf8)!: String(errorCode)])
            }
            //: voiceRoom_releaseAllResource()
            waysAndMeansResource()
        }
    }

    // MARK: - 推流回调

    /// 当前用户推流状态回调
    //: func onPublisherStateUpdate(_ state: ZegoPublisherState, errorCode: Int32, extendedData: [AnyHashable: Any]?, streamID: String) {
    func onPublisherStateUpdate(_: ZegoPublisherState, errorCode: Int32, extendedData _: [AnyHashable: Any]?, streamID: String) {
        //: guard errorCode == 0 || errorCode == 1003020 || errorCode == 1002055 else {
        guard errorCode == 0 || errorCode == 1_003_020 || errorCode == 1_002_055 else { // 推流失败，用户下麦
            //: UploadLogTool.writeLog(msg: "VOICEROOM: 当前用户推流失败，roomID: \(partyModel.roomId), errorCode: \(errorCode), streamID: \(streamID).")
            BuildLogTool.fileView(msg: (String(kCenterMsg) + String(data_playerKey)) + "\(partyModel.roomId)" + (String(const_bubbleName.suffix(6)) + "rCode: ") + "\(errorCode)" + (String(main_replyName.suffix(4)) + "reamID:" + String(constEnterTitle)) + "\(streamID).")
            //: func__showStatusBarErrorMsg(showMsg: "An error occurred, please try again".localized)
            explain(showMsg: String(bytes: appListSizeId.map{inPlayer(shape: $0)}, encoding: .utf8)!.localized)
            //: let dict = getAllMikePositions()
            let dict = nurseLog()
            //: for (position, model) in dict where String(model.uid) == IconContainerAppManager.share.loginUid {
            for (position, model) in dict where String(model.uid) == IconContainerAppManager.share.loginUid {
                //: voiceRoom_reqChangeMike(type: 2, position: position)
                vocalizationAllOver(type: 2, position: position)
                //: resetMikePosition(position: position, model: model)
                startingBlockSize(position: position, model: model)
            }
            //: if errorCode == 1002078 {
            if errorCode == 1_002_078 { // token过期，关闭房间
                //: voiceRoom_releaseAllResource()
                waysAndMeansResource()
            }
            //: return
            return
        }
    }

    // MARK: - 拉流回调

    /// 当前用户拉流状态回调
    //: func onPlayerStateUpdate(_ state: ZegoPlayerState, errorCode: Int32, extendedData: [AnyHashable: Any]?, streamID: String) {
    func onPlayerStateUpdate(_: ZegoPlayerState, errorCode: Int32, extendedData _: [AnyHashable: Any]?, streamID: String) {
        //: guard errorCode == 0 else {
        guard errorCode == 0 else { // 拉流失败
            //: UploadLogTool.writeLog(msg: "VOICEROOM: 拉流失败，roomID: \(partyModel.roomId), errorCode: \(errorCode), streamID: \(streamID).")
            BuildLogTool.fileView(msg: (String(showIntervalId) + String(userSaveMsg) + k_viewPath + "oomID: ") + "\(partyModel.roomId)" + (String(const_bubbleName.suffix(6)) + "rCode: ") + "\(errorCode)" + (String(main_replyName.suffix(4)) + "reamID:" + String(constEnterTitle)) + "\(streamID).")
            //: if errorCode == 1002078 {
            if errorCode == 1_002_078 { // token过期，关闭房间
                //: voiceRoom_releaseAllResource()
                waysAndMeansResource()
            }

            //: return
            return
        }
    }

    // MARK: - 流更新回调

    /// 流更新回调
    //: func onRoomStreamUpdate(_ updateType: ZegoUpdateType, streamList: [ZegoStream], extendedData: [AnyHashable: Any]?, roomID: String) {
    func onRoomStreamUpdate(_ updateType: ZegoUpdateType, streamList: [ZegoStream], extendedData _: [AnyHashable: Any]?, roomID: String) {
        //: guard roomID == partyModel.roomId else { return }
        guard roomID == partyModel.roomId else { return }
        //: switch updateType {
        switch updateType {
        //: case .add:
        case .add:
            //: streamList.forEach { zego in
            streamList.forEach { zego in
                // 开始拉流
                //: ZegoExpressEngine.shared().startPlayingStream(zego.streamID)
                ZegoExpressEngine.shared().startPlayingStream(zego.streamID)
            }

        //: case .delete:
        case .delete:
            //: streamList.forEach { zego in
            streamList.forEach { zego in
                // 停止拉流
                //: ZegoExpressEngine.shared().stopPlayingStream(zego.streamID)
                ZegoExpressEngine.shared().stopPlayingStream(zego.streamID)
            }

        //: @unknown default:
        @unknown default:
            //: fatalError()
            fatalError()
        }
    }

    // MARK: - 声浪等级监听回调

    /// 本地声浪等级
    //: func onCapturedSoundLevelUpdate(_ soundLevel: NSNumber) {
    func onCapturedSoundLevelUpdate(_ soundLevel: NSNumber) {
        //: guard Int(truncating: soundLevel) > 0 else { return }
        guard Int(truncating: soundLevel) > 0 else { return }
        //: voiceRoomVC?.mikePositionView.refreshSoundLevelAnimation(position: partyModel.position, level: soundLevel)
        voiceRoomVC?.mikePositionView.fastener(position: partyModel.position, level: soundLevel)
    }

    /// 远端声浪等级
    //: func onRemoteSoundLevelUpdate(_ soundLevels: [String: NSNumber]) {
    func onRemoteSoundLevelUpdate(_ soundLevels: [String: NSNumber]) {
        //: let dict = getAllMikePositions()
        let dict = nurseLog()
        //: for (streamID, level) in soundLevels {
        for (streamID, level) in soundLevels {
            //: let components = streamID.split(separator: "-").map(String.init)
            let components = streamID.split(separator: "-").map(String.init)
            //: guard components.count > 2 else { continue }
            guard components.count > 2 else { continue }
            //: if let position = dict.first(where: { String($0.value.uid) == components[1] })?.key {
            if let position = dict.first(where: { String($0.value.uid) == components[1] })?.key {
                //: voiceRoomVC?.mikePositionView.refreshSoundLevelAnimation(position: position, level: level)
                voiceRoomVC?.mikePositionView.fastener(position: position, level: level)
            }
        }
    }

    // MARK: - 信令

    /// 上麦、下麦、锁麦、解锁麦、切麦操作会收到后端下发信令
    //: func onIMRecvCustomCommand(_ command: String, from fromUser: ZegoUser, roomID: String) {
    func onIMRecvCustomCommand(_ command: String, from _: ZegoUser, roomID: String) {
        //: guard roomID == partyModel.roomId else { return }
        guard roomID == partyModel.roomId else { return }
        //: guard let decodedStr = command.removingPercentEncoding else {
        guard let decodedStr = command.removingPercentEncoding else {
            //: UploadLogTool.writeLog(msg: "VOICEROOM: 信令解析失败，command: \(command), roomID: \(roomID).")
            BuildLogTool.fileView(msg: (String(userAddText) + String(dataImageTextId)) + "\(command)" + (String(constGuideName)) + "\(roomID).")
            //: return
            return
        }
        // 更新麦位信息，刷新列表UI
        //: let json = JSON(parseJSON: decodedStr)
        let json = JSON(parseJSON: decodedStr)
        //: let position = json["data"]["position"].intValue
        let position = json[(String(k_valueMessage))][(mainExtraSectionUrl.replacingOccurrences(of: "frame", with: "p"))].intValue
        //: guard position < MIKE_SEAT_COUNT else { return }
        guard position < kLiveName else { return }
        //: guard let mikeModel = TalkingMikeListItemModel.deserialize(from: json["data"].dictionaryObject) else { return }
        guard let mikeModel = SupercedeModelType.deserialize(from: json[(String(k_valueMessage))].dictionaryObject) else { return }
        //: printLog(message: "xxxx-- \(json.rawValue)")
        printLog(message: (kLabelFromTitle.lowercased() + String(appIndexSendMakeName.prefix(5))) + "\(json.rawValue)")
        //: if json["opType"].stringValue == "party:changeMike" { // 房主/观众 麦位操作信令
        if json[(String(noti_strengthKey.suffix(6)))].stringValue == (String(user_iconStyleName.prefix(8)) + String(data_cornerFollowPath.suffix(8))) { // 房主/观众 麦位操作信令
            //: let type = json["data"]["type"].intValue
            let type = json[(String(k_valueMessage))][(String(data_viewSharedId))].intValue
            //: switch type {
            switch type {
            //: case 1:
            case 1: // 上麦
                //: mikeModel.needGift = true
                mikeModel.needGift = true
                //: updateMikePosition(position: position, model: mikeModel)
                mikeStatus(position: position, model: mikeModel)

            //: case 2:
            case 2: // 下麦
                //: if let oldModel = getMikePosition(key: position) {
                if let oldModel = viewClick(key: position) {
                    //: resetMikePosition(position: position, model: oldModel)
                    startingBlockSize(position: position, model: oldModel)
                }

            //: case 3:
            case 3: // 切麦
                //: resetMikePosition(position: position, model: mikeModel)
                startingBlockSize(position: position, model: mikeModel)
                //: let toPosition = json["data"]["toPosition"].intValue
                let toPosition = json[(String(k_valueMessage))][(String(kPlusData) + String(show_toModelUrl))].intValue
                //: mikeModel.needGift = true
                mikeModel.needGift = true
                //: updateMikePosition(position: toPosition, model: mikeModel)
                mikeStatus(position: toPosition, model: mikeModel)

            //: case 4, 5:
            case 4, 5: // 开麦、闭麦
                //: updateMikePosition(position: position, model: mikeModel)
                mikeStatus(position: position, model: mikeModel)

            //: default:
            default:
                //: break
                break
            }
            // 刷新礼物面板头像
            //: if type == 1 || type == 2 || type == 3 {
            if type == 1 || type == 2 || type == 3 {
                //: voiceRoomVC?.bottomView.refreshGiftIcon()
                voiceRoomVC?.bottomView.appOrIcon()
            }

            //: } else if json["opType"].stringValue == "party:adminChangeMike" { // 房主对他人操作信令
        } else if json[(String(noti_strengthKey.suffix(6)))].stringValue == (user_makePositionStr.replacingOccurrences(of: "style", with: "pa") + "y:adm" + String(noti_startStr.prefix(6)) + String(constLogSizeName)) { // 房主对他人操作信令
            //: switch json["data"]["type"].intValue {
            switch json[(String(k_valueMessage))][(String(data_viewSharedId))].intValue {
            //: case 1, 2:
            case 1, 2: // 锁麦、解锁麦
                //: updateMikePosition(position: position, model: mikeModel)
                mikeStatus(position: position, model: mikeModel)

            //: case 3:
            case 3: // 踢下麦
                //: if let oldModel = getMikePosition(key: position) {
                if let oldModel = viewClick(key: position) {
                    //: resetMikePosition(position: position, model: oldModel)
                    startingBlockSize(position: position, model: oldModel)
                    // 刷新礼物面板头像
                    //: voiceRoomVC?.bottomView.refreshGiftIcon()
                    voiceRoomVC?.bottomView.appOrIcon()
                    //: guard String(oldModel.uid) == IconContainerAppManager.share.loginUid else { return }
                    guard String(oldModel.uid) == IconContainerAppManager.share.loginUid else { return }
                    //: ZegoExpressEngine.shared().stopPublishingStream()
                    ZegoExpressEngine.shared().stopPublishingStream()
                    //: func__showStatusBarErrorMsg(showMsg: "You were kicked off the seat".localized)
                    explain(showMsg: (String(k_blockAddVarMsg) + String(const_indexValue.prefix(9)) + String(data_appId.prefix(4)) + "the seat").localized)
                }

            //: case 4:
            case 4: // 开麦
                //: updateMikePosition(position: position, model: mikeModel)
                mikeStatus(position: position, model: mikeModel)
                //: guard String(mikeModel.uid) == IconContainerAppManager.share.loginUid else { return }
                guard String(mikeModel.uid) == IconContainerAppManager.share.loginUid else { return }
                //: ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                ZegoExpressEngine.shared().mutePublishStreamAudio(false)

            //: case 5:
            case 5: // 闭麦
                //: updateMikePosition(position: position, model: mikeModel)
                mikeStatus(position: position, model: mikeModel)
                //: guard String(mikeModel.uid) == IconContainerAppManager.share.loginUid else { return }
                guard String(mikeModel.uid) == IconContainerAppManager.share.loginUid else { return }
                //: ZegoExpressEngine.shared().mutePublishStreamAudio(true)
                ZegoExpressEngine.shared().mutePublishStreamAudio(true)

            //: default:
            default:
                //: break
                break
            }

            //: } else if json["opType"].stringValue == "party:changeCoin" { // 更新用户麦位金币
        } else if json[(String(noti_strengthKey.suffix(6)))].stringValue == (user_nameKey.replacingOccurrences(of: "info", with: "r") + ":changeC" + data_actualEqualStr.lowercased()) { // 更新用户麦位金币
            //: TalkingVoiceRoomManager.shared().getAllMikePositions().forEach { position, model in
            PopEventHandler.addMonth().nurseLog().forEach { position, model in
                //: if model.uid == mikeModel.uid {
                if model.uid == mikeModel.uid {
                    //: model.coin = mikeModel.coin
                    model.coin = mikeModel.coin
                    //: updateMikePosition(position: position, model: model)
                    mikeStatus(position: position, model: model)
                    //: return
                    return
                }
            }

            //: } else if json["opType"].stringValue == "party:closeStream" { // 推流失败
        } else if json[(String(noti_strengthKey.suffix(6)))].stringValue == (String(userViewMsg) + String(show_mainStr.prefix(5)) + "seStream") { // 推流失败
            //: ZegoExpressEngine.shared().stopPublishingStream()
            ZegoExpressEngine.shared().stopPublishingStream()
        }
    }
}

// MARK: - 信令处理

//: extension TalkingVoiceRoomManager {
extension PopEventHandler {
    /// 更新麦位数据，刷新UI
    /// - Parameters:
    ///   - position: 麦位
    ///   - model: 数据
    //: private func updateMikePosition(position: Int, model: TalkingMikeListItemModel) {
    private func mikeStatus(position: Int, model: SupercedeModelType) {
        //: modifyMikePosition(key: position, model: model)
        modelPosition(key: position, model: model)
        //: voiceRoomVC?.mikePositionView.refreshSingleMikeView(position: position)
        voiceRoomVC?.mikePositionView.anShow(position: position)
        //: updateBottomMicStatusIfCurrentUser(uid: model.uid)
        currentMake(uid: model.uid)
    }

    /// 重置麦位数据，刷新UI
    /// - Parameters:
    ///   - position: 麦位
    ///   - model: 数据
    //: private func resetMikePosition(position: Int, model: TalkingMikeListItemModel) {
    private func startingBlockSize(position: Int, model: SupercedeModelType) {
        //: modifyMikePosition(key: position, model: TalkingMikeListItemModel())
        modelPosition(key: position, model: SupercedeModelType())
        //: voiceRoomVC?.mikePositionView.refreshSingleMikeView(position: position)
        voiceRoomVC?.mikePositionView.anShow(position: position)
        //: updateBottomMicStatusIfCurrentUser(uid: model.uid)
        currentMake(uid: model.uid)
    }

    /// 更新本人麦位、底部麦克风状态
    /// - Parameter uid: 用户Id
    //: private func updateBottomMicStatusIfCurrentUser(uid: Int) {
    private func currentMake(uid: Int) {
        //: guard String(uid) == IconContainerAppManager.share.loginUid else { return }
        guard String(uid) == IconContainerAppManager.share.loginUid else { return }
        //: let dict = getAllMikePositions()
        let dict = nurseLog()
        //: if let position = dict.first(where: { $0.value.uid == uid })?.key {
        if let position = dict.first(where: { $0.value.uid == uid })?.key {
            //: partyModel.position = position
            partyModel.position = position
            //: } else {
        } else {
            //: partyModel.position = -1
            partyModel.position = -1
        }
        //: voiceRoomVC?.bottomView.refreshBottomMicStatus()
        voiceRoomVC?.bottomView.sex()
    }
}

// MARK: - 麦位字典安全操作

//: extension TalkingVoiceRoomManager {
extension PopEventHandler {
    /// 获取初始化模型
    //: private func initMikePosition() -> [Int: TalkingMikeListItemModel] {
    private func thread() -> [Int: SupercedeModelType] {
        //: for index in 0 ..< MIKE_SEAT_COUNT {
        for index in 0 ..< kLiveName {
            //: modifyMikePosition(key: index, model: TalkingMikeListItemModel())
            modelPosition(key: index, model: SupercedeModelType())
        }
        //: return mikePositionDict
        return mikePositionDict
    }

    /// 添加/修改 麦位
    //: func modifyMikePosition(key: Int, model: TalkingMikeListItemModel) {
    func modelPosition(key: Int, model: SupercedeModelType) {
        //: mplock.lock()
        mplock.lock()
        //: defer { mplock.unlock() }
        defer { mplock.unlock() }
        //: mikePositionDict[key] = model
        mikePositionDict[key] = model
    }

    /// 获取麦位
    //: func getMikePosition(key: Int) -> TalkingMikeListItemModel? {
    func viewClick(key: Int) -> SupercedeModelType? {
        //: mplock.lock()
        mplock.lock()
        //: defer { mplock.unlock() }
        defer { mplock.unlock() }
        //: return mikePositionDict[key]
        return mikePositionDict[key]
    }

    /// 获取所有麦位
    //: func getAllMikePositions() -> [Int: TalkingMikeListItemModel] {
    func nurseLog() -> [Int: SupercedeModelType] {
        //: mplock.lock()
        mplock.lock()
        //: defer { mplock.unlock() }
        defer { mplock.unlock() }
        //: return mikePositionDict
        return mikePositionDict
    }
}
