
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let mainLogName:[UInt8] = [0xef,0xe8,0xef,0xf2,0xae,0xe5,0xe9,0xe2,0xe3,0xf4,0xbc,0xaf,0xa6,0xee,0xe7,0xf5,0xa6,0xe8,0xe9,0xf2,0xa6,0xe4,0xe3,0xe3,0xe8,0xa6,0xef,0xeb,0xf6,0xea,0xe3,0xeb,0xe3,0xe8,0xf2,0xe3,0xe2]

				private func numberTime(block num: UInt8) -> UInt8 {
					return num ^ 134
				}

/*: "Followers" :*/
fileprivate let notiInputKey:String = "Followshow value model data var"
fileprivate let data_cellName:[Character] = ["e","r","s"]

/*: "Following" :*/
fileprivate let showTableValue:String = "icon left equal list upFoll"

/*: "#E6E6E6" :*/
fileprivate let notiFieldKey:[Character] = ["#","E"]
fileprivate let showIndexFormat:[Character] = ["6","E","6","E","6"]

/*: "btn_message_more" :*/
fileprivate let data_buttonKindPath:String = "share size string sharebtn_"
fileprivate let app_priceMessage:String = "age_self key title last voice"
fileprivate let dataAccountFormat:String = "morexplain"

/*: "level_ :*/
fileprivate let k_appModeLetValue:String = "level_class shared tool add"

/*: "Gift" :*/
fileprivate let appTitleFirstMsg:[UInt8] = [0x7e,0x50,0x5f,0x4d]

				private func toolError(lip num: UInt8) -> UInt8 {
					return num ^ 57
				}

/*: "Mike" :*/
fileprivate let show_viewName:[UInt8] = [0x65,0x6b,0x69,0x4d]

/*: "Follow" :*/
fileprivate let notiAllText:[UInt8] = [0x36,0x5f,0x5c,0x5c,0x5f,0x67]

				fileprivate func shareMake(of num: UInt8) -> UInt8 {
					let value = Int(num) + 16
					if value > 255 {
						return UInt8(value - 256)
					} else {
						return UInt8(value)
					}
				}

/*: "gift_voice_card" :*/
fileprivate let dataLabValue:[Character] = ["g","i"]
fileprivate let dataContainerTitle:String = "ft_vreturn create return left var"
fileprivate let data_signUrl:String = "cnamed"

/*: "btn_party_voice_nor" :*/
fileprivate let k_sendDateKey:[Character] = ["b"]
fileprivate let notiColorPath:String = "tn_pagroup background normal color extension"
fileprivate let k_makeText:String = "oice_norcategory bind"

/*: "btn_party_voice_pre" :*/
fileprivate let const_bottomMsg:[Character] = ["b","t","n","_","p","a"]
fileprivate let app_keyId:String = "request action returnrty_"
fileprivate let constBodyName:[Character] = ["e","_","p","r","e"]

/*: "attentionUid" :*/
fileprivate let kMakePath:String = "viewttent"

/*: "source" :*/
fileprivate let noti_infoPath:[Character] = ["s","o","u","r","c","e"]

/*: "Kick out" :*/
fileprivate let app_cookieKey:String = "pop guard makeKick o"
fileprivate let noti_kitStr:String = "uname"

/*: "Unmute" :*/
fileprivate let constAllFormat:String = "Unmuteindex total self text"

/*: "Mute" :*/
fileprivate let show_centerManagerStr:String = "var manager class priceMute"

/*: "Kick off" :*/
fileprivate let kTimeContent:[Character] = ["K","i","c","k"," ","o","f"]
fileprivate let k_afterFormat:String = "F"

/*: "Block" :*/
fileprivate let app_totalMsg:String = "self self serverBlock"

/*: "Report" :*/
fileprivate let appAddUrl:[Character] = ["R","e","p","o","r","t"]

/*: "#F6F6F6" :*/
fileprivate let app_styleMessage:String = "#F6F6F6for return"

/*: "Confirm to kick out the room?" :*/
fileprivate let app_clickUrl:[Character] = ["C","o","n","f","i","r","m"," ","t","o"," ","k","i","c","k"," "]
fileprivate let main_topArrayMsg:String = "conversation title should selected toout th"
fileprivate let showGroupKey:String = "return centere room?"

/*: "Cancel" :*/
fileprivate let user_cellMessage:[Character] = ["C","a","n","c","e","l"]

/*: "Sure" :*/
fileprivate let noti_sharedMakePartyMessage:[Character] = ["S","u","r","e"]

/*: "Are you sure you want to cancel the mute?" :*/
fileprivate let user_clickText:[UInt8] = [0x6d,0x5e,0x49,0xc,0x55,0x43,0x59,0xc,0x5f,0x59,0x5e,0x49,0xc,0x55,0x43,0x59,0xc,0x5b,0x4d,0x42,0x58,0xc,0x58,0x43,0xc,0x4f,0x4d,0x42,0x4f,0x49,0x40,0xc,0x58,0x44,0x49,0xc,0x41,0x59,0x58,0x49,0x13]

				private func telephoneMessagePlayer(up num: UInt8) -> UInt8 {
					return num ^ 44
				}

/*: "Confirm" :*/
fileprivate let dataStopMsg:[Character] = ["C","o","n","f","i"]
fileprivate let constUserValue:String = "head"

/*: "The mute has been lifted~" :*/
fileprivate let kUpgradeFormat:[Character] = ["T","h","e"," ","m","u","t","e"," ","h","a"]
fileprivate let kPlayerBarTitle:[Character] = ["s"," ","b","e","e","n"," "]
fileprivate let appDataValue:[Character] = ["l","i","f","t","e","d","~"]

/*: "Confirm to kick off the seat?" :*/
fileprivate let user_backMsg:[Character] = ["C","o","n","f","i","r","m"," ","t","o"," ","k","i","c","k"," ","o","f","f"," ","t","h","e"," ","s","e"]
fileprivate let app_textActualId:String = "mic selected user popat?"

/*: "Shielding Success" :*/
fileprivate let dataCornerStr:[Character] = ["S","h","i","e","l","d","i","n"]
fileprivate let app_stylePath:[Character] = ["g"," "]
fileprivate let user_indexModelMakeId:[Character] = ["S","u","c","c","e","s","s"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  BestCardView.swift
//  AbroadTalking
//

//: import UIKit
import UIKit

//: @objc protocol TalkingVoiceRoomCardViewDelegate: NSObjectProtocol {
@objc protocol CombineViewDelegate: NSObjectProtocol {
    // @
    //: func func__atUserClick(uid: String, nickname: String)
    func gameTo(uid: String, nickname: String)

    //: func func__giftUserClick(uid: String)
    func itemWith(uid: String)

    //: func func__muteUserMickClick(uid: String, mikeStatus: Int, position: Int)
    func lowerClassInWindow(uid: String, mikeStatus: Int, position: Int)

    //: func func__followUserSucess(uid: String)
    func crosshead(uid: String)
}

//: class TalkingVoiceRoomCardView: UIView {
class BestCardView: UIView {
    //: var popView: TalkingPopView?
    var popView: DataReactiveCompatible?
    //: var data =  [String: Any]()
    var data = [String: Any]()
    //: var type: LiveUserCardType?
    var type: IndividualStrideable?
    //: var uid = ""
    var uid = ""
    //: var liveUserId = String(TalkingVoiceRoomManager.shared().partyModel.streamerInfo.uid)
    var liveUserId = String(PopEventHandler.addMonth().partyModel.streamerInfo.uid) // 主播ID

    //: var userModel = TalkingUserInfoModel.init()
    var userModel = CustomHandyJSON()

    //: open weak var delegate: TalkingVoiceRoomCardViewDelegate?
    open weak var delegate: CombineViewDelegate?

    //: init(frame: CGRect, uid: String) {
    init(frame: CGRect, uid: String) {
        //: super.init(frame: frame)
        super.init(frame: frame)

        //: if uid == IconContainerAppManager.share.loginUserMode.userID {
        if uid == IconContainerAppManager.share.loginUserMode.userID {
            //: self.type = .user_me
            self.type = .user_me
            //: } else {
        } else {
            //: self.type = .user_other
            self.type = .user_other
        }
        //: self.uid = uid
        self.uid = uid
        //: setupSubviews()
        location()
        //: setupSubViewsConstraint()
        forefrontForConstraint()
        //: reqUserCardInfo()
        cellPop()
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: mainLogName.map{numberTime(block: $0)}, encoding: .utf8)!)
    }

    //: deinit {
    deinit {}

    // 懒加载初始化

    //: lazy var contentView: UIView = {
    lazy var contentView: UIView = {
        //: let view = UIView.init()
        let view = UIView()
        //: view.backgroundColor = UIColor.white
        view.backgroundColor = UIColor.white
        //: view.layer.cornerRadius = 8
        view.layer.cornerRadius = 8
        //: return view
        return view
        //: }()
    }()

    //: lazy var uidLb: UILabel = {
    lazy var uidLb: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Regular, fontSize: 12)
        label.font = .replyName(type: .Regular, fontSize: 12)
        //: label.textColor = UIColor.appValueDetailColor()
        label.textColor = UIColor.valueColor()
        //: return label
        return label
        //: }()
    }()

    //: lazy var topIcon: UIButton = {
    lazy var topIcon: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.addTarget(self, action: #selector(iconBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(penalize), for: .touchUpInside)
        //: btn.layer.cornerRadius = 40
        btn.layer.cornerRadius = 40
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.imageView?.contentMode = .scaleAspectFill
        btn.imageView?.contentMode = .scaleAspectFill
        //: btn.layer.borderWidth = 2
        btn.layer.borderWidth = 2
        //: btn.layer.borderColor = UIColor.white.cgColor
        btn.layer.borderColor = UIColor.white.cgColor
        //: return btn
        return btn

        //: }()
    }()

    //: lazy var nameLb: UILabel = {
    lazy var nameLb: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Semibold, fontSize: 15)
        label.font = .replyName(type: .Semibold, fontSize: 15)
        //: label.textColor = UIColor.appTitleColor()
        label.textColor = UIColor.sendTitle()
        //: return label
        return label
        //: }()
    }()

    //: lazy var levelIcon: UIImageView = {
    lazy var levelIcon: UIImageView = {
        //: let imag = UIImageView.init()
        let imag = UIImageView()
        //: imag.contentMode = .scaleAspectFill
        imag.contentMode = .scaleAspectFill
        //: return imag
        return imag
        //: }()
    }()

    //: lazy var signLb: UILabel = {
    lazy var signLb: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Regular, fontSize: 13)
        label.font = .replyName(type: .Regular, fontSize: 13)
        //: label.textColor = UIColor.appValueDetailColor()
        label.textColor = UIColor.valueColor()
        //: label.textAlignment = .center
        label.textAlignment = .center
        //: label.numberOfLines = 2
        label.numberOfLines = 2
        //: return label
        return label
        //: }()
    }()

    //: private lazy var followersLab: UILabel = {
    private lazy var followersLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .appValueDetailColor()
        lab.textColor = .valueColor()
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "Followers".localized
        lab.text = (String(notiInputKey.prefix(6)) + String(data_cellName)).localized
        //: lab.font = UIFont.pingfangFont(type: .Semibold, fontSize: 12)
        lab.font = UIFont.replyName(type: .Semibold, fontSize: 12)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var followersNum: UILabel = {
    private lazy var followersNum: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .appTitleColor()
        lab.textColor = .sendTitle()
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "0"
        lab.text = "0"
        //: lab.font = UIFont.pingfangFont(type: .Semibold, fontSize: 15)
        lab.font = UIFont.replyName(type: .Semibold, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var followingLab: UILabel = {
    private lazy var followingLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .appValueDetailColor()
        lab.textColor = .valueColor()
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "Following".localized
        lab.text = (String(showTableValue.suffix(4)) + "owing").localized
        //: lab.font = UIFont.pingfangFont(type: .Semibold, fontSize: 12)
        lab.font = UIFont.replyName(type: .Semibold, fontSize: 12)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var followingNum: UILabel = {
    private lazy var followingNum: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .appTitleColor()
        lab.textColor = .sendTitle()
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "0"
        lab.text = "0"
        //: lab.font = UIFont.pingfangFont(type: .Semibold, fontSize: 15)
        lab.font = UIFont.replyName(type: .Semibold, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var lineView: UIView = {
    private lazy var lineView: UIView = {
        //: let view = UIView()
        let view = UIView()
        //: view.backgroundColor = UIColor.init(hex: "#E6E6E6")
        view.backgroundColor = UIColor(hex: (String(notiFieldKey) + String(showIndexFormat)))
        //: return view
        return view
        //: }()
    }()

    //: private lazy var stackView: UIStackView = {
    private lazy var stackView: UIStackView = {
        //: let v = UIStackView()
        let v = UIStackView()
        //: v.axis = .horizontal
        v.axis = .horizontal
        //: v.alignment = .fill
        v.alignment = .fill
        //: v.distribution = .fillEqually
        v.distribution = .fillEqually
        //: return v
        return v
        //: }()
    }()

    //: private lazy var moreBtn: UIButton = {
    private lazy var moreBtn: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.imageView?.contentMode = .scaleAspectFill
        btn.imageView?.contentMode = .scaleAspectFill
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_message_more"), for: .normal)
        btn.setImage(UIImage.bundle(name: (String(data_buttonKindPath.suffix(4)) + "mess" + String(app_priceMessage.prefix(4)) + dataAccountFormat.replacingOccurrences(of: "explain", with: "e"))), for: .normal)
        //: btn.addTarget(self, action: #selector(moreBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(demonstrate), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var muteView: TalkingLiveRoomMuteView = {
    private lazy var muteView: MaleView = {
        //: let v = TalkingLiveRoomMuteView()
        let v = MaleView()
        //: v.viewType  = .voiceRoom
        v.viewType = .voiceRoom
        //: return v
        return v
        //: }()
    }()
}

//: extension TalkingVoiceRoomCardView {
extension BestCardView {
    //: func reqUserCardInfo() {
    func cellPop() {
        //: TalkingLiveManager.req__userCardInfo(uid: self.uid, streamerUid: liveUserId, roomId: TalkingVoiceRoomManager.shared().partyModel.roomId) { succeed, result, errorModel in
        DraftCopyThen.upBy(uid: self.uid, streamerUid: liveUserId, roomId: PopEventHandler.addMonth().partyModel.roomId) { succeed, result, _ in
            //: guard succeed else { return }
            guard succeed else { return }

            //: if let userModel = JSONDeserializer<TalkingUserInfoModel>.deserializeFrom(dict: result as? Dictionary<String, Any>, designatedPath: nil) {
            if let userModel = JSONDeserializer<CustomHandyJSON>.deserializeFrom(dict: result as? [String: Any], designatedPath: nil) {
                //: self.userModel = userModel
                self.userModel = userModel
                //: self.setUserCardData()
                self.appearData()
            }
        }
    }

    //: func setUserCardData() {
    func appearData() {
        //: uidLb.text = "UID: \(self.userModel.uid)"
        uidLb.text = "UID: \(self.userModel.uid)"
        //: topIcon.setUrlImage(urlStr: self.userModel.headPic)
        topIcon.sayFarewell(urlStr: self.userModel.headPic)
        //: nameLb.text = self.userModel.nickname
        nameLb.text = self.userModel.nickname
        //: levelIcon.image = UIImage.BundleImageNamed(name: "level_\(self.userModel.level)")
        levelIcon.image = UIImage.bundle(name: (String(k_appModeLetValue.prefix(6))) + "\(self.userModel.level)")
        //: signLb.text = self.userModel.signature
        signLb.text = self.userModel.signature
        //: followersNum.text = self.userModel.attentionNum
        followersNum.text = self.userModel.attentionNum
        //: followingNum.text = self.userModel.fansNum
        followingNum.text = self.userModel.fansNum

        //: if self.type == .user_other {
        if self.type == .user_other {
            //: setBottomView(sex: self.userModel.sex)
            observerOf(sex: self.userModel.sex)
        }
    }

    //: func setBottomView(sex: String) {
    func observerOf(sex _: String) {
        //: var dataSource = Array<String>()
        var dataSource = [String]()
        //: if self.userModel.isOnMike {
        if self.userModel.isOnMike {
            //: dataSource.append("Gift")
            dataSource.append(String(bytes: appTitleFirstMsg.map{toolError(lip: $0)}, encoding: .utf8)!)
            //: if liveUserId == IconContainerAppManager.share.loginUserMode.userID && self.userModel.mikeStatus != 0  {
            if liveUserId == IconContainerAppManager.share.loginUserMode.userID, self.userModel.mikeStatus != 0 { // 当前用户是主播非自己闭麦  闭麦/开麦 按钮
                //: dataSource.append("Mike")
                dataSource.append(String(bytes: show_viewName.reversed(), encoding: .utf8)!)
            }
        }
        //: dataSource.append("@")
        dataSource.append("@")

        //: if self.userModel.isAttention == false {
        if self.userModel.isAttention == false {
            //: dataSource.append("Follow")
            dataSource.append(String(bytes: notiAllText.map{shareMake(of: $0)}, encoding: .utf8)!)
        }

        //: for i in 0 ..< dataSource.count {
        for i in 0 ..< dataSource.count {
            //: let btnStr = dataSource[i]
            let btnStr = dataSource[i]
            //: let btn = UIButton()
            let btn = UIButton()
            //: btn.accessibilityLabel = btnStr
            btn.accessibilityLabel = btnStr
            //: btn.setTitleColor(UIColor.appThemeColor(), for: UIControl.State.normal)
            btn.setTitleColor(UIColor.actionLive(), for: UIControl.State.normal)
            //: if btnStr == "Gift" {
            if btnStr == String(bytes: appTitleFirstMsg.map{toolError(lip: $0)}, encoding: .utf8)! {
                //: btn.setImage(UIImage.BundleImageNamed(name: "gift_voice_card"), for: .normal)
                btn.setImage(UIImage.bundle(name: (String(dataLabValue) + String(dataContainerTitle.prefix(4)) + "oice_" + data_signUrl.replacingOccurrences(of: "name", with: "ar"))), for: .normal)
                //: }else if btnStr == "Mike"{
            } else if btnStr == String(bytes: show_viewName.reversed(), encoding: .utf8)! {
                //: btn.setImage(UIImage.BundleImageNamed(name: "btn_party_voice_nor"), for: .normal)
                btn.setImage(UIImage.bundle(name: (String(k_sendDateKey) + String(notiColorPath.prefix(5)) + "rty_v" + String(k_makeText.prefix(8)))), for: .normal)
                //: btn.setImage(UIImage.BundleImageNamed(name: "btn_party_voice_pre"), for: .selected)
                btn.setImage(UIImage.bundle(name: (String(const_bottomMsg) + String(app_keyId.suffix(4)) + "voic" + String(constBodyName))), for: .selected)
                //: if self.userModel.mikeStatus != 2 {
                if self.userModel.mikeStatus != 2 {
                    //: btn.isSelected = true
                    btn.isSelected = true
                }
                //: }else{
            } else {
                //: btn.titleLabel?.font = UIFont.pingfangMediumFont(fontSize: 15)
                btn.titleLabel?.font = UIFont.font(fontSize: 15)
                //: btn.setTitle(btnStr.localized, for: UIControl.State.normal)
                btn.setTitle(btnStr.localized, for: UIControl.State.normal)
            }
            //: btn.addTarget(self, action: #selector(itemTouch(sender:)), for: UIControl.Event.touchUpInside)
            btn.addTarget(self, action: #selector(disappearAt(sender:)), for: UIControl.Event.touchUpInside)
            //: self.stackView.addArrangedSubview(btn)
            self.stackView.addArrangedSubview(btn)

            //: if i != 0 {
            if i != 0 {
                //: let lineView = UIView.init()
                let lineView = UIView()
                //: lineView.backgroundColor = UIColor.init(hex: "#E6E6E6")
                lineView.backgroundColor = UIColor(hex: (String(notiFieldKey) + String(showIndexFormat)))
                //: btn.addSubview(lineView)
                btn.addSubview(lineView)
                //: lineView.snp.makeConstraints { make in
                lineView.snp.makeConstraints { make in
                    //: make.top.equalTo(10)
                    make.top.equalTo(10)
                    //: make.leading.equalToSuperview()
                    make.leading.equalToSuperview()
                    //: make.width.equalTo(1)
                    make.width.equalTo(1)
                    //: make.height.equalTo(24)
                    make.height.equalTo(24)
                }
            }
        }
    }

    //: @objc func itemTouch(sender: UIButton) {
    @objc func disappearAt(sender: UIButton) {
        //: let accessLabel = sender.accessibilityLabel ?? ""
        let accessLabel = sender.accessibilityLabel ?? ""
        //: switch accessLabel {
        switch accessLabel {
        //: case "Gift":
        case String(bytes: appTitleFirstMsg.map{toolError(lip: $0)}, encoding: .utf8)!:
            //: self.delegate?.func__giftUserClick(uid: self.userModel.uid)
            self.delegate?.itemWith(uid: self.userModel.uid)
            //: self.dismiss()
            self.allowStatusDismiss()
        //: case "Mike":
        case String(bytes: show_viewName.reversed(), encoding: .utf8)!:
            //: self.delegate?.func__muteUserMickClick(uid: self.userModel.uid, mikeStatus: self.userModel.mikeStatus, position: self.userModel.mikePosition)
            self.delegate?.lowerClassInWindow(uid: self.userModel.uid, mikeStatus: self.userModel.mikeStatus, position: self.userModel.mikePosition)
            //: self.dismiss()
            self.allowStatusDismiss()
        //: case "@":
        case "@":
            //: self.delegate?.func__atUserClick(uid: self.userModel.uid, nickname: self.userModel.nickname)
            self.delegate?.gameTo(uid: self.userModel.uid, nickname: self.userModel.nickname)
            //: self.dismiss()
            self.allowStatusDismiss()
        //: case "Follow":
        case String(bytes: notiAllText.map{shareMake(of: $0)}, encoding: .utf8)!:
            //: freshfollowUser(sender: sender)
            somebodyWindow(sender: sender)
        //: default: break
        default: break
        }
    }

    //: func freshfollowUser(sender: UIButton) {
    func somebodyWindow(sender: UIButton) {
        //: req_followUser(uid: self.userModel.uid) { succeed in
        cellCancel(uid: self.userModel.uid) { succeed in
            //: if succeed {
            if succeed {
                //: self.followingNum.text = "\((Int(self.userModel.fansNum ) ?? 0) + 1)"
                self.followingNum.text = "\((Int(self.userModel.fansNum) ?? 0) + 1)"
                //: self.stackView.removeArrangedSubview(sender)
                self.stackView.removeArrangedSubview(sender)
                //: sender.isHidden = true
                sender.isHidden = true
                //: self.delegate?.func__followUserSucess(uid: self.userModel.uid)
                self.delegate?.crosshead(uid: self.userModel.uid)
            }
        }
    }

    //: func req_followUser(uid: String, completion: ((_ succeed: Bool) -> Void)? = nil) {
    func cellCancel(uid: String, completion: ((_ succeed: Bool) -> Void)? = nil) {
        //: var dict = [String: Any]()
        var dict = [String: Any]()
        //: dict["attentionUid"] = uid
        dict[(kMakePath.replacingOccurrences(of: "view", with: "a") + "ionUid")] = uid
        //: dict["source"] = "2"
        dict[(String(noti_infoPath))] = "2"
        //: StreamRequestTool.req_atationTool(isAttention: true, params: dict) { succeed, result, errorModel in
        StreamRequestTool.sprechstimme(isAttention: true, params: dict) { succeed, _, _ in
            //: completion?(succeed)
            completion?(succeed)
        }
    }

    //: @objc private func iconBtnClick() {
    @objc private func penalize() {
        //: CombineAccountEnclaveDecisionMakerThen.share.func__pushToUserDetailVC(uid: self.userModel.uid)
        CombineAccountEnclaveDecisionMakerThen.share.windowBy(uid: self.userModel.uid)
        //: self.dismiss()
        self.allowStatusDismiss()
    }
}

//: extension TalkingVoiceRoomCardView {
extension BestCardView {
    //: @objc func moreBtnClick() {
    @objc func demonstrate() {
        //: var titleArray: [String] = []
        var titleArray: [String] = []
        /// 主播 & 用户在房间内
        //: if liveUserId == IconContainerAppManager.share.loginUserMode.userID && self.userModel.isInRoom {
        if liveUserId == IconContainerAppManager.share.loginUserMode.userID, self.userModel.isInRoom {
            //: titleArray.append("Kick out".localized)
            titleArray.append((String(app_cookieKey.suffix(6)) + noti_kitStr.replacingOccurrences(of: "name", with: "t")).localized)
            //: if self.userModel.isPartyMute {
            if self.userModel.isPartyMute {
                //: titleArray.append("Unmute".localized)
                titleArray.append((String(constAllFormat.prefix(6))).localized)
                //: }else {
            } else {
                //: titleArray.append("Mute".localized)
                titleArray.append((String(show_centerManagerStr.suffix(4))).localized)
            }
            //: if self.userModel.isOnMike {
            if self.userModel.isOnMike {
                //: titleArray.append("Kick off".localized)
                titleArray.append((String(kTimeContent) + k_afterFormat.lowercased()).localized)
            }
            //: if !self.userModel.inMyBlackList {
            if !self.userModel.inMyBlackList {
                //: titleArray.append("Block".localized)
                titleArray.append((String(app_totalMsg.suffix(5))).localized)
            }
        }
        //: titleArray.append("Report".localized)
        titleArray.append((String(appAddUrl)).localized)

        //: Config.MenuCellConfig.menuCellHeight = 26
        Config.MinEffect.menuCellHeight = 26
        //: let dropMenu = DropDownMenuView.pullDropDrownMenu(anchorView: moreBtn, titleArray: titleArray, imageArray: [])
        let dropMenu = DownwardView.withColor(anchorView: moreBtn, titleArray: titleArray, imageArray: [])
        //: dropMenu.menuBgColor = UIColor.init(hex: "#F6F6F6")!
        dropMenu.menuBgColor = UIColor(hex: (String(app_styleMessage.prefix(7))))!
        //: dropMenu.menuDelegate = self
        dropMenu.menuDelegate = self
        //: dropMenu.menuStyle = .MenuCustStyle
        dropMenu.menuStyle = .MenuCustStyle
    }

    //: func show() {
    func meWith() {
        //: popView = TalkingPopView.init(frame: UIScreen.main.bounds)
        popView = DataReactiveCompatible(frame: UIScreen.main.bounds)
        //: self.frame = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight)
        self.frame = CGRect(x: 0, y: 0, width: main_colorData, height: constRecordServerKey)
        //: popView?.initWithView(view: self)
        popView?.destroy(view: self)
        //: popView?.showInView(view: self.currentViewController()?.view ?? MaleMacroDefine.getWindow())
        popView?.textParty(view: self.actionLatest()?.view ?? MaleMacroDefine.penumbra())
        //: popView?.isRemoveTapGes = true
        popView?.isRemoveTapGes = true
    }

    //: @objc func dismiss() {
    @objc func allowStatusDismiss() {
        //: popView?.dismissView()
        popView?.laughAwayLog()
        //: popView = nil
        popView = nil
    }
}

//: extension TalkingVoiceRoomCardView: DropDownMenuViewDelegate {
extension BestCardView: PlonkDownThirdViewDelegate {
    //: func didClickSelectedRow(index: Int, title: String) {
    func quits(index _: Int, title: String) {
        //: switch title {
        switch title {
        //: case "Kick out".localized:  // 踢出房间
        case (String(app_cookieKey.suffix(6)) + noti_kitStr.replacingOccurrences(of: "name", with: "t")).localized: // 踢出房间
            //: TalkingAlertShow.alert(title: nil, message: "Confirm to kick out the room?".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Sure".localized) {
            CommentSourceThen.outRequestAdd(title: nil, message: (String(app_clickUrl) + String(main_topArrayMsg.suffix(6)) + String(showGroupKey.suffix(7))).localized, leftBtnTitle: (String(user_cellMessage)).localized, rightBtnTitle: (String(noti_sharedMakePartyMessage)).localized) {
                //: } rightBlock: {
            } rightBlock: {
                //: ProgressHUD.show()
                MakeView.notToPic()
                //: TalkingVoiceRoomReqTool.req_VoiceRoomKickout(targetUid: self.userModel.uid, roomId: TalkingVoiceRoomManager.shared().partyModel.roomId) { succeed, result, errorModel in
                TipReqTool.completionSize(targetUid: self.userModel.uid, roomId: PopEventHandler.addMonth().partyModel.roomId) { _, _, _ in
                    //: ProgressHUD.dismiss()
                    MakeView.presentAddLab()
                    //: self.dismiss()
                    self.allowStatusDismiss()
                }
            }
        //: case "Unmute".localized: // 解除禁言
        case (String(constAllFormat.prefix(6))).localized: // 解除禁言
            //: TalkingAlertShow.alert(title: nil, message: "Are you sure you want to cancel the mute?".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Confirm".localized) {
            CommentSourceThen.outRequestAdd(title: nil, message: String(bytes: user_clickText.map{telephoneMessagePlayer(up: $0)}, encoding: .utf8)!.localized, leftBtnTitle: (String(user_cellMessage)).localized, rightBtnTitle: (String(dataStopMsg) + constUserValue.replacingOccurrences(of: "head", with: "rm")).localized) {
                //: } rightBlock: {
            } rightBlock: {
                //: ProgressHUD.show()
                MakeView.notToPic()
                //: TalkingVoiceRoomReqTool.req_VoiceRoomUnMute(targetUid: self.userModel.uid, roomID:TalkingVoiceRoomManager.shared().partyModel.roomId, completion: { succeed, result, errorModel in
                TipReqTool.holdfastCompletion(targetUid: self.userModel.uid, roomID: PopEventHandler.addMonth().partyModel.roomId, completion: { succeed, _, _ in
                    //: ProgressHUD.dismiss()
                    MakeView.presentAddLab()
                    //: if succeed {
                    if succeed {
                        //: self.func__showStatusBarSuccessMsg(showMsg: "The mute has been lifted~".localized)
                        self.barPush(showMsg: (String(kUpgradeFormat) + String(kPlayerBarTitle) + String(appDataValue)).localized)
                    }
                    //: self.dismiss()
                    self.allowStatusDismiss()
                    //: })
                })
            }
        //: case "Mute".localized: // 设置禁言
        case (String(show_centerManagerStr.suffix(4))).localized: // 设置禁言
            //: muteView.showView()
            muteView.priceView()
            //: muteView.targetUid = self.userModel.uid
            muteView.targetUid = self.userModel.uid
            //: self.dismiss()
            self.allowStatusDismiss()

        //: case "Kick off".localized: /// 踢下麦
        case (String(kTimeContent) + k_afterFormat.lowercased()).localized: /// 踢下麦
            //: TalkingAlertShow.alert(title: nil, message: "Confirm to kick off the seat?".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Sure".localized) {
            CommentSourceThen.outRequestAdd(title: nil, message: (String(user_backMsg) + String(app_textActualId.suffix(3))).localized, leftBtnTitle: (String(user_cellMessage)).localized, rightBtnTitle: (String(noti_sharedMakePartyMessage)).localized) {
                //: } rightBlock: {
            } rightBlock: {
                //: TalkingVoiceRoomManager.shared().voiceRoom_reqAdminChangeMike(uid: self.userModel.uid.intValue, type: 3, position: self.userModel.mikePosition)
                PopEventHandler.addMonth().exhibit(uid: self.userModel.uid.intValue, type: 3, position: self.userModel.mikePosition)
                //: self.dismiss()
                self.allowStatusDismiss()
            }

        //: case "Report".localized:
        case (String(appAddUrl)).localized:
            //: let reportView = TalkingReportAlertView(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.userModel.uid)
            let reportView = SuccessDataSource(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.userModel.uid)
            //: reportView.showReportViewIn(view: nil)
            reportView.beyondShow(view: nil)
        //: case "Block".localized:
        case (String(app_totalMsg.suffix(5))).localized:
            //: TalkingAlertShow.alert(title: nil, message: kMessage_blocking, leftBtnTitle: "Cancel".localized, rightBtnTitle: "OK".localized) {
            CommentSourceThen.outRequestAdd(title: nil, message: app_nameMessage, leftBtnTitle: (String(user_cellMessage)).localized, rightBtnTitle: "OK".localized) {
                //: TalkingAlertShow.hideAlert()
                CommentSourceThen.alertDataTo()
                //: } rightBlock: {
            } rightBlock: {
                //: TalkingAlertShow.hideAlert()
                CommentSourceThen.alertDataTo()
                //: TalkingUserRequestManager.func__pullBlackWithUserId(uid: self.userModel.uid, isBlack: true) { succeed, result, errorModel in
                ToReactiveCompatible.nonsolidColorPath(uid: self.userModel.uid, isBlack: true) { succeed, _, _ in
                    //: if succeed == true {
                    if succeed == true {
                        //: self.func__showStatusBarSuccessMsg(showMsg: "Shielding Success".localized)
                        self.barPush(showMsg: (String(dataCornerStr) + String(app_stylePath) + String(user_indexModelMakeId)).localized)
                        //: self.dismiss()
                        self.allowStatusDismiss()
                    }
                }
            }
        //: default: break
        default: break
        }
    }
}

//: extension TalkingVoiceRoomCardView {
extension BestCardView {
    // 添加视图
    //: private func setupSubviews() {
    private func location() {
        //: self.addSubview(contentView)
        self.addSubview(contentView)
        //: self.addSubview(topIcon)
        self.addSubview(topIcon)
        //: contentView.addSubview(uidLb)
        contentView.addSubview(uidLb)
        //: contentView.addSubview(nameLb)
        contentView.addSubview(nameLb)
        //: contentView.addSubview(levelIcon)
        contentView.addSubview(levelIcon)
        //: contentView.addSubview(signLb)
        contentView.addSubview(signLb)
        //: contentView.addSubview(followersLab)
        contentView.addSubview(followersLab)
        //: contentView.addSubview(followersNum)
        contentView.addSubview(followersNum)
        //: contentView.addSubview(followingLab)
        contentView.addSubview(followingLab)
        //: contentView.addSubview(followingNum)
        contentView.addSubview(followingNum)

        //: contentView.layoutIfNeeded()
        contentView.layoutIfNeeded()

        //: if self.type == .user_other {
        if self.type == .user_other {
            //: contentView.addSubview(lineView)
            contentView.addSubview(lineView)
            //: contentView.addSubview(stackView)
            contentView.addSubview(stackView)
            //: contentView.addSubview(moreBtn)
            contentView.addSubview(moreBtn)
        }
    }

    // 添加约束
    //: private func setupSubViewsConstraint() {
    private func forefrontForConstraint() {
        //: contentView.snp.makeConstraints { make in
        contentView.snp.makeConstraints { make in
            //: make.bottom.equalToSuperview().offset(-44)
            make.bottom.equalToSuperview().offset(-44)
            //: make.leading.equalTo(10)
            make.leading.equalTo(10)
            //: make.trailing.equalTo(-10)
            make.trailing.equalTo(-10)
            //: if self.type == .user_me {
            if self.type == .user_me {
                //: make.height.equalTo(192)
                make.height.equalTo(192)
                //: }else {
            } else {
                //: make.height.equalTo(233)
                make.height.equalTo(233)
            }
        }
        //: topIcon.snp.makeConstraints { make in
        topIcon.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.top.equalTo(contentView).offset(-33)
            make.top.equalTo(contentView).offset(-33)
            //: make.width.height.equalTo(80)
            make.width.height.equalTo(80)
        }
        //: uidLb.snp.makeConstraints { make in
        uidLb.snp.makeConstraints { make in
            //: make.leading.top.equalTo(12)
            make.leading.top.equalTo(12)
            //: make.trailing.equalTo(topIcon.snp.leading).offset(-10)
            make.trailing.equalTo(topIcon.snp.leading).offset(-10)
        }
        //: nameLb.snp.makeConstraints { make in
        nameLb.snp.makeConstraints { make in
            //: make.top.equalTo(topIcon.snp.bottom).offset(12)
            make.top.equalTo(topIcon.snp.bottom).offset(12)
            //: make.centerX.equalToSuperview().offset(-12)
            make.centerX.equalToSuperview().offset(-12)
            //: make.width.lessThanOrEqualTo(ScreenWidth/2)
            make.width.lessThanOrEqualTo(main_colorData / 2)
            //: make.height.equalTo(18)
            make.height.equalTo(18)
        }
        //: levelIcon.snp.makeConstraints { make in
        levelIcon.snp.makeConstraints { make in
            //: make.leading.equalTo(nameLb.snp.trailing).offset(5)
            make.leading.equalTo(nameLb.snp.trailing).offset(5)
            //: make.centerY.equalTo(nameLb)
            make.centerY.equalTo(nameLb)
            //: make.size.equalTo(CGSize(width: 32, height: 14))
            make.size.equalTo(CGSize(width: 32, height: 14))
        }
        //: signLb.snp.makeConstraints { make in
        signLb.snp.makeConstraints { make in
            //: make.top.equalTo(nameLb.snp.bottom).offset(12)
            make.top.equalTo(nameLb.snp.bottom).offset(12)
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
            //: make.height.equalTo(38)
            make.height.equalTo(38)
        }
        //: followersNum.snp.makeConstraints { make in
        followersNum.snp.makeConstraints { make in
            //: make.top.equalTo(signLb.snp.bottom).offset(12)
            make.top.equalTo(signLb.snp.bottom).offset(12)
            //: make.centerX.equalTo(contentView).offset(-ScreenWidth/4+10)
            make.centerX.equalTo(contentView).offset(-main_colorData / 4 + 10)
            //: make.height.equalTo(18)
            make.height.equalTo(18)
        }
        //: followingNum.snp.makeConstraints { make in
        followingNum.snp.makeConstraints { make in
            //: make.top.equalTo(signLb.snp.bottom).offset(12)
            make.top.equalTo(signLb.snp.bottom).offset(12)
            //: make.centerX.equalTo(contentView).offset(ScreenWidth/4-10)
            make.centerX.equalTo(contentView).offset(main_colorData / 4 - 10)
            //: make.height.equalTo(18)
            make.height.equalTo(18)
        }
        //: followersLab.snp.makeConstraints { make in
        followersLab.snp.makeConstraints { make in
            //: make.top.equalTo(followersNum.snp.bottom).offset(5)
            make.top.equalTo(followersNum.snp.bottom).offset(5)
            //: make.centerX.equalTo(followersNum)
            make.centerX.equalTo(followersNum)
            //: make.height.equalTo(14)
            make.height.equalTo(14)
        }
        //: followingLab.snp.makeConstraints { make in
        followingLab.snp.makeConstraints { make in
            //: make.top.equalTo(followingNum.snp.bottom).offset(5)
            make.top.equalTo(followingNum.snp.bottom).offset(5)
            //: make.centerX.equalTo(followingNum)
            make.centerX.equalTo(followingNum)
            //: make.height.equalTo(14)
            make.height.equalTo(14)
        }

        //: if self.type == .user_other {
        if self.type == .user_other {
            //: lineView.snp.makeConstraints { make in
            lineView.snp.makeConstraints { make in
                //: make.top.equalTo(followersLab.snp.bottom).offset(12)
                make.top.equalTo(followersLab.snp.bottom).offset(12)
                //: make.leading.equalTo(7)
                make.leading.equalTo(7)
                //: make.trailing.equalTo(-7)
                make.trailing.equalTo(-7)
                //: make.height.equalTo(1)
                make.height.equalTo(1)
            }
            //: stackView.snp.makeConstraints { make in
            stackView.snp.makeConstraints { make in
                //: make.top.equalTo(lineView)
                make.top.equalTo(lineView)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(44)
                make.height.equalTo(44)
            }
            //: moreBtn.snp.makeConstraints { make in
            moreBtn.snp.makeConstraints { make in
                //: make.trailing.equalToSuperview()
                make.trailing.equalToSuperview()
                //: make.top.equalTo(12)
                make.top.equalTo(12)
                //: make.size.equalTo(CGSize(width: 44, height: 20))
                make.size.equalTo(CGSize(width: 44, height: 20))
            }
        }
    }

    //: override func touchesBegan(_ touches: Set<UITouch>, with event: UIEvent?) {
    override func touchesBegan(_ touches: Set<UITouch>, with _: UIEvent?) {
        //: let touch = touches.first!
        let touch = touches.first!
        //: var point = touch.location(in: self.currentViewController()?.view)
        var point = touch.location(in: self.actionLatest()?.view)
        //: point = contentView.layer.convert(point, from: self.currentViewController()?.view.layer)
        point = contentView.layer.convert(point, from: self.actionLatest()?.view.layer)
        //: if !contentView.layer.contains(point) {
        if !contentView.layer.contains(point) {
            //: self.dismiss()
            self.allowStatusDismiss()
        }
    }
}
