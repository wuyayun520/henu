
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let notiDetailPath:[UInt8] = [0x97,0x9c,0x97,0xa2,0x56,0x91,0x9d,0x92,0x93,0xa0,0x68,0x57,0x4e,0x96,0x8f,0xa1,0x4e,0x9c,0x9d,0xa2,0x4e,0x90,0x93,0x93,0x9c,0x4e,0x97,0x9b,0x9e,0x9a,0x93,0x9b,0x93,0x9c,0xa2,0x93,0x92]

				fileprivate func partyColor(data num: UInt8) -> UInt8 {
					let value = Int(num) - 46
					if value < 0 {
						return UInt8(value + 256)
					} else {
						return UInt8(value)
					}
				}

/*: "headPic" :*/
fileprivate let mainSucceedText:String = "headPicback task show"

/*: "nickname" :*/
fileprivate let showItemArrayFloatTitle:[Character] = ["n","i","c"]
fileprivate let app_layerValue:[Character] = ["k","n","a","m","e"]

/*: "birthday" :*/
fileprivate let main_sendUrl:String = "bmodelt"

/*: "videoUrl" :*/
fileprivate let show_halfMessage:String = "height equalvideoUr"
fileprivate let appLayerText:[Character] = ["l"]

/*: "location" :*/
fileprivate let mainTotalData:[Character] = ["l","o","c","a","t","i","o"]
fileprivate let k_listKey:String = "push"

/*: "refuseCall" :*/
fileprivate let notiModeSizeQuoteStr:[Character] = ["r","e","f","u","s","e"]
fileprivate let kNameMessage:String = "row area make topCall"

/*: "data" :*/
fileprivate let data_valueTouchStr:[UInt8] = [0xea,0xef,0xfa,0xef]

				private func kitAs(time num: UInt8) -> UInt8 {
					return num ^ 142
				}

/*: "Call interruption" :*/
fileprivate let noti_labAreaName:[Character] = ["C","a","l","l"," ","i","n","t","e","r","r","u","p","t","i","o","n"]

/*: "stopCall" :*/
fileprivate let userIndexValue:String = "stlengthp"

/*: "logId" :*/
fileprivate let constResultId:[UInt8] = [0x70,0x73,0x6b,0x4d,0x68]

				fileprivate func replyAction(change num: UInt8) -> UInt8 {
					let value = Int(num) - 4
					if value < 0 {
						return UInt8(value + 256)
					} else {
						return UInt8(value)
					}
				}

/*: "cancelCall" :*/
fileprivate let show_labelContent:[Character] = ["c","a","n","c","e","l","C","a","l","l"]

/*: "No answer" :*/
fileprivate let show_valueCenterStr:[Character] = ["N","o"," ","a","n","s","w","e","r"]

/*: "duration" :*/
fileprivate let constProgressKey:String = "dlanguagerati"
fileprivate let data_normalFormat:String = "omodel"

/*: "She has hung up" :*/
fileprivate let userPopTitle:String = "She hcatch type voice model"
fileprivate let const_formatMsg:[Character] = ["a"]
fileprivate let kBirthMsg:[Character] = ["s"," ","h","u","n","g"," ","u","p"]

/*: "cancelSex" :*/
fileprivate let appTempStr:[Character] = ["c","a","n","c","e","l"]
fileprivate let userSumValue:String = "Sextext model"

/*: "He has hung up" :*/
fileprivate let main_timeUrl:[Character] = ["H","e"," ","h","a","s"," ","h","u","n","g"," ","u","p"]

/*: "endType" :*/
fileprivate let showCardNameMessage:String = "edata"
fileprivate let userHiddenMessage:String = "dTypeas component type false bag"

/*: "stopSex" :*/
fileprivate let k_belowQuoteText:String = "stopSetitle position show"
fileprivate let data_viewUrl:String = "live"

/*: "Insufficient gold coins, call has ended" :*/
fileprivate let dataIndexUrl:[UInt8] = [0x7e,0x59,0x44,0x42,0x51,0x51,0x5e,0x54,0x5e,0x52,0x59,0x43,0x17,0x50,0x58,0x5b,0x53,0x17,0x54,0x58,0x5e,0x59,0x44,0x1b,0x17,0x54,0x56,0x5b,0x5b,0x17,0x5f,0x56,0x44,0x17,0x52,0x59,0x53,0x52,0x53]

/*: "The other hung up the phone" :*/
fileprivate let show_groupUrl:String = "The player manager let track"
fileprivate let showImageValueData:String = "false gender return hung "
fileprivate let k_backgroundPath:String = "view lab number insert phone"

/*: "type" :*/
fileprivate let user_labelValue:String = "contentype"

/*: "jumpUrl" :*/
fileprivate let kBottomId:String = "block report model else mainjumpUr"
fileprivate let dataKeyStr:String = "size"

/*: "Cancel" :*/
fileprivate let main_phoneStr:[Character] = ["C","a","n","c","e","l"]

/*: "Get gold coins" :*/
fileprivate let main_headCoverData:String = "Get goto make view"
fileprivate let appMakeContent:String = "no"
fileprivate let user_toolName:String = "d coinsmake var show transform bind"

/*: "addBean" :*/
fileprivate let dataLabelValue:[Character] = ["a","d","d","B","e","a"]
fileprivate let mainBackArrayGiftData:[Character] = ["n"]

/*: "refuseSex" :*/
fileprivate let const_managerModeData:String = "cos"
fileprivate let data_detailKey:String = "array to toefuseSex"

/*: "onAcceptCall" :*/
fileprivate let userPathCurUrl:String = "object current image user playonAcce"
fileprivate let notiToBlockMsg:[Character] = ["p","t","C","a","l","l"]

/*: "acceptCall" :*/
fileprivate let show_menuValue:[Character] = ["a","c","c","e","p","t","C","a","l","l"]

/*: "Calls are cancelled" :*/
fileprivate let constDismissTitle:String = "move touchCal"
fileprivate let notiTextTitleFormat:String = "ls atransaction leading button"
fileprivate let show_appValue:String = "viewcelled"

/*: "The call has ended" :*/
fileprivate let kMessageLabFormat:String = "The calabel make error pop post"
fileprivate let show_layerId:String = "as endedfatal succeed ad"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  IconPartyDelegate.swift
//  AbroadTalking
//
//  Created by young on 2022/11/11.
//

//: import CallKit.CXCallObserver
import CallKit.CXCallObserver
//: import UIKit
import UIKit

// 通话监听(必须为全局属性，否则无法接听)
//: let voiceCallHelper = CXCallObserver()
let app_bottomScreenPath = CXCallObserver()
//: class TalkingVoiceChatViewController: TalkingBaseViewController {
class IconPartyDelegate: LightThen {
    //: var chatModel: TalkingVideoChatModel?
    var chatModel: PokingChatModel? // 语音通话模型
    //: private var player: TalkingCallBGMPlayer?
    private var player: LittlenessThen? // 拨打中背景音乐
    //: private var timeOutTimer: Timer?
    private var timeOutTimer: Timer? // 未接听超时定时器
    //: private var expiredDate = Date()
    private var expiredDate = Date() // 失效时间
    //: private var miniView: TalkingVoiceMiniView?
    private var miniView: AppMiniView? // 音频通话小窗口

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName _: String?, bundle _: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        // 提前绑定delegate
        //: TalkingSocketManager.shared.callerDelegate = self
        GiftCommentSocketDelegate.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        GiftCommentSocketDelegate.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        GiftCommentSocketDelegate.shared.errorDelegate = self
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: notiDetailPath.map{partyColor(data: $0)}, encoding: .utf8)!)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: let nav = self.navigationController as! TalkingNavigationController
        let nav = self.navigationController as! IndexResponseReactiveCompatible
        //: nav.protectedPushVc = nil
        nav.protectedPushVc = nil

        //: UIApplication.shared.isIdleTimerDisabled = true
        UIApplication.shared.isIdleTimerDisabled = true // 设备不会进入休眠状态
        //: self.startBGM()
        self.infoEqual()
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved { // 接收方验证权限
            //: checkVoiceAuth()
            drawAuth()
        }
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: if self.miniView != nil {
        if self.miniView != nil { // 移除小窗口
            //: self.miniView?.removeFromSuperview()
            self.miniView?.removeFromSuperview()
            //: self.miniView = nil
            self.miniView = nil
            //: TalkingSocketManager.shared.currNameVC = nil
            GiftCommentSocketDelegate.shared.currNameVC = nil
        }
        //: interactionView.initCachEffectData()
        interactionView.targetUp()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: setupSubviews()
        loadSubviews()
        //: setupSubViewsConstraint()
        examine()
        //: initNotificationObserver()
        themeWith()
        //: req_callGetUserInfo()
        noLabelInfo()
        //: timeoutRules()
        encephalon()
        //: resumeTalk()
        resumeSection()
        //: setUpBGMPlayer()
        orderedSeriesPlayer()
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if IconContainerAppManager.share.appConfigMode.disableShootScreen {
            if IconContainerAppManager.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = AtThen(frame: CGRect(x: 0, y: 0, width: main_colorData, height: constRecordServerKey))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: deinit {
    deinit {
        //: UIApplication.shared.isIdleTimerDisabled = false
        UIApplication.shared.isIdleTimerDisabled = false
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: TalkingSocketManager.shared.isName = false
        GiftCommentSocketDelegate.shared.isName = false
        //: self.stopVCAllActivties()
        self.overPlay()
    }

    // MARK: - Lazy Load

    /// 音频通话操作按钮
    //: private lazy var interactionView: Talking1v1VoiceInteractionView = {
    private lazy var interactionView: EffectIntervaleractionView = {
        //: let v = Talking1v1VoiceInteractionView(frame: .zero, chatModel: self.chatModel)
        let v = EffectIntervaleractionView(frame: .zero, chatModel: self.chatModel)
        //: v.delegate = self
        v.delegate = self
        //: return v
        return v
        //: }()
    }()

    //: private lazy var voiceManager: TalkingVoiceChatManager = {
    private lazy var voiceManager: ManageressReactiveCompatible = {
        //: let m = TalkingVoiceChatManager()
        let m = ManageressReactiveCompatible()
        //: m.handsfreeBtn = self.interactionView.handsfreeBtn
        m.handsfreeBtn = self.interactionView.handsfreeBtn
        //: return m
        return m
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingVoiceChatViewController {
extension IconPartyDelegate {
    /// 查询接收者信息
    //: func req_callGetUserInfo() {
    func noLabelInfo() {
        //: guard let pairUid = chatModel?.pairUid else { return }
        guard let pairUid = chatModel?.pairUid else { return }
        //: TalkingVideoChatManager.req_callGetUserInfo(uid: pairUid) { [weak self] succeed, result, errorModel in
        RouteChatManager.mapCompletion(uid: pairUid) { [weak self] succeed, result, _ in
            //: guard succeed == true else { return }
            guard succeed == true else { return }
            //: guard let self = self else { return }
            guard let self = self else { return }

            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())

            //: self.chatModel?.pairUid = json["uid"].stringValue
            self.chatModel?.pairUid = json["uid"].stringValue
            //: self.chatModel?.pairHeadPic = json["headPic"].stringValue
            self.chatModel?.pairHeadPic = json[(String(mainSucceedText.prefix(7)))].stringValue
            //: self.chatModel?.pairNickName = json["nickname"].stringValue
            self.chatModel?.pairNickName = json[(String(showItemArrayFloatTitle) + String(app_layerValue))].stringValue
            //: self.chatModel?.pairBirthday = json["birthday"].stringValue
            self.chatModel?.pairBirthday = json[(main_sendUrl.replacingOccurrences(of: "model", with: "ir") + "hday")].stringValue
            //: self.chatModel?.videoUrl = json["videoUrl"].stringValue
            self.chatModel?.videoUrl = json[(String(show_halfMessage.suffix(7)) + String(appLayerText))].stringValue
            //: self.chatModel?.location = json["location"].stringValue
            self.chatModel?.location = json[(String(mainTotalData) + k_listKey.replacingOccurrences(of: "push", with: "n"))].stringValue

            // 刷新通话视图
            //: self.interactionView.afterGetInfo()
            self.interactionView.punchCard()
        }
    }
}

// MARK: - Event

//: extension TalkingVoiceChatViewController {
extension IconPartyDelegate {
    /// 权限校验，播放BGM
    //: private func checkVoiceAuth() {
    private func drawAuth() {
        //: TalkingPermissionTool.func__openRecordServiceWithBlock(true) { isOpen in
        CellPermissionTool.towardAction(true) { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.explain(showMsg: main_deviceName)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                GiftCommentSocketDelegate.shared.agnomen(info: ["cmd": (String(notiModeSizeQuoteStr) + String(kNameMessage.suffix(4))), String(bytes: data_valueTouchStr.map{kitAs(time: $0)}, encoding: .utf8)!: ["uid": self.chatModel?.pairUid]])
                //: self.popCurrentViewController()
                self.modelSubAnimated()
                //: return
                return
            }

            //: self.stopBGM()
            self.collectionBgm()
            //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: .Response)
            self.player = LittlenessThen.varietyType(type: .Response)
            //: self.startBGM()
            self.infoEqual()
        }
    }

    /// 释放当前页面所有资源
    //: private func stopVCAllActivties() {
    private func overPlay() {
        //: if self.timeOutTimer != nil {
        if self.timeOutTimer != nil {
            //: self.timeOutTimer?.invalidate()
            self.timeOutTimer?.invalidate()
            //: self.timeOutTimer = nil
            self.timeOutTimer = nil
        }

        //: self.stopBGM()
        self.collectionBgm()
        //: TalkingSocketManager.shared.callerDelegate = nil
        GiftCommentSocketDelegate.shared.callerDelegate = nil
        //: TalkingSocketManager.shared.calledPartyDelegate = nil
        GiftCommentSocketDelegate.shared.calledPartyDelegate = nil
        //: TalkingSocketManager.shared.errorDelegate = nil
        GiftCommentSocketDelegate.shared.errorDelegate = nil
    }

    // 将当前视图从栈中移除
    //: private func popCurrentViewController(animated: Bool = true) {
    private func modelSubAnimated(animated: Bool = true) {
        //: self.stopBGM()
        self.collectionBgm()

        //: if self.navigationController?.topViewController == self {
        if self.navigationController?.topViewController == self {
            //: self.navigationController?.popViewController(animated: animated)
            self.navigationController?.popViewController(animated: animated)
            //: } else {
        } else {
            //: if var vcArr = self.navigationController?.viewControllers {
            if var vcArr = self.navigationController?.viewControllers {
                //: if let index = vcArr.firstIndex(of: self) {
                if let index = vcArr.firstIndex(of: self) {
                    //: vcArr.remove(at: index)
                    vcArr.remove(at: index)
                    //: self.navigationController?.setViewControllers(vcArr, animated: false)
                    self.navigationController?.setViewControllers(vcArr, animated: false)
                }
            }
        }
    }

    /// 创建音频通话房间
    //: private func createTalkRoom() {
    private func slab() {
        //: self.voiceManager.createRoomAndStartPublish(roomId: self.chatModel!.logId)
        self.voiceManager.manage(roomId: self.chatModel!.logId)
    }

    /// 通话重连
    //: private func resumeTalk() {
    private func resumeSection() {
        //: if self.chatModel?.resumeTalk == true {
        if self.chatModel?.resumeTalk == true {
            //: createTalkRoom()
            slab()
        }
    }
}

// MARK: - 通知

//: extension TalkingVoiceChatViewController {
extension IconPartyDelegate {
    /// 初始化通知
    //: private func initNotificationObserver() {
    private func themeWith() {
        // 监听 App 从后台切前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(appWillEnterForeground(notif:)),
                                               selector: #selector(deleteNotif(notif:)),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // socket断网重连监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(conversationChange(note:)),
                                               //: name: SOCKET_IS_OPEN_NOTIFICATION,
                                               name: user_restoreId,
                                               //: object: nil)
                                               object: nil)

        //: voiceCallHelper.setDelegate(self, queue: DispatchQueue.main)
        app_bottomScreenPath.setDelegate(self, queue: DispatchQueue.main)
    }

    /// app进入前台
    //: @objc private func appWillEnterForeground(notif: Notification) {
    @objc private func deleteNotif(notif _: Notification) {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: let date = Date()
        let date = Date()
        //: let interval = date.timeIntervalSince(self.expiredDate)
        let interval = date.timeIntervalSince(self.expiredDate)
        //: if interval >= 0 {
        if interval >= 0 {
            //: popCurrentViewController()
            modelSubAnimated()
        }
    }

    /// 断网重连socket
    //: @objc private func reachabilityChanged(note: Notification) {
    @objc private func conversationChange(note _: Notification) {
        //: guard TalkingSocketManager.shared.needReconnect == true else { return }
        guard GiftCommentSocketDelegate.shared.needReconnect == true else { return }
        //: TalkingSocketManager.shared.callerDelegate = self
        GiftCommentSocketDelegate.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        GiftCommentSocketDelegate.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        GiftCommentSocketDelegate.shared.errorDelegate = self
        //: self.voiceManager.restartTalkWhenDisconnectedAndNetAvailable(roomId: self.chatModel?.logId ?? 0)
        self.voiceManager.menuRow(roomId: self.chatModel?.logId ?? 0)
    }
}

// MARK: - CXCallObserverDelegate【通话监听】

//: extension TalkingVoiceChatViewController: CXCallObserverDelegate {
extension IconPartyDelegate: CXCallObserverDelegate {
    //: func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
    func callObserver(_: CXCallObserver, callChanged call: CXCall) {
        //: if call.hasConnected == true {
        if call.hasConnected == true { // 通话中
            //: self.func__showStatusBarErrorMsg(showMsg: "Call interruption".localized)
            self.explain(showMsg: (String(noti_labAreaName)).localized)
            //: switch(self.chatModel?.videoStage) {
            switch self.chatModel?.videoStage {
            //: case .Calling:
            case .Calling:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                GiftCommentSocketDelegate.shared.agnomen(info: ["cmd": (userIndexValue.replacingOccurrences(of: "length", with: "o") + "Call"), String(bytes: data_valueTouchStr.map{kitAs(time: $0)}, encoding: .utf8)!: [String(bytes: constResultId.map{replyAction(change: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Waiting:
            case .Waiting:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
                GiftCommentSocketDelegate.shared.agnomen(info: ["cmd": (String(show_labelContent)), String(bytes: data_valueTouchStr.map{kitAs(time: $0)}, encoding: .utf8)!: [String(bytes: constResultId.map{replyAction(change: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Recieved:
            case .Recieved:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                GiftCommentSocketDelegate.shared.agnomen(info: ["cmd": (String(notiModeSizeQuoteStr) + String(kNameMessage.suffix(4))), String(bytes: data_valueTouchStr.map{kitAs(time: $0)}, encoding: .utf8)!: ["uid": self.chatModel?.pairUid]])
            //: case .none:
            case .none:
                //: break
                break
            }

            //: if miniView != nil {
            if miniView != nil {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.mp()
                //: TalkingSocketManager.shared.currNameVC = nil
                GiftCommentSocketDelegate.shared.currNameVC = nil
            }
            //: popCurrentViewController()
            modelSubAnimated()

            //: } else if call.hasEnded == true || call.isOnHold == true {
        } else if call.hasEnded == true || call.isOnHold == true { // 电话挂断 || 无人接听挂断
            //: self.voiceManager.enableAudioModule(enable: true)
            self.voiceManager.stream(enable: true)
        }
    }
}

// MARK: - 通话音乐

//: extension TalkingVoiceChatViewController {
extension IconPartyDelegate {
    /// 初始化音频通话背景音乐
    //: private func setUpBGMPlayer() {
    private func orderedSeriesPlayer() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let type = self.chatModel!.isCaller == true ? CallBGMPlayerType.Call : CallBGMPlayerType.Response
        let type = self.chatModel!.isCaller == true ? CustomHashableRepresentation.Call : CustomHashableRepresentation.Response
        //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: type)
        self.player = LittlenessThen.varietyType(type: type)
    }

    /// 开始播放背景音乐
    //: private func startBGM() {
    private func infoEqual() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: guard self.chatModel?.resumeTalk != true else { return }
        guard self.chatModel?.resumeTalk != true else { return }

        //: self.player?.play()
        self.player?.viewPlay()
    }

    /// 停止播放背景音乐
    //: private func stopBGM() {
    private func collectionBgm() {
        //: guard self.player != nil else { return }
        guard self.player != nil else { return }
        //: self.player?.stop()
        self.player?.theFilterEvent()
        //: self.player = nil
        self.player = nil
    }
}

// MARK: - 定时器

//: extension TalkingVoiceChatViewController {
extension IconPartyDelegate {
    /// 超时定时器
    //: private func timeoutRules() {
    private func encephalon() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let timeout = self.chatModel?.isCaller == true ? VIDEO_1v1_CALL_TIMEOUT : self.chatModel?.remainAnswerCallTime()
        let timeout = self.chatModel?.isCaller == true ? showDeviceTitle : self.chatModel?.greetTime()
        //: if timeout! <= 0 {
        if timeout! <= 0 {
            //: buildTalkTimeOutAction()
            prepareTo()
        }
        //: self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
        self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.buildTalkTimeOutAction()
            self.prepareTo()
            //: }, repeats: false)
        }, repeats: false)

        //: self.expiredDate = Date().addingTimeInterval(60)
        self.expiredDate = Date().addingTimeInterval(60)
    }

    /// 倒计时超时逻辑处理
    //: private func buildTalkTimeOutAction() {
    private func prepareTo() {
        //: guard !TalkingSocketManager.shared.isName else { return }
        guard !GiftCommentSocketDelegate.shared.isName else { return }

        //: var socketDict: [String: Any]
        var socketDict: [String: Any]
        //: if self.chatModel?.isCaller == true {
        if self.chatModel?.isCaller == true { // 拨打方
            //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
            self.explain(showMsg: (String(show_valueCenterStr)).localized)
            //: socketDict = ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]]
            socketDict = ["cmd": (String(show_labelContent)), String(bytes: data_valueTouchStr.map{kitAs(time: $0)}, encoding: .utf8)!: [String(bytes: constResultId.map{replyAction(change: $0)}, encoding: .utf8)!: self.chatModel?.logId]]
            //: } else {
        } else { // 接收方
            //: socketDict = ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]]
            socketDict = ["cmd": (String(notiModeSizeQuoteStr) + String(kNameMessage.suffix(4))), String(bytes: data_valueTouchStr.map{kitAs(time: $0)}, encoding: .utf8)!: ["uid": self.chatModel?.pairUid]]
        }
        //: TalkingSocketManager.shared.sendMessage(info: socketDict)
        GiftCommentSocketDelegate.shared.agnomen(info: socketDict)
        //: popCurrentViewController()
        modelSubAnimated()
    }
}

// MARK: - resume talk【恢复通话】

//: extension TalkingVoiceChatViewController {
extension IconPartyDelegate {
    /// 恢复音频通话，更新chatModel
    /// - Parameter resumeData: 数据
    //: func updateInfoViaResumeTalkData(resumeData: [String: Any]) {
    func collectionName(resumeData: [String: Any]) {
        //: let json = JSON(resumeData)
        let json = JSON(resumeData)
        //: let logId = json["logId"].intValue
        let logId = json[String(bytes: constResultId.map{replyAction(change: $0)}, encoding: .utf8)!].intValue
        //: let duration = json["duration"].intValue
        let duration = json[(constProgressKey.replacingOccurrences(of: "language", with: "u") + data_normalFormat.replacingOccurrences(of: "model", with: "n"))].intValue
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let startTalkTime = floor(interval) - Double(duration)
        let startTalkTime = floor(interval) - Double(duration)
        //: self.chatModel?.startTime = startTalkTime
        self.chatModel?.startTime = startTalkTime
        //: self.interactionView.updateStartTime(startTime: startTalkTime)
        self.interactionView.beforeImage(startTime: startTalkTime)

        //: if self.chatModel?.videoStage != .Calling {
        if self.chatModel?.videoStage != .Calling {
            //: self.chatModel?.logId = logId
            self.chatModel?.logId = logId
            //: self.chatModel?.videoStage = .Calling
            self.chatModel?.videoStage = .Calling
            //: self.interactionView.showViewsViaStage()
            self.interactionView.giftHorse()
            //: createTalkRoom()
            slab()
            //: self.stopBGM()
            self.collectionBgm()
        }
    }
}

// MARK: - GestureObjectProtocol【拨打方】

//: extension TalkingVoiceChatViewController: SocketManagerVideoChatCallerDelegate {
extension IconPartyDelegate: GestureObjectProtocol {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {
    func imageSocketModel(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(userPopTitle.prefix(5)) + String(const_formatMsg) + String(kBirthMsg)).localized
            //: if json["cancelSex"].stringValue == Gender.male.rawValue {
            if json[(String(appTempStr) + String(userSumValue.prefix(3)))].stringValue == ThemeTitleScalarLiteral.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(main_timeUrl)).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.explain(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.mp()
            //: TalkingSocketManager.shared.currNameVC = nil
            GiftCommentSocketDelegate.shared.currNameVC = nil
        }

        //: popCurrentViewController()
        modelSubAnimated()
    }

    /// 对方接受了来电，建立音频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func untilAttentionRemove(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: self.chatModel?.logId = json["logId"].intValue
        self.chatModel?.logId = json[String(bytes: constResultId.map{replyAction(change: $0)}, encoding: .utf8)!].intValue
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow

        //: self.stopBGM()
        self.collectionBgm()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.giftHorse()
        //: self.createTalkRoom()
        self.slab()
    }

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {
    func bindRequest(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: let endType = json["endType"].intValue
        let endType = json[(showCardNameMessage.replacingOccurrences(of: "data", with: "n") + String(userHiddenMessage.prefix(5)))].intValue
        //: var typeStr = "She has hung up".localized
        var typeStr = (String(userPopTitle.prefix(5)) + String(const_formatMsg) + String(kBirthMsg)).localized
        //: if json["stopSex"].stringValue == Gender.male.rawValue {
        if json[(String(k_belowQuoteText.prefix(6)) + data_viewUrl.replacingOccurrences(of: "live", with: "x"))].stringValue == ThemeTitleScalarLiteral.male.rawValue {
            //: typeStr = "He has hung up".localized
            typeStr = (String(main_timeUrl)).localized
        }

        //: if endType == 1 {
        if endType == 1 { // 付费方余额不足
            //: if IconContainerAppManager.share.appStatus == AppSkinStatus.normal.rawValue {
            if IconContainerAppManager.share.appStatus == MinExpressionConvertible.normal.rawValue {
                //: typeStr = "Insufficient gold coins, call has ended".localized
                typeStr = String(bytes: dataIndexUrl.map{$0^55}, encoding: .utf8)!.localized
                //: } else {
            } else {
                //: typeStr = "The other hung up the phone".localized
                typeStr = (String(show_groupUrl.prefix(4)) + "other" + String(showImageValueData.suffix(6)) + "up the" + String(k_backgroundPath.suffix(6))).localized
            }
        }
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.explain(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.mp()
            //: TalkingSocketManager.shared.currNameVC = nil
            GiftCommentSocketDelegate.shared.currNameVC = nil
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.atMessage()
        //: self.popCurrentViewController()
        self.modelSubAnimated()
        //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: dataKeyFormat, object: nil)
    }

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {
    func contentData(msg: String, data: [String: Any]) {
        //: guard IconContainerAppManager.share.appStatus == AppSkinStatus.normal.rawValue else { return }
        guard IconContainerAppManager.share.appStatus == MinExpressionConvertible.normal.rawValue else { return }

        //: let index = data["type"] as? String ?? ""
        let index = data[(user_labelValue.replacingOccurrences(of: "content", with: "t"))] as? String ?? ""
        //: if index == "2" {
        if index == "2" {
            //: guard let jumpUrl = data["jumpUrl"] as? String  else { return }
            guard let jumpUrl = data[(String(kBottomId.suffix(6)) + dataKeyStr.replacingOccurrences(of: "size", with: "l"))] as? String else { return }
            //: let httpsUrl = IconContainerAppManager.share.appConfigMode.urlH5Domain+jumpUrl
            let httpsUrl = IconContainerAppManager.share.appConfigMode.urlH5Domain + jumpUrl
            //: var config = TalkingWebConfig.init()
            var config = RawDataConfig()
            //: config.isDiscountsView = true
            config.isDiscountsView = true
            //: CombineAccountEnclaveDecisionMakerThen.share.func__pushToWebVC(urlStr: httpsUrl, webConfig: config)
            CombineAccountEnclaveDecisionMakerThen.share.sizeConfig(urlStr: httpsUrl, webConfig: config)
            //: } else {
        } else {
            //: TalkingAlertShow.alert(title: nil,
            CommentSourceThen.outRequestAdd(title: nil,
                                           //: message: msg,
                                           message: msg,
                                           //: leftBtnTitle: "Cancel".localized,
                                           leftBtnTitle: (String(main_phoneStr)).localized,
                                           //: rightBtnTitle: "Get gold coins".localized) {
                                           rightBtnTitle: (String(main_headCoverData.prefix(6)) + appMakeContent.replacingOccurrences(of: "no", with: "l") + String(user_toolName.prefix(7))).localized)
            {
                //: TalkingAlertShow.hideAlert()
                CommentSourceThen.alertDataTo()
                //: } rightBlock: {
            } rightBlock: {
                //: CombineAccountEnclaveDecisionMakerThen.share.func__jumpToWebRecharge(clickEvent: clickLess1minButton, sufficient: false)
                CombineAccountEnclaveDecisionMakerThen.share.toMake(clickEvent: dataErrorMessage, sufficient: false)
            }
        }
    }

    /// 女性通话积分激励动效展示
    //: func socket_videoChat_onCallMotivation(data: [String: Any]) {
    func vox(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: let addBean = json["addBean"].stringValue
        let addBean = json[(String(dataLabelValue) + String(mainBackArrayGiftData))].stringValue
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: interactionView.showPointAnimaData(point: addBean)
            interactionView.themeName(point: addBean)
            //: } else {
        } else {
            //: self.miniView?.showPointAnimaData(point: addBean)
            self.miniView?.nextForVideo(point: addBean)
        }
    }
}

// MARK: - CaliberObjectProtocol【接收方】

//: extension TalkingVoiceChatViewController: SocketManagerVideoChatCalledPartyDelegate {
extension IconPartyDelegate: CaliberObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func socketOrRawDataEqual(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(userPopTitle.prefix(5)) + String(const_formatMsg) + String(kBirthMsg)).localized
            //: if json["refuseSex"].stringValue == Gender.male.rawValue {
            if json[(const_managerModeData.replacingOccurrences(of: "cos", with: "r") + String(data_detailKey.suffix(8)))].stringValue == ThemeTitleScalarLiteral.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(main_timeUrl)).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.explain(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.mp()
            //: TalkingSocketManager.shared.currNameVC = nil
            GiftCommentSocketDelegate.shared.currNameVC = nil
        }
        //: popCurrentViewController()
        modelSubAnimated()
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {
    func theTool(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: self.chatModel?.logId = json["logId"].intValue
        self.chatModel?.logId = json[String(bytes: constResultId.map{replyAction(change: $0)}, encoding: .utf8)!].intValue
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow
        //: self.stopBGM()
        self.collectionBgm()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.giftHorse()
        //: self.createTalkRoom()
        self.slab()
    }
}

// MARK: - SupplantObjectProtocol【异常处理】

//: extension TalkingVoiceChatViewController: TalkingSocketManagerErrorDelegate {
extension IconPartyDelegate: SupplantObjectProtocol {
    // 处理error情况
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func titleData(errorNo: Int, cmd: String, msg: String, data _: [String: Any]) {
        //: if cmd == "refuseCall" { // 对方拒绝了通话
        if cmd == (String(notiModeSizeQuoteStr) + String(kNameMessage.suffix(4))) { // 对方拒绝了通话
            //: if self.miniView == nil {
            if self.miniView == nil {
                //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
                self.explain(showMsg: (String(show_valueCenterStr)).localized)
                //: } else {
            } else {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.mp()
                //: TalkingSocketManager.shared.currNameVC = nil
                GiftCommentSocketDelegate.shared.currNameVC = nil
            }
            //: self.popCurrentViewController()
            self.modelSubAnimated()

            //: } else if cmd == "onAcceptCall" && errorNo == 101 { // 接听通话时，余额不足
        } else if cmd == (String(userPathCurUrl.suffix(6)) + String(notiToBlockMsg)) && errorNo == 101 { // 接听通话时，余额不足
            //: guard IconContainerAppManager.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard IconContainerAppManager.share.appStatus == MinExpressionConvertible.normal.rawValue else { return }
            //: CombineAccountEnclaveDecisionMakerThen.share.func__jumpToWebRecharge(sufficient: false)
            CombineAccountEnclaveDecisionMakerThen.share.toMake(sufficient: false)

            //: } else if cmd == "onAcceptCall" && errorNo == 102 { // 对方已取消通话
        } else if cmd == (String(userPathCurUrl.suffix(6)) + String(notiToBlockMsg)) && errorNo == 102 { // 对方已取消通话
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.explain(showMsg: msg)
            //: self.popCurrentViewController()
            self.modelSubAnimated()
        }
    }
}

// MARK: - SourceObjectProtocol【通话按钮操作事件】

//: extension TalkingVoiceChatViewController: Talking1v1VoiceInteractionViewDelegate {
extension IconPartyDelegate: SourceObjectProtocol {
    /// 接听音频通话
    //: func interactionView_shouldAcceptCall() {
    func demonstrateView() {
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "acceptCall", "data": ["uid": self.chatModel?.pairUid]])
        GiftCommentSocketDelegate.shared.agnomen(info: ["cmd": (String(show_menuValue)), String(bytes: data_valueTouchStr.map{kitAs(time: $0)}, encoding: .utf8)!: ["uid": self.chatModel?.pairUid]])
    }

    /// 拨打方结束通话
    //: func interactionView_shouldEndCall() {
    func stigmatize() {
        //: if self.chatModel?.videoStage == .Waiting {
        if self.chatModel?.videoStage == .Waiting { // 拨打方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.explain(showMsg: (String(constDismissTitle.suffix(3)) + String(notiTextTitleFormat.prefix(4)) + "re ca" + show_appValue.replacingOccurrences(of: "view", with: "n")).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
            GiftCommentSocketDelegate.shared.agnomen(info: ["cmd": (String(show_labelContent)), String(bytes: data_valueTouchStr.map{kitAs(time: $0)}, encoding: .utf8)!: [String(bytes: constResultId.map{replyAction(change: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: } else if self.chatModel?.videoStage == .Recieved {
        } else if self.chatModel?.videoStage == .Recieved { // 接收方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.explain(showMsg: (String(constDismissTitle.suffix(3)) + String(notiTextTitleFormat.prefix(4)) + "re ca" + show_appValue.replacingOccurrences(of: "view", with: "n")).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
            GiftCommentSocketDelegate.shared.agnomen(info: ["cmd": (String(notiModeSizeQuoteStr) + String(kNameMessage.suffix(4))), String(bytes: data_valueTouchStr.map{kitAs(time: $0)}, encoding: .utf8)!: ["uid": self.chatModel?.pairUid]])
            //: } else {
        } else { // 【通话中】
            //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
            self.explain(showMsg: (String(kMessageLabFormat.prefix(6)) + "ll h" + String(show_layerId.prefix(8))).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
            GiftCommentSocketDelegate.shared.agnomen(info: ["cmd": (userIndexValue.replacingOccurrences(of: "length", with: "o") + "Call"), String(bytes: data_valueTouchStr.map{kitAs(time: $0)}, encoding: .utf8)!: [String(bytes: constResultId.map{replyAction(change: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.atMessage()
        //: self.popCurrentViewController()
        self.modelSubAnimated()
    }

    /// 是否静音
    //: func interactionView_shouldChangeMute(isMute: Bool) {
    func occurrencePath(isMute: Bool) {
        //: self.voiceManager.setMute(isMute: isMute)
        self.voiceManager.statusPop(isMute: isMute)
    }

    /// 是否免提
    //: func interactionView_shouldChangeSpeaker(isHandsfree: Bool) {
    func toHandsfree(isHandsfree: Bool) {
        //: self.voiceManager.setHandsfree(isHandsfree: isHandsfree)
        self.voiceManager.titleHandsfree(isHandsfree: isHandsfree)
    }

    /// 展示语音通话小窗口
    //: func interactionView_shouldDisplayMiniWindow() {
    func generateOff() {
        //: TalkingSocketManager.shared.currNameVC = self
        GiftCommentSocketDelegate.shared.currNameVC = self
        //: self.miniView = TalkingVoiceMiniView.buildVoiceMiniView()
        self.miniView = AppMiniView.viewReport()
        //: self.miniView?.tapGestureBlock = { [weak self] in
        self.miniView?.tapGestureBlock = { [weak self] in // 还原回通话界面
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.currentViewController()?.navigationController?.pushViewController(self, animated: true)
            self.actionLatest()?.navigationController?.pushViewController(self, animated: true)
        }
        //: let talkingTime = self.interactionView.getCurrentTalkingTime()
        let talkingTime = self.interactionView.pointTime()
        //: self.miniView?.updateTalkingTime(talkingTime)
        self.miniView?.referencePoint(talkingTime)
        //: popCurrentViewController()
        modelSubAnimated()
    }

    /// 更新小窗口通话时间
    //: func interactionView_shouldUpdateTime(time: Double) {
    func breakup(time: Double) {
        //: guard self.miniView != nil else { return }
        guard self.miniView != nil else { return }
        //: self.miniView?.updateTalkingTime(time)
        self.miniView?.referencePoint(time)
    }

    /// 收到礼物消息展示
    //: @objc func receiveGiftPlay(giftArr: Array<Any>) {
    @objc func textualMatter(giftArr: [Any]) {
        //: interactionView.addGiftEffectModelArr(giftArr: giftArr)
        interactionView.appearGift(giftArr: giftArr)
    }
}

// MARK: - Layout

//: extension TalkingVoiceChatViewController {
extension IconPartyDelegate {
    /// 添加视图
    //: private func setupSubviews() {
    private func loadSubviews() {
        //: self.view.addSubview(interactionView)
        self.view.addSubview(interactionView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func examine() {
        //: interactionView.snp.makeConstraints { make in
        interactionView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
    }
}
